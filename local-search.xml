<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
  
  <entry>
    <title>2025-1-1 大堰河-我的保姆</title>
    <link href="/2025/01/01/2025-1-1%20%E5%A4%A7%E5%A0%B0%E6%B2%B3-%E6%88%91%E7%9A%84%E4%BF%9D%E5%A7%86/"/>
    <url>/2025/01/01/2025-1-1%20%E5%A4%A7%E5%A0%B0%E6%B2%B3-%E6%88%91%E7%9A%84%E4%BF%9D%E5%A7%86/</url>
    
    <content type="html"><![CDATA[<h1 id="大堰河——我的保姆"><a href="#大堰河——我的保姆" class="headerlink" title="大堰河——我的保姆"></a>大堰河——我的保姆</h1><p>【作者】艾青 【朝代】现代</p><p>大堰河，是我的保姆。<br>她的名字就是生她的村庄的名字，<br>她是童养媳，<br>大堰河，是我的保姆。<br>我是地主的儿子；<br>也是吃了大堰河的奶而长大了的<br>大堰河的儿子。<br>大堰河以养育我而养育她的家，<br>而我，是吃了你的奶而被养育了的，<br>大堰河啊，我的保姆。<br>大堰河，今天我看到雪使我想起了你：<br>你的被雪压着的草盖的坟墓，<br>你的关闭了的故居檐头的枯死的瓦菲，<br>你的被典押了的一丈平方的园地，<br>你的门前的长了青苔的石椅，<br>大堰河，今天我看到雪使我想起了你。<br>你用你厚大的手掌把我抱在怀里，抚摸我；<br>在你搭好了灶火之后，<br>在你拍去了围裙上的炭灰之后，<br>在你尝到饭已煮熟了之后，<br>在你把乌黑的酱碗放到乌黑的桌子上之后，<br>在你补好了儿子们的为山腰的荆棘扯破的衣服之后，<br>在你把小儿被柴刀砍伤了的手包好之后，<br>在你把夫儿们的衬衣上的虱子一颗颗地掐死之后，<br>在你拿起了今天的第一颗鸡蛋之后，<br>你用你厚大的手掌把我抱在怀里，抚摸我。<br>我是地主的儿子，<br>在我吃光了你大堰河的奶之后，<br>我被生我的父母领回到自己的家里。<br>啊，大堰河，你为什么要哭？<br>我做了生我的父母家里的新客了！<br>我摸着红漆雕花的家具，<br>我摸着父母的睡床上金色的花纹，<br>我呆呆地看着檐头的我不认得的“天伦叙乐”的匾，<br>我摸着新换上的衣服的丝的和贝壳的纽扣，<br>我看着母亲怀里的不熟识的妹妹，<br>我坐着油漆过的安了火钵的炕凳，<br>我吃着碾了三番的白米的饭，<br>但，我是这般忸怩（niǔní）不安！因为我<br>我做了生我的父母家里的新客了。<br>大堰河，为了生活，<br>在她流尽了她的乳汁之后，<br>她就开始用抱过我的两臂劳动了；<br>她含着笑，洗着我们的衣服，<br>她含着笑，提着菜篮到村边的结冰的池塘去，<br>她含着笑，切着冰屑悉索的萝卜，<br>她含着笑，用手掏着猪吃的麦糟，<br>她含着笑，扇着炖肉的炉子的火，<br>她含着笑，背了团箕到广场上去，<br>晒好那些大豆和小麦，<br>大堰河，为了生活，<br>在她流尽了她的乳液之后，<br>她就用抱过我的两臂，劳动了。<br>大堰河，深爱着她的乳儿；<br>在年节里，为了他，忙着切那冬米的糖，<br>为了他，常悄悄地走到村边的她的家里去，<br>为了他，走到她的身边叫一声“妈”，<br>大堰河，把他画的大红大绿的关云长<br>贴在灶边的墙上，<br>大堰河，会对她的邻居夸口赞美她的乳儿；<br>大堰河曾做了一个不能对人说的梦：<br>在梦里，她吃着她的乳儿的婚酒，<br>坐在辉煌的结彩的堂上，<br>而她的娇美的媳妇亲切的叫她“婆婆”<br>。…..<br>大堰河，深爱着她的乳儿！<br>大堰河，在她的梦没有做醒的时候已死了。<br>她死时，乳儿不在她的旁侧，<br>她死时，平时打骂她的丈夫也为她流泪，<br>五个儿子，个个哭得很悲，<br>她死时，轻轻地呼着她的乳儿的名字，<br>大堰河，已死了，<br>她死时，乳儿不在她的旁侧。<br>大堰河，含泪的去了！<br>同着四十几年的人世生活的凌侮，<br>同着数不尽的奴隶的凄苦，<br>同着四块钱的棺材和几束稻草，<br>同着几尺长方的埋棺材的土地，<br>同着一手把的纸钱的灰，<br>大堰河，她含泪的去了。<br>这是大堰河所不知道的：<br>她的醉酒的丈夫已死去，<br>大儿做了土匪，<br>第二个死在炮火的烟里，<br>第三，第四，第五<br>在师傅和地主的叱骂声里过着日子。<br>而我，我是在写着给予这不公道的世界的咒语。<br>当我经了长长的漂泊回到故土时，<br>在山腰里，田野上，<br>兄弟们碰见时，是比六七年前更要亲密！<br>这，这是为你，静静地睡着的大堰河<br>所不知道的啊！<br>大堰河，今天，你的乳儿是在狱里，<br>写着一首呈给你的赞美诗，<br>呈给你黄土下紫色的灵魂，<br>呈给你拥抱过我的直伸着的手，<br>呈给你吻过我的唇，<br>呈给你泥黑的温柔的脸颜，<br>呈给你养育了我的乳房，<br>呈给你的儿子们，我的兄弟们，<br>呈给大地上一切的，<br>我的大堰河般的保姆和她们的儿子，<br>呈给爱我如爱她自己的儿子般的大堰河。<br>大堰河，<br>我是吃了你的奶而长大了的<br>你的儿子，<br>我敬你<br>爱你！<br>一九三三年一月十四日，雪</p><p>《大堰河——我的保姆》是现代诗人<a href="https://baike.baidu.com/item/%E8%89%BE%E9%9D%92/173395?fromModule=lemma_inlink">艾青</a>的诗作。此诗以诗人幼年生活为背景，通过对诗人乳母大堰河的回忆与追思，集中描述了大堰河一生的悲苦经历，抒发了诗人对贫苦农妇大堰河及劳动人民的真挚怀念和感激之情，表达了诗人对中国广大劳动妇女的赞美以及对这“不公道的世界”的诅咒，从而激发世人对旧中国劳苦大众悲惨命运的同情以及对旧世界的强烈仇恨。全诗采用散文化的自由句式，不拘泥于句子的长短，不生硬地押韵，显得自然贴切，大量运用洋溢着农村风俗气息的生活细节，使得人物形象生动而真实。</p><h2 id="创作背景"><a href="#创作背景" class="headerlink" title="创作背景"></a>创作背景</h2><p>《大堰河——我的保姆》1933年1月作于狱中。1931年4、5月间，诗人从法国回到上海。不久，参加“左翼美术家联盟”，与<a href="https://baike.baidu.com/item/%E6%B1%9F%E4%B8%B0/27036?fromModule=lemma_inlink">江丰</a>、<a href="https://baike.baidu.com/item/%E5%8A%9B%E6%89%AC/6348446?fromModule=lemma_inlink">力扬</a>等艺术青年，组织了“春地美术研究所”。1932年，诗人被当局逮捕，押在第二特区看守所，后以“宣传与三民主义不相容主义”罪被判入狱6年。他在狱中写下了这首诗。</p><h2 id="作品鉴赏"><a href="#作品鉴赏" class="headerlink" title="作品鉴赏"></a>作品鉴赏</h2><p>《大堰河，我的保姆》是艾青的成名之作。这是一个地主阶级叛逆的儿子献给他的真正母亲——中国大地善良而不幸的普通农妇的颂歌。</p><p>这首诗感情真挚深切。诗中反复陈述：“大堰河，是我的保姆”，诗人是地主的儿子，长在“大堰河”的怀中，吮吸着她的乳汁，这不仅养育了诗人的身体，也养育了诗人的感情。诗人深深领受了她的爱，及至到了上学的年龄离开养母回到亲生父母身边的时候，他感到父母的陌生，更感到养母的对他的重要。养母正直、善良、朴素的品格影响了诗人的一生。这首诗从头到尾，始终围绕“我”与“她”的关系来写，他对大堰河深厚的感情，都表现在娓娓动情的陈述之中，他在监狱里，看见了雪就想到大堰河“被雪压着的草盖的坟墓”，想起她的故居园地，想起她对他的关怀和爱……于是他用他的深情的诗，表现了大堰河的具体劳作情景，也写了她心灵深处的感情波纹，就连她美丽的梦境，也同对乳儿的“幸福命运”的祝愿融合在一起。有了这样的真情，这样的心灵，才使这位劳动妇女形象更加崇高、完美，所以诗人要把热烈的颂扬，“呈给大地上一切的&#x2F;我的大堰河般的保姆和他们的儿子&#x2F;呈给爱我如爱她自己的儿子般的大堰河”。这样就使“大堰河”以某种象征意义，升华为永远与山河、村庄同在的人民的化身，或者说是中国农民的化身。</p><p>艾青在《大堰河，我的保姆》开始表现他诗作的艺术特色，他首先是从“感觉”出发,像印象派画家那么重视感觉和感受，而且注意主观情感对感觉的渗入与融合。并在二者的融合中产生出多层次的联想，创造出既是清晰的,又具有广阔象征意义的视觉形象。诗总是具体的、有着鲜明形象的，如这首诗写大堰河的劳作,写大堰河的笑，写大堰河的爱和死。都呈现可视可感的立体的意象符号附加形容。最后叠句排比句的运用，如“呈给你黄土下紫色的灵魂&#x2F;呈给你拥抱过我的直伸着的手&#x2F;呈给你吻过我的唇。&#x2F;呈给你泥黑的温柔的脸颜&#x2F;呈给你养育我的乳房……”具体的描写，保证语言的形象性，这也是艾青诗的艺术魅力的奥秘所在，他后来的诗作，更自觉地将它发扬光大了。</p><p>这是一首献给保姆大堰河的诗篇。诗人叙述了这位普通中国妇女平凡而坎坷、不幸的一生，表达了对这位伟大母亲由衷的感恩之情。大堰河，也是千千万万中国母亲的代表，正是这片如同慈母一样宽阔的土地和这个伟大的祖国，尽管她受尽欺辱，满身疮痍，历尽沧桑，然而却永远不失母性和母爱伟大的光辉诗歌饱含深情，反复咏唱，如泣如诉。 [4-5]</p><h2 id="作品评价"><a href="#作品评价" class="headerlink" title="作品评价"></a>作品评价</h2><p>现代文学家<a href="https://baike.baidu.com/item/%E8%8C%85%E7%9B%BE/312620?fromModule=lemma_inlink">茅盾</a>：用沉郁的笔调细写了乳娘兼女佣（《大堰河》）的生活痛苦。（《中国现代文学管窥》） [6]</p><p>现代文艺理论家<a href="https://baike.baidu.com/item/%E8%83%A1%E9%A3%8E/866457?fromModule=lemma_inlink">胡风</a>：至于《大堰河——我的保姆》，在这里有了一个用乳汁用母爱喂养别人的孩子，用劳力用忠诚服侍别人的农妇的形象，乳儿的作者用着朴素的真实的言语对这形象呈诉了切切的爱心。在这里他提出了对于‘这不公道的世界’的诅咒，告白了他和被侮辱的兄弟们比以前‘更要亲密’。虽然全篇流着私情地温暖，但他和我们之间已没有了难越的界限了。（《通三统：一种文学史实验》） [7]</p><p>现代诗歌评论家<a href="https://baike.baidu.com/item/%E5%BC%A0%E5%90%8C%E5%90%BE/4553767?fromModule=lemma_inlink">张同吾</a>：它像一颗光华熠熠的新星，出现在30年代的中国诗坛上；它以深沉隽永的情思，在广大读者的心田里镌刻着久远而常新的记忆。（《张同吾文集》） [8]</p><p>现代文学史家<a href="https://baike.baidu.com/item/%E7%8E%8B%E7%91%B6/4051972?fromModule=lemma_inlink">王瑶</a>：《大堰河》一首以真挚的感情刻画了一个勤劳善良的旧中国农村妇女的典型形象，描绘了穷苦悲惨的旧中国农村，暗示着诗人对地主阶级的叛逆和回到农民中去的愿望。（《<a href="https://baike.baidu.com/item/%E4%B8%AD%E5%9B%BD%E6%96%B0%E6%96%87%E5%AD%A6%E5%8F%B2%E7%A8%BF/16513156?fromModule=lemma_inlink">中国新文学史稿</a>》）</p><p>现代文学史家<a href="https://baike.baidu.com/item/%E5%88%98%E7%BB%B6%E6%9D%BE/5674195?fromModule=lemma_inlink">刘绶松</a>：这首诗不仅属于艾青个人，更属于时代与社会。艾青背叛了他的家庭所属的地主阶级，而将自己的思想感情完全献给了中国广大勤劳质朴却深受苦难的劳动人民。（《中国新文学史初稿》）</p><h2 id="后世影响"><a href="#后世影响" class="headerlink" title="后世影响"></a>后世影响</h2><p>《大堰河——我的保姆》被选入2007年人教版高中语文必修1第一单元以及2023年部编版高中语文选择性必修下册第6课。这首诗在艾青诗歌中有重要地位，艾青的第一部诗集就取名《大堰河》。 </p><p>《大堰河——我的保姆》抒写对劳动人民刻骨铭心的感激和热爱，为后来的诗歌提供了启示。大多数的文学史评价赋予了艾青的这首诗极高的地位，认为它是艾青诗歌创作的开端。如《中国现代文学三十年》中评价道，艾青的“芦笛”吹出的第一首歌便是给乳母大堰河的赞歌，作为起点，它密切联系着我们民族多灾多难的土地与人民。</p><h2 id="作者简介"><a href="#作者简介" class="headerlink" title="作者简介"></a>作者简介</h2><p><img src="https://picture-base.oss-cn-hangzhou.aliyuncs.com/image-20250101201926204.png" alt="image-20250101201926204"></p><p>艾青（1910—1996），中国现代诗人。原名蒋海澄，曾用笔名莪伽、克阿、纳雍、林壁等。浙江金华人。被认为是中国<a href="https://baike.baidu.com/item/%E6%96%B0%E8%AF%97/97194?fromModule=lemma_inlink">新诗</a>史上产生过重要影响，具有独特风格的现实主义诗人。成名作《大堰河——我的保姆》奠定了他诗歌的基本艺术特征和他在现代文学史上的重要地位。其作品被译成几十种文字，著有《大堰河》《北方》《向太阳》《黎明的通知》等诗集。 [12]</p><p><img src="https://picture-base.oss-cn-hangzhou.aliyuncs.com/image-20250101202157371.png" alt="image-20250101202157371"></p><p><img src="https://picture-base.oss-cn-hangzhou.aliyuncs.com/image-20250101202225640.png" alt="image-20250101202225640"></p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>2024-12-09 docker daemon.json</title>
    <link href="/2024/12/10/2024-12-09%20docker%20daemon.json/"/>
    <url>/2024/12/10/2024-12-09%20docker%20daemon.json/</url>
    
    <content type="html"><![CDATA[<p>在国内使用 Docker 的朋友们，可能都遇到过配置镜像源来加速镜像拉取的操作。然而，最近几个月发现许多曾经常用的国内镜像站（包括各种云服务商和高校镜像站）已经无法使用。因此，本人开始搜索并汇总了目前可用的镜像站和镜像加速地址，并计划定期测试它们的可用性，并更新这个列表。如果您知道新的可用站点，也欢迎随时补充哦！</p><h1 id="Docker-镜像加速列表（20241202已更新）"><a href="#Docker-镜像加速列表（20241202已更新）" class="headerlink" title="Docker 镜像加速列表（20241202已更新）"></a>Docker 镜像加速列表（20241202已更新）</h1><figure class="highlight prolog"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br></pre></td><td class="code"><pre><code class="hljs prolog">cat &gt; /etc/docker/daemon.json &lt;&lt; <span class="hljs-symbol">EOF</span><br>&#123;<br>  <span class="hljs-string">&quot;registry-mirrors&quot;</span>: [<span class="hljs-string">&quot;https://docker.m.daocloud.io&quot;</span>,<br>                       <span class="hljs-string">&quot;https://hub.roker.org&quot;</span>,<br>                       <span class="hljs-string">&quot;https://si7y70hh.mirror.aliyuncs.com&quot;</span>,<br>                       <span class="hljs-string">&quot;https://registry.docker-cn.com&quot;</span>,<br>                       <span class="hljs-string">&quot;https://hub.geekery.cn&quot;</span>,<br>                       <span class="hljs-string">&quot;https://hub.littlediary.cn&quot;</span>,<br>                       <span class="hljs-string">&quot;https://docker.rainbond.cc&quot;</span>,<br>                       <span class="hljs-string">&quot;https://docker.unsee.tech&quot;</span>,<br>                       <span class="hljs-string">&quot;https://hub.crdz.gq&quot;</span>,<br>                       <span class="hljs-string">&quot;https://docker.nastool.de&quot;</span>,<br>                       <span class="hljs-string">&quot;https://hub.firefly.store&quot;</span>,<br>                       <span class="hljs-string">&quot;https://registry.dockermirror.com&quot;</span>,<br>                       <span class="hljs-string">&quot;https://docker.1panelproxy.com&quot;</span>,<br>                       <span class="hljs-string">&quot;https://rhub.rat.dev&quot;</span>,<br>                       <span class="hljs-string">&quot;https://docker.kejilion.pro&quot;</span>,<br>                       <span class="hljs-string">&quot;https://dhub.kubesre.xyz&quot;</span>,<br>                       <span class="hljs-string">&quot;https://docker.1panel.live&quot;</span>,<br>                       <span class="hljs-string">&quot;https://dockerpull.org&quot;</span>,<br>                       <span class="hljs-string">&quot;https://docker.udayun.com&quot;</span>,<br>                       <span class="hljs-string">&quot;https://docker.hlmirror.com&quot;</span>,<br>                       <span class="hljs-string">&quot;https://docker.mirrors.ustc.edu.cn&quot;</span>,<br>                       <span class="hljs-string">&quot;http://hub-mirror.c.163.com&quot;</span>,<br>                       <span class="hljs-string">&quot;https://dockerhub.azk8s.cn&quot;</span>,<br>                       <span class="hljs-string">&quot;https://hub.uuuadc.top&quot;</span>,<br>                       <span class="hljs-string">&quot;https://docker.anyhub.us.kg&quot;</span>,<br>                       <span class="hljs-string">&quot;https://dockerhub.jobcher.com&quot;</span>,<br>                       <span class="hljs-string">&quot;https://dockerhub.icu&quot;</span>,<br>                       <span class="hljs-string">&quot;https://cloudsx.top&quot;</span>,<br>                       <span class="hljs-string">&quot;https://docker.ckyl.me&quot;</span>,<br>                       <span class="hljs-string">&quot;https://docker.awsl9527.cn&quot;</span>,<br>                       <span class="hljs-string">&quot;https://docker.chenby.cn&quot;</span>,<br>                       <span class="hljs-string">&quot;https://docker.hpcloud.cloud&quot;</span>,<br>                       <span class="hljs-string">&quot;https://atomhub.openatom.cn&quot;</span>],<br>  <span class="hljs-string">&quot;exec-opts&quot;</span>: [<span class="hljs-string">&quot;native.cgroupdriver=systemd&quot;</span>],<br>  <span class="hljs-string">&quot;insecure-registries&quot;</span>: [<span class="hljs-string">&quot;http://192.168.200.250&quot;</span>],<br>  <span class="hljs-string">&quot;log-driver&quot;</span>: <span class="hljs-string">&quot;json-file&quot;</span>,<br>  <span class="hljs-string">&quot;log-opts&quot;</span>: &#123;<br>    <span class="hljs-string">&quot;max-size&quot;</span>: <span class="hljs-string">&quot;1000m&quot;</span>,<span class="hljs-string">&quot;max-file&quot;</span>: <span class="hljs-string">&quot;6&quot;</span><br>  &#125;,<br>  <span class="hljs-string">&quot;storage-driver&quot;</span>: <span class="hljs-string">&quot;overlay2&quot;</span><br>&#125;<br><span class="hljs-symbol">EOF</span><br></code></pre></td></tr></table></figure><blockquote><p>请注意！有些镜像站仅提供基础镜像或白名单镜像，如果某个加速地址无法拉取到所需的镜像，可以尝试切换到其他地址。有些代理站点是热心网友自费搭建的，请务必合理使用。如果侵犯了您的权益，请随时联系我，我会及时删除相关信息。感谢您的理解与支持！</p></blockquote><table><thead><tr><th>DockerHub 镜像仓库</th><th>是否正常</th></tr></thead><tbody><tr><td><code>hub.geekery.cn</code></td><td>正常</td></tr><tr><td><code>hub.littlediary.cn</code></td><td>正常</td></tr><tr><td><code>docker.rainbond.cc</code></td><td>正常</td></tr><tr><td><code>docker.unsee.tech</code></td><td>正常</td></tr><tr><td><code>docker.m.daocloud.io</code></td><td>正常</td></tr><tr><td><code>hub.crdz.gq</code></td><td>正常</td></tr><tr><td><code>docker.nastool.de</code></td><td>正常</td></tr><tr><td><code>hub.firefly.store</code></td><td>正常</td></tr><tr><td><code>registry.dockermirror.com</code></td><td>正常</td></tr><tr><td><code>docker.1panelproxy.com</code></td><td>正常</td></tr><tr><td><code>hub.rat.dev</code></td><td>正常</td></tr><tr><td><code>docker.udayun.com</code></td><td>正常</td></tr><tr><td><code>docker.kejilion.pro</code></td><td>正常</td></tr><tr><td><code>dhub.kubesre.xyz</code></td><td>正常</td></tr><tr><td><code>docker.1panel.live</code></td><td>正常</td></tr><tr><td><code>dockerpull.org</code></td><td>正常</td></tr><tr><td><code>docker.hlmirror.com</code></td><td>新增</td></tr><tr><td><code>hub.xdark.top</code></td><td>失效</td></tr><tr><td><code>noohub.ru</code></td><td>失效</td></tr><tr><td><code>freeno.xyz</code></td><td>失效</td></tr><tr><td><code>docker-cf.registry.cyou</code></td><td>失效</td></tr><tr><td><code>dockerpull.com</code></td><td>失效</td></tr><tr><td><code>lynn520.xyz</code></td><td>失效</td></tr><tr><td><code>hub.yuzuha.cc</code></td><td>失效</td></tr><tr><td><code>ccr.ccs.tencentyun.com</code></td><td>失效</td></tr><tr><td><code>docker.chenby.cn</code></td><td>失效</td></tr><tr><td><code>docker.linkedbus.com</code></td><td>失效</td></tr><tr><td><code>docker.hlyun.org</code></td><td>失效</td></tr><tr><td><code>dockerproxy.cn</code></td><td>失效</td></tr><tr><td><code>docker.registry.cyou</code></td><td>失效</td></tr><tr><td><code>dockerproxy.com</code></td><td>失效</td></tr><tr><td><code>ginger20240704.asia</code></td><td>失效</td></tr><tr><td><code>hub.docker-ttc.xyz</code></td><td>失效</td></tr><tr><td><code>docker.mrxn.net</code></td><td>失效</td></tr><tr><td><code>docker.wget.at</code></td><td>失效</td></tr><tr><td><code>dislabaiot.xyz</code></td><td>失效</td></tr><tr><td><code>docker.hpcloud.cloud</code></td><td>失效</td></tr><tr><td><code>atomhub.openatom.cn</code></td><td>失效</td></tr><tr><td><code>docker.nat.tf</code></td><td>失效</td></tr><tr><td><code>doublezonline.cloud</code></td><td>失效</td></tr><tr><td><code>dockerproxy.github.io</code></td><td>失效</td></tr></tbody></table><h1 id="配置方式1：临时使用"><a href="#配置方式1：临时使用" class="headerlink" title="配置方式1：临时使用"></a>配置方式1：临时使用</h1><p>直接使用，直接拿镜像域名拼接上官方镜像名，例如要拉去镜像 <code>istio/distroless</code>，可以用下面写法（不要带 <code>https://</code>）</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell">docker pull docker.unsee.tech/istio/distroless<br></code></pre></td></tr></table></figure><h1 id="配置方式2：长久有效"><a href="#配置方式2：长久有效" class="headerlink" title="配置方式2：长久有效"></a>配置方式2：长久有效</h1><blockquote><p>Ubuntu 16.04+、Debian 8+、CentOS 7+</p></blockquote><p>修改文件 <code>/etc/docker/daemon.json</code>（如果不存在则需要创建创建，注意不要写入中文，要带 <code>https://</code>），并重启服务。</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><code class="hljs shell"><span class="hljs-meta prompt_"># </span><span class="language-bash">创建目录</span><br>sudo mkdir -p /etc/docker<br><span class="hljs-meta prompt_"></span><br><span class="hljs-meta prompt_"># </span><span class="language-bash">写入配置文件</span><br>sudo tee /etc/docker/daemon.json &lt;&lt;-&#x27;EOF&#x27;<br>&#123;<br>    &quot;registry-mirrors&quot;: [<br>    &quot;https://docker.unsee.tech&quot;,<br>        &quot;https://dockerpull.org&quot;,<br>        &quot;docker.1panel.live&quot;,<br>        &quot;https://dockerhub.icu&quot;<br>    ]<br>&#125;<br>EOF<br><span class="hljs-meta prompt_"></span><br><span class="hljs-meta prompt_"># </span><span class="language-bash">重启docker服务</span><br>sudo systemctl daemon-reload &amp;&amp; sudo systemctl restart docker<br></code></pre></td></tr></table></figure><p>可直接使用 <code>docker pull</code>拉去镜像进行测试：</p><p><img src="https://picture-base.oss-cn-hangzhou.aliyuncs.com/202411011436681.png" alt="img"></p><p>或用以下命令检查是否生效：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell">ping -c 3 docker.unsee.tech<br></code></pre></td></tr></table></figure><p><img src="https://picture-base.oss-cn-hangzhou.aliyuncs.com/202411011438549.png" alt="img"></p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>2024-12-09 helmchart 部署flask应用</title>
    <link href="/2024/12/10/2024-12-09%20helmchart%20%E9%83%A8%E7%BD%B2flask%E5%BA%94%E7%94%A8/"/>
    <url>/2024/12/10/2024-12-09%20helmchart%20%E9%83%A8%E7%BD%B2flask%E5%BA%94%E7%94%A8/</url>
    
    <content type="html"><![CDATA[<p>要使用 Helm 部署一个 Flask 应用，你需要进行几个步骤，主要包括：</p><ol><li><strong>创建一个 Flask 应用</strong>。</li><li><strong>编写 Dockerfile</strong> 来将 Flask 应用容器化。</li><li><strong>推送容器镜像</strong> 到容器镜像仓库。</li><li><strong>创建 Helm Chart</strong> 来描述如何部署应用。</li></ol><h3 id="步骤-1-创建一个简单的-Flask-应用"><a href="#步骤-1-创建一个简单的-Flask-应用" class="headerlink" title="步骤 1: 创建一个简单的 Flask 应用"></a>步骤 1: 创建一个简单的 Flask 应用</h3><p>首先，确保你已经创建了一个简单的 Flask 应用。例如：</p><p><code>app.py</code>：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-keyword">from</span> flask <span class="hljs-keyword">import</span> Flask<br><br>app = Flask(__name__)<br><br><span class="hljs-meta">@app.route(<span class="hljs-params"><span class="hljs-string">&#x27;/&#x27;</span></span>)</span><br><span class="hljs-keyword">def</span> <span class="hljs-title function_">hello_world</span>():<br>    <span class="hljs-keyword">return</span> <span class="hljs-string">&quot;Hello, World!&quot;</span><br><br><span class="hljs-keyword">if</span> __name__ == <span class="hljs-string">&#x27;__main__&#x27;</span>:<br>    app.run(host=<span class="hljs-string">&#x27;0.0.0.0&#x27;</span>, port=<span class="hljs-number">5000</span>)<br></code></pre></td></tr></table></figure><h3 id="步骤-2-编写-Dockerfile"><a href="#步骤-2-编写-Dockerfile" class="headerlink" title="步骤 2: 编写 Dockerfile"></a>步骤 2: 编写 Dockerfile</h3><p>然后，为你的 Flask 应用创建一个 Dockerfile，将 Flask 应用容器化。假设你将 Flask 应用保存为 <code>app.py</code>。</p><p><code>Dockerfile</code>：</p><figure class="highlight dockerfile"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><code class="hljs Dockerfile"><span class="hljs-comment"># 使用官方 Python 镜像</span><br><span class="hljs-keyword">FROM</span> python:<span class="hljs-number">3.9</span>-slim<br><br><span class="hljs-comment"># 设置工作目录</span><br><span class="hljs-keyword">WORKDIR</span><span class="language-bash"> /app</span><br><br><span class="hljs-comment"># 安装 Flask</span><br><span class="hljs-keyword">COPY</span><span class="language-bash"> requirements.txt /app/</span><br><span class="hljs-keyword">RUN</span><span class="language-bash"> pip install --no-cache-dir -r requirements.txt</span><br><span class="hljs-keyword">RUN</span><span class="language-bash"> pip install flask werkzeug==1.0.1</span><br><br><br><span class="hljs-comment"># 复制 Flask 应用</span><br><span class="hljs-keyword">COPY</span><span class="language-bash"> . /app</span><br><br><span class="hljs-comment"># 运行 Flask 应用</span><br><span class="hljs-keyword">CMD</span><span class="language-bash"> [<span class="hljs-string">&quot;python&quot;</span>, <span class="hljs-string">&quot;app.py&quot;</span>]</span><br></code></pre></td></tr></table></figure><p>你需要创建 <code>requirements.txt</code> 文件来列出依赖项：</p><p><code>requirements.txt</code>：</p><figure class="highlight abnf"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs abnf"><span class="hljs-attribute">Flask</span><span class="hljs-operator">=</span><span class="hljs-operator">=</span><span class="hljs-number">2.0</span>.<span class="hljs-number">2</span><br></code></pre></td></tr></table></figure><h3 id="步骤-3-构建并推送-Docker-镜像"><a href="#步骤-3-构建并推送-Docker-镜像" class="headerlink" title="步骤 3: 构建并推送 Docker 镜像"></a>步骤 3: 构建并推送 Docker 镜像</h3><p>使用以下命令构建 Docker 镜像：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs bash">docker build -t 547475331/flask-app:latest .<br><br>docker tag <br><br><br>docker push 547475331/flasktest:latest<br></code></pre></td></tr></table></figure><p>然后将镜像推送到 Docker 仓库（如 Docker Hub 或其他容器镜像仓库）：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">docker push your-dockerhub-username/flask-app:latest<br></code></pre></td></tr></table></figure><p>在 Ubuntu 上安装 Helm 的步骤如下：</p><h3 id="步骤-1-更新系统的包管理器索引"><a href="#步骤-1-更新系统的包管理器索引" class="headerlink" title="步骤 1: 更新系统的包管理器索引"></a>步骤 1: 更新系统的包管理器索引</h3><p>首先，确保你的包管理器索引是最新的：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> apt-get update<br></code></pre></td></tr></table></figure><h3 id="步骤-2-安装-Helm-依赖项"><a href="#步骤-2-安装-Helm-依赖项" class="headerlink" title="步骤 2: 安装 Helm 依赖项"></a>步骤 2: 安装 Helm 依赖项</h3><p>你需要确保系统上安装了 <code>curl</code> 和 <code>apt-transport-https</code>，这些工具用于下载 Helm 安装脚本和管理 APT 仓库。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> apt-get install curl apt-transport-https<br></code></pre></td></tr></table></figure><h3 id="步骤-3-添加-Helm-的官方-APT-仓库"><a href="#步骤-3-添加-Helm-的官方-APT-仓库" class="headerlink" title="步骤 3: 添加 Helm 的官方 APT 仓库"></a>步骤 3: 添加 Helm 的官方 APT 仓库</h3><p>添加 Helm 的官方 APT 仓库的 GPG 密钥：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">curl https://baltocdn.com/helm/signing.asc | <span class="hljs-built_in">sudo</span> apt-key add -<br></code></pre></td></tr></table></figure><p>然后，将 Helm 仓库添加到系统的 APT 源列表中：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> apt-get install -y software-properties-common<br><span class="hljs-built_in">sudo</span> add-apt-repository <span class="hljs-string">&quot;deb https://baltocdn.com/helm/stable/debian/ all main&quot;</span><br></code></pre></td></tr></table></figure><h3 id="步骤-4-安装-Helm"><a href="#步骤-4-安装-Helm" class="headerlink" title="步骤 4: 安装 Helm"></a>步骤 4: 安装 Helm</h3><p>更新包管理器的索引：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> apt-get update<br></code></pre></td></tr></table></figure><p>然后，安装 Helm：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> apt-get install helm<br></code></pre></td></tr></table></figure><h3 id="步骤-5-验证-Helm-安装"><a href="#步骤-5-验证-Helm-安装" class="headerlink" title="步骤 5: 验证 Helm 安装"></a>步骤 5: 验证 Helm 安装</h3><p>安装完成后，你可以通过以下命令验证 Helm 是否安装成功：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">helm version<br></code></pre></td></tr></table></figure><p>如果 Helm 安装成功，你应该看到类似如下的输出：</p><figure class="highlight crmsh"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs crmsh"><span class="hljs-keyword">version</span>.Build<span class="hljs-literal">Inf</span>o&#123;<span class="hljs-keyword">Version</span>:<span class="hljs-string">&quot;v3.9.0&quot;</span>, GitCommit:<span class="hljs-string">&quot;abcdef123456&quot;</span>, GitTreeState:<span class="hljs-string">&quot;clean&quot;</span>, GoVersion:<span class="hljs-string">&quot;go1.16.7&quot;</span>&#125;<br></code></pre></td></tr></table></figure><h3 id="步骤-6-可选-配置-Helm-仓库"><a href="#步骤-6-可选-配置-Helm-仓库" class="headerlink" title="步骤 6: 可选 - 配置 Helm 仓库"></a>步骤 6: 可选 - 配置 Helm 仓库</h3><p>你可以使用以下命令添加 Helm 仓库：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">helm repo add stable https://charts.helm.sh/stable<br></code></pre></td></tr></table></figure><p>然后更新你的 Helm 仓库列表：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">helm repo update<br></code></pre></td></tr></table></figure><h3 id="完成"><a href="#完成" class="headerlink" title="完成"></a>完成</h3><p>现在，Helm 应该已成功安装并配置完毕。你可以开始使用 Helm 部署 Kubernetes 应用、管理 Helm Charts 等。如果你有其他问题或遇到困难，欢迎随时提问！</p><h3 id="步骤-4-创建-Helm-Chart"><a href="#步骤-4-创建-Helm-Chart" class="headerlink" title="步骤 4: 创建 Helm Chart"></a>步骤 4: 创建 Helm Chart</h3><p>接下来，使用 Helm 创建一个 Chart 来部署 Flask 应用。假设你已经安装了 Helm。</p><ol><li><strong>创建一个新的 Helm Chart：</strong></li></ol><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">helm create flask-app<br></code></pre></td></tr></table></figure><p>这将创建一个名为 <code>flask-app</code> 的文件夹，并且包含了一些 Helm 默认的文件和模板。</p><ol><li><strong>编辑 Helm Chart 配置：</strong></li></ol><p>打开 <code>flask-app/values.yaml</code>，并修改以下内容：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs yaml"><span class="hljs-comment"># 修改镜像的名称和标签</span><br><span class="hljs-attr">image:</span><br>  <span class="hljs-attr">repository:</span> <span class="hljs-string">your-dockerhub-username/flask-app</span><br>  <span class="hljs-attr">tag:</span> <span class="hljs-string">latest</span><br>  <span class="hljs-attr">pullPolicy:</span> <span class="hljs-string">IfNotPresent</span><br><br><span class="hljs-comment"># 修改服务端口</span><br><span class="hljs-attr">service:</span><br>  <span class="hljs-attr">port:</span> <span class="hljs-number">80</span><br></code></pre></td></tr></table></figure><ol><li><strong>修改 <code>flask-app/templates/deployment.yaml</code>：</strong></li></ol><p>确保 <code>deployment.yaml</code> 中的容器端口与 Flask 应用的端口匹配（Flask 默认是 5000），但因为我们通常希望将它暴露在 80 端口上，记得在服务的配置中进行修改。</p><p>编辑 <code>flask-app/templates/deployment.yaml</code> 中的 <code>containerPort</code>：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs yaml"><span class="hljs-attr">ports:</span><br>  <span class="hljs-bullet">-</span> <span class="hljs-attr">containerPort:</span> <span class="hljs-number">5000</span><br></code></pre></td></tr></table></figure><h3 id="步骤-5-部署-Flask-应用"><a href="#步骤-5-部署-Flask-应用" class="headerlink" title="步骤 5: 部署 Flask 应用"></a>步骤 5: 部署 Flask 应用</h3><p>使用以下命令安装 Helm Chart：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">helm install flask-app ./flask-app<br></code></pre></td></tr></table></figure><p>这将使用你刚刚创建的 Helm Chart 部署 Flask 应用。</p><h3 id="步骤-6-检查部署状态"><a href="#步骤-6-检查部署状态" class="headerlink" title="步骤 6: 检查部署状态"></a>步骤 6: 检查部署状态</h3><p>你可以使用以下命令检查部署的状态：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">kubectl get pods<br></code></pre></td></tr></table></figure><p>查看服务的暴露方式，确保你可以通过外部访问到 Flask 应用。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">kubectl get svc<br></code></pre></td></tr></table></figure><p>你应该能够看到一个服务正在监听 HTTP 流量，并且你的 Flask 应用已经成功部署在 Kubernetes 集群中。</p><hr><p>这样，你就通过 Helm 部署了一个简单的 Flask 应用。如果你有其他特定的需求或者遇到问题，欢迎继续询问！</p><p>是的，你的 Dockerfile 基本上已经正确地设置了 Python 和 Flask 应用的运行环境，并且通过显式安装指定版本的 <code>werkzeug</code>（1.0.1）来解决兼容性问题。不过，还有一些优化和改进可以让 Dockerfile 更加高效和标准化。</p><p>以下是优化后的 Dockerfile：</p><h3 id="优化后的-Dockerfile"><a href="#优化后的-Dockerfile" class="headerlink" title="优化后的 Dockerfile"></a>优化后的 Dockerfile</h3><figure class="highlight dockerfile"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><code class="hljs dockerfile"><span class="hljs-comment"># 使用官方 Python 3.9-slim 镜像作为基础镜像</span><br><span class="hljs-keyword">FROM</span> python:<span class="hljs-number">3.9</span>-slim<br><br><span class="hljs-comment"># 设置工作目录</span><br><span class="hljs-keyword">WORKDIR</span><span class="language-bash"> /app</span><br><br><span class="hljs-comment"># 复制依赖文件（优先复制 requirements.txt，利用 Docker 缓存机制）</span><br><span class="hljs-keyword">COPY</span><span class="language-bash"> requirements.txt /app/</span><br><br><span class="hljs-comment"># 安装依赖（显式安装 werkzeug 的特定版本）</span><br><span class="hljs-keyword">RUN</span><span class="language-bash"> pip install --no-cache-dir -r requirements.txt &amp;&amp; \</span><br><span class="language-bash">    pip install werkzeug==1.0.1</span><br>    <br><span class="hljs-keyword">RUN</span><span class="language-bash"> apt-get update &amp;&amp; apt-get install -y curl</span><br><br><span class="hljs-comment"># 复制应用代码到工作目录</span><br><span class="hljs-keyword">COPY</span><span class="language-bash"> . /app</span><br><br><span class="hljs-comment"># 暴露应用端口（可选，通常 Flask 默认运行在 5000 端口）</span><br><span class="hljs-keyword">EXPOSE</span> <span class="hljs-number">5000</span><br><br><span class="hljs-comment"># 运行 Flask 应用</span><br><span class="hljs-keyword">CMD</span><span class="language-bash"> [<span class="hljs-string">&quot;python&quot;</span>, <span class="hljs-string">&quot;app.py&quot;</span>]</span><br></code></pre></td></tr></table></figure><h3 id="改进点说明"><a href="#改进点说明" class="headerlink" title="改进点说明"></a>改进点说明</h3><ol><li><strong>缓存优化</strong><br> 将 <code>COPY requirements.txt /app/</code> 和 <code>RUN pip install</code> 单独放在一起，这样如果应用代码（比如 <code>.py</code> 文件）变更而依赖未改变时，Docker 构建可以重用缓存，避免重复安装依赖。</li><li><strong>明确暴露端口</strong><br> 使用 <code>EXPOSE 5000</code> 使镜像默认说明 Flask 应用监听的端口。虽然不强制，但有助于文档化容器的用途。</li><li><strong>合并安装命令</strong><br> 在安装 <code>requirements.txt</code> 和单独安装 <code>werkzeug</code> 时，合并为一条命令，减少镜像层数并提高构建效率。</li></ol><h3 id="构建和运行步骤"><a href="#构建和运行步骤" class="headerlink" title="构建和运行步骤"></a>构建和运行步骤</h3><h4 id="1-构建镜像"><a href="#1-构建镜像" class="headerlink" title="1. 构建镜像"></a>1. 构建镜像</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">docker build -t flask-app .<br></code></pre></td></tr></table></figure><h4 id="2-运行容器"><a href="#2-运行容器" class="headerlink" title="2. 运行容器"></a>2. 运行容器</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">docker run -d -p 5000:5000 flask-app<br></code></pre></td></tr></table></figure><h3 id="注意事项"><a href="#注意事项" class="headerlink" title="注意事项"></a>注意事项</h3><ol><li><p><strong>检查 <code>requirements.txt</code> 文件</strong><br> 确保 <code>requirements.txt</code> 中没有指定 <code>werkzeug</code> 的其他版本。否则可能会覆盖 <code>werkzeug==1.0.1</code> 的安装。</p><p>示例 <code>requirements.txt</code>：</p><figure class="highlight abnf"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs abnf"><span class="hljs-attribute">flask</span><span class="hljs-operator">=</span><span class="hljs-operator">=</span><span class="hljs-number">2.0</span>.<span class="hljs-number">3</span><br><span class="hljs-attribute">some-other-library</span><span class="hljs-operator">=</span><span class="hljs-operator">=</span><span class="hljs-number">1.2</span>.<span class="hljs-number">3</span><br></code></pre></td></tr></table></figure></li><li><p><strong>代码文件结构</strong><br> 确保你的 Flask 应用文件夹结构类似以下内容：</p><figure class="highlight stata"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs stata"><span class="hljs-keyword">app</span>/<br>├── <span class="hljs-keyword">app</span>.py<br>├── requirements.txt<br>└── templates/<br></code></pre></td></tr></table></figure></li><li><p><strong>运行测试</strong><br> 在容器内验证应用是否正常工作：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">docker <span class="hljs-built_in">exec</span> -it &lt;container-id&gt; bash<br>curl http://localhost:5000<br></code></pre></td></tr></table></figure></li></ol><p>按照优化后的 Dockerfile 和步骤重新构建并运行容器，应该能够顺利启动你的 Flask 应用！如果仍然有问题，可以随时提供更多信息，我会继续协助你排查。</p><p>根据报错信息，<code>werkzeug</code> 的版本可能与 Flask 不兼容。<code>ContextVar</code> 是较新的 <code>werkzeug</code> 中引入的模块，而你的镜像中可能安装了一个旧版本的 <code>werkzeug</code>。</p><h3 id="解决方法："><a href="#解决方法：" class="headerlink" title="解决方法："></a>解决方法：</h3><ol><li><p><strong>检查并更新 <code>werkzeug</code> 版本</strong><br> 确保你的 Flask 应用依赖的 <code>werkzeug</code> 版本是兼容的。</p><p>编辑 <code>requirements.txt</code> 文件，添加或更新如下内容：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs plaintext">flask==2.1.3<br>werkzeug&gt;=2.1.0<br></code></pre></td></tr></table></figure><p>根据 Flask 的版本更新 <code>werkzeug</code>，这里假设使用 Flask 2.x 版本。</p></li><li><p><strong>重建 Docker 镜像</strong><br> 在更新 <code>requirements.txt</code> 后，重新构建镜像：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">docker build -t flask-app .<br></code></pre></td></tr></table></figure></li><li><p><strong>验证更新后的镜像</strong><br> 运行容器：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">docker run -d -p 5000:5000 flask-app<br>docker logs -f &lt;container-id&gt;<br></code></pre></td></tr></table></figure></li></ol><h3 id="其他排查"><a href="#其他排查" class="headerlink" title="其他排查"></a>其他排查</h3><ul><li><p>如果问题依然存在，可以检查 <code>werkzeug</code> 版本：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">docker run -it flask-app python -c <span class="hljs-string">&quot;import werkzeug; print(werkzeug.__version__)&quot;</span><br></code></pre></td></tr></table></figure><p>确认是否是预期的版本。</p></li><li><p>如果无法解决，可以直接尝试安装一个具体的兼容版本：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">pip install werkzeug==2.1.2<br></code></pre></td></tr></table></figure></li></ul><p>试试这些步骤，看是否能解决问题！</p><p>你的 Dockerfile 结构是正确的，不过可以稍微优化一些细节：</p><ol><li>你可以将 <code>apt-get</code> 安装命令与 Python 包安装合并为一个层，这样可以减少镜像层数，提高构建效率。</li><li>在安装 <code>curl</code> 之前，如果是基于 <code>python:3.9-slim</code> 镜像，最好先安装 <code>apt-transport-https</code>，确保 <code>apt</code> 能正常使用。</li><li>可以在安装 Python 依赖时，使用 <code>--no-cache-dir</code>，避免缓存安装包，使镜像更小。</li></ol><p>以下是改进后的 Dockerfile：</p><figure class="highlight dockerfile"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><code class="hljs dockerfile"><span class="hljs-comment"># 使用官方 Python 3.9-slim 镜像作为基础镜像</span><br><span class="hljs-keyword">FROM</span> python:<span class="hljs-number">3.9</span>-slim<br><br><span class="hljs-comment"># 设置工作目录</span><br><span class="hljs-keyword">WORKDIR</span><span class="language-bash"> /app</span><br><br><span class="hljs-comment"># 安装系统依赖，包含 curl 和 apt-transport-https</span><br><span class="hljs-keyword">RUN</span><span class="language-bash"> apt-get update &amp;&amp; apt-get install -y \</span><br><span class="language-bash">    apt-transport-https \</span><br><span class="language-bash">    curl</span><br><br><span class="hljs-comment"># 复制依赖文件（优先复制 requirements.txt，利用 Docker 缓存机制）</span><br><span class="hljs-keyword">COPY</span><span class="language-bash"> requirements.txt /app/</span><br><br><span class="hljs-comment"># 安装 Python 依赖，并安装特定版本的 werkzeug</span><br><span class="hljs-keyword">RUN</span><span class="language-bash"> pip install --no-cache-dir -r requirements.txt &amp;&amp; \</span><br><span class="language-bash">    pip install werkzeug==1.0.1</span><br><br><span class="hljs-comment"># 复制应用代码到工作目录</span><br><span class="hljs-keyword">COPY</span><span class="language-bash"> . /app</span><br><br><span class="hljs-comment"># 暴露应用端口（可选，通常 Flask 默认运行在 5000 端口）</span><br><span class="hljs-keyword">EXPOSE</span> <span class="hljs-number">5000</span><br><br><span class="hljs-comment"># 运行 Flask 应用</span><br><span class="hljs-keyword">CMD</span><span class="language-bash"> [<span class="hljs-string">&quot;python&quot;</span>, <span class="hljs-string">&quot;app.py&quot;</span>]</span><br></code></pre></td></tr></table></figure><p>这样 Dockerfile 会先更新系统包并安装 <code>curl</code>，然后安装 Python 依赖，最后复制代码并启动应用。</p><p><strong>构建镜像：</strong></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">docker build -t flask-app .<br></code></pre></td></tr></table></figure><p><strong>运行容器：</strong></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">docker run -d -p 5000:5000 flask-app<br></code></pre></td></tr></table></figure><p>然后你就可以进入容器并通过 <code>curl</code> 测试应用了。</p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>2024-12-10 docker registrry</title>
    <link href="/2024/12/10/2024-12-10%20docker%20registrry/"/>
    <url>/2024/12/10/2024-12-10%20docker%20registrry/</url>
    
    <content type="html"><![CDATA[<p>在 Ubuntu 上搭建一个轻量级的 Docker 镜像仓库，可以使用 <strong>Harbor</strong> 或者 <strong>Docker Registry</strong>。如果你想要一个简单、轻量的仓库，<code>Docker Registry</code> 是一个非常合适的选择，因为它是 Docker 官方提供的。</p><h3 id="步骤-1：安装-Docker"><a href="#步骤-1：安装-Docker" class="headerlink" title="步骤 1：安装 Docker"></a>步骤 1：安装 Docker</h3><p>确保你的系统上已安装 Docker，如果还没有，可以通过以下命令安装：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> apt update<br><span class="hljs-built_in">sudo</span> apt install -y docker.io<br></code></pre></td></tr></table></figure><h3 id="步骤-2：使用-Docker-Registry-搭建镜像仓库"><a href="#步骤-2：使用-Docker-Registry-搭建镜像仓库" class="headerlink" title="步骤 2：使用 Docker Registry 搭建镜像仓库"></a>步骤 2：使用 Docker Registry 搭建镜像仓库</h3><p>Docker Registry 是 Docker 官方提供的开源镜像仓库，使用它可以轻松搭建私有镜像仓库。</p><ol><li><p><strong>拉取 Docker Registry 镜像</strong>： Docker 官方提供了一个 Docker Registry 镜像，运行这个镜像就可以搭建一个私有仓库。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> docker pull registry:2<br></code></pre></td></tr></table></figure></li><li><p><strong>运行 Docker Registry</strong>： 启动一个 Docker Registry 实例，默认情况下会使用端口 5000。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> docker run -d -p 5000:5000 --name registry registry:2<br></code></pre></td></tr></table></figure><p>这条命令将会拉取并运行一个 <code>registry</code> 容器，并将本地的 5000 端口映射到容器的 5000 端口。</p></li><li><p><strong>验证镜像仓库是否正常运行</strong>： 在浏览器中访问 <code>http://&lt;your-server-ip&gt;:5000/v2/</code>，如果一切顺利，你会看到一个响应，表示仓库正在运行。</p></li></ol><h3 id="步骤-3：配置-Docker-客户端连接私有仓库"><a href="#步骤-3：配置-Docker-客户端连接私有仓库" class="headerlink" title="步骤 3：配置 Docker 客户端连接私有仓库"></a>步骤 3：配置 Docker 客户端连接私有仓库</h3><ol><li><p><strong>标记镜像</strong>： 假设你有一个本地的镜像 <code>my-app</code>，你可以将其标记为指向私有仓库：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> docker tag my-app:latest localhost:5000/my-app:latest<br><br><span class="hljs-built_in">sudo</span> docker tag flask-app:latest localhost:5000/547475331/flasktest:latest<br></code></pre></td></tr></table></figure></li><li><p><strong>推送镜像到私有仓库</strong>： 你可以将标记过的镜像推送到你刚刚搭建的私有仓库：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> docker push localhost:5000/my-app:latest<br><br><span class="hljs-built_in">sudo</span> docker push localhost:5000/547475331/flasktest:latest<br></code></pre></td></tr></table></figure></li><li><p><strong>从私有仓库拉取镜像</strong>： 如果你想从私有仓库拉取镜像，只需运行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> docker pull localhost:5000/my-app:latest<br><br><span class="hljs-built_in">sudo</span> docker pull localhost:5000/547475331/flasktest:latest<br></code></pre></td></tr></table></figure></li></ol><h3 id="可选：启用-HTTPS（生产环境）"><a href="#可选：启用-HTTPS（生产环境）" class="headerlink" title="可选：启用 HTTPS（生产环境）"></a>可选：启用 HTTPS（生产环境）</h3><p>如果你准备将这个仓库用于生产环境，建议启用 HTTPS 来加密数据传输。你可以使用自签名证书或者从认证机构（如 Let’s Encrypt）获得证书。</p><ol><li><p><strong>生成自签名证书</strong>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">mkdir</span> -p /certs<br>openssl req \<br>  -newkey rsa:4096 -nodes -keyout /certs/domain.key \<br>  -x509 -out /certs/domain.crt<br></code></pre></td></tr></table></figure></li><li><p><strong>更新 Docker Registry 启动命令</strong>： 修改启动命令，添加证书和密钥的路径。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> docker run -d -p 5000:5000 --name registry \<br>  -v /certs:/certs \<br>  -e REGISTRY_HTTP_SECRET=&lt;your-secret&gt; \<br>  -e REGISTRY_HTTP_TLS_CERTIFICATE=/certs/domain.crt \<br>  -e REGISTRY_HTTP_TLS_KEY=/certs/domain.key \<br>  registry:2<br></code></pre></td></tr></table></figure></li></ol><p>通过这种方式，你就搭建了一个简单、轻量的 Docker 镜像仓库。如果你希望更高效、易用的管理界面，可以考虑使用 <strong>Harbor</strong>，它有更丰富的功能，但也需要更多的资源来支持。</p><p>是的，<strong>Docker Registry</strong> 可以配置持久化存储。默认情况下，Docker Registry 存储镜像数据在容器的本地文件系统中，但为了持久化存储和确保镜像数据不会在容器重启或删除时丢失，你需要将存储目录挂载到宿主机的持久存储上。</p><h3 id="Docker-Registry-持久化存储配置"><a href="#Docker-Registry-持久化存储配置" class="headerlink" title="Docker Registry 持久化存储配置"></a>Docker Registry 持久化存储配置</h3><p>要让 Docker Registry 使用持久化存储，通常做法是将宿主机的一个目录挂载到 Registry 容器的 <code>/var/lib/registry</code> 路径上，这样存储的数据将保存在宿主机的文件系统中，不会随着容器的删除而丢失。</p><h4 id="1-使用宿主机挂载目录："><a href="#1-使用宿主机挂载目录：" class="headerlink" title="1. 使用宿主机挂载目录："></a>1. 使用宿主机挂载目录：</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><code class="hljs bash">docker run -d -p 5000:5000 --name registry \<br>  -v /path/on/host:/var/lib/registry \<br>  registry:2<br>  <br>  <br>docker run -d -p 5000:5000 --name registry \<br>  -v /registry-data:/var/lib/registry \<br>  registry:2<br>  <br>  <br>docker run -d -p 5000:5000 --name registry \<br>  -v /registry-data:/var/lib/registry \<br>  --restart always \<br>  registry:2<br><br></code></pre></td></tr></table></figure><ul><li><code>/path/on/host</code>: 这个路径是宿主机上的目录，会被挂载到 Docker Registry 容器的 <code>/var/lib/registry</code> 目录。你需要确保这个目录有足够的磁盘空间，以存储镜像。</li></ul><h4 id="2-使用云存储或网络存储（如-NFS、AWS-S3-等）："><a href="#2-使用云存储或网络存储（如-NFS、AWS-S3-等）：" class="headerlink" title="2. 使用云存储或网络存储（如 NFS、AWS S3 等）："></a>2. 使用云存储或网络存储（如 NFS、AWS S3 等）：</h4><p>如果你需要在多个主机之间共享 Registry 存储，或者希望将镜像存储到云存储中，可以使用支持的后端存储。</p><p>Docker Registry 支持多种持久化存储后端，包括：</p><ul><li><strong>Amazon S3</strong></li><li><strong>Google Cloud Storage</strong></li><li><strong>Microsoft Azure Storage</strong></li><li><strong>OpenStack Swift</strong></li><li><strong>NFS</strong></li></ul><p>例如，使用 S3 存储：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs bash">docker run -d -p 5000:5000 --name registry \<br>  -e REGISTRY_STORAGE=s3 \<br>  -e REGISTRY_STORAGE_S3_ACCESSKEY=&lt;access-key&gt; \<br>  -e REGISTRY_STORAGE_S3_SECRETKEY=&lt;secret-key&gt; \<br>  -e REGISTRY_STORAGE_S3_BUCKET=&lt;bucket-name&gt; \<br>  registry:2<br></code></pre></td></tr></table></figure><p>在此配置中，镜像数据将存储在指定的 S3 桶中。</p><h4 id="3-配置持久化数据库："><a href="#3-配置持久化数据库：" class="headerlink" title="3. 配置持久化数据库："></a>3. 配置持久化数据库：</h4><p>如果你需要配置 Docker Registry 以持久化配置信息和身份验证等数据，可以考虑使用外部数据库（如 MySQL、PostgreSQL）来存储这些信息。</p><h3 id="总结："><a href="#总结：" class="headerlink" title="总结："></a>总结：</h3><ul><li>默认情况下，Docker Registry 的存储是临时的，仅在容器生命周期内有效。</li><li>要启用持久化存储，可以通过挂载宿主机目录或使用外部存储服务（如 AWS S3）来持久保存镜像数据。</li></ul>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>2024-12-08 openstack需要几台虚拟机</title>
    <link href="/2024/12/09/2024-12-08%20openstack%E9%9C%80%E8%A6%81%E5%87%A0%E5%8F%B0%E8%99%9A%E6%8B%9F%E6%9C%BA/"/>
    <url>/2024/12/09/2024-12-08%20openstack%E9%9C%80%E8%A6%81%E5%87%A0%E5%8F%B0%E8%99%9A%E6%8B%9F%E6%9C%BA/</url>
    
    <content type="html"><![CDATA[<p>使用 <strong>OpenStack Ansible</strong> 部署 OpenStack 时，通常会按照不同的角色分配到多台虚拟机上，具体的虚拟机数量和分配方式取决于你选择的部署规模和设计架构。对于小规模的测试和实验环境，可以在一台虚拟机上部署所有组件，而对于生产环境，通常会将 OpenStack 的各个组件分布到不同的虚拟机上，以确保更好的性能和高可用性。</p><p>以下是部署 OpenStack 时可能需要的虚拟机和每台虚拟机的角色说明：</p><h3 id="1-控制节点（Controller-Node）"><a href="#1-控制节点（Controller-Node）" class="headerlink" title="1. 控制节点（Controller Node）"></a>1. <strong>控制节点（Controller Node）</strong></h3><ul><li><p><strong>数量</strong>：1-2 台</p></li><li><p>作用</p><p>：</p><ul><li>控制节点负责管理 OpenStack 环境的所有核心服务，包括身份认证（Keystone）、图像服务（Glance）、计算服务（Nova）、网络服务（Neutron）、块存储服务（Cinder）、仪表板（Horizon）等。</li><li>控制节点通常部署 OpenStack 的 <strong>管理服务</strong>，这些服务需要处理来自计算节点和存储节点的请求。</li><li>需要更多的 CPU 和内存资源以处理控制和管理任务，尤其是数据库（如 MySQL）、消息队列（如 RabbitMQ）等需要在控制节点上运行。</li><li>对于生产环境，建议部署两个控制节点以提供高可用性，确保如果一个控制节点失败，另一个节点可以接管管理任务。</li></ul></li></ul><h3 id="2-计算节点（Compute-Node）"><a href="#2-计算节点（Compute-Node）" class="headerlink" title="2. 计算节点（Compute Node）"></a>2. <strong>计算节点（Compute Node）</strong></h3><ul><li><p><strong>数量</strong>：至少 1 台（根据需要扩展）</p></li><li><p>作用</p><p>：</p><ul><li>计算节点负责运行虚拟机实例，提供计算资源。计算节点上会运行 Nova 计算服务，它是 OpenStack 中管理虚拟机生命周期的核心组件。</li><li>计算节点需要安装 KVM（或其他虚拟化技术）来运行虚拟机，并且需要充足的 CPU、内存和存储资源。</li><li>对于生产环境，通常会有多个计算节点，以分散虚拟机负载并提高容错能力。</li></ul></li></ul><h3 id="3-网络节点（Network-Node）"><a href="#3-网络节点（Network-Node）" class="headerlink" title="3. 网络节点（Network Node）"></a>3. <strong>网络节点（Network Node）</strong></h3><ul><li><p><strong>数量</strong>：1 台（可选，取决于网络需求）</p></li><li><p>作用</p><p>：</p><ul><li>网络节点负责处理 OpenStack 环境中的网络服务，通常会运行 Neutron 网络服务的关键组件（如 L3 路由器、DHCP、VPN 等）。</li><li>如果你需要使用 Neutron 的高级网络功能（如 VLAN、VXLAN、SDN 等），则建议单独设置网络节点。</li><li>在较小的环境中，网络服务通常可以与控制节点合并，但对于大规模部署，最好将网络服务独立出来。</li></ul></li></ul><h3 id="4-存储节点（Storage-Node）"><a href="#4-存储节点（Storage-Node）" class="headerlink" title="4. 存储节点（Storage Node）"></a>4. <strong>存储节点（Storage Node）</strong></h3><ul><li><p><strong>数量</strong>：1-2 台（根据存储需求）</p></li><li><p>作用</p><p>：</p><ul><li>存储节点用于提供 OpenStack 的块存储服务，运行 Cinder 服务并提供底层存储。</li><li>存储节点可以提供硬盘存储来支持虚拟机的磁盘挂载，特别是在需要大规模存储的环境中非常重要。</li><li>如果你还计划使用 Ceph 存储或其他分布式存储解决方案，那么存储节点的数量可能会更多，且会配置相应的存储服务。</li></ul></li></ul><h3 id="5-数据库节点（Database-Node）"><a href="#5-数据库节点（Database-Node）" class="headerlink" title="5. 数据库节点（Database Node）"></a>5. <strong>数据库节点（Database Node）</strong></h3><ul><li><p><strong>数量</strong>：1 台（可以与控制节点合并）</p></li><li><p>作用</p><p>：</p><ul><li>数据库节点用于承载 OpenStack 的数据库服务（如 MySQL 或 PostgreSQL）。</li><li>OpenStack 的控制节点通常依赖数据库来存储和管理各种元数据（如虚拟机信息、网络信息、用户权限等）。</li><li>在小规模环境中，数据库可以与控制节点合并，但对于生产环境，通常会将数据库服务分离，以提高性能和可用性。</li></ul></li></ul><h3 id="6-消息队列节点（Message-Queue-Node）"><a href="#6-消息队列节点（Message-Queue-Node）" class="headerlink" title="6. 消息队列节点（Message Queue Node）"></a>6. <strong>消息队列节点（Message Queue Node）</strong></h3><ul><li><p><strong>数量</strong>：1 台（可以与控制节点合并）</p></li><li><p>作用</p><p>：</p><ul><li>OpenStack 使用消息队列（通常是 <strong>RabbitMQ</strong> 或 <strong>Kafka</strong>）来实现各个服务之间的异步通信。消息队列节点负责管理消息传递和队列。</li><li>与数据库节点一样，消息队列服务可以与控制节点合并，但在大规模部署中，通常会将其单独部署以保证高可用性。</li></ul></li></ul><h3 id="7-负载均衡节点（Load-Balancer-Node）"><a href="#7-负载均衡节点（Load-Balancer-Node）" class="headerlink" title="7. 负载均衡节点（Load Balancer Node）"></a>7. <strong>负载均衡节点（Load Balancer Node）</strong></h3><ul><li><p><strong>数量</strong>：可选（通常用于生产环境）</p></li><li><p>作用</p><p>：</p><ul><li>在 OpenStack 中，负载均衡通常用于在多个控制节点之间分配流量，确保高可用性。可以使用 <strong>HAProxy</strong> 或 <strong>Keepalived</strong> 来实现负载均衡。</li><li>负载均衡节点可以与其他服务（如控制节点或网络节点）共享，但通常为了高可用性，建议单独配置。</li></ul></li></ul><h3 id="8-其他（如监控节点）"><a href="#8-其他（如监控节点）" class="headerlink" title="8. 其他（如监控节点）"></a>8. <strong>其他（如监控节点）</strong></h3><ul><li><p><strong>数量</strong>：可选</p></li><li><p>作用</p><p>：</p><ul><li>你可能还需要部署监控节点来监控 OpenStack 环境的健康状况、性能指标等。常用的监控工具包括 <strong>Prometheus</strong>、<strong>Grafana</strong>、<strong>Zabbix</strong> 等。</li><li>如果你使用的是 <strong>Ceph</strong> 存储系统，还需要配置监控和管理 Ceph 集群的节点。</li></ul></li></ul><hr><h3 id="示例架构（小规模部署）："><a href="#示例架构（小规模部署）：" class="headerlink" title="示例架构（小规模部署）："></a>示例架构（小规模部署）：</h3><p>对于小规模的部署，你可以将所有组件部署在 3-4 台虚拟机上：</p><ol><li><strong>虚拟机 1</strong>：控制节点 + 数据库 + 消息队列</li><li><strong>虚拟机 2</strong>：计算节点 + 存储节点</li><li><strong>虚拟机 3</strong>：网络节点（可选，如果需要复杂的网络服务）</li><li><strong>虚拟机 4</strong>：负载均衡节点（可选）</li></ol><h3 id="示例架构（中大型部署）："><a href="#示例架构（中大型部署）：" class="headerlink" title="示例架构（中大型部署）："></a>示例架构（中大型部署）：</h3><p>对于中到大型部署，通常会使用更多的虚拟机来分散负载：</p><ol><li><strong>虚拟机 1-2</strong>：控制节点（两个控制节点以保证高可用性）</li><li><strong>虚拟机 3-6</strong>：计算节点（多台计算节点来支持更多虚拟机）</li><li><strong>虚拟机 7</strong>：网络节点（用于提供高级网络功能，如 VLAN、VXLAN 等）</li><li><strong>虚拟机 8-10</strong>：存储节点（提供 Cinder 存储服务，可能还包括 Ceph 存储）</li><li><strong>虚拟机 11</strong>：负载均衡节点（提供高可用性）</li><li><strong>虚拟机 12</strong>：监控节点（可选，用于集成监控和日志收集服务）</li></ol><hr><h3 id="总结："><a href="#总结：" class="headerlink" title="总结："></a>总结：</h3><ul><li><strong>最小部署</strong>：至少需要 1-2 台虚拟机，控制节点和计算节点可以合并。</li><li><strong>生产环境</strong>：建议分配 3 台以上虚拟机，分别用于控制节点、计算节点和网络节点（如有需要）。存储和数据库等服务可以根据需求进一步拆分到不同节点。</li></ul><p>实际的虚拟机数量和资源需求还要根据你的部署规模、网络设计、存储需求等因素进行调整。你可以根据需要的服务数量来决定每个角色的分配，并逐步扩展节点。</p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>2024-12-08 块存储和对象储存区别</title>
    <link href="/2024/12/09/2024-12-08%20%E5%9D%97%E5%AD%98%E5%82%A8%E5%92%8C%E5%AF%B9%E8%B1%A1%E5%82%A8%E5%AD%98%E5%8C%BA%E5%88%AB/"/>
    <url>/2024/12/09/2024-12-08%20%E5%9D%97%E5%AD%98%E5%82%A8%E5%92%8C%E5%AF%B9%E8%B1%A1%E5%82%A8%E5%AD%98%E5%8C%BA%E5%88%AB/</url>
    
    <content type="html"><![CDATA[<p><strong>块存储</strong>和<strong>对象存储</strong>是两种常见的存储方式，它们在存储模型、使用场景、性能和管理方式等方面存在显著差异。理解这两者的区别，有助于选择适合的存储解决方案来满足特定应用和业务需求。</p><h3 id="1-存储模型的区别"><a href="#1-存储模型的区别" class="headerlink" title="1. 存储模型的区别"></a>1. <strong>存储模型的区别</strong></h3><ul><li><strong>块存储（Block Storage）</strong>：<ul><li><strong>数据存储方式</strong>：块存储将数据分割为固定大小的块（通常为 512 字节或 4 KB）。每个块都有一个唯一的地址，计算机可以单独访问这些块。</li><li><strong>如何工作</strong>：块存储像硬盘一样提供一个裸存储卷，应用程序和操作系统可以直接访问这些块。通常用作操作系统的磁盘、数据库存储等。</li><li><strong>使用方式</strong>：通常用于需要高速访问和低延迟的应用场景，如数据库、高性能应用和虚拟机存储等。</li></ul></li><li><strong>对象存储（Object Storage）</strong>：<ul><li><strong>数据存储方式</strong>：对象存储是将数据以“对象”的形式存储，每个对象由数据、元数据和唯一的标识符（对象ID）组成。对象存储是一个无层次、扁平的存储系统，不像文件系统那样有目录结构。</li><li><strong>如何工作</strong>：对象存储通过 RESTful API（如 Amazon S3 API）来访问数据，数据以对象形式存储并通过唯一的 URL 进行检索。每个对象通常是一个文件（如图片、视频、文档等），可以是任意大小。</li><li><strong>使用方式</strong>：适用于大量的非结构化数据存储，如备份、日志存储、大数据存储、网站文件存储等。</li></ul></li></ul><h3 id="2-性能和可扩展性"><a href="#2-性能和可扩展性" class="headerlink" title="2. 性能和可扩展性"></a>2. <strong>性能和可扩展性</strong></h3><ul><li><p>块存储</p><p>：</p><ul><li><strong>性能</strong>：块存储通常提供较低的延迟和较高的吞吐量，因为它直接与操作系统交互，适合需要频繁、快速读取和写入操作的应用（如数据库、高性能计算、虚拟机等）。</li><li><strong>可扩展性</strong>：扩展块存储通常需要通过增加磁盘或者在存储阵列中增加更多的存储单元来实现。扩展时可能需要手动干预或配置。</li></ul></li><li><p>对象存储</p><p>：</p><ul><li><strong>性能</strong>：对象存储的性能通常不如块存储，因为它是基于 HTTP 协议的 RESTful API 来进行访问，因此延迟会相对较高。不过，适合存储大规模的数据并进行低频访问（如备份、归档等）。</li><li><strong>可扩展性</strong>：对象存储天然具备高可扩展性，能够轻松处理 PB 级别的存储需求。它通过分布式架构和数据冗余技术，自动在多个节点之间扩展存储空间，具有更好的横向扩展性。</li></ul></li></ul><h3 id="3-数据访问方式"><a href="#3-数据访问方式" class="headerlink" title="3. 数据访问方式"></a>3. <strong>数据访问方式</strong></h3><ul><li><strong>块存储</strong>：<ul><li><strong>访问方式</strong>：可以通过操作系统的文件系统直接访问和管理块存储，支持传统的文件系统结构（如 NTFS、EXT4、XFS 等），操作系统对块存储提供低级的读写操作。</li><li><strong>适用场景</strong>：适合对存储进行频繁随机读写的应用，比如数据库、虚拟化环境中的虚拟机磁盘等。</li></ul></li><li><strong>对象存储</strong>：<ul><li><strong>访问方式</strong>：对象存储通过 API（如 RESTful API）进行访问，支持通过 Web 请求获取对象数据。数据不需要通过操作系统文件系统进行访问，而是通过唯一的 URL 地址或对象键来检索。</li><li><strong>适用场景</strong>：适合存储大量非结构化数据，如视频、图片、音频、备份数据、日志文件、静态网页内容等。</li></ul></li></ul><h3 id="4-管理和操作"><a href="#4-管理和操作" class="headerlink" title="4. 管理和操作"></a>4. <strong>管理和操作</strong></h3><ul><li><strong>块存储</strong>：<ul><li><strong>管理方式</strong>：块存储的管理通常更复杂，特别是在大规模部署时。你需要分配、格式化和挂载每个磁盘，进行数据备份、恢复等操作。</li><li><strong>优点</strong>：高性能，支持对文件系统、数据库等的直接管理。</li><li><strong>缺点</strong>：管理开销较大，扩展时可能较为复杂，需要手动干预或配置。</li></ul></li><li><strong>对象存储</strong>：<ul><li><strong>管理方式</strong>：对象存储的管理相对简单，存储的对象通过唯一标识符（如对象ID）进行访问，自动管理冗余和扩展。通过简单的 API 和 Web 控制面板即可完成操作。</li><li><strong>优点</strong>：易于扩展、管理和访问，适合存储大规模、非结构化的数据。</li><li><strong>缺点</strong>：不适合低延迟或高频率随机读写操作，因为访问是基于 HTTP 协议，且没有传统文件系统的目录结构。</li></ul></li></ul><h3 id="5-常见使用场景"><a href="#5-常见使用场景" class="headerlink" title="5. 常见使用场景"></a>5. <strong>常见使用场景</strong></h3><ul><li><strong>块存储</strong>：<ul><li><strong>数据库存储</strong>：块存储适用于需要高 I&#x2F;O 性能的数据库应用，如关系型数据库（MySQL、PostgreSQL）或 NoSQL 数据库（MongoDB、Cassandra）。</li><li><strong>虚拟化</strong>：虚拟机存储虚拟硬盘（如 VMware 或 KVM）使用块存储。</li><li><strong>高性能应用</strong>：需要快速数据访问的高性能计算（HPC）应用、交易系统等。</li></ul></li><li><strong>对象存储</strong>：<ul><li><strong>大数据存储</strong>：存储海量的非结构化数据，如视频、音频、日志文件、图像等。</li><li><strong>备份与归档</strong>：适合大规模备份和长期存储，很多公司将对象存储用于云备份解决方案。</li><li><strong>内容分发网络（CDN）</strong>：将静态网站内容、视频流等分发到全球各地的节点，利用对象存储优化分发速度。</li><li><strong>静态网页存储</strong>：存储 HTML 文件、图片和其他静态资源。</li></ul></li></ul><h3 id="6-冗余与容错机制"><a href="#6-冗余与容错机制" class="headerlink" title="6. 冗余与容错机制"></a>6. <strong>冗余与容错机制</strong></h3><ul><li><strong>块存储</strong>：<ul><li><strong>冗余</strong>：块存储的冗余通常由存储设备（如 SAN、NAS）提供，可以使用 RAID 等技术确保数据的高可用性和冗余。</li><li><strong>容错机制</strong>：块存储支持快速备份和恢复，但如果单台硬盘或存储设备出现故障，可能会影响存储的可用性。</li></ul></li><li><strong>对象存储</strong>：<ul><li><strong>冗余</strong>：对象存储通常内建数据冗余机制，像 AWS S3 等平台自动将数据复制到多个数据中心或节点，以确保数据的持久性和可用性。</li><li><strong>容错机制</strong>：对象存储的高可用性和数据冗余通常通过分布式架构自动处理，具备更强的容错能力。</li></ul></li></ul><hr><h3 id="7-总结"><a href="#7-总结" class="headerlink" title="7. 总结"></a>7. <strong>总结</strong></h3><table><thead><tr><th>特性</th><th>块存储 (Block Storage)</th><th>对象存储 (Object Storage)</th></tr></thead><tbody><tr><td><strong>存储结构</strong></td><td>数据被分割成固定大小的块</td><td>数据以对象（包括数据、元数据和ID）存储</td></tr><tr><td><strong>访问方式</strong></td><td>通过操作系统文件系统直接访问</td><td>通过 API（如 RESTful API）访问</td></tr><tr><td><strong>性能</strong></td><td>适合高性能、低延迟的应用</td><td>适合大规模、非结构化数据的存储和归档</td></tr><tr><td><strong>管理复杂性</strong></td><td>需要手动管理和配置，管理较为复杂</td><td>管理简便，自动化较强</td></tr><tr><td><strong>扩展性</strong></td><td>扩展较为复杂，可能需要人工干预</td><td>极高的扩展性，适合大规模存储</td></tr><tr><td><strong>典型应用场景</strong></td><td>数据库、虚拟机、性能要求高的应用</td><td>大数据存储、备份、归档、静态内容存储</td></tr><tr><td><strong>冗余与容错</strong></td><td>由存储设备（如 RAID）提供冗余</td><td>自动数据冗余和容错机制</td></tr></tbody></table><p>总的来说，<strong>块存储</strong>适用于需要频繁随机读写和高性能的应用，如数据库和虚拟机磁盘；而<strong>对象存储</strong>适用于大规模的非结构化数据存储，如备份、归档、媒体内容分发等。</p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>2024-12-08 openstack和kubernetes区别</title>
    <link href="/2024/12/09/2024-12-08%20openstack%E5%92%8Ckubernetes%E5%8C%BA%E5%88%AB/"/>
    <url>/2024/12/09/2024-12-08%20openstack%E5%92%8Ckubernetes%E5%8C%BA%E5%88%AB/</url>
    
    <content type="html"><![CDATA[<p><strong>OpenStack</strong> 和 <strong>Kubernetes</strong> 都是现代云计算和容器化技术中的重要组件，但它们关注的领域和解决的问题有所不同。下面是对两者关系的详细解释。</p><h3 id="1-OpenStack-是什么？"><a href="#1-OpenStack-是什么？" class="headerlink" title="1. OpenStack 是什么？"></a>1. <strong>OpenStack 是什么？</strong></h3><p><strong>OpenStack</strong> 是一个开源的云计算平台，旨在提供 IaaS（基础设施即服务），主要用于构建公共或私有云环境。它由一系列的模块组成，用于管理计算、存储、网络、安全等资源。</p><h4 id="OpenStack-的核心组件："><a href="#OpenStack-的核心组件：" class="headerlink" title="OpenStack 的核心组件："></a>OpenStack 的核心组件：</h4><ul><li><strong>Nova</strong>：计算服务，负责虚拟机（VM）的管理和调度。</li><li><strong>Neutron</strong>：网络服务，提供虚拟网络、子网、路由等功能。</li><li><strong>Cinder</strong>：块存储服务，用于管理磁盘存储。</li><li><strong>Glance</strong>：镜像服务，提供虚拟机镜像的存储和管理。</li><li><strong>Keystone</strong>：身份认证服务，管理用户和权限。</li><li><strong>Horizon</strong>：Web 控制面板，用于管理 OpenStack 环境。</li></ul><p>OpenStack 主要面向虚拟化资源管理（主要是虚拟机），用于提供基础设施服务，支持创建、管理和销毁虚拟机、虚拟存储和虚拟网络。</p><h3 id="2-Kubernetes-是什么？"><a href="#2-Kubernetes-是什么？" class="headerlink" title="2. Kubernetes 是什么？"></a>2. <strong>Kubernetes 是什么？</strong></h3><p><strong>Kubernetes</strong> 是一个开源的容器编排平台，用于自动化容器的部署、扩展和管理。它解决了容器化应用程序在大规模运行中的编排问题，特别是多容器环境的管理。</p><h4 id="Kubernetes-的核心功能："><a href="#Kubernetes-的核心功能：" class="headerlink" title="Kubernetes 的核心功能："></a>Kubernetes 的核心功能：</h4><ul><li><strong>Pod</strong>：Kubernetes 的最小部署单元，包含一个或多个容器。</li><li><strong>Service</strong>：定义了容器之间如何通信，并为容器提供一个稳定的网络入口。</li><li><strong>Deployment</strong>：管理应用程序的副本和更新。</li><li><strong>Node</strong>：运行容器的工作节点。</li><li><strong>ReplicaSet</strong>：确保某个特定数量的 Pod 副本在任何时间点都在运行。</li><li><strong>ConfigMap&#x2F;Secret</strong>：管理应用的配置和敏感信息。</li></ul><p>Kubernetes 主要面向 <strong>容器化</strong> 的应用部署和管理，尤其适用于微服务架构和分布式应用程序。</p><h3 id="3-OpenStack-和-Kubernetes-的关系"><a href="#3-OpenStack-和-Kubernetes-的关系" class="headerlink" title="3. OpenStack 和 Kubernetes 的关系"></a>3. <strong>OpenStack 和 Kubernetes 的关系</strong></h3><p>OpenStack 和 Kubernetes 解决的是不同层次的问题，但它们也可以结合使用，为云环境提供更强大的功能。</p><h4 id="OpenStack-提供基础设施，Kubernetes-提供容器编排："><a href="#OpenStack-提供基础设施，Kubernetes-提供容器编排：" class="headerlink" title="OpenStack 提供基础设施，Kubernetes 提供容器编排："></a><strong>OpenStack 提供基础设施，Kubernetes 提供容器编排</strong>：</h4><ul><li><strong>OpenStack</strong> 提供了虚拟机（VM）、存储、网络等基础设施层面的资源，可以被 Kubernetes 用来运行和管理容器化应用。</li><li><strong>Kubernetes</strong> 运行在 OpenStack 提供的计算资源（虚拟机）上，并管理在这些资源上运行的容器化应用。</li></ul><p>可以把 OpenStack 看作是为容器提供运行环境的基础设施层，而 Kubernetes 则是在此基础上提供容器编排和应用管理的功能。</p><h4 id="结合使用的场景："><a href="#结合使用的场景：" class="headerlink" title="结合使用的场景："></a><strong>结合使用的场景</strong>：</h4><ol><li><strong>OpenStack + Kubernetes 作为公有云和私有云的基础</strong>：<ul><li>OpenStack 提供私有云或混合云的基础设施，Kubernetes 可以在 OpenStack 虚拟机上运行，为应用提供容器编排。</li><li>在这种模式下，OpenStack 负责虚拟机和资源的管理，而 Kubernetes 负责容器的部署、扩展和管理。</li></ul></li><li><strong>Kubernetes 在 OpenStack 中作为计算资源管理</strong>：<ul><li>在 OpenStack 环境中，Kubernetes 可以部署为应用管理平台，利用 OpenStack 提供的虚拟机、存储和网络资源，运行容器化应用。</li><li>OpenStack 可能使用 Kubernetes 来管理容器化应用的运行，而 Kubernetes 使用 OpenStack 的 Nova 计算服务提供容器实例的计算资源。</li></ul></li><li><strong>OpenStack Magnum</strong>：<ul><li>OpenStack 提供了一个名为 <strong>Magnum</strong> 的项目，它允许在 OpenStack 环境中直接运行 Kubernetes 集群。Magnum 是一个容器管理服务，可以通过 OpenStack 来管理和调度 Kubernetes 集群。</li><li>Magnum 可以通过 <strong>Docker</strong> 或 <strong>Kubernetes</strong> 来创建和管理容器集群，这使得 Kubernetes 可以无缝地运行在 OpenStack 的基础设施上。</li></ul></li><li><strong>共享资源和集成</strong>：<ul><li>OpenStack 的虚拟化和网络功能可以为 Kubernetes 提供底层资源支持，比如 OpenStack 的虚拟机（Nova）、网络（Neutron）和存储（Cinder）服务。</li><li>Kubernetes 可以使用 OpenStack 提供的 <strong>Block Storage（Cinder）</strong> 和 <strong>Object Storage（Swift）</strong>，以及 <strong>Neutron 网络服务</strong>，实现更高效的存储和网络管理。</li></ul></li></ol><h3 id="4-OpenStack-和-Kubernetes-的区别："><a href="#4-OpenStack-和-Kubernetes-的区别：" class="headerlink" title="4. OpenStack 和 Kubernetes 的区别："></a>4. <strong>OpenStack 和 Kubernetes 的区别</strong>：</h3><p>尽管 OpenStack 和 Kubernetes 可以结合使用，但它们之间有以下一些本质的区别：</p><table><thead><tr><th>特性</th><th>OpenStack</th><th>Kubernetes</th></tr></thead><tbody><tr><td><strong>目标</strong></td><td>提供 IaaS，管理虚拟机、存储、网络等基础设施</td><td>提供容器编排，管理容器化应用的生命周期</td></tr><tr><td><strong>资源管理</strong></td><td>管理计算、存储、网络等物理资源（如虚拟机）</td><td>管理容器资源，自动化部署、扩展、监控容器应用</td></tr><tr><td><strong>部署方式</strong></td><td>运行在虚拟机或物理主机上，提供虚拟机、存储等</td><td>部署容器应用并自动管理容器的扩展、负载均衡等</td></tr><tr><td><strong>架构</strong></td><td>主要基于虚拟化技术，支持虚拟机和存储等资源</td><td>基于容器技术，管理微服务架构和容器化应用</td></tr><tr><td><strong>工作负载类型</strong></td><td>虚拟机和物理机器上的应用</td><td>容器化的应用和微服务</td></tr><tr><td><strong>使用场景</strong></td><td>适用于私有云、公有云、混合云的基础设施搭建</td><td>适用于大规模、分布式的微服务架构和容器化应用</td></tr></tbody></table><h3 id="5-总结："><a href="#5-总结：" class="headerlink" title="5. 总结："></a>5. <strong>总结：</strong></h3><ul><li><strong>OpenStack</strong> 是一个基础设施即服务（IaaS）平台，主要解决虚拟化资源的管理和提供，包括虚拟机、存储、网络等。</li><li><strong>Kubernetes</strong> 是一个容器编排平台，专注于管理容器化应用的部署、扩展和运行。</li><li>它们是可以协同工作的：<strong>OpenStack</strong> 提供计算资源（虚拟机、存储、网络），而 <strong>Kubernetes</strong> 在这些计算资源上运行容器化应用，并自动进行应用的调度和管理。</li><li>通过 OpenStack 和 Kubernetes 的结合，用户可以在私有云或公有云环境中，灵活地使用虚拟机和容器来部署和管理应用，满足不同规模和需求的应用架构。</li></ul><p>如果你的目标是构建一个高效的云环境，OpenStack 和 Kubernetes 是可以互补的，能够为基础设施层和应用层提供强大的支持。</p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>2024-12-08 nano操作</title>
    <link href="/2024/12/09/2024-12-08%20nano%E6%93%8D%E4%BD%9C/"/>
    <url>/2024/12/09/2024-12-08%20nano%E6%93%8D%E4%BD%9C/</url>
    
    <content type="html"><![CDATA[<p><code>nano</code> 是一个在 Unix-like 系统中非常流行的文本编辑器，适合新手和快速编辑配置文件。它是一个基于命令行的编辑器，功能简洁且易于使用。下面是一些基本的 <code>nano</code> 使用方法：</p><h3 id="1-打开文件"><a href="#1-打开文件" class="headerlink" title="1. 打开文件"></a>1. <strong>打开文件</strong></h3><p>要编辑一个文件，可以在命令行中输入：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">nano &lt;文件路径&gt;<br></code></pre></td></tr></table></figure><p>如果文件不存在，<code>nano</code> 会创建一个新的文件。例如，要编辑 <code>local.conf</code> 文件，可以使用：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">nano local.conf<br></code></pre></td></tr></table></figure><h3 id="2-编辑文件"><a href="#2-编辑文件" class="headerlink" title="2. 编辑文件"></a>2. <strong>编辑文件</strong></h3><p>进入 <code>nano</code> 编辑器后，你可以直接开始输入文本。光标可以用方向键移动，编辑内容就像在普通的文本编辑器中一样。</p><h3 id="3-保存文件"><a href="#3-保存文件" class="headerlink" title="3. 保存文件"></a>3. <strong>保存文件</strong></h3><ul><li>按下 **<code>Ctrl + O</code>**（即按住 <code>Ctrl</code> 键，然后按 <code>O</code> 键）保存当前文件。屏幕底部会显示 <code>File Name to Write: &lt;文件路径&gt;</code>，表示文件已准备好保存。</li><li>按 <strong>Enter</strong> 键确认保存文件。</li></ul><h3 id="4-退出-nano"><a href="#4-退出-nano" class="headerlink" title="4. 退出 nano"></a>4. <strong>退出 nano</strong></h3><ul><li><p>按 </p><p><code>Ctrl + X</code></p><p> 退出 </p><figure class="highlight ebnf"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs ebnf"><span class="hljs-attribute">nano</span><br></code></pre></td></tr></table></figure><p>。如果你在退出前修改了文件，</p><figure class="highlight ebnf"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs ebnf"><span class="hljs-attribute">nano</span><br></code></pre></td></tr></table></figure><p> 会提示你是否保存更改：</p><ul><li>如果想保存，按 <strong>Y</strong>（Yes），然后按 <strong>Enter</strong>。</li><li>如果不想保存更改，按 <strong>N</strong>（No）。</li></ul></li></ul><h3 id="5-基本命令"><a href="#5-基本命令" class="headerlink" title="5. 基本命令"></a>5. <strong>基本命令</strong></h3><p>在 <code>nano</code> 中，很多操作是通过 <strong>Ctrl + 字母键</strong> 来完成的。下面是一些常见的快捷键：</p><ul><li><strong><code>Ctrl + O</code></strong>: 保存文件（Write Out）。</li><li><strong><code>Ctrl + X</code></strong>: 退出编辑器（Exit）。</li><li><strong><code>Ctrl + W</code></strong>: 搜索文本（Where Is）。输入要搜索的内容，按 <strong>Enter</strong> 搜索。</li><li><strong><code>Ctrl + K</code></strong>: 剪切当前行。</li><li><strong><code>Ctrl + U</code></strong>: 粘贴剪切的内容。</li><li><strong><code>Ctrl + J</code></strong>: 格式化文本（自动换行）。</li><li><strong><code>Ctrl + C</code></strong>: 显示光标当前位置（行号和列号）。</li><li><strong><code>Ctrl + T</code></strong>: 调用拼写检查。</li></ul><h3 id="6-移动光标"><a href="#6-移动光标" class="headerlink" title="6. 移动光标"></a>6. <strong>移动光标</strong></h3><ul><li><strong>箭头键</strong>：上下左右移动光标。</li><li>**<code>Ctrl + A</code>**：移动到行首。</li><li>**<code>Ctrl + E</code>**：移动到行尾。</li><li>**<code>Ctrl + Y</code>**：向上滚动一页。</li><li>**<code>Ctrl + V</code>**：向下滚动一页。</li></ul><h3 id="7-删除文本"><a href="#7-删除文本" class="headerlink" title="7. 删除文本"></a>7. <strong>删除文本</strong></h3><ul><li>**<code>Backspace</code>**：删除光标前面的字符。</li><li>**<code>Ctrl + D</code>**：删除光标下的字符。</li></ul><h3 id="8-剪切、复制和粘贴"><a href="#8-剪切、复制和粘贴" class="headerlink" title="8. 剪切、复制和粘贴"></a>8. <strong>剪切、复制和粘贴</strong></h3><ul><li>**<code>Ctrl + K</code>**：剪切光标所在行。</li><li>**<code>Ctrl + U</code>**：粘贴剪切的内容。</li><li>**<code>Ctrl + ^</code>**（按住 <code>Ctrl</code> 后按 <code>^</code>）开始选择文本，移动光标选择一块区域，选择完后可以通过 <strong>Ctrl + K</strong> 剪切选中的文本，或通过 <strong>Ctrl + U</strong> 粘贴。</li></ul><h3 id="9-帮助信息"><a href="#9-帮助信息" class="headerlink" title="9. 帮助信息"></a>9. <strong>帮助信息</strong></h3><p>如果你需要帮助，按 <strong><code>Ctrl + G</code></strong> 可以查看 <code>nano</code> 的帮助文档，了解所有快捷键和功能。</p><hr><h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p><code>nano</code> 是一个轻量级、易于使用的命令行文本编辑器。它通过简洁的快捷键使得文件编辑更加直观，不需要记住太多复杂的命令，适合用来快速编辑配置文件或进行简单的文本编辑。</p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>2024-12-09 mutilpass部署openstack devstack形式</title>
    <link href="/2024/12/09/2024-12-08%20mutilpass%E9%83%A8%E7%BD%B2openstack/"/>
    <url>/2024/12/09/2024-12-08%20mutilpass%E9%83%A8%E7%BD%B2openstack/</url>
    
    <content type="html"><![CDATA[<p>在 macOS 上通过 <strong>Multipass</strong> 部署 OpenStack 测试环境是一个非常方便的方式，尤其是使用 <strong>All-in-One</strong> 部署方式来简化环境配置。All-in-One 部署将 OpenStack 的所有核心组件（如 Nova、Keystone、Glance、Cinder、Neutron 等）部署在一台虚拟机上，适合用于测试和开发环境。</p><h3 id="使用-Multipass-部署-OpenStack"><a href="#使用-Multipass-部署-OpenStack" class="headerlink" title="使用 Multipass 部署 OpenStack"></a>使用 Multipass 部署 OpenStack</h3><h4 id="1-准备工作："><a href="#1-准备工作：" class="headerlink" title="1. 准备工作："></a>1. <strong>准备工作：</strong></h4><ul><li><strong>安装 Multipass</strong>：确保你已经安装了 Multipass。你可以通过官网（<a href="https://multipass.run/">Multipass 官方网站</a>）下载并安装。</li><li><strong>安装 Ubuntu 虚拟机</strong>：创建一个 Ubuntu 虚拟机来部署 OpenStack。</li></ul><h4 id="2-创建虚拟机："><a href="#2-创建虚拟机：" class="headerlink" title="2. 创建虚拟机："></a>2. <strong>创建虚拟机：</strong></h4><p>使用 Multipass 创建 Ubuntu 虚拟机时，你可以指定虚拟机的资源配置（如 CPU 核数、内存和硬盘空间）。以下是创建虚拟机的命令：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">multipass launch --name openstack-vm --cpus 4 --mem 8G --disk 40G 22.04<br></code></pre></td></tr></table></figure><ul><li><code>--name openstack-vm</code>：虚拟机的名称。</li><li><code>--cpus 4</code>：分配 4 个 CPU 核心。</li><li><code>--mem 8G</code>：分配 8GB 内存。</li><li><code>--disk 40G</code>：分配 40GB 硬盘空间。</li><li><code>20.04</code>：指定使用 Ubuntu 20.04 LTS 版本。</li></ul><p>这将创建一台名为 <code>openstack-vm</code> 的虚拟机，具有 4 核 CPU、8GB 内存和 40GB 硬盘空间。</p><h4 id="3-连接到虚拟机："><a href="#3-连接到虚拟机：" class="headerlink" title="3. 连接到虚拟机："></a>3. <strong>连接到虚拟机：</strong></h4><p>创建虚拟机后，你可以通过以下命令连接到虚拟机的终端：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">multipass shell openstack-vm<br></code></pre></td></tr></table></figure><p>进入虚拟机后，你可以开始在里面安装 OpenStack。</p><h4 id="4-在虚拟机中部署-OpenStack："><a href="#4-在虚拟机中部署-OpenStack：" class="headerlink" title="4. 在虚拟机中部署 OpenStack："></a>4. <strong>在虚拟机中部署 OpenStack：</strong></h4><p>有几种方式可以在 Ubuntu 虚拟机中安装 OpenStack：</p><ul><li><strong>使用 DevStack</strong>：这是最常见的开发环境部署工具，适合在单一节点（All-in-One）部署 OpenStack。DevStack 会自动配置所有 OpenStack 组件，并且可以通过简单的配置来启动整个环境。</li><li><strong>使用 Packstack</strong>：这是 Red Hat 开发的脚本工具，适用于 OpenStack 的快速部署。</li></ul><p>下面是通过 DevStack 在虚拟机中部署 OpenStack 的简单步骤：</p><h4 id="5-使用-DevStack-安装-OpenStack"><a href="#5-使用-DevStack-安装-OpenStack" class="headerlink" title="5. 使用 DevStack 安装 OpenStack"></a>5. <strong>使用 DevStack 安装 OpenStack</strong></h4><ol><li><p><strong>更新系统</strong>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> apt update<br><span class="hljs-built_in">sudo</span> apt upgrade -y<br><span class="hljs-built_in">sudo</span> apt install git -y<br></code></pre></td></tr></table></figure></li><li><p><strong>克隆 DevStack 仓库</strong>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs bash"><br><br>git <span class="hljs-built_in">clone</span> https://opendev.org/openstack/devstack<br><span class="hljs-built_in">cd</span> devstack<br><br></code></pre></td></tr></table></figure></li><li><p><strong>配置 DevStack</strong>： 你可以使用默认的配置文件，也可以创建一个自定义配置文件。在 <code>devstack</code> 目录下创建一个配置文件 <code>local.conf</code>，例如：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">vim local.conf<br></code></pre></td></tr></table></figure><p>配置内容如下（修改为你自己的用户名和密码）：</p><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs ini"><span class="hljs-section">[[local|localrc]]</span><br><span class="hljs-attr">ADMIN_PASSWORD</span>=yourpassword<br><span class="hljs-attr">DATABASE_PASSWORD</span>=<span class="hljs-variable">$ADMIN_PASSWORD</span><br><span class="hljs-attr">RABBIT_PASSWORD</span>=<span class="hljs-variable">$ADMIN_PASSWORD</span><br><span class="hljs-attr">SERVICE_PASSWORD</span>=<span class="hljs-variable">$ADMIN_PASSWORD</span><br><br><span class="hljs-section">[[local|localrc]]</span><br><span class="hljs-attr">ADMIN_PASSWORD</span>=<span class="hljs-number">1234</span><br><span class="hljs-attr">DATABASE_PASSWORD</span>=<span class="hljs-number">1234</span><br><span class="hljs-attr">RABBIT_PASSWORD</span>=<span class="hljs-number">1234</span><br><span class="hljs-attr">SERVICE_PASSWORD</span>=<span class="hljs-number">1234</span><br></code></pre></td></tr></table></figure></li><li><p><strong>运行 DevStack 安装 OpenStack</strong>： 运行以下命令来安装 OpenStack：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs bash">./stack.sh<br><br>FORCE=<span class="hljs-built_in">yes</span> ./stack.sh<br></code></pre></td></tr></table></figure><p>这个过程会下载和安装所有需要的依赖，并配置 OpenStack 所有组件。安装过程可能需要一些时间。</p></li></ol><p>你遇到的错误信息表明 <code>stack.sh</code> 脚本在执行过程中遇到了一些问题。根据报错信息，可能是以下几个原因导致的：</p><ol><li><strong>权限问题或目录不存在</strong>：<ul><li>错误消息显示 <code>functions-common</code> 脚本试图访问 <code>/opt/stack/logs/error.log</code> 文件时找不到该目录。这通常是由于目标目录没有创建或当前用户没有足够权限导致的。</li></ul></li><li><strong>缺少必要的目录</strong>：<ul><li>OpenStack 的安装脚本会创建一些日志和临时目录。如果这些目录没有正确创建，可能会导致后续操作失败。</li></ul></li></ol><h3 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h3><h4 id="1-创建缺少的目录"><a href="#1-创建缺少的目录" class="headerlink" title="1. 创建缺少的目录"></a>1. <strong>创建缺少的目录</strong></h4><p>尝试手动创建日志目录 <code>/opt/stack/logs</code>，并设置正确的权限：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> <span class="hljs-built_in">mkdir</span> -p /opt/stack/logs<br><span class="hljs-built_in">sudo</span> <span class="hljs-built_in">chown</span> -R ubuntu:ubuntu /opt/stack<br></code></pre></td></tr></table></figure><p>这里假设你是以 <code>ubuntu</code> 用户身份运行的（根据错误信息中的 <code>ubuntu@openstack-vm</code>）。</p><h4 id="2-确保脚本具有执行权限"><a href="#2-确保脚本具有执行权限" class="headerlink" title="2. 确保脚本具有执行权限"></a>2. <strong>确保脚本具有执行权限</strong></h4><p>如果是权限问题，确保脚本具有正确的执行权限：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">chmod</span> +x stack.sh<br></code></pre></td></tr></table></figure><h4 id="3-使用-FORCE-yes-重新运行脚本"><a href="#3-使用-FORCE-yes-重新运行脚本" class="headerlink" title="3. 使用 FORCE=yes 重新运行脚本"></a>3. <strong>使用 <code>FORCE=yes</code> 重新运行脚本</strong></h4><p>如果你想绕过当前的错误并强制运行安装脚本，可以使用 <code>FORCE=yes</code> 环境变量来继续执行脚本：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">FORCE=<span class="hljs-built_in">yes</span> ./stack.sh<br></code></pre></td></tr></table></figure><p>这会强制脚本继续执行，但需要注意，这种方式可能会跳过一些错误检查。</p><h4 id="4-查看更详细的日志"><a href="#4-查看更详细的日志" class="headerlink" title="4. 查看更详细的日志"></a>4. <strong>查看更详细的日志</strong></h4><p>如果问题依旧没有解决，查看脚本日志的详细信息可以帮助找出根本原因。你可以查看 <code>stack.sh</code> 脚本所在目录中的其他日志文件，通常这些文件会有更多的错误信息。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">cat</span> /opt/stack/logs/error.log<br></code></pre></td></tr></table></figure><h4 id="5-清理之前的安装并重新开始"><a href="#5-清理之前的安装并重新开始" class="headerlink" title="5. 清理之前的安装并重新开始"></a>5. <strong>清理之前的安装并重新开始</strong></h4><p>如果前面的步骤都没有解决问题，可以尝试清理之前的安装并重新运行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">./unstack.sh<br>./clean.sh<br></code></pre></td></tr></table></figure><p>然后再尝试重新运行 <code>stack.sh</code>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">./stack.sh<br></code></pre></td></tr></table></figure><h4 id="6-检查依赖和配置"><a href="#6-检查依赖和配置" class="headerlink" title="6. 检查依赖和配置"></a>6. <strong>检查依赖和配置</strong></h4><p>确保系统已安装所有必要的依赖项，特别是网络相关的工具（如 <code>git</code>、<code>python</code> 等），并且你的配置文件（如 <code>local.conf</code>）没有遗漏或配置错误。</p><hr><h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><ul><li>检查并创建缺少的目录 <code>/opt/stack/logs</code>。</li><li>确保脚本具有正确的执行权限。</li><li>尝试使用 <code>FORCE=yes</code> 重新运行脚本。</li><li>查看错误日志获取更多信息。</li><li>清理并重新开始安装过程。</li></ul><p>这些步骤应该能帮助你解决安装过程中的问题。</p><h4 id="6-完成安装后访问-OpenStack："><a href="#6-完成安装后访问-OpenStack：" class="headerlink" title="6. 完成安装后访问 OpenStack："></a>6. <strong>完成安装后访问 OpenStack</strong>：</h4><p>安装完成后，DevStack 会启动一个默认的 Horizon Web 界面，你可以在浏览器中访问并登录。</p><ol><li><p>获取虚拟机的 IP 地址：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">multipass list<br></code></pre></td></tr></table></figure></li><li><p>在浏览器中访问 Horizon Web 界面： 访问 <code>http://&lt;VM-IP&gt;/dashboard</code>，并使用 DevStack 安装过程中设置的 <code>ADMIN_PASSWORD</code> 登录。</p></li></ol><hr><h3 id="推荐配置"><a href="#推荐配置" class="headerlink" title="推荐配置"></a>推荐配置</h3><ul><li><strong>CPU</strong>：4 核 CPU（可以根据需要调整，2 核也能满足基础体验）</li><li><strong>内存</strong>：8GB 内存（OpenStack 虽然可以在 4GB 内存上运行，但 8GB 会带来更好的体验，尤其是在启动虚拟机和运行服务时）</li><li><strong>硬盘空间</strong>：40GB 硬盘（40GB 适合测试环境，如果需要运行多个虚拟机，可能需要更多存储空间）</li></ul><h3 id="总结-1"><a href="#总结-1" class="headerlink" title="总结"></a>总结</h3><p>通过 Multipass 和 DevStack，你可以轻松在 macOS 上的 Ubuntu 虚拟机中部署一个测试用的 OpenStack 环境。资源配置方面，4 核 CPU、8GB 内存和 40GB 硬盘是一个合适的基础配置，可以支撑 OpenStack 的基础功能和一些虚拟机的运行。如果需要更多的虚拟机或服务，可以根据需求适当调整资源配置。</p><figure class="highlight awk"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs awk"><span class="hljs-regexp">/opt/</span>stack<span class="hljs-regexp">/requirements/u</span>pper-constraints.txt<br><br>zipp<br></code></pre></td></tr></table></figure><p><img src="https://picture-base.oss-cn-hangzhou.aliyuncs.com/image-20241208082002247.png"></p><p><img src="https://picture-base.oss-cn-hangzhou.aliyuncs.com/image-20241208081930313.png" alt="image-20241208081930313"></p><p><img src="https://picture-base.oss-cn-hangzhou.aliyuncs.com/image-20241208082146901.png" alt="image-20241208082146901"></p><p><code>admin-openrc.sh</code> 是一个环境配置文件，它包含了 OpenStack 管理员账户的认证信息，通常在安装和部署 OpenStack 时生成。这个文件用于设置环境变量，使得你可以通过 <code>openstack</code> 命令行工具与 OpenStack 进行交互。</p><h3 id="如何使用-admin-openrc-sh-文件："><a href="#如何使用-admin-openrc-sh-文件：" class="headerlink" title="如何使用 admin-openrc.sh 文件："></a>如何使用 <code>admin-openrc.sh</code> 文件：</h3><ol><li><p><strong>下载 <code>admin-openrc.sh</code> 文件</strong></p><p>如果你从 OpenStack 的 Web 控制面板（Horizon）下载了 <code>admin-openrc.sh</code> 文件，你需要在本地环境中使用它来设置命令行环境。</p><p>通常，你可以在 Horizon 控制面板中找到 <strong>“Download OpenRC file”</strong> 的选项，选择下载 <strong>admin-openrc.sh</strong>，这个文件会包含你 OpenStack 管理员账号的认证信息（如用户名、密码等）。</p></li><li><p><strong>使用 <code>source</code> 命令加载环境变量</strong></p><p>打开终端并定位到 <code>admin-openrc.sh</code> 文件所在的目录，使用 <code>source</code> 命令加载该文件，这样文件中定义的环境变量就会在当前的 shell 会话中生效。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">source</span> admin-openrc.sh<br></code></pre></td></tr></table></figure><p>执行后，它会要求你输入 OpenStack 管理员账号的密码。输入密码后，环境变量会设置好，你就可以使用 OpenStack 的命令行工具进行操作了。</p></li><li><p><strong>验证环境变量设置</strong></p><p>加载环境变量后，可以通过 <code>openstack</code> 命令验证是否设置成功。例如，查看当前项目的信息：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">openstack project list<br></code></pre></td></tr></table></figure><p>如果环境变量设置正确，你应该能够看到当前 OpenStack 环境中的所有项目。</p></li><li><p><strong>使用 <code>openstack</code> 命令行工具</strong></p><p>加载了环境变量之后，你就可以开始使用 OpenStack 的命令行工具（如 <code>openstack server create</code>，<code>openstack image list</code>，<code>openstack flavor list</code> 等）来管理你的 OpenStack 实例和资源。</p><p>示例：</p><ul><li><p>查看所有镜像：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">openstack image list<br></code></pre></td></tr></table></figure></li><li><p>查看所有实例：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">openstack server list<br></code></pre></td></tr></table></figure></li><li><p>创建一个新的实例：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">openstack server create --image &lt;image_name&gt; --flavor &lt;flavor_name&gt; --network &lt;network_name&gt; --key-name &lt;key_name&gt; &lt;server_name&gt;<br></code></pre></td></tr></table></figure></li></ul></li></ol><h3 id="示例步骤：如何使用-admin-openrc-sh-创建实例"><a href="#示例步骤：如何使用-admin-openrc-sh-创建实例" class="headerlink" title="示例步骤：如何使用 admin-openrc.sh 创建实例"></a>示例步骤：如何使用 <code>admin-openrc.sh</code> 创建实例</h3><ol><li><p><strong>加载 OpenRC 文件</strong>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">source</span> admin-openrc.sh<br></code></pre></td></tr></table></figure></li><li><p><strong>查看可用的镜像</strong>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">openstack image list<br></code></pre></td></tr></table></figure><p>你会看到类似下面的输出：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs bash">+--------------------------------------+------------------+--------+<br>| ID                                   | Name             | Status |<br>+--------------------------------------+------------------+--------+<br>| 1234abcd-5678-efgh-ijkl-mnopqrstuv  | ubuntu-22.04     | active |<br>| abcd1234-5678-efgh-ijkl-mnopqrstuv  | centos-7         | active |<br>+--------------------------------------+------------------+--------+<br></code></pre></td></tr></table></figure></li><li><p><strong>查看可用的 flavor</strong>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">openstack flavor list<br></code></pre></td></tr></table></figure><p>你会看到如下输出：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs bash">+---------+--------+------+------+<br>| ID      | Name   | RAM  | Disk |<br>+---------+--------+------+------+<br>| 1       | m1.small | 2048 | 20   |<br>| 2       | m1.medium | 4096 | 40   |<br>+---------+--------+------+------+<br></code></pre></td></tr></table></figure></li><li><p><strong>创建实例</strong>：</p><p>假设你想使用 <code>ubuntu-22.04</code> 镜像，<code>m1.small</code> 规格，并连接到名为 <code>private</code> 的网络，命令如下：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">openstack server create --image ubuntu-22.04 --flavor m1.small --network private --key-name my-key test-instance<br></code></pre></td></tr></table></figure><p>这会创建一个名为 <code>test-instance</code> 的实例。</p></li><li><p><strong>查看实例状态</strong>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">openstack server list<br></code></pre></td></tr></table></figure><p>你会看到实例的状态变为 <code>ACTIVE</code>，并且可以访问它。</p></li></ol><hr><h3 id="总结-2"><a href="#总结-2" class="headerlink" title="总结"></a>总结</h3><ul><li><code>admin-openrc.sh</code> 是一个包含 OpenStack 管理员认证信息的文件，加载它之后，你可以使用 OpenStack 的命令行工具来管理资源。</li><li>使用 <code>source admin-openrc.sh</code> 加载文件后，你可以创建实例、查看镜像、管理网络等。</li><li>加载后，你就可以使用 <code>openstack</code> 命令行工具执行各种操作，例如创建实例、查看网络、配置安全组等。</li></ul><p>通过这些步骤，你可以成功使用 OpenStack 的 CLI 创建实例并管理资源。</p><figure class="highlight gherkin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs gherkin">openstack compute service list<br><br><br>ubuntu<span class="hljs-meta">@openstack-vm:~$</span> openstack compute service list<br>+--------------------------------------+----------------+--------------+----------+---------+-------+----------------------------+<br>|<span class="hljs-string"> ID                                   </span>|<span class="hljs-string"> Binary         </span>|<span class="hljs-string"> Host         </span>|<span class="hljs-string"> Zone     </span>|<span class="hljs-string"> Status  </span>|<span class="hljs-string"> State </span>|<span class="hljs-string"> Updated At                 </span>|<br>+--------------------------------------+----------------+--------------+----------+---------+-------+----------------------------+<br>|<span class="hljs-string"> 0aab74ff-7591-4f21-9d1e-eafc61e1a76f </span>|<span class="hljs-string"> nova-scheduler </span>|<span class="hljs-string"> openstack-vm </span>|<span class="hljs-string"> internal </span>|<span class="hljs-string"> enabled </span>|<span class="hljs-string"> up    </span>|<span class="hljs-string"> 2024-12-08T22:40:48.000000 </span>|<br>|<span class="hljs-string"> c2a831c6-6a29-4222-aa46-a6241de24d4f </span>|<span class="hljs-string"> nova-conductor </span>|<span class="hljs-string"> openstack-vm </span>|<span class="hljs-string"> internal </span>|<span class="hljs-string"> enabled </span>|<span class="hljs-string"> up    </span>|<span class="hljs-string"> 2024-12-08T22:40:58.000000 </span>|<br>|<span class="hljs-string"> ff2187dc-c994-44a2-92e3-db4be37f1005 </span>|<span class="hljs-string"> nova-conductor </span>|<span class="hljs-string"> openstack-vm </span>|<span class="hljs-string"> internal </span>|<span class="hljs-string"> enabled </span>|<span class="hljs-string"> up    </span>|<span class="hljs-string"> 2024-12-08T22:41:21.000000 </span>|<br>|<span class="hljs-string"> 6d0341c1-bacb-409e-8628-61e870bb83ac </span>|<span class="hljs-string"> nova-compute   </span>|<span class="hljs-string"> openstack-vm </span>|<span class="hljs-string"> nova     </span>|<span class="hljs-string"> enabled </span>|<span class="hljs-string"> up    </span>|<span class="hljs-string"> 2024-12-08T22:41:16.000000 </span>|<br>+--------------------------------------+----------------+--------------+----------+---------+-------+----------------------------+<br>ubuntu<span class="hljs-meta">@openstack-vm:~$</span><br></code></pre></td></tr></table></figure><p>在 DevStack 环境中重启 Nova 服务有几种方式：</p><ol><li><p>使用 <code>systemctl</code> 命令（如果是 systemd 管理的服务）：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 重启所有 nova 相关服务</span><br><span class="hljs-built_in">sudo</span> systemctl restart devstack@n-*<br><br><span class="hljs-comment"># 重启具体服务</span><br><span class="hljs-built_in">sudo</span> systemctl restart devstack@n-api.service<br><span class="hljs-built_in">sudo</span> systemctl restart devstack@n-cpu.service<br><span class="hljs-built_in">sudo</span> systemctl restart devstack@n-cond.service<br><span class="hljs-built_in">sudo</span> systemctl restart devstack@n-sch.service<br></code></pre></td></tr></table></figure></li><li><p>使用 DevStack 自带的脚本：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 进入 devstack 目录</span><br><span class="hljs-built_in">cd</span> /opt/stack/devstack<br><br><span class="hljs-comment"># 重启所有服务</span><br>./unstack.sh<br>./stack.sh<br><br><span class="hljs-comment"># 或者使用 rejoin-stack.sh</span><br>./rejoin-stack.sh<br></code></pre></td></tr></table></figure></li><li><p>使用 screen 会话方式：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 查看所有 screen 会话</span><br>screen -<span class="hljs-built_in">ls</span><br><br><span class="hljs-comment"># 重新连接到 nova 相关的 screen</span><br>screen -r stack<br><span class="hljs-comment"># 按 Ctrl-A + &quot; 切换到需要的窗口（n-api, n-cpu 等）</span><br><span class="hljs-comment"># 按 Ctrl-C 停止服务</span><br><span class="hljs-comment"># 重新运行服务命令</span><br></code></pre></td></tr></table></figure></li><li><p>检查服务状态：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 查看 nova 服务状态</span><br>openstack compute service list<br><br><span class="hljs-comment"># 查看具体服务日志</span><br><span class="hljs-built_in">sudo</span> <span class="hljs-built_in">tail</span> -f /var/log/nova/nova-api.log<br><span class="hljs-built_in">sudo</span> <span class="hljs-built_in">tail</span> -f /var/log/nova/nova-compute.log<br></code></pre></td></tr></table></figure></li></ol><p>如果遇到问题：</p><ol><li><p>检查日志文件：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 查看系统日志</span><br><span class="hljs-built_in">sudo</span> journalctl -u devstack@n-*<br><br><span class="hljs-comment"># 查看 nova 日志</span><br><span class="hljs-built_in">cd</span> /var/log/nova/<br><span class="hljs-built_in">tail</span> -f *.<span class="hljs-built_in">log</span><br></code></pre></td></tr></table></figure></li><li><p>检查服务配置：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 检查 nova 配置文件</span><br><span class="hljs-built_in">cat</span> /etc/nova/nova.conf<br><br><span class="hljs-comment"># 检查服务状态</span><br>openstack compute service list<br>nova service-list<br></code></pre></td></tr></table></figure></li><li><p>常见问题处理：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 清理数据库（谨慎使用）</span><br>mysql -u root -p<br>&gt; use nova;<br>&gt; show tables;<br>&gt; <span class="hljs-built_in">truncate</span> table instances;<br><br><span class="hljs-comment"># 重置 nova 数据库</span><br><span class="hljs-built_in">cd</span> /opt/stack/devstack<br>./unstack.sh<br>mysql -u root -p -e <span class="hljs-string">&quot;drop database nova;&quot;</span><br>./stack.sh<br></code></pre></td></tr></table></figure></li></ol><p>注意事项：</p><ol><li>重启前最好先检查服务状态</li><li>建议查看日志确认是否有错误</li><li>如果单个服务重启失败，可能需要重启相关依赖服务</li><li>重要数据操作前做好备份</li></ol><p>使用 <code>unstack.sh</code> 关闭后再用 <code>stack.sh</code> 重启，实例是否还在取决于几个因素：</p><ol><li>默认情况下：</li></ol><ul><li>实例的数据会保留在数据库中</li><li>实例的状态可能会变为 <code>SHUTOFF</code> 或 <code>ERROR</code></li><li>重启后需要手动启动实例</li></ul><ol start="2"><li><p>检查和恢复实例：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 查看实例状态</span><br>openstack server list --all<br><br><span class="hljs-comment"># 启动处于关闭状态的实例</span><br>openstack server start 实例ID<br><br><span class="hljs-comment"># 如果实例状态异常，可以尝试重置状态</span><br>openstack server reset-state --active 实例ID<br></code></pre></td></tr></table></figure></li><li><p>如果想完全清除环境：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 清理所有数据（包括实例）</span><br><span class="hljs-built_in">cd</span> /opt/stack/devstack<br>./clean.sh<br><br><span class="hljs-comment"># 或者清理数据库</span><br>mysql -u root -p<br>&gt; drop database nova;<br>&gt; drop database neutron;<br></code></pre></td></tr></table></figure></li></ol><p>注意事项：</p><ol><li>建议在关闭前备份重要数据</li><li>如果实例很重要，最好在关闭前做快照</li><li>重启后检查网络和存储是否正常挂载</li><li>可能需要重新配置安全组和浮动IP</li></ol><p>最佳实践：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 关闭前</span><br>openstack server list --all  <span class="hljs-comment"># 记录实例信息</span><br>openstack server stop 实例ID  <span class="hljs-comment"># 优雅关闭实例</span><br><br><span class="hljs-comment"># 关闭 devstack</span><br>./unstack.sh<br><br><span class="hljs-comment"># 重启后</span><br>./stack.sh<br>openstack server start 实例ID  <span class="hljs-comment"># 启动实例</span><br></code></pre></td></tr></table></figure>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>2024-12-09 openstack ssh连接</title>
    <link href="/2024/12/09/2024-12-09%20openstack%20ssh%E8%BF%9E%E6%8E%A5/"/>
    <url>/2024/12/09/2024-12-09%20openstack%20ssh%E8%BF%9E%E6%8E%A5/</url>
    
    <content type="html"><![CDATA[<h1 id="DevStack-网络"><a href="#DevStack-网络" class="headerlink" title="DevStack 网络"></a>DevStack 网络</h1><p>DevStack 体验的一个重要部分是默认为创建的来宾提供网络连接。这可能不是您特定测试环境的最佳选择，因此本文档尽力解释发生了什么。</p><h2 id="默认值"><a href="#默认值" class="headerlink" title="默认值"></a>默认值</h2><p>如果您不指定任何配置，您将获得以下内容：</p><ul><li>neutron（包括带有 openvswitch 的 l3）</li><li>每个 openstack 项目的私有项目网络</li><li>浮动 IP 范围为 172.24.4.0&#x2F;24，网关为 172.24.4.1</li><li>演示项目在从 10.0.0.0&#x2F;22 范围分配的子网上配置了固定 IP</li><li>由 neutron 控制的所有网络的接口<code>br-ex</code>（不连接到任何物理接口）。</li><li>根据主机的 resolv.conf 为访客提供 DNS 解析</li><li>允许创建的访客路由出去的 IP masq 规则</li></ul><p>这将创建一个与单个主机隔离的环境。访客可以访问外部网络以获取软件包更新。Tempest 测试将在此环境中进行。</p><p>笔记</p><p>默认情况下，所有 OpenStack 环境都有安全组规则，阻止所有发往客户的入站数据包。如果您希望能够 ssh&#x2F;ping 您创建的客户，则应运行以下命令。</p><figure class="highlight gherkin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs gherkin">ubuntu<span class="hljs-meta">@openstack-vm:~$</span> openstack security group list<br><br>+--------------------------------------+---------+------------------------+----------------------------------+------+<br>|<span class="hljs-string"> ID                                   </span>|<span class="hljs-string"> Name    </span>|<span class="hljs-string"> Description            </span>|<span class="hljs-string"> Project                          </span>|<span class="hljs-string"> Tags </span>|<br>+--------------------------------------+---------+------------------------+----------------------------------+------+<br>|<span class="hljs-string"> 109ac0cd-daf6-481c-9594-867dbbdb129b </span>|<span class="hljs-string"> default </span>|<span class="hljs-string"> Default security group </span>|<span class="hljs-string"> 14278ce7bc604abd91e85b82e78ea45e </span>|<span class="hljs-string"> []   </span>|<br>|<span class="hljs-string"> 73b1a108-a0db-49a3-973f-dc91c030d4dd </span>|<span class="hljs-string"> default </span>|<span class="hljs-string"> Default security group </span>|<span class="hljs-string"> 6175cac8870541d1973a0f1010981180 </span>|<span class="hljs-string"> []   </span>|<br>|<span class="hljs-string"> 75e4b266-40bf-4bd8-a7d5-b2f0b0d6e84d </span>|<span class="hljs-string"> default </span>|<span class="hljs-string"> Default security group </span>|<span class="hljs-string"> a942dd11388440a6b8b9922fb405ad55 </span>|<span class="hljs-string"> []   </span>|<br>|<span class="hljs-string"> 9f07480a-fded-4172-852b-c997653324dc </span>|<span class="hljs-string"> t1      </span>|<span class="hljs-string">                        </span>|<span class="hljs-string"> 6175cac8870541d1973a0f1010981180 </span>|<span class="hljs-string"> []   </span>|<br>+--------------------------------------+---------+------------------------+----------------------------------+------+<br>ubuntu<span class="hljs-meta">@openstack-vm:~$</span><br></code></pre></td></tr></table></figure><p>你可以使用以下命令删除指定的安全组：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">openstack security group delete 109ac0cd-daf6-481c-9594-867dbbdb129b<br>openstack security group delete 75e4b266-40bf-4bd8-a7d5-b2f0b0d6e84d<br></code></pre></td></tr></table></figure><p>确保该安全组没有被任何实例使用，否则删除可能会失败。如果安全组被实例使用，你需要先将其从实例中移除，或者选择一个新的安全组。</p><p>如果你遇到问题，随时告诉我！</p><figure class="highlight pgsql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs pgsql">openstack <span class="hljs-keyword">security</span> <span class="hljs-keyword">group</span> <span class="hljs-keyword">rule</span> <span class="hljs-keyword">create</span> <span class="hljs-comment">--proto icmp --dst-port 0 default</span><br>openstack <span class="hljs-keyword">security</span> <span class="hljs-keyword">group</span> <span class="hljs-keyword">rule</span> <span class="hljs-keyword">create</span> <span class="hljs-comment">--proto tcp --dst-port 22 default</span><br><br><br>openstack <span class="hljs-keyword">security</span> <span class="hljs-keyword">group</span> <span class="hljs-keyword">rule</span> <span class="hljs-keyword">create</span> <span class="hljs-comment">--proto icmp --dst-port 0 73b1a108-a0db-49a3-973f-dc91c030d4dd</span><br>openstack <span class="hljs-keyword">security</span> <span class="hljs-keyword">group</span> <span class="hljs-keyword">rule</span> <span class="hljs-keyword">create</span> <span class="hljs-comment">--proto tcp --dst-port 22 73b1a108-a0db-49a3-973f-dc91c030d4dd</span><br><br>openstack <span class="hljs-keyword">security</span> <span class="hljs-keyword">group</span> <span class="hljs-keyword">rule</span> <span class="hljs-keyword">create</span> <span class="hljs-comment">--proto icmp --dst-port 0  9f07480a-fded-4172-852b-c997653324dc</span><br>openstack <span class="hljs-keyword">security</span> <span class="hljs-keyword">group</span> <span class="hljs-keyword">rule</span> <span class="hljs-keyword">create</span> <span class="hljs-comment">--proto icmp --dst-port 22  9f07480a-fded-4172-852b-c997653324dc</span><br></code></pre></td></tr></table></figure><h2 id="本地可访问的访客"><a href="#本地可访问的访客" class="headerlink" title="本地可访问的访客"></a>本地可访问的访客</h2><p>如果您想让您的客人可以从网络上的其他机器访问，我们必须连接<code>br-ex</code>到物理接口。</p><h3 id="专用客户机界面"><a href="#专用客户机界面" class="headerlink" title="专用客户机界面"></a>专用客户机界面</h3><p>如果您的 devstack 服务器上有 2 个或更多接口，您可以分配一个接口给 neutron 进行全面管理。这<strong>不应该</strong> 与您用于 ssh 进入 devstack 服务器本身的接口相同。</p><p>这是通过设置<code>PUBLIC_INTERFACE</code>属性来完成的。</p><figure class="highlight lua"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs lua"><span class="hljs-string">[[local|localrc]]</span><br>PUBLIC_INTERFACE=eth1<br></code></pre></td></tr></table></figure><p>这会将来自您的客户机的所有第 2 层流量放到主网络上。在此模式下运行时，ip masq 规则不会<strong>添加</strong>到您的 devstack 中，您负责确保本地网络上的路由工作。</p><h3 id="共享客户机接口"><a href="#共享客户机接口" class="headerlink" title="共享客户机接口"></a>共享客户机接口</h3><p>警告</p><p>这不是推荐的配置。由于 OVS 和桥接之间的相互作用，如果您在启用网络的情况下重新启动设备，则可能会丢失系统的网络连接。</p><p>如果您需要访客在网络上可访问，但只有 1 个接口（使用 NUC 之类的设备），您可以共享一个网络。但为了实现这一点，您需要手动设置大量地址，并确保所有地址都完全正确。</p><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs ini"><span class="hljs-section">[[local|localrc]]</span><br><span class="hljs-attr">PUBLIC_INTERFACE</span>=eth0<br><span class="hljs-attr">HOST_IP</span>=<span class="hljs-number">10.42</span>.<span class="hljs-number">0.52</span><br><span class="hljs-attr">FLOATING_RANGE</span>=<span class="hljs-number">10.42</span>.<span class="hljs-number">0.0</span>/<span class="hljs-number">24</span><br><span class="hljs-attr">PUBLIC_NETWORK_GATEWAY</span>=<span class="hljs-number">10.42</span>.<span class="hljs-number">0.1</span><br><span class="hljs-attr">Q_FLOATING_ALLOCATION_POOL</span>=start=<span class="hljs-number">10.42</span>.<span class="hljs-number">0.250</span>,end=<span class="hljs-number">10.42</span>.<span class="hljs-number">0.254</span><br></code></pre></td></tr></table></figure><p>为了使此方案有效，浮动 IP 网络必须与您服务器上的默认网络相匹配。这会破坏 HOST_IP 检测，因为我们默认排除浮动范围，因此您必须手动指定。</p><p>这<code>PUBLIC_NETWORK_GATEWAY</code>是服务器通常用来脱离网络的网关。<code>Q_FLOATING_ALLOCATION_POOL</code>控制将要分配的浮动 IP 范围。由于我们正在共享您现有的网络，因此您需要为其分配本地 dhcp 服务器未分配的片段。否则，您很容易出现 IP 地址冲突，并对您的本地网络造成严重破坏。</p><h2 id="私有网络寻址"><a href="#私有网络寻址" class="headerlink" title="私有网络寻址"></a>私有网络寻址</h2><p><code>IPV4_ADDRS_SAFE_TO_USE</code> 私有网络地址由和变量控制<code>IPV6_ADDRS_SAFE_TO_USE</code>。这允许用户指定一个要使用的安全内部 IP 变量，无论是否使用子网池，都会引用该变量。</p><p>对于IPv4，<code>FIXED_RANGE</code>和<code>SUBNETPOOL_PREFIX_V4</code>将直接默认为的值<code>IPV4_ADDRS_SAFE_TO_USE</code>。</p><p>对于 IPv6，<code>FIXED_RANGE_V6</code>将默认为 的值的前 &#x2F;64 <code>IPV6_ADDRS_SAFE_TO_USE</code>。如果<code>IPV6_ADDRS_SAFE_TO_USE</code>为 &#x2F;64 或更小， <code>FIXED_RANGE_V6</code>将直接使用该值。 <code>SUBNETPOOL_PREFIX_V6</code>将直接默认为 的值 <code>IPV6_ADDRS_SAFE_TO_USE</code>。</p><h2 id="SSH-访问实例"><a href="#SSH-访问实例" class="headerlink" title="SSH 访问实例"></a>SSH 访问实例</h2><p>为了验证连接性，您可以使用网络创建一个实例 <code>$PRIVATE_NETWORK_NAME</code>（默认值：）<code>private</code>，使用<code>$PUBLIC_NETWORK_NAME</code>网络创建一个浮动 IP（默认值：）<code>public</code>，然后将此浮动 IP 附加到该实例：</p><figure class="highlight pgsql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs pgsql">openstack keypair <span class="hljs-keyword">create</span> <span class="hljs-comment">--public-key ~/.ssh/id_rsa.pub test-keypair</span><br>openstack <span class="hljs-keyword">server</span> <span class="hljs-keyword">create</span> <span class="hljs-comment">--network private --key-name test-keypair ... test-server</span><br>fip_id=$(openstack floating ip <span class="hljs-keyword">create</span> <span class="hljs-built_in">public</span> -f <span class="hljs-keyword">value</span> -c id)<br>openstack <span class="hljs-keyword">server</span> <span class="hljs-keyword">add</span> floating ip test-<span class="hljs-keyword">server</span> $&#123;fip_id&#125;<br></code></pre></td></tr></table></figure><p>完成后，请确保已为用于实例的安全组启用 SSH 和 ICMP（ping）访问。您可以创建自定义安全组并在创建实例时指定它，也可以在创建后添加它，或者您可以修改<code>default</code>每个项目默认创建的安全组。让我们做后者：</p><figure class="highlight pgsql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs pgsql">openstack <span class="hljs-keyword">security</span> <span class="hljs-keyword">group</span> <span class="hljs-keyword">rule</span> <span class="hljs-keyword">create</span> <span class="hljs-comment">--proto icmp --dst-port 0 default</span><br>openstack <span class="hljs-keyword">security</span> <span class="hljs-keyword">group</span> <span class="hljs-keyword">rule</span> <span class="hljs-keyword">create</span> <span class="hljs-comment">--proto tcp --dst-port 22 default</span><br></code></pre></td></tr></table></figure><p>最后，通过 SSH 进入实例。如果你使用了默认上传的 Cirros 实例，那么你可以运行以下命令：</p><figure class="highlight pgsql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs pgsql">openstack <span class="hljs-keyword">server</span> ssh test-<span class="hljs-keyword">server</span> <span class="hljs-comment">-- -l cirros</span><br></code></pre></td></tr></table></figure><p>这将使用<code>cirros</code>您在创建实例时配置的用户和密钥对进行连接。</p><h2 id="远程-SSH-访问实例"><a href="#远程-SSH-访问实例" class="headerlink" title="远程 SSH 访问实例"></a>远程 SSH 访问实例</h2><p>您还可以从其他主机通过 SSH 连接到 DevStack 主机上创建的实例。如果您正在现有云上的 VM 中部署 DevStack 并希望在本地计算机上进行开发，这将非常有用。有几种方法可以做到这一点。</p><p>配置实例可供本地访问</p><p>最明显的方法是将客户机配置为本地可访问，如上所述<a href="https://docs.openstack.org/devstack/latest/LocallyAccessibleGuests">。</a>这样做的好处是不需要在客户端上做进一步的工作。但是，这更复杂，需要云的支持或一些不明智的解决方法。</p><p>使用 DevStack 主机作为跳转主机</p><p>您可以选择使用 DevStack 主机作为跳转主机。要通过这种方式通过 SSH 连接到实例，请将标准<code>-J</code>选项传递给&#x2F; 命令。例如：<code>openstack ssh``ssh</code></p><figure class="highlight pgsql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs pgsql">openstack <span class="hljs-keyword">server</span> ssh test-<span class="hljs-keyword">server</span> <span class="hljs-comment">-- -l cirros -J username@devstack-host</span><br></code></pre></td></tr></table></figure><p>（其中<code>test-server</code>是现有实例的名称，如前所述 <a href="https://docs.openstack.org/devstack/latest/networking.html#ssh">，</a>和<code>username</code>是<code>devstack-host</code>您的 DevStack 主机的用户名和主机名）。</p><p>这也可以通过您的文件进行配置<code>~/.ssh/config</code>，因此非常轻松。但是，它仅允许 SSH 访问。如果您想要访问实例上的 Web 应用程序，则需要配置 SSH 隧道并使用选项转发选定端口<code>-L</code>。例如，要转发 HTTP 流量：</p><figure class="highlight pgsql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs pgsql">openstack <span class="hljs-keyword">server</span> ssh test-<span class="hljs-keyword">server</span> <span class="hljs-comment">-- -l cirros -L 8080:username@devstack-host:80</span><br></code></pre></td></tr></table></figure><p>（其中<code>test-server</code>是现有实例的名称，如前所述 <a href="https://docs.openstack.org/devstack/latest/networking.html#ssh">，</a>和<code>username</code>是<code>devstack-host</code>您的 DevStack 主机的用户名和主机名）。</p><p>你可以想象，这很快就会失控，特别是对于具有多个端口的更复杂的客户应用程序而言。</p><p>使用代理或 VPN 工具</p><p><code>$PUBLIC_NETWORK_NAME</code>您可以使用代理或 VPN 工具为由 (默认值：<code>public</code>) 定义的网络浮动 IP 地址范围(<code>$FLOATING_RANGE</code>默认值：<code>172.24.4.0/24</code>) 启用隧道。有许多此类工具可用于执行此操作。例如，我们可以使用一个名为<a href="https://github.com/sshuttle/sshuttle">shuttle</a>的实用实用程序。要使用 启用隧道<code>shuttle</code>，首先确保您已允许 SSH 和 HTTP(S) 流量到您的 DevStack 主机。允许 HTTP(S) 流量是必要的，这样您就可以远程使用 OpenStack API。如何执行此操作取决于您的 DevStack 主机的运行位置。完成后，<code>sshuttle</code>在您的本地主机上安装：</p><figure class="highlight cmake"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cmake">sudo apt-get <span class="hljs-keyword">install</span> sshuttle || yum <span class="hljs-keyword">install</span> sshuttle<br></code></pre></td></tr></table></figure><p>最后，<code>sshuttle</code>使用浮动 IP 地址范围在本地主机上启动。例如，假设您使用 的默认值 <code>$FLOATING_RANGE</code>，您可以执行以下操作：</p><figure class="highlight nginx"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs nginx"><span class="hljs-attribute">sshuttle</span> -r username<span class="hljs-variable">@devstack</span>-host <span class="hljs-number">172.24.4.0</span>/<span class="hljs-number">24</span><br></code></pre></td></tr></table></figure><p>（其中<code>username</code>和<code>devstack-host</code>是您的 DevStack 主机的用户名和主机名）。</p><p>您现在应该能够创建一个实例并通过 SSH 进入它：</p><figure class="highlight pgsql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs pgsql">openstack <span class="hljs-keyword">server</span> ssh test-<span class="hljs-keyword">server</span> <span class="hljs-comment">-- -l cirros</span><br></code></pre></td></tr></table></figure><p>（其中<code>test-server</code>是现有实例的名称， <a href="https://docs.openstack.org/devstack/latest/networking.html#ssh">如前所述</a>）</p><hr><p>你可以使用 <code>openstack server start</code> 命令来启动一个已经存在的虚拟机实例。</p><p>根据你提供的实例列表，虚拟机名称为 <code>t2</code>，它的状态是 <code>SHUTOFF</code>。你可以通过以下命令来启动它：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">openstack server start t2<br></code></pre></td></tr></table></figure><p>如果你想启动多个实例，可以用空格分隔多个实例名称或 ID。例如：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">openstack server start t2 another_instance<br></code></pre></td></tr></table></figure><p>另外，如果你想查看实例启动状态，可以使用：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">openstack server show t2<br></code></pre></td></tr></table></figure><p>如果有任何问题或需要进一步的帮助，随时告诉我！</p><p>要在外部连接 OpenStack 创建的实例，需要完成几个网络配置步骤：</p><ol><li>确保安全组配置正确：</li></ol><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 创建安全组规则，允许 SSH 访问（如果是 Linux 实例）</span><br>openstack security group rule create --protocol tcp --dst-port 22:22 --remote-ip 0.0.0.0/0 default<br><br><span class="hljs-comment"># 如果是 Windows 实例，需要开放 RDP 端口</span><br>openstack security group rule create --protocol tcp --dst-port 3389:3389 --remote-ip 0.0.0.0/0 default<br></code></pre></td></tr></table></figure><ol start="2"><li>分配浮动 IP：</li></ol><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 创建浮动 IP</span><br>openstack floating ip create external-network<br><br><span class="hljs-comment"># 将浮动 IP 绑定到实例</span><br>openstack server add floating ip INSTANCE_NAME FLOATING_IP<br></code></pre></td></tr></table></figure><ol start="3"><li>检查网络配置：</li></ol><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 查看实例的网络详情</span><br>openstack server show INSTANCE_NAME<br><br><span class="hljs-comment"># 查看网络列表</span><br>openstack network list<br><br><span class="hljs-comment"># 查看子网列表</span><br>openstack subnet list<br></code></pre></td></tr></table></figure><p>主要配置步骤：</p><ol><li>网络配置：</li></ol><ul><li>确保有外部网络（external network）</li><li>创建路由器连接内部网络和外部网络</li><li>配置正确的 CIDR 和网关</li></ul><ol start="2"><li>路由器配置：</li></ol><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 创建路由器</span><br>openstack router create ROUTER_NAME<br><br><span class="hljs-comment"># 设置路由器的外部网关</span><br>openstack router <span class="hljs-built_in">set</span> ROUTER_NAME --external-gateway external-network<br><br><span class="hljs-comment"># 将路由器连接到内部网络</span><br>openstack router add subnet ROUTER_NAME INTERNAL_SUBNET<br></code></pre></td></tr></table></figure><ol start="3"><li>验证连接：</li></ol><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># Linux 实例</span><br>ssh -i private_key.pem user@FLOATING_IP<br><br><span class="hljs-comment"># Windows 实例</span><br><span class="hljs-comment"># 使用远程桌面连接 FLOATING_IP</span><br></code></pre></td></tr></table></figure><p>常见问题检查：</p><ol><li>确保安全组规则正确配置</li><li>验证网络拓扑是否正确</li><li>检查路由器配置</li><li>确认浮动 IP 是否正确绑定</li><li>检查实例的网络接口配置</li></ol><p>如果仍然无法连接：</p><ol><li>检查实例内部的网络配置</li><li>验证防火墙规则</li><li>查看 OpenStack 网络服务日志</li><li>确认 IP 转发是否启用</li><li>检查 DNS 配置</li></ol><p>浮动IP（Floating IP）可以理解为公网IP，它允许OpenStack内部的实例能够被外网访问。让我用一个简单的类比来解释：</p><p>想象一个小区（OpenStack环境）：</p><ul><li>内部网络（Private IP）就像小区内部的房间号，只能在小区内部使用</li><li>浮动IP（Floating IP）就像小区的对外门牌号，外部人员可以通过这个找到你</li></ul><p>具体工作原理：</p><ol><li>默认情况下，OpenStack实例只有内部IP：</li></ol><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs plaintext">实例A: 192.168.1.10 (内部IP，只能在OpenStack内部访问)<br></code></pre></td></tr></table></figure><ol start="2"><li>分配浮动IP后：</li></ol><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs plaintext">实例A: <br>- 内部IP: 192.168.1.10<br>- 浮动IP: 203.0.113.101 (可以从外网访问)<br></code></pre></td></tr></table></figure><p>配置步骤：</p><ol><li>创建浮动IP：</li></ol><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 从外部网络池中创建一个浮动IP</span><br>openstack floating ip create external-network<br></code></pre></td></tr></table></figure><ol start="2"><li>查看可用的浮动IP：</li></ol><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">openstack floating ip list<br></code></pre></td></tr></table></figure><ol start="3"><li>分配给实例：</li></ol><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 方式1：通过命令行</span><br>openstack server add floating ip 实例名称 浮动IP地址<br><br><span class="hljs-comment"># 方式2：通过Web界面</span><br><span class="hljs-comment"># Instances -&gt; Actions -&gt; Associate Floating IP</span><br></code></pre></td></tr></table></figure><p>使用场景：</p><ol><li>需要从外网SSH连接到实例</li><li>实例需要提供对外服务（如网站）</li><li>实例需要访问外网资源</li></ol><p>注意事项：</p><ol><li>浮动IP是有限资源，用完后需要及时释放</li><li>一个实例可以有多个浮动IP</li><li>浮动IP可以在实例之间迁移</li><li>需要配置正确的安全组规则才能访问</li></ol>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>2024-12-08 devstack</title>
    <link href="/2024/12/09/2024-12-08%20devstack/"/>
    <url>/2024/12/09/2024-12-08%20devstack/</url>
    
    <content type="html"><![CDATA[<h1 id="DevStack"><a href="#DevStack" class="headerlink" title="DevStack"></a>DevStack</h1><p><img src="https://picture-base.oss-cn-hangzhou.aliyuncs.com/logo-blue.png" alt="_images/徽标-蓝色.png"></p><p>DevStack 是一系列可扩展的脚本，用于根据来自 git master 的所有内容的最新版本快速构建完整的 OpenStack 环境。它可交互地用作开发环境，并作为 OpenStack 项目大部分功能测试的基础。</p><p>源代码位于<a href="https://opendev.org/openstack/devstack%E3%80%82">https://opendev.org/openstack/devstack。</a></p><p>警告</p><p>DevStack 将在安装过程中对您的系统进行重大更改。仅在专用于此目的的服务器或虚拟机上运行 DevStack。</p><h2 id="快速入门"><a href="#快速入门" class="headerlink" title="快速入门"></a>快速入门</h2><h3 id="安装-Linux"><a href="#安装-Linux" class="headerlink" title="安装 Linux"></a>安装 Linux</h3><p>从干净且最小限度的 Linux 系统安装开始。DevStack 尝试支持 Ubuntu 的两个最新 LTS 版本：Rocky Linux 9 和 openEuler。</p><p>如果您没有偏好，Ubuntu 22.04（Jammy）是经过最多测试的，并且可能会运行得最顺利。</p><h3 id="添加-Stack-用户（可选）"><a href="#添加-Stack-用户（可选）" class="headerlink" title="添加 Stack 用户（可选）"></a>添加 Stack 用户（可选）</h3><p>DevStack 应该以非 root 用户身份运行，并启用 sudo（通常使用“ubuntu”或“cloud-user”等云镜像的标准登录就可以了）。</p><p>如果你不使用云镜像，你可以创建一个单独的堆栈用户来运行 DevStack</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell"><span class="hljs-meta prompt_">$ </span><span class="language-bash"><span class="hljs-built_in">sudo</span> useradd -s /bin/bash -d /opt/stack -m stack</span><br></code></pre></td></tr></table></figure><p>确保用户的主目录<code>stack</code>对所有人都具有可执行权限，因为基于 RHEL 的发行版会创建它<code>700</code>，而 Ubuntu 21.04+<code>750</code> 可能会在部署期间导致问题。</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell"><span class="hljs-meta prompt_">$ </span><span class="language-bash"><span class="hljs-built_in">sudo</span> <span class="hljs-built_in">chmod</span> +x /opt/stack</span><br></code></pre></td></tr></table></figure><p>由于该用户将对您的系统进行许多更改，因此它应该具有 sudo 权限：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs shell"><span class="hljs-meta prompt_">$ </span><span class="language-bash"><span class="hljs-built_in">echo</span> <span class="hljs-string">&quot;stack ALL=(ALL) NOPASSWD: ALL&quot;</span> | <span class="hljs-built_in">sudo</span> <span class="hljs-built_in">tee</span> /etc/sudoers.d/stack</span><br><span class="hljs-meta prompt_">$ </span><span class="language-bash"><span class="hljs-built_in">sudo</span> -u stack -i</span><br></code></pre></td></tr></table></figure><h3 id="下载-DevStack"><a href="#下载-DevStack" class="headerlink" title="下载 DevStack"></a>下载 DevStack</h3><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs shell"><span class="hljs-meta prompt_">$ </span><span class="language-bash">git <span class="hljs-built_in">clone</span> https://opendev.org/openstack/devstack</span><br><span class="hljs-meta prompt_">$ </span><span class="language-bash"><span class="hljs-built_in">cd</span> devstack</span><br></code></pre></td></tr></table></figure><p>该<code>devstack</code>repo 包含安装 OpenStack 的脚本和配置文件的模板。</p><h3 id="创建-local-conf"><a href="#创建-local-conf" class="headerlink" title="创建 local.conf"></a>创建 local.conf</h3><p><code>local.conf</code>在 devstack git repo 的根目录创建一个预设有四个密码的文件。</p><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs ini"><span class="hljs-section">[[local|localrc]]</span><br><span class="hljs-attr">ADMIN_PASSWORD</span>=secret<br><span class="hljs-attr">DATABASE_PASSWORD</span>=<span class="hljs-variable">$ADMIN_PASSWORD</span><br><span class="hljs-attr">RABBIT_PASSWORD</span>=<span class="hljs-variable">$ADMIN_PASSWORD</span><br><span class="hljs-attr">SERVICE_PASSWORD</span>=<span class="hljs-variable">$ADMIN_PASSWORD</span><br></code></pre></td></tr></table></figure><p>这是开始使用 DevStack 所需的最低配置。</p><p>笔记</p><p>devstack 存储库中的<em>samples</em>目录下有一个示例<a href="https://docs.openstack.org/devstack/latest/_downloads/d6fbba8d6ab5e970a86dd2ca0b884098/local.conf"><code>local.conf</code></a>文件。</p><p>警告</p><p>密码中只能使用字母数字字符，因为某些服务在使用特殊字符时无法运行。</p><h3 id="开始安装"><a href="#开始安装" class="headerlink" title="开始安装"></a>开始安装</h3><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell"><span class="hljs-meta prompt_">$ </span><span class="language-bash">./stack.sh</span><br></code></pre></td></tr></table></figure><p>这将需要 15 - 30 分钟，主要取决于您的互联网连接速度。在此过程中将安装许多 git 树和包。</p><h3 id="利润！"><a href="#利润！" class="headerlink" title="利润！"></a>利润！</h3><p>您现在有一个可以运行的 DevStack！恭喜！</p><p>您的 devstack 将安装<code>keystone</code>、<code>glance</code>、<code>nova</code>、 <code>placement</code>、<code>cinder</code>、<code>neutron</code>和<code>horizon</code>。浮动 IP 将可用，访客可以访问外部世界。</p><p>您可以访问 Horizon 来体验 OpenStack 的 Web 界面，并从那里管理虚拟机、网络、卷和图像。</p><p>您可以在您的 shell 中，然后使用 命令行工具来管理您的 devstack。<code>source openrc``openstack</code></p><p>您可以<a href="https://docs.openstack.org/devstack/latest/networking.html#ssh">创建一个虚拟机并通过 SSH 连接到它</a>。</p><p>您可以运行已配置为与您的 devstack 配合使用的 tempest 测试。<code>cd /opt/stack/tempest</code></p><p>您可以<a href="https://docs.openstack.org/devstack/latest/development.html">对 OpenStack 代码进行更改并验证它们</a>。</p><h3 id="进一步了解"><a href="#进一步了解" class="headerlink" title="进一步了解"></a>进一步了解</h3><p>详细了解我们的<a href="https://docs.openstack.org/devstack/latest/configuration.html">配置系统，</a>以便根据您的需要定制 devstack。包括对默认<a href="https://docs.openstack.org/devstack/latest/networking.html">网络</a>进行调整。</p><p>阅读人们所具有的特定设置的<a href="https://docs.openstack.org/devstack/latest/guides.html">指南</a>（注意：指南是即时贡献，可能并不总是与最新的 devstack 保持同步）。</p><p>启用<a href="https://docs.openstack.org/devstack/latest/plugins.html">devstack 插件</a>来支持基本 devstack 中不存在的附加服务、功能和配置。</p><p>在您的 CI 中将 devstack 与<a href="https://docs.openstack.org/devstack/latest/zuul_roles.html">Ansible 角色</a>和 Zuul V3<a href="https://docs.openstack.org/devstack/latest/zuul_jobs.html">作业结合使用。使用此完整迁移</a><a href="https://docs.openstack.org/devstack/latest/zuul_ci_jobs_migration.html">方法</a>将您的 devstack Zuul V2 作业迁移到 Zuul V3 。</p><p>了解<a href="https://docs.openstack.org/devstack/latest/overview.html">我们</a>尝试使用 devstack 做什么，并通过<a href="https://docs.openstack.org/devstack/latest/hacking.html">对项目做出贡献</a>来帮助我们。</p><p>如果您是 devstack 的新贡献者，请参阅：<a href="https://docs.openstack.org/devstack/latest/contributor/contributing.html">所以您想贡献……</a></p><h2 id="内容"><a href="#内容" class="headerlink" title="内容"></a>内容</h2><ul><li>配置<ul><li><a href="https://docs.openstack.org/devstack/latest/configuration.html#local-conf">本地配置文件</a></li><li><a href="https://docs.openstack.org/devstack/latest/configuration.html#openrc">openrc</a></li><li><a href="https://docs.openstack.org/devstack/latest/configuration.html#minimal-configuration">最低配置</a></li><li><a href="https://docs.openstack.org/devstack/latest/configuration.html#historical-notes">历史注释</a></li><li><a href="https://docs.openstack.org/devstack/latest/configuration.html#configuration-notes">配置注意事项</a></li></ul></li><li>系统范围的调试<ul><li><a href="https://docs.openstack.org/devstack/latest/debugging.html#dstat">统计信息</a></li><li><a href="https://docs.openstack.org/devstack/latest/debugging.html#memory-tracker">内存跟踪器</a></li><li><a href="https://docs.openstack.org/devstack/latest/debugging.html#file-tracker">文件跟踪器</a></li><li><a href="https://docs.openstack.org/devstack/latest/debugging.html#tcpdump">tcpdump</a></li></ul></li><li>使用 Devstack 进行开发<ul><li><a href="https://docs.openstack.org/devstack/latest/development.html#inspecting-services">检查服务</a></li><li><a href="https://docs.openstack.org/devstack/latest/development.html#patching-a-service">修补服务</a></li><li><a href="https://docs.openstack.org/devstack/latest/development.html#testing-a-patch-series">测试补丁系列</a></li><li><a href="https://docs.openstack.org/devstack/latest/development.html#testing-changes-to-libraries">测试库的更改</a></li></ul></li><li>常问问题<ul><li><a href="https://docs.openstack.org/devstack/latest/faq.html#general-questions">常见问题</a></li><li><a href="https://docs.openstack.org/devstack/latest/faq.html#operation-and-configuration">操作和配置</a></li><li><a href="https://docs.openstack.org/devstack/latest/faq.html#miscellaneous">各种各样的</a></li></ul></li><li>指南<ul><li><a href="https://docs.openstack.org/devstack/latest/guides.html#all-in-one-single-vm">一体化单虚拟机</a></li><li><a href="https://docs.openstack.org/devstack/latest/guides.html#all-in-one-single-machine">一体机</a></li><li><a href="https://docs.openstack.org/devstack/latest/guides.html#all-in-one-lxc-container">一体化 LXC 容器</a></li><li><a href="https://docs.openstack.org/devstack/latest/guides.html#multi-node-lab">多节点实验室</a></li><li><a href="https://docs.openstack.org/devstack/latest/guides.html#devstack-with-neutron-networking">带有 Neutron 网络的 DevStack</a></li><li><a href="https://docs.openstack.org/devstack/latest/guides.html#devstack-with-kvm-based-nested-virtualization">基于 KVM 的嵌套虚拟化的 DevStack</a></li><li><a href="https://docs.openstack.org/devstack/latest/guides.html#nova-and-devstack">Nova 和 devstack</a></li><li><a href="https://docs.openstack.org/devstack/latest/guides.html#configure-octavia">配置 Octavia</a></li><li><a href="https://docs.openstack.org/devstack/latest/guides.html#deploying-devstack-with-ldap">使用 LDAP 部署 DevStack</a></li></ul></li><li>为 DevStack 做出贡献<ul><li><a href="https://docs.openstack.org/devstack/latest/hacking.html#general">一般的</a></li><li><a href="https://docs.openstack.org/devstack/latest/hacking.html#repo-layout">仓库布局</a></li><li><a href="https://docs.openstack.org/devstack/latest/hacking.html#scripts">脚本</a></li><li><a href="https://docs.openstack.org/devstack/latest/hacking.html#stackrc">stackrc</a></li><li><a href="https://docs.openstack.org/devstack/latest/hacking.html#documentation">文档</a></li><li><a href="https://docs.openstack.org/devstack/latest/hacking.html#whitespace-rules">空格规则</a></li><li><a href="https://docs.openstack.org/devstack/latest/hacking.html#control-structure-rules">控制结构规则</a></li><li><a href="https://docs.openstack.org/devstack/latest/hacking.html#variables-and-functions">变量和函数</a></li><li><a href="https://docs.openstack.org/devstack/latest/hacking.html#review-criteria">评审准则</a></li><li><a href="https://docs.openstack.org/devstack/latest/hacking.html#making-changes-testing-and-ci">进行更改、测试和 CI</a></li></ul></li><li>DevStack 网络<ul><li><a href="https://docs.openstack.org/devstack/latest/networking.html#defaults">默认值</a></li><li><a href="https://docs.openstack.org/devstack/latest/networking.html#locally-accessible-guests">当地无障碍宾客</a></li><li><a href="https://docs.openstack.org/devstack/latest/networking.html#private-network-addressing">专用网络寻址</a></li><li><a href="https://docs.openstack.org/devstack/latest/networking.html#ssh-access-to-instances">SSH 访问实例</a></li><li><a href="https://docs.openstack.org/devstack/latest/networking.html#remote-ssh-access-to-instances">远程 SSH 访问实例</a></li></ul></li><li>概述<ul><li><a href="https://docs.openstack.org/devstack/latest/overview.html#supported-components">支持的组件</a></li></ul></li><li><a href="https://docs.openstack.org/devstack/latest/plugin-registry.html">DevStack 插件注册表</a></li><li>插件<ul><li><a href="https://docs.openstack.org/devstack/latest/plugins.html#prerequisites">先决条件</a></li><li><a href="https://docs.openstack.org/devstack/latest/plugins.html#plugin-interface">插件接口</a></li><li><a href="https://docs.openstack.org/devstack/latest/plugins.html#plugin-sh-contract">plugin.sh 合约</a></li><li><a href="https://docs.openstack.org/devstack/latest/plugins.html#example-plugin">示例插件</a></li><li><a href="https://docs.openstack.org/devstack/latest/plugins.html#plugin-execution-order">插件执行顺序</a></li><li><a href="https://docs.openstack.org/devstack/latest/plugins.html#system-packages">系统包</a></li><li><a href="https://docs.openstack.org/devstack/latest/plugins.html#using-plugins-in-the-openstack-gate">在 OpenStack Gate 中使用插件</a></li><li><a href="https://docs.openstack.org/devstack/latest/plugins.html#see-also">参见</a></li></ul></li><li>在 DevStack 中使用 Systemd<ul><li><a href="https://docs.openstack.org/devstack/latest/systemd.html#why-this-instead-of-screen">为什么用这个来代替屏幕？</a></li><li><a href="https://docs.openstack.org/devstack/latest/systemd.html#unit-structure">单元结构</a></li><li><a href="https://docs.openstack.org/devstack/latest/systemd.html#manipulating-units">操控单位</a></li><li><a href="https://docs.openstack.org/devstack/latest/systemd.html#querying-logs">查询日志</a></li><li><a href="https://docs.openstack.org/devstack/latest/systemd.html#debugging">调试</a></li><li><a href="https://docs.openstack.org/devstack/latest/systemd.html#future-work">未来工作</a></li><li><a href="https://docs.openstack.org/devstack/latest/systemd.html#references">参考</a></li></ul></li><li><a href="https://docs.openstack.org/devstack/latest/tempest.html">暴风雨</a></li><li>将 Zuul V2 CI 作业迁移到 V3<ul><li><a href="https://docs.openstack.org/devstack/latest/zuul_ci_jobs_migration.html#where-to-host-the-job-definitions">在哪里托管作业定义。</a></li><li><a href="https://docs.openstack.org/devstack/latest/zuul_ci_jobs_migration.html#which-base-job-to-start-from">从哪个基础工作开始</a></li><li><a href="https://docs.openstack.org/devstack/latest/zuul_ci_jobs_migration.html#job-variables">作业变量</a></li><li><a href="https://docs.openstack.org/devstack/latest/zuul_ci_jobs_migration.html#devstack-gate-flags">DevStack Gate 标志</a></li></ul></li><li><a href="https://docs.openstack.org/devstack/latest/zuul_jobs.html">Zuul CI 职位</a></li><li>[Zuul CI 角色</li><li><a href="https://docspenstack.org/devstack/latest/zuul_roles.html"></a></li></ul><p>.&#x2F;stack.sh status</p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>2024-12-08 mutilpass操作</title>
    <link href="/2024/12/09/2024-12-08%20mutilpass%E6%93%8D%E4%BD%9C/"/>
    <url>/2024/12/09/2024-12-08%20mutilpass%E6%93%8D%E4%BD%9C/</url>
    
    <content type="html"><![CDATA[<p>如果你想销毁一个已经创建的 <code>multipass</code> 虚拟机（例如 <code>openstack-vm</code>），可以按照以下步骤操作：</p><h3 id="1-停止虚拟机"><a href="#1-停止虚拟机" class="headerlink" title="1. 停止虚拟机"></a>1. <strong>停止虚拟机</strong></h3><p>首先，停止虚拟机：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">multipass stop openstack-vm<br></code></pre></td></tr></table></figure><p>这将停止虚拟机，但不会删除它。如果你只想关闭虚拟机，可以跳过删除步骤。</p><h3 id="2-删除虚拟机"><a href="#2-删除虚拟机" class="headerlink" title="2. 删除虚拟机"></a>2. <strong>删除虚拟机</strong></h3><p>要彻底销毁虚拟机并删除它的所有相关文件，你需要使用 <code>multipass delete</code> 命令：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">multipass delete openstack-vm<br></code></pre></td></tr></table></figure><p>这将删除虚拟机，但是文件系统和相关数据可能会被保留。</p><h3 id="3-清理虚拟机数据"><a href="#3-清理虚拟机数据" class="headerlink" title="3. 清理虚拟机数据"></a>3. <strong>清理虚拟机数据</strong></h3><p>如果你希望完全清除虚拟机及其所有相关数据（包括磁盘映像、配置文件等），可以使用 <code>multipass purge</code> 命令：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">multipass purge<br></code></pre></td></tr></table></figure><p>这将清除所有已删除虚拟机的所有磁盘和缓存数据。</p><h3 id="完整的操作步骤"><a href="#完整的操作步骤" class="headerlink" title="完整的操作步骤"></a>完整的操作步骤</h3><ol><li><p><strong>停止虚拟机</strong>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">multipass stop openstack-vm<br></code></pre></td></tr></table></figure></li><li><p><strong>删除虚拟机</strong>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">multipass delete openstack-vm<br></code></pre></td></tr></table></figure></li><li><p><strong>清理磁盘和缓存数据</strong>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">multipass purge<br></code></pre></td></tr></table></figure></li></ol><h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><ul><li><code>multipass stop &lt;vm-name&gt;</code>：停止虚拟机。</li><li><code>multipass delete &lt;vm-name&gt;</code>：删除虚拟机。</li><li><code>multipass purge</code>：清除删除的虚拟机的所有相关数据。</li></ul><p>这些步骤将帮助你完全销毁虚拟机。如果你需要重新创建虚拟机，可以再次使用 <code>multipass launch</code> 命令进行创建。</p><p><img src="https://picture-base.oss-cn-hangzhou.aliyuncs.com/image-20241208082002247.png" alt="image-20241208082002247"></p><p><img src="https://picture-base.oss-cn-hangzhou.aliyuncs.com/image-20241208081930313.png"></p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>2024-12-07 microk8s</title>
    <link href="/2024/12/09/2024-12-07%20microk8s/"/>
    <url>/2024/12/09/2024-12-07%20microk8s/</url>
    
    <content type="html"><![CDATA[<p>在mutipass中启动ubuntu虚拟机</p><figure class="highlight mipsasm"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs mipsasm"><span class="hljs-keyword">multipass </span><span class="hljs-keyword">shell </span>microk8s-vm<br></code></pre></td></tr></table></figure><p><strong>MicroK8s</strong> 是一个由 <strong>Canonical</strong>（Ubuntu 的开发商）维护的轻量级 Kubernetes 发行版。它旨在为开发者、运维人员和边缘计算场景提供一个快速、简便的 Kubernetes 部署方式，适用于本地开发、测试环境、边缘设备以及生产场景。</p><hr><h3 id="MicroK8s-的特点"><a href="#MicroK8s-的特点" class="headerlink" title="MicroK8s 的特点"></a><strong>MicroK8s 的特点</strong></h3><ol><li><strong>轻量级</strong><ul><li>微内核设计，占用资源少，适合运行在笔记本电脑、工作站、树莓派或其他资源受限的设备上。</li><li>默认禁用许多附加组件，只有基本的 Kubernetes 核心功能。</li></ul></li><li><strong>易安装</strong><ul><li>提供单命令安装，尤其在 Ubuntu 系统上，使用 <code>snap</code> 包管理器即可快速安装。</li><li>支持多种操作系统，包括 Linux（Ubuntu、CentOS 等）、Windows 和 macOS（通过虚拟化工具）。</li></ul></li><li><strong>模块化</strong><ul><li>提供了多种可选的附加组件（addons），如 <code>dns</code>、<code>ingress</code>、<code>storage</code>、<code>helm</code> 等，用户可以根据需要启用或禁用这些组件。</li></ul></li><li><strong>开箱即用</strong><ul><li>直接内置了 Kubernetes 集群的核心组件，如 API Server、kubelet 和调度器，用户无需额外配置即可开始使用。</li></ul></li><li><strong>自动升级</strong><ul><li>使用 <code>snap</code> 管理，支持自动更新到最新版本。</li></ul></li><li><strong>跨平台</strong><ul><li>支持多种硬件架构（如 x86、ARM），适合开发、测试和物联网边缘计算场景。</li></ul></li><li><strong>兼容性</strong><ul><li>完全兼容 Kubernetes API，可以运行大多数 Kubernetes 应用程序。</li><li>支持与 kubectl、Helm 等工具集成。</li></ul></li></ol><hr><h3 id="MicroK8s-的用途"><a href="#MicroK8s-的用途" class="headerlink" title="MicroK8s 的用途"></a><strong>MicroK8s 的用途</strong></h3><ol><li><strong>本地开发与测试</strong><ul><li>开发人员可以在本地快速启动一个 Kubernetes 集群，用于应用开发、测试和调试。</li></ul></li><li><strong>学习 Kubernetes</strong><ul><li>对于新手来说，MicroK8s 是学习 Kubernetes 的理想工具，因为它简单易用且资源需求低。</li></ul></li><li><strong>边缘计算</strong><ul><li>MicroK8s 轻量级的特性使其适合运行在物联网设备或资源有限的边缘设备上。</li></ul></li><li><strong>CI&#x2F;CD 测试环境</strong><ul><li>用于快速创建临时 Kubernetes 环境以运行持续集成或自动化测试。</li></ul></li><li><strong>单节点或小型生产环境</strong><ul><li>在资源有限的情况下，可以作为单节点 Kubernetes 集群使用。</li></ul></li></ol><hr><h3 id="MicroK8s-的安装与使用"><a href="#MicroK8s-的安装与使用" class="headerlink" title="MicroK8s 的安装与使用"></a><strong>MicroK8s 的安装与使用</strong></h3><h4 id="安装-MicroK8s"><a href="#安装-MicroK8s" class="headerlink" title="安装 MicroK8s"></a><strong>安装 MicroK8s</strong></h4><ol><li><p><strong>在 Ubuntu 上安装</strong> 使用 <code>snap</code> 进行安装：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> snap install microk8s --classic<br></code></pre></td></tr></table></figure><p>安装完成后，检查版本：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">microk8s version<br></code></pre></td></tr></table></figure></li><li><p><strong>在其他 Linux 系统上安装</strong> 在支持 <code>snap</code> 的 Linux 系统上，同样可以使用 <code>snap</code> 安装。</p></li><li><p><strong>在 macOS 或 Windows 上安装</strong> 使用 <code>Multipass</code> 或其他虚拟化工具（如 VirtualBox）运行 MicroK8s。</p></li></ol><h4 id="常用命令"><a href="#常用命令" class="headerlink" title="常用命令"></a><strong>常用命令</strong></h4><ol><li><p>启用或禁用附加组件：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">microk8s <span class="hljs-built_in">enable</span> dns ingress storage helm3<br>microk8s <span class="hljs-built_in">disable</span> dns<br></code></pre></td></tr></table></figure></li><li><p>查看 Kubernetes 资源： 使用内置的 <code>kubectl</code>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">microk8s kubectl get nodes<br>microk8s kubectl get pods -A<br></code></pre></td></tr></table></figure></li><li><p>检查 MicroK8s 的状态：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">microk8s status<br></code></pre></td></tr></table></figure></li><li><p>重启或停止服务：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">microk8s stop<br>microk8s start<br></code></pre></td></tr></table></figure></li><li><p>配置用户权限（非 root 用户）：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> usermod -a -G microk8s <span class="hljs-variable">$USER</span><br><span class="hljs-built_in">sudo</span> <span class="hljs-built_in">chown</span> -R <span class="hljs-variable">$USER</span> ~/.kube<br>newgrp microk8s<br></code></pre></td></tr></table></figure></li></ol><hr><h3 id="MicroK8s-与其他-Kubernetes-发行版的对比"><a href="#MicroK8s-与其他-Kubernetes-发行版的对比" class="headerlink" title="MicroK8s 与其他 Kubernetes 发行版的对比"></a><strong>MicroK8s 与其他 Kubernetes 发行版的对比</strong></h3><table><thead><tr><th>特性</th><th>MicroK8s</th><th>Minikube</th><th>K3s</th></tr></thead><tbody><tr><td><strong>轻量化</strong></td><td>是</td><td>部分轻量化</td><td>极轻量化</td></tr><tr><td><strong>安装方式</strong></td><td>单命令（snap 包）</td><td>VirtualBox 或其他虚拟化工具</td><td>二进制或安装脚本</td></tr><tr><td><strong>资源需求</strong></td><td>低</td><td>较高</td><td>极低</td></tr><tr><td><strong>生产环境支持</strong></td><td>支持（单节点或小型环境）</td><td>不推荐</td><td>支持</td></tr><tr><td><strong>附加组件管理</strong></td><td>支持，模块化组件</td><td>手动安装</td><td>部分模块化</td></tr><tr><td><strong>云原生兼容性</strong></td><td>完全兼容</td><td>完全兼容</td><td>部分特性简化</td></tr></tbody></table><hr><h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a><strong>总结</strong></h3><p>MicroK8s 是一个轻量级且功能强大的 Kubernetes 发行版，专为开发者、测试环境和边缘计算场景设计。它具有易安装、易管理、资源占用低等优点，同时保留了 Kubernetes 的核心功能，非常适合想快速入门 Kubernetes 或在本地进行开发测试的用户。</p><p>如果你需要一个即开即用的 Kubernetes 集群，无论是本地开发还是边缘计算，MicroK8s 都是一个理想的选择。</p><p>在 <strong>macOS</strong> 上安装 <strong>MicroK8s</strong> 相较于 Linux 环境略有不同，因为 macOS 本身不支持直接使用 <code>snap</code> 安装。要在 macOS 上安装 MicroK8s，你需要借助 <strong>Multipass</strong>（一个轻量级虚拟机管理工具）来运行一个虚拟机，从而部署 MicroK8s。</p><p>以下是详细的安装步骤：</p><hr><h3 id="1-安装-Multipass"><a href="#1-安装-Multipass" class="headerlink" title="1. 安装 Multipass"></a><strong>1. 安装 Multipass</strong></h3><p><strong>Multipass</strong> 是一个由 Canonical（Ubuntu 的开发公司）开发的工具，能够轻松管理 Ubuntu 虚拟机。在 macOS 上安装 MicroK8s 的步骤首先是通过 Multipass 启动一个虚拟机。</p><h4 id="（1）安装-Multipass"><a href="#（1）安装-Multipass" class="headerlink" title="（1）安装 Multipass"></a>（1）安装 Multipass</h4><p>你可以通过 Homebrew 安装 Multipass：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">brew install --cask multipass<br></code></pre></td></tr></table></figure><h4 id="（2）检查安装是否成功"><a href="#（2）检查安装是否成功" class="headerlink" title="（2）检查安装是否成功"></a>（2）检查安装是否成功</h4><p>安装完成后，可以检查 Multipass 是否安装成功：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">multipass version<br></code></pre></td></tr></table></figure><p>如果安装成功，你将看到 Multipass 的版本信息。</p><hr><h3 id="2-使用-Multipass-启动-Ubuntu-虚拟机"><a href="#2-使用-Multipass-启动-Ubuntu-虚拟机" class="headerlink" title="2. 使用 Multipass 启动 Ubuntu 虚拟机"></a><strong>2. 使用 Multipass 启动 Ubuntu 虚拟机</strong></h3><p>MicroK8s 需要 Ubuntu 系统来运行，因此接下来你需要在 macOS 上通过 Multipass 创建一个 Ubuntu 虚拟机。</p><h4 id="（1）启动-Ubuntu-虚拟机"><a href="#（1）启动-Ubuntu-虚拟机" class="headerlink" title="（1）启动 Ubuntu 虚拟机"></a>（1）启动 Ubuntu 虚拟机</h4><p>执行以下命令启动一个 Ubuntu 虚拟机（例如，启动一个 4GB 内存的 Ubuntu 20.04 虚拟机）：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">multipass launch --name microk8s-vm --mem 4G --disk 20G 20.04<br></code></pre></td></tr></table></figure><p>这将启动一个名为 <code>microk8s-vm</code> 的虚拟机，分配 4GB 内存和 20GB 硬盘。</p><h4 id="（2）进入虚拟机"><a href="#（2）进入虚拟机" class="headerlink" title="（2）进入虚拟机"></a>（2）进入虚拟机</h4><p>进入虚拟机：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">multipass shell microk8s-vm<br></code></pre></td></tr></table></figure><hr><h3 id="3-在虚拟机中安装-MicroK8s"><a href="#3-在虚拟机中安装-MicroK8s" class="headerlink" title="3. 在虚拟机中安装 MicroK8s"></a><strong>3. 在虚拟机中安装 MicroK8s</strong></h3><p>进入 Ubuntu 虚拟机后，你可以按照以下步骤安装 MicroK8s。</p><h4 id="（1）安装-MicroK8s"><a href="#（1）安装-MicroK8s" class="headerlink" title="（1）安装 MicroK8s"></a>（1）安装 MicroK8s</h4><p>在虚拟机内，运行以下命令安装 MicroK8s：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> snap install microk8s --classic<br></code></pre></td></tr></table></figure><h4 id="（2）验证安装"><a href="#（2）验证安装" class="headerlink" title="（2）验证安装"></a>（2）验证安装</h4><p>安装完成后，检查 MicroK8s 是否正常运行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">microk8s status --wait-ready<br></code></pre></td></tr></table></figure><p>这条命令会等待直到 MicroK8s 准备好并且可以正常使用。</p><figure class="highlight livecodeserver"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs livecodeserver">ubuntu@microk8s-vm:~$ sudo snap install microk8s <span class="hljs-comment">--classic</span><br>microk8s (<span class="hljs-number">1.31</span>/stable) v1<span class="hljs-number">.31</span><span class="hljs-number">.3</span> <span class="hljs-built_in">from</span> Canonical✓ installed<br>ubuntu@microk8s-vm:~$ microk8s status <span class="hljs-comment">--wait-ready</span><br>Insufficient permissions <span class="hljs-built_in">to</span> access MicroK8s.<br>You can either <span class="hljs-keyword">try</span> again <span class="hljs-keyword">with</span> sudo <span class="hljs-keyword">or</span> <span class="hljs-built_in">add</span> <span class="hljs-keyword">the</span> user ubuntu <span class="hljs-built_in">to</span> <span class="hljs-keyword">the</span> <span class="hljs-string">&#x27;microk8s&#x27;</span> group:<br><br>    sudo usermod -<span class="hljs-keyword">a</span> -G microk8s ubuntu<br>    sudo chown -R ubuntu ~/.kube<br><br>After this, reload <span class="hljs-keyword">the</span> user groups either via <span class="hljs-keyword">a</span> reboot <span class="hljs-keyword">or</span> <span class="hljs-keyword">by</span> running <span class="hljs-string">&#x27;newgrp microk8s&#x27;</span>.<br>ubuntu@microk8s-vm:~$<br></code></pre></td></tr></table></figure><p>这个错误信息表示，当前用户 <code>ubuntu</code> 没有足够的权限来访问 MicroK8s。为了修复这个问题，你需要将 <code>ubuntu</code> 用户添加到 <strong>microk8s</strong> 组，并更改相应的权限。错误信息中已经给出了修复步骤，下面是详细的解释和步骤：</p><h3 id="问题的原因"><a href="#问题的原因" class="headerlink" title="问题的原因"></a><strong>问题的原因</strong></h3><p>MicroK8s 在安装时会创建一个名为 <code>microk8s</code> 的组，只有该组的成员才能访问 MicroK8s 的配置和执行相关命令。默认情况下，普通用户并不在 <code>microk8s</code> 组中，因此无法访问和管理 MicroK8s。</p><h3 id="解决方法"><a href="#解决方法" class="headerlink" title="解决方法"></a><strong>解决方法</strong></h3><ol><li><p><strong>将用户添加到 <code>microk8s</code> 组</strong> 你需要将 <code>ubuntu</code> 用户添加到 <code>microk8s</code> 组中。这可以通过以下命令完成：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> usermod -a -G microk8s ubuntu<br></code></pre></td></tr></table></figure></li><li><p><strong>修改权限</strong> 接下来，需要修改 Kubernetes 配置文件的权限，使得 <code>ubuntu</code> 用户可以访问它：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> <span class="hljs-built_in">chown</span> -R ubuntu ~/.kube<br></code></pre></td></tr></table></figure></li><li><p><strong>重新加载用户组</strong> 上述步骤会将用户添加到 <code>microk8s</code> 组，并将 <code>.kube</code> 目录的权限更新。为了让权限生效，重新加载用户组，可以使用以下命令：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">newgrp microk8s<br></code></pre></td></tr></table></figure><p>这会立即使 <code>ubuntu</code> 用户的权限更新，而不需要重启计算机。</p></li></ol><h3 id="4-再次检查-MicroK8s-状态"><a href="#4-再次检查-MicroK8s-状态" class="headerlink" title="4. 再次检查 MicroK8s 状态"></a><strong>4. 再次检查 MicroK8s 状态</strong></h3><p>完成上述步骤后，你应该可以再次运行 <code>microk8s status</code> 命令来查看 MicroK8s 的状态：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">microk8s status --wait-ready<br></code></pre></td></tr></table></figure><h3 id="总结-1"><a href="#总结-1" class="headerlink" title="总结"></a><strong>总结</strong></h3><ul><li>你需要将 <code>ubuntu</code> 用户添加到 <code>microk8s</code> 组中，才能让该用户访问 MicroK8s。</li><li>更改 Kubernetes 配置文件的权限，并通过 <code>newgrp</code> 命令重新加载用户组，以确保权限生效。</li></ul><p>执行这些步骤后，问题应该得到解决，你可以正常使用 MicroK8s。</p><p>错误信息 <code>chown: cannot access &#39;/home/ubuntu/.kube&#39;: No such file or directory</code> 表示在 <code>/home/ubuntu/</code> 目录下找不到 <code>.kube</code> 文件夹。原因可能是当前用户还没有在 <code>~/.kube</code> 目录下创建 Kubernetes 配置文件。通常，这是因为还没有执行过 <code>kubectl</code> 命令，或者在配置文件复制步骤中没有正确生成该目录。</p><h3 id="解决方法-1"><a href="#解决方法-1" class="headerlink" title="解决方法"></a><strong>解决方法</strong></h3><h4 id="1-确保-kube-目录存在"><a href="#1-确保-kube-目录存在" class="headerlink" title="1. 确保 .kube 目录存在"></a><strong>1. 确保 <code>.kube</code> 目录存在</strong></h4><p>首先，你需要确保 Kubernetes 配置文件已经生成。执行以下命令生成 Kubernetes 配置文件：</p><ol><li><p><strong>在 MicroK8s 内获取 kubeconfig 文件</strong>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> microk8s kubectl config view --raw &gt; kubeconfig<br></code></pre></td></tr></table></figure></li><li><p><strong>将 <code>kubeconfig</code> 文件移动到 <code>~/.kube</code> 目录</strong>： 创建 <code>.kube</code> 目录，并将 <code>kubeconfig</code> 文件移动到该目录下：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">mkdir</span> -p ~/.kube<br><span class="hljs-built_in">mv</span> kubeconfig ~/.kube/config<br></code></pre></td></tr></table></figure></li><li><p><strong>更新目录权限</strong>： 现在，你已经成功将 <code>kubeconfig</code> 文件放置在 <code>.kube</code> 目录中，可以重新执行权限修改命令：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> <span class="hljs-built_in">chown</span> -R ubuntu ~/.kube<br></code></pre></td></tr></table></figure></li></ol><h4 id="2-配置-kubectl-使用-MicroK8s-配置"><a href="#2-配置-kubectl-使用-MicroK8s-配置" class="headerlink" title="2. 配置 kubectl 使用 MicroK8s 配置"></a><strong>2. 配置 <code>kubectl</code> 使用 MicroK8s 配置</strong></h4><p>如果你已经将配置文件正确放入 <code>~/.kube/config</code>，可以尝试使用以下命令来测试是否成功配置了 <code>kubectl</code>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">kubectl get nodes<br></code></pre></td></tr></table></figure><p>如果配置正确，你应该能够看到集群中的节点信息。</p><h4 id="3-重新加载用户组（如果必要）"><a href="#3-重新加载用户组（如果必要）" class="headerlink" title="3. 重新加载用户组（如果必要）"></a><strong>3. 重新加载用户组（如果必要）</strong></h4><p>如果你已经将 <code>ubuntu</code> 用户添加到 <code>microk8s</code> 组，但还没有刷新用户组，可以执行以下命令：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">newgrp microk8s<br></code></pre></td></tr></table></figure><hr><h3 id="总结-2"><a href="#总结-2" class="headerlink" title="总结"></a><strong>总结</strong></h3><ul><li>错误的根本原因是 <code>.kube</code> 目录不存在。你需要确保配置文件 <code>kubeconfig</code> 被正确生成，并放置到 <code>.kube/config</code> 中。</li><li>执行 <code>mkdir -p ~/.kube &amp;&amp; mv kubeconfig ~/.kube/config</code> 可以确保配置文件正确放置。</li><li>修改文件权限时，使用 <code>chown -R ubuntu ~/.kube</code> 来确保当前用户有正确的访问权限。</li></ul><p>完成这些步骤后，你应该能够正常访问 Kubernetes 集群并使用 <code>kubectl</code> 命令了。</p><hr><h3 id="4-配置-kubectl-访问-MicroK8s"><a href="#4-配置-kubectl-访问-MicroK8s" class="headerlink" title="4. 配置 kubectl 访问 MicroK8s"></a><strong>4. 配置 kubectl 访问 MicroK8s</strong></h3><p>为了能够从宿主机（macOS）直接使用 <code>kubectl</code> 来管理 MicroK8s 集群，你需要配置访问权限。</p><h4 id="（1）获取-kubectl-配置文件"><a href="#（1）获取-kubectl-配置文件" class="headerlink" title="（1）获取 kubectl 配置文件"></a>（1）获取 kubectl 配置文件</h4><p>通过 Multipass 虚拟机执行以下命令，获取 Kubernetes 配置文件：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> microk8s kubectl config view --raw &gt; kubeconfig<br></code></pre></td></tr></table></figure><h4 id="（2）将配置文件复制到宿主机"><a href="#（2）将配置文件复制到宿主机" class="headerlink" title="（2）将配置文件复制到宿主机"></a>（2）将配置文件复制到宿主机</h4><p>将该配置文件复制到宿主机（macOS）上的合适目录，比如 <code>~/.kube/config</code>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">mkdir</span> -p ~/.kube<br><span class="hljs-built_in">cp</span> kubeconfig ~/.kube/config<br></code></pre></td></tr></table></figure><h4 id="（3）验证-kubectl-配置"><a href="#（3）验证-kubectl-配置" class="headerlink" title="（3）验证 kubectl 配置"></a>（3）验证 kubectl 配置</h4><p>验证 <code>kubectl</code> 是否能够连接到 MicroK8s 集群：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">kubectl get nodes<br></code></pre></td></tr></table></figure><p>你应该会看到 <code>microk8s-vm</code> 作为节点，表示 <code>kubectl</code> 成功连接到 MicroK8s 集群。</p><hr><h3 id="5-启用必要的-MicroK8s-插件"><a href="#5-启用必要的-MicroK8s-插件" class="headerlink" title="5. 启用必要的 MicroK8s 插件"></a><strong>5. 启用必要的 MicroK8s 插件</strong></h3><p>MicroK8s 提供了多个附加插件，可以根据需要启用它们。</p><p>例如，启用 DNS 和 Ingress：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">microk8s <span class="hljs-built_in">enable</span> dns ingress<br></code></pre></td></tr></table></figure><p>你可以根据需求启用其他插件，如存储、Metrics Server 等：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs bash">microk8s <span class="hljs-built_in">enable</span> storage<br>microk8s <span class="hljs-built_in">enable</span> dashboard<br><br><br>microk8s <span class="hljs-built_in">disable</span> ingress<br>microk8s <span class="hljs-built_in">disable</span> storage<br>microk8s <span class="hljs-built_in">disable</span> dashboard<br></code></pre></td></tr></table></figure><hr><h3 id="6-访问-Kubernetes-Dashboard（可选）"><a href="#6-访问-Kubernetes-Dashboard（可选）" class="headerlink" title="6. 访问 Kubernetes Dashboard（可选）"></a><strong>6. 访问 Kubernetes Dashboard（可选）</strong></h3><p>如果启用了 Kubernetes Dashboard 插件，可以通过以下命令获取访问 Dashboard 的 token：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">microk8s dashboard-proxy<br></code></pre></td></tr></table></figure><p>此命令会显示一个 URL，你可以在浏览器中访问该 URL，以便通过 Web 界面管理集群。</p><hr><h3 id="7-关闭和删除虚拟机"><a href="#7-关闭和删除虚拟机" class="headerlink" title="7. 关闭和删除虚拟机"></a><strong>7. 关闭和删除虚拟机</strong></h3><p>如果不再需要 MicroK8s，可以通过以下命令停止虚拟机：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">multipass stop microk8s-vm<br></code></pre></td></tr></table></figure><p>如果希望删除虚拟机及其所有数据，可以运行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">multipass delete microk8s-vm<br>multipass purge<br></code></pre></td></tr></table></figure><hr><h3 id="总结-3"><a href="#总结-3" class="headerlink" title="总结"></a><strong>总结</strong></h3><p>在 <strong>macOS</strong> 上安装 <strong>MicroK8s</strong> 的关键步骤是使用 <strong>Multipass</strong> 启动一个 Ubuntu 虚拟机，并在虚拟机内安装和配置 MicroK8s。这样，你就可以在本地开发、测试和学习 Kubernetes 集群，或者部署轻量级的应用程序。如果需要更强大的功能，还可以启用多种插件。</p><p>进入虚拟机</p><figure class="highlight mipsasm"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs mipsasm"><span class="hljs-keyword">multipass </span><span class="hljs-keyword">shell </span>microk8s-v<br></code></pre></td></tr></table></figure><p>如果你想重新启动已停止的 <code>microk8s-vm</code> 实例，你可以使用以下命令：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">multipass start microk8s-vm<br></code></pre></td></tr></table></figure><p>这将重新启动并使虚拟机恢复运行状态。如果你想查看当前虚拟机的状态，可以使用：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">multipass list<br></code></pre></td></tr></table></figure><p>这个命令会列出所有虚拟机及其当前状态（如运行中或已停止）。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs bash">wget https://github.com/derailed/k9s/releases/download/v0.32.7/k9s_linux_amd64.deb &amp;&amp; apt install ./k9s_linux_amd64.deb &amp;&amp; <span class="hljs-built_in">rm</span> k9s_linux_amd64.deb<br><br>wget https://github.com/derailed/k9s/releases/download/v0.32.7/k9s_linux_amd64.deb<br><br>apt install ./k9s_linux_amd64.deb<br><br><span class="hljs-built_in">rm</span> k9s_linux_amd64.deb<br><br></code></pre></td></tr></table></figure><figure class="highlight pgsql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs pgsql"><span class="hljs-built_in">Warning</span>  FailedCreatePodSandBox  <span class="hljs-number">92</span>s                 kubelet  Failed <span class="hljs-keyword">to</span> <span class="hljs-keyword">create</span> pod sandbox: rpc error: code = DeadlineExceeded <span class="hljs-keyword">desc</span> = failed <span class="hljs-keyword">to</span> g │<br>│ et sandbox image &quot;registry.k8s.io/pause:3.7&quot;: failed <span class="hljs-keyword">to</span> pull image &quot;registry.k8s.io/pause:3.7&quot;: failed <span class="hljs-keyword">to</span> pull <span class="hljs-keyword">and</span> unpack image &quot;registry.k8s.io/pa │<br>│ use:3.7&quot;: failed <span class="hljs-keyword">to</span> resolve reference &quot;registry.k8s.io/pause:3.7&quot;: failed <span class="hljs-keyword">to</span> <span class="hljs-keyword">do</span> request: Head &quot;https://us-west2-docker.pkg.dev/v2/k8s-artifacts-pro │<br>│ d/images/pause/manifests/3.7&quot;: dial tcp <span class="hljs-number">74.125</span><span class="hljs-number">.199</span><span class="hljs-number">.82</span>:<span class="hljs-number">443</span>: i/o timeout                                                                              │<br>│   <span class="hljs-built_in">Warning</span>  FailedCreatePodSandBox  <span class="hljs-number">6</span>s (x7 <span class="hljs-keyword">over</span> <span class="hljs-number">5</span>m15s)  kubelet  Failed <span class="hljs-keyword">to</span> <span class="hljs-keyword">create</span> pod sandbox: rpc error: code = DeadlineExceeded <span class="hljs-keyword">desc</span> = failed <span class="hljs-keyword">to</span> g │<br>│ et sandbox image &quot;registry.k8s.io/pause:3.7&quot;: failed <span class="hljs-keyword">to</span> pull image &quot;registry.k8s.io/pause:3.7&quot;: failed <span class="hljs-keyword">to</span> pull <span class="hljs-keyword">and</span> unpack image &quot;registry.k8s.io/pa │<br>│ use:3.7&quot;: failed <span class="hljs-keyword">to</span> resolve reference &quot;registry.k8s.io/pause:3.7&quot;: failed <span class="hljs-keyword">to</span> <span class="hljs-keyword">do</span> request: Head &quot;https://us-west2-docker.pkg.dev/v2/k8s-artifacts-pro │<br>│ d/images/pause/manifests/3.7&quot;: dial tcp <span class="hljs-number">172.253</span><span class="hljs-number">.117</span><span class="hljs-number">.82</span>:<span class="hljs-number">443</span>: i/o timeout<br></code></pre></td></tr></table></figure>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>2024-2-22 k8s架构师面试大全</title>
    <link href="/2024/12/05/2024-2-22%20k8s%E6%9E%B6%E6%9E%84%E5%B8%88%E9%9D%A2%E8%AF%95%E5%A4%A7%E5%85%A8/"/>
    <url>/2024/12/05/2024-2-22%20k8s%E6%9E%B6%E6%9E%84%E5%B8%88%E9%9D%A2%E8%AF%95%E5%A4%A7%E5%85%A8/</url>
    
    <content type="html"><![CDATA[<p><strong>一、Kubernetes 基础知识面试题10 道面试题</strong></p><p><strong>1、什么是 Kubernetes？</strong></p><h5 id="Kubernetes-是一个开源容器管理工具，负责容器部署，容器扩缩容以及负载平衡。它提供了出色的社区，并与所有云提供商合作。因此，我们可以说-Kubernetes-不是一个容器化平台，而是一个多容器管理解决方案。"><a href="#Kubernetes-是一个开源容器管理工具，负责容器部署，容器扩缩容以及负载平衡。它提供了出色的社区，并与所有云提供商合作。因此，我们可以说-Kubernetes-不是一个容器化平台，而是一个多容器管理解决方案。" class="headerlink" title="Kubernetes 是一个开源容器管理工具，负责容器部署，容器扩缩容以及负载平衡。它提供了出色的社区，并与所有云提供商合作。因此，我们可以说 Kubernetes 不是一个容器化平台，而是一个多容器管理解决方案。"></a>Kubernetes 是一个开源容器管理工具，负责容器部署，容器扩缩容以及负载平衡。它提供了出色的社区，并与所有云提供商合作。因此，我们可以说 Kubernetes 不是一个容器化平台，而是一个多容器管理解决方案。</h5><p><strong>2、 Kubernetes 与 docker 什么关系？</strong></p><h5 id="Docker-提供容器的生命周期管理，Docker-镜像构建运行时容器。但是，由于这些单独的容器必须通信，因此使用-Kubernetes。因此，我们说-Docker-构建容器，这些容器通过-Kubernetes-相互通信。因此，可以使用-Kubernetes-手动关联和编排在多个主机上运行的容器。"><a href="#Docker-提供容器的生命周期管理，Docker-镜像构建运行时容器。但是，由于这些单独的容器必须通信，因此使用-Kubernetes。因此，我们说-Docker-构建容器，这些容器通过-Kubernetes-相互通信。因此，可以使用-Kubernetes-手动关联和编排在多个主机上运行的容器。" class="headerlink" title="Docker 提供容器的生命周期管理，Docker 镜像构建运行时容器。但是，由于这些单独的容器必须通信，因此使用 Kubernetes。因此，我们说 Docker 构建容器，这些容器通过 Kubernetes 相互通信。因此，可以使用 Kubernetes 手动关联和编排在多个主机上运行的容器。"></a>Docker 提供容器的生命周期管理，Docker 镜像构建运行时容器。但是，由于这些单独的容器必须通信，因此使用 Kubernetes。因此，我们说 Docker 构建容器，这些容器通过 Kubernetes 相互通信。因此，可以使用 Kubernetes 手动关联和编排在多个主机上运行的容器。</h5><p><strong>3、Kubernetes 与 Docker Swarm 的区别？</strong></p><h5 id="Docker-Swarm-和-Kubernetes-都可以用于类似目的。它们都是容器编排工具。"><a href="#Docker-Swarm-和-Kubernetes-都可以用于类似目的。它们都是容器编排工具。" class="headerlink" title="Docker Swarm 和 Kubernetes 都可以用于类似目的。它们都是容器编排工具。"></a>Docker Swarm 和 Kubernetes 都可以用于类似目的。它们都是容器编排工具。</h5><p><img src="https://picture-base.oss-cn-hangzhou.aliyuncs.com/picture/202402221059641.png" alt="image-20240222105935530"></p><p><strong>4、在主机和容器上部署应用程序有什么区别？</strong></p><p><img src="https://picture-base.oss-cn-hangzhou.aliyuncs.com/picture/202402221059626.png" alt="image-20240222105949456"></p><h5 id="上图左侧架构表示在主机上部署应用程序。因此，这种架构将具有操作系统，然后操作系统将具有内核，该内核将在应用程序所需的操作系统上安装各种库。因此，在这种框架中，你可以拥有-n-个应用程序，并且所有应用程序将共享该操作系统中存在的库。"><a href="#上图左侧架构表示在主机上部署应用程序。因此，这种架构将具有操作系统，然后操作系统将具有内核，该内核将在应用程序所需的操作系统上安装各种库。因此，在这种框架中，你可以拥有-n-个应用程序，并且所有应用程序将共享该操作系统中存在的库。" class="headerlink" title="上图左侧架构表示在主机上部署应用程序。因此，这种架构将具有操作系统，然后操作系统将具有内核，该内核将在应用程序所需的操作系统上安装各种库。因此，在这种框架中，你可以拥有 n 个应用程序，并且所有应用程序将共享该操作系统中存在的库。"></a><strong>上图左</strong>侧架构表示在主机上部署应用程序。因此，这种架构将具有操作系统，然后操作系统将具有内核，该内核将在应用程序所需的操作系统上安装各种库。因此，在这种框架中，你可以拥有 n 个应用程序，并且所有应用程序将共享该操作系统中存在的库。</h5><h5 id="上图右侧架构是容器中部署应用程序。这种架构将有一个内核，这是唯一一个在所有应用程序之间唯一共同的东西。各个块基本上是容器化的，并且这些块与其他应用程序隔离。因此，应用程序具有与系统其余部分隔离的必要库和二进制文件，并且不能被任何其他应用程序侵占。"><a href="#上图右侧架构是容器中部署应用程序。这种架构将有一个内核，这是唯一一个在所有应用程序之间唯一共同的东西。各个块基本上是容器化的，并且这些块与其他应用程序隔离。因此，应用程序具有与系统其余部分隔离的必要库和二进制文件，并且不能被任何其他应用程序侵占。" class="headerlink" title="上图右侧架构是容器中部署应用程序。这种架构将有一个内核，这是唯一一个在所有应用程序之间唯一共同的东西。各个块基本上是容器化的，并且这些块与其他应用程序隔离。因此，应用程序具有与系统其余部分隔离的必要库和二进制文件，并且不能被任何其他应用程序侵占。"></a><strong>上图右</strong>侧架构是容器中部署应用程序。这种架构将有一个内核，这是唯一一个在所有应用程序之间唯一共同的东西。各个块基本上是容器化的，并且这些块与其他应用程序隔离。因此，应用程序具有与系统其余部分隔离的必要库和二进制文件，并且不能被任何其他应用程序侵占。</h5><p><strong>5、Kubernetes 如何简化容器化部署？</strong></p><h5 id="跨主机的容器都需要相互通信。因此，要做到这一点，你需要一些能够负载平衡，扩展和监控容器的东西。由于-Kubernetes-与云无关并且可以在任何公共-私有提供商上运行，因此可以简化容器化部署程序。"><a href="#跨主机的容器都需要相互通信。因此，要做到这一点，你需要一些能够负载平衡，扩展和监控容器的东西。由于-Kubernetes-与云无关并且可以在任何公共-私有提供商上运行，因此可以简化容器化部署程序。" class="headerlink" title="跨主机的容器都需要相互通信。因此，要做到这一点，你需要一些能够负载平衡，扩展和监控容器的东西。由于 Kubernetes 与云无关并且可以在任何公共&#x2F;私有提供商上运行，因此可以简化容器化部署程序。"></a>跨主机的容器都需要相互通信。因此，要做到这一点，你需要一些能够负载平衡，扩展和监控容器的东西。由于 Kubernetes 与云无关并且可以在任何公共&#x2F;私有提供商上运行，因此可以简化容器化部署程序。</h5><p><strong>6、什么是 kubectl？</strong></p><h5 id="Kubectl-是一个平台，可以使用该平台将命令传递给集群。因此，它基本上为CLI-提供了针对-Kubernetes-集群运行命令的方法，以及创建和管理-Kubernetes组件的各种方法。"><a href="#Kubectl-是一个平台，可以使用该平台将命令传递给集群。因此，它基本上为CLI-提供了针对-Kubernetes-集群运行命令的方法，以及创建和管理-Kubernetes组件的各种方法。" class="headerlink" title="Kubectl 是一个平台，可以使用该平台将命令传递给集群。因此，它基本上为CLI 提供了针对 Kubernetes 集群运行命令的方法，以及创建和管理 Kubernetes组件的各种方法。"></a>Kubectl 是一个平台，可以使用该平台将命令传递给集群。因此，它基本上为CLI 提供了针对 Kubernetes 集群运行命令的方法，以及创建和管理 Kubernetes组件的各种方法。</h5><p><strong>7、什么是 kubelet？</strong></p><h5 id="这是一个代理服务，它在每个节点上运行，并使从服务器与主服务器通信。因此，Kubelet-处理-PodSpec-中提供给它的容器的描述，并确保-PodSpec-中描述的容器运行正常。可以创建-pod、删除-pod。"><a href="#这是一个代理服务，它在每个节点上运行，并使从服务器与主服务器通信。因此，Kubelet-处理-PodSpec-中提供给它的容器的描述，并确保-PodSpec-中描述的容器运行正常。可以创建-pod、删除-pod。" class="headerlink" title="这是一个代理服务，它在每个节点上运行，并使从服务器与主服务器通信。因此，Kubelet 处理 PodSpec 中提供给它的容器的描述，并确保 PodSpec 中描述的容器运行正常。可以创建 pod、删除 pod。"></a>这是一个代理服务，它在每个节点上运行，并使从服务器与主服务器通信。因此，Kubelet 处理 PodSpec 中提供给它的容器的描述，并确保 PodSpec 中描述的容器运行正常。可以创建 pod、删除 pod。</h5><p><strong>8、k8s 有哪些组件？</strong></p><h5 id="1、etcd-保存了整个集群的状态；"><a href="#1、etcd-保存了整个集群的状态；" class="headerlink" title="1、etcd 保存了整个集群的状态；"></a>1、etcd 保存了整个集群的状态；</h5><h5 id="2、apiserver-提供了资源操作的唯一入口，并提供认证、授权、访问控制、API-注册和发现等机制；"><a href="#2、apiserver-提供了资源操作的唯一入口，并提供认证、授权、访问控制、API-注册和发现等机制；" class="headerlink" title="2、apiserver 提供了资源操作的唯一入口，并提供认证、授权、访问控制、API 注册和发现等机制；"></a>2、apiserver 提供了资源操作的唯一入口，并提供认证、授权、访问控制、API 注册和发现等机制；</h5><h5 id="3、controller-manager-负责维护集群的状态，比如故障检测、自动扩展、滚动更新等；"><a href="#3、controller-manager-负责维护集群的状态，比如故障检测、自动扩展、滚动更新等；" class="headerlink" title="3、controller manager 负责维护集群的状态，比如故障检测、自动扩展、滚动更新等；"></a>3、controller manager 负责维护集群的状态，比如故障检测、自动扩展、滚动更新等；</h5><h5 id="4、scheduler-负责资源的调度，按照预定的调度策略将-Pod-调度到相应的机器上；"><a href="#4、scheduler-负责资源的调度，按照预定的调度策略将-Pod-调度到相应的机器上；" class="headerlink" title="4、scheduler 负责资源的调度，按照预定的调度策略将 Pod 调度到相应的机器上；"></a>4、scheduler 负责资源的调度，按照预定的调度策略将 Pod 调度到相应的机器上；</h5><h5 id="5、kubelet-负责维护容器的生命周期，同时也负责-Volume（CVI）和网络（CNI）的管理；"><a href="#5、kubelet-负责维护容器的生命周期，同时也负责-Volume（CVI）和网络（CNI）的管理；" class="headerlink" title="5、kubelet 负责维护容器的生命周期，同时也负责 Volume（CVI）和网络（CNI）的管理；"></a>5、kubelet 负责维护容器的生命周期，同时也负责 Volume（CVI）和网络（CNI）的管理；</h5><h5 id="6、Container-runtime-负责镜像管理以及-Pod-和容器的真正运行（CRI）；"><a href="#6、Container-runtime-负责镜像管理以及-Pod-和容器的真正运行（CRI）；" class="headerlink" title="6、Container runtime 负责镜像管理以及 Pod 和容器的真正运行（CRI）；"></a>6、Container runtime 负责镜像管理以及 Pod 和容器的真正运行（CRI）；</h5><h5 id="7、kube-proxy-负责为-Service-提供-cluster-内部的服务发现和负载均衡。"><a href="#7、kube-proxy-负责为-Service-提供-cluster-内部的服务发现和负载均衡。" class="headerlink" title="7、kube-proxy 负责为 Service 提供 cluster 内部的服务发现和负载均衡。"></a>7、kube-proxy 负责为 Service 提供 cluster 内部的服务发现和负载均衡。</h5><p><strong>9、Pod SVC Node Container 之间如何相互访问</strong>：</p><h5 id="1、同-Pod-内的容器：同一个-Pod-的容器共享同一个网络命名空间可以直接进行通讯"><a href="#1、同-Pod-内的容器：同一个-Pod-的容器共享同一个网络命名空间可以直接进行通讯" class="headerlink" title="1、同 Pod 内的容器：同一个 Pod 的容器共享同一个网络命名空间可以直接进行通讯"></a>1、同 Pod 内的容器：同一个 Pod 的容器共享同一个网络命名空间可以直接进行通讯</h5><h5 id="2、同-Node-内不同-Pod-的容器：多个-Pod-都关联在同一个-Docker0-网桥上，通过-docker0-网桥完成相互通讯。"><a href="#2、同-Node-内不同-Pod-的容器：多个-Pod-都关联在同一个-Docker0-网桥上，通过-docker0-网桥完成相互通讯。" class="headerlink" title="2、同 Node 内不同 Pod 的容器：多个 Pod 都关联在同一个 Docker0 网桥上，通过 docker0 网桥完成相互通讯。"></a>2、同 Node 内不同 Pod 的容器：多个 Pod 都关联在同一个 Docker0 网桥上，通过 docker0 网桥完成相互通讯。</h5><h5 id="3、不同-Node-内-Pod-的容器：不同-Node-上的-docker0-可能会相同，PodIP-和-docker0-是同网段的，所以需要将-PodIP-和-NodeIP-进行关联且保障唯"><a href="#3、不同-Node-内-Pod-的容器：不同-Node-上的-docker0-可能会相同，PodIP-和-docker0-是同网段的，所以需要将-PodIP-和-NodeIP-进行关联且保障唯" class="headerlink" title="3、不同 Node 内 Pod 的容器：不同 Node 上的 docker0 可能会相同，PodIP 和 docker0 是同网段的，所以需要将 PodIP 和 NodeIP 进行关联且保障唯"></a>3、不同 Node 内 Pod 的容器：不同 Node 上的 docker0 可能会相同，PodIP 和 docker0 是同网段的，所以需要将 PodIP 和 NodeIP 进行关联且保障唯</h5><h5 id="4，不同-Pod-之间的数据通过物理机的端口进行转发即可完成通讯。"><a href="#4，不同-Pod-之间的数据通过物理机的端口进行转发即可完成通讯。" class="headerlink" title="4，不同 Pod 之间的数据通过物理机的端口进行转发即可完成通讯。"></a>4，不同 Pod 之间的数据通过物理机的端口进行转发即可完成通讯。</h5><p><strong>二、Kubernetes 架构面试题10 道面试题</strong></p><p><strong>1、kubernetes 控制节点组件有哪些？</strong></p><p><img src="https://picture-base.oss-cn-hangzhou.aliyuncs.com/picture/202402221100911.png" alt="image-20240222110025841"></p><h5 id="Kubernetes-控制节点组件：kube-controller-manager，kube-apiserver，kube-scheduler、Kubernetes"><a href="#Kubernetes-控制节点组件：kube-controller-manager，kube-apiserver，kube-scheduler、Kubernetes" class="headerlink" title="Kubernetes 控制节点组件：kube-controller-manager，kube-apiserver，kube-scheduler、Kubernetes"></a><strong>Kubernetes 控制节点组件：</strong>kube-controller-manager，kube-apiserver，kube-scheduler、Kubernetes</h5><h5 id="工作节点组件：kubelet-和-kube-proxy"><a href="#工作节点组件：kubelet-和-kube-proxy" class="headerlink" title="工作节点组件：kubelet 和 kube-proxy"></a><strong>工作节点组件：</strong>kubelet 和 kube-proxy</h5><p><strong>2、谈谈你对 kube-proxy 的理解？</strong></p><h5 id="kube-proxy-是-Kubernetes-的核心组件，部署在每个-Node-节点上，它是实现Kubernetes-Service-的通信与负载均衡机制的重要组件-kube-proxy-负责为-Pod-创建代理服务，从-apiserver-获取所有-server-信息，并根据-server-信息创建代理服务，实现server-到-Pod-的请求路由和转发，从而实现-K8s-层级的虚拟转发网络。"><a href="#kube-proxy-是-Kubernetes-的核心组件，部署在每个-Node-节点上，它是实现Kubernetes-Service-的通信与负载均衡机制的重要组件-kube-proxy-负责为-Pod-创建代理服务，从-apiserver-获取所有-server-信息，并根据-server-信息创建代理服务，实现server-到-Pod-的请求路由和转发，从而实现-K8s-层级的虚拟转发网络。" class="headerlink" title="kube-proxy 是 Kubernetes 的核心组件，部署在每个 Node 节点上，它是实现Kubernetes Service 的通信与负载均衡机制的重要组件; kube-proxy 负责为 Pod 创建代理服务，从 apiserver 获取所有 server 信息，并根据 server 信息创建代理服务，实现server 到 Pod 的请求路由和转发，从而实现 K8s 层级的虚拟转发网络。"></a>kube-proxy 是 Kubernetes 的核心组件，部署在每个 Node 节点上，它是实现Kubernetes Service 的通信与负载均衡机制的重要组件; kube-proxy 负责为 Pod 创建代理服务，从 apiserver 获取所有 server 信息，并根据 server 信息创建代理服务，实现server 到 Pod 的请求路由和转发，从而实现 K8s 层级的虚拟转发网络。</h5><p><strong>3、kube-apiserver 和 kube-scheduler 的作用是什么？</strong></p><h5 id="kube-apiserver-遵循横向扩展架构，是主节点控制平面的前端。将公开Kubernetes-主节点组件的所有-API，并负责在-Kubernetes-节点和-Kubernetes主组件之间建立通信。"><a href="#kube-apiserver-遵循横向扩展架构，是主节点控制平面的前端。将公开Kubernetes-主节点组件的所有-API，并负责在-Kubernetes-节点和-Kubernetes主组件之间建立通信。" class="headerlink" title="kube -apiserver 遵循横向扩展架构，是主节点控制平面的前端。将公开Kubernetes 主节点组件的所有 API，并负责在 Kubernetes 节点和 Kubernetes主组件之间建立通信。"></a><strong>kube -apiserver</strong> 遵循横向扩展架构，是主节点控制平面的前端。将公开Kubernetes 主节点组件的所有 API，并负责在 Kubernetes 节点和 Kubernetes主组件之间建立通信。</h5><h5 id="kube-scheduler-是调度器，负责根据资源需求选择最合适的节点来运行未调度的-pod，并跟踪资源利用率。它确保不在资源已满的节点上调度-pod。"><a href="#kube-scheduler-是调度器，负责根据资源需求选择最合适的节点来运行未调度的-pod，并跟踪资源利用率。它确保不在资源已满的节点上调度-pod。" class="headerlink" title="kube-scheduler 是调度器，负责根据资源需求选择最合适的节点来运行未调度的 pod，并跟踪资源利用率。它确保不在资源已满的节点上调度 pod。"></a><strong>kube-scheduler</strong> 是调度器，负责根据资源需求选择最合适的节点来运行未调度的 pod，并跟踪资源利用率。它确保不在资源已满的节点上调度 pod。</h5><p><strong>4、你能简要介绍一下 Kubernetes 控制管理器吗？</strong></p><h5 id="Controller-Manager-作为集群内部的管理控制中心，负责集群内的-Node、Pod-副本、服务端点（Endpoint）、命名空间（Namespace）、服务账号（ServiceAccount）、资源定额（ResourceQuota）的管理，当某个-Node-意外宕机时，Controller-Manager-会及时发现并执行自动化修复流程，确保集群始终处于预期的工作状态。"><a href="#Controller-Manager-作为集群内部的管理控制中心，负责集群内的-Node、Pod-副本、服务端点（Endpoint）、命名空间（Namespace）、服务账号（ServiceAccount）、资源定额（ResourceQuota）的管理，当某个-Node-意外宕机时，Controller-Manager-会及时发现并执行自动化修复流程，确保集群始终处于预期的工作状态。" class="headerlink" title="Controller Manager 作为集群内部的管理控制中心，负责集群内的 Node、Pod 副本、服务端点（Endpoint）、命名空间（Namespace）、服务账号（ServiceAccount）、资源定额（ResourceQuota）的管理，当某个 Node 意外宕机时，Controller Manager 会及时发现并执行自动化修复流程，确保集群始终处于预期的工作状态。"></a>Controller Manager 作为集群内部的管理控制中心，负责集群内的 Node、Pod 副本、服务端点（Endpoint）、命名空间（Namespace）、服务账号（ServiceAccount）、资源定额（ResourceQuota）的管理，当某个 Node 意外宕机时，Controller Manager 会及时发现并执行自动化修复流程，确保集群始终处于预期的工作状态。</h5><p><img src="https://picture-base.oss-cn-hangzhou.aliyuncs.com/picture/202402221100300.png" alt="image-20240222110044243"></p><p><strong>5、Kubernetes 如何简化容器化部署？</strong></p><h5 id="跨主机的容器都需要相互通信。因此，要做到这一点，你需要一些能够负载平衡，扩展和监控容器的东西。由于-Kubernetes-与云无关并且可以在任何公共-私有提供商上运行，因此可以简化容器化部署程序。-1"><a href="#跨主机的容器都需要相互通信。因此，要做到这一点，你需要一些能够负载平衡，扩展和监控容器的东西。由于-Kubernetes-与云无关并且可以在任何公共-私有提供商上运行，因此可以简化容器化部署程序。-1" class="headerlink" title="跨主机的容器都需要相互通信。因此，要做到这一点，你需要一些能够负载平衡，扩展和监控容器的东西。由于 Kubernetes 与云无关并且可以在任何公共&#x2F;私有提供商上运行，因此可以简化容器化部署程序。"></a>跨主机的容器都需要相互通信。因此，要做到这一点，你需要一些能够负载平衡，扩展和监控容器的东西。由于 Kubernetes 与云无关并且可以在任何公共&#x2F;私有提供商上运行，因此可以简化容器化部署程序。</h5><p><strong>6、什么是 ETCD?</strong></p><h5 id="Etcd-是用-Go-编程语言编写的，是一个分布式键值存储，用于协调分布式工作。因此，Etcd-存储-Kubernetes-集群的配置数据，表示在任何给定时间点的集群状态。"><a href="#Etcd-是用-Go-编程语言编写的，是一个分布式键值存储，用于协调分布式工作。因此，Etcd-存储-Kubernetes-集群的配置数据，表示在任何给定时间点的集群状态。" class="headerlink" title="Etcd 是用 Go 编程语言编写的，是一个分布式键值存储，用于协调分布式工作。因此，Etcd 存储 Kubernetes 集群的配置数据，表示在任何给定时间点的集群状态。"></a>Etcd 是用 Go 编程语言编写的，是一个分布式键值存储，用于协调分布式工作。因此，Etcd 存储 Kubernetes 集群的配置数据，表示在任何给定时间点的集群状态。</h5><p><strong>7、什么是 Ingress，它是如何工作的？</strong></p><h5 id="Ingress-网络是一组规则，充当-Kubernetes-集群的入口点。这允许入站连接，可以将其配置为通过可访问的-URL，负载平衡流量或通过提供基于名称的虚拟主机从外部提供服务。因此，Ingress-是一个-API-对象，通常通过-HTTP-管理集群中服务的外部访问，是暴露服务的最有效方式。"><a href="#Ingress-网络是一组规则，充当-Kubernetes-集群的入口点。这允许入站连接，可以将其配置为通过可访问的-URL，负载平衡流量或通过提供基于名称的虚拟主机从外部提供服务。因此，Ingress-是一个-API-对象，通常通过-HTTP-管理集群中服务的外部访问，是暴露服务的最有效方式。" class="headerlink" title="Ingress 网络是一组规则，充当 Kubernetes 集群的入口点。这允许入站连接，可以将其配置为通过可访问的 URL，负载平衡流量或通过提供基于名称的虚拟主机从外部提供服务。因此，Ingress 是一个 API 对象，通常通过 HTTP 管理集群中服务的外部访问，是暴露服务的最有效方式。"></a>Ingress 网络是一组规则，充当 Kubernetes 集群的入口点。这允许入站连接，可以将其配置为通过可访问的 URL，负载平衡流量或通过提供基于名称的虚拟主机从外部提供服务。因此，Ingress 是一个 API 对象，通常通过 HTTP 管理集群中服务的外部访问，是暴露服务的最有效方式。</h5><p><strong>8、什么是 Headless Service？</strong></p><h5 id="Headless-Service-类似于“普通”服务，但没有群集-IP。此服务使您可以直接访问pod，而无需通过代理访问它。"><a href="#Headless-Service-类似于“普通”服务，但没有群集-IP。此服务使您可以直接访问pod，而无需通过代理访问它。" class="headerlink" title="Headless Service 类似于“普通”服务，但没有群集 IP。此服务使您可以直接访问pod，而无需通过代理访问它。"></a>Headless Service 类似于“普通”服务，但没有群集 IP。此服务使您可以直接访问pod，而无需通过代理访问它。</h5><p><strong>2.1.9 什么是集群联邦</strong></p><h5 id="在联邦集群的帮助下，可以将多个-Kubernetes-集群作为单个集群进行管理。因此，您可以在数据中心-云中创建多个-Kubernetes-集群，并使用联邦来在一个位置控制-管理它们。"><a href="#在联邦集群的帮助下，可以将多个-Kubernetes-集群作为单个集群进行管理。因此，您可以在数据中心-云中创建多个-Kubernetes-集群，并使用联邦来在一个位置控制-管理它们。" class="headerlink" title="在联邦集群的帮助下，可以将多个 Kubernetes 集群作为单个集群进行管理。因此，您可以在数据中心&#x2F;云中创建多个 Kubernetes 集群，并使用联邦来在一个位置控制&#x2F;管理它们。"></a>在联邦集群的帮助下，可以将多个 Kubernetes 集群作为单个集群进行管理。因此，您可以在数据中心&#x2F;云中创建多个 Kubernetes 集群，并使用联邦来在一个位置控制&#x2F;管理它们。</h5><p><strong>9、 k8s 提供几种服务发现</strong></p><p><strong>两种服务发现：</strong></p><h5 id="1、环境变量：当创建一个-Pod-的时候，kubelet-会在该-Pod-中注入集群内所有Service-的相关环境变量。"><a href="#1、环境变量：当创建一个-Pod-的时候，kubelet-会在该-Pod-中注入集群内所有Service-的相关环境变量。" class="headerlink" title="1、环境变量：当创建一个 Pod 的时候，kubelet 会在该 Pod 中注入集群内所有Service 的相关环境变量。"></a>1、环境变量：当创建一个 Pod 的时候，kubelet 会在该 Pod 中注入集群内所有Service 的相关环境变量。</h5><h5 id="2、DNS：可以通过-cluster-add-on-的方式轻松的创建-CoreDNS-来对集群内的Service-进行服务发现。"><a href="#2、DNS：可以通过-cluster-add-on-的方式轻松的创建-CoreDNS-来对集群内的Service-进行服务发现。" class="headerlink" title="2、DNS：可以通过 cluster add-on 的方式轻松的创建 CoreDNS 来对集群内的Service 进行服务发现。"></a>2、DNS：可以通过 cluster add-on 的方式轻松的创建 CoreDNS 来对集群内的Service 进行服务发现。</h5><h5 id="以上两种方式，一个是基于-TCP，DNS-基于-UDP，它们都是建立在四层协议之上。"><a href="#以上两种方式，一个是基于-TCP，DNS-基于-UDP，它们都是建立在四层协议之上。" class="headerlink" title="以上两种方式，一个是基于 TCP，DNS 基于 UDP，它们都是建立在四层协议之上。"></a>以上两种方式，一个是基于 TCP，DNS 基于 UDP，它们都是建立在四层协议之上。</h5><p><strong>10、Pod 中的应用共享几种资源</strong></p><h5 id="1、PID-命名空间：Pod-中的不同应用程序可以看到其他应用程序的进程-ID。"><a href="#1、PID-命名空间：Pod-中的不同应用程序可以看到其他应用程序的进程-ID。" class="headerlink" title="1、PID 命名空间：Pod 中的不同应用程序可以看到其他应用程序的进程 ID。"></a>1、PID 命名空间：Pod 中的不同应用程序可以看到其他应用程序的进程 ID。</h5><h5 id="2、网络命名空间：Pod-中的多个容器能够访问同一个-IP-和端口范围。"><a href="#2、网络命名空间：Pod-中的多个容器能够访问同一个-IP-和端口范围。" class="headerlink" title="2、网络命名空间：Pod 中的多个容器能够访问同一个 IP 和端口范围。"></a>2、网络命名空间：Pod 中的多个容器能够访问同一个 IP 和端口范围。</h5><h5 id="3、IPC-命名空间：Pod-中的多个容器能够使用-SystemV-IPC-或-POSIX-消息队列进行通信。"><a href="#3、IPC-命名空间：Pod-中的多个容器能够使用-SystemV-IPC-或-POSIX-消息队列进行通信。" class="headerlink" title="3、IPC 命名空间：Pod 中的多个容器能够使用 SystemV IPC 或 POSIX 消息队列进行通信。"></a>3、IPC 命名空间：Pod 中的多个容器能够使用 SystemV IPC 或 POSIX 消息队列进行通信。</h5><h5 id="4、UTS-命名空间：Pod-中的多个容器共享一个主机名。"><a href="#4、UTS-命名空间：Pod-中的多个容器共享一个主机名。" class="headerlink" title="4、UTS 命名空间：Pod 中的多个容器共享一个主机名。"></a>4、UTS 命名空间：Pod 中的多个容器共享一个主机名。</h5><h5 id="5、Volumes（共享存储卷）：Pod-中的各个容器可以访问在-Pod-级别定义的Volumes。"><a href="#5、Volumes（共享存储卷）：Pod-中的各个容器可以访问在-Pod-级别定义的Volumes。" class="headerlink" title="5、Volumes（共享存储卷）：Pod 中的各个容器可以访问在 Pod 级别定义的Volumes。"></a>5、Volumes（共享存储卷）：Pod 中的各个容器可以访问在 Pod 级别定义的Volumes。</h5><p><strong>三、k8s 使用场景面试题10 道面试题</strong></p><p><strong>场景 1：</strong></p><h5 id="假设一家基于单一架构的公司处理众多产品。现在，随着公司在当今的扩展行业的扩展，他们的单一架构开始引发问题。"><a href="#假设一家基于单一架构的公司处理众多产品。现在，随着公司在当今的扩展行业的扩展，他们的单一架构开始引发问题。" class="headerlink" title="假设一家基于单一架构的公司处理众多产品。现在，随着公司在当今的扩展行业的扩展，他们的单一架构开始引发问题。"></a>假设一家基于单一架构的公司处理众多产品。现在，随着公司在当今的扩展行业的扩展，他们的单一架构开始引发问题。</h5><h5 id="您如何看待公司从单一服务转向微服务并部署其服务容器？"><a href="#您如何看待公司从单一服务转向微服务并部署其服务容器？" class="headerlink" title="您如何看待公司从单一服务转向微服务并部署其服务容器？"></a>您如何看待公司从单一服务转向微服务并部署其服务容器？</h5><p><strong>解：</strong></p><h5 id="由于公司的目标是从单一应用程序转向微服务，它们最终可以逐个构建，并行构建，只需在后台切换配置。然后他们可以将这些内置微服务放在-Kubernetes-平台上。因此他们可以从一次或两次迁移服务开始，并监控它们以确保一切运行稳定。一旦他们觉得一切顺利，他们就可以将其余的应用程序迁移到他们的-Kubernetes-集群中。"><a href="#由于公司的目标是从单一应用程序转向微服务，它们最终可以逐个构建，并行构建，只需在后台切换配置。然后他们可以将这些内置微服务放在-Kubernetes-平台上。因此他们可以从一次或两次迁移服务开始，并监控它们以确保一切运行稳定。一旦他们觉得一切顺利，他们就可以将其余的应用程序迁移到他们的-Kubernetes-集群中。" class="headerlink" title="由于公司的目标是从单一应用程序转向微服务，它们最终可以逐个构建，并行构建，只需在后台切换配置。然后他们可以将这些内置微服务放在 Kubernetes 平台上。因此他们可以从一次或两次迁移服务开始，并监控它们以确保一切运行稳定。一旦他们觉得一切顺利，他们就可以将其余的应用程序迁移到他们的 Kubernetes 集群中。"></a>由于公司的目标是从单一应用程序转向微服务，它们最终可以逐个构建，并行构建，只需在后台切换配置。然后他们可以将这些内置微服务放在 Kubernetes 平台上。因此他们可以从一次或两次迁移服务开始，并监控它们以确保一切运行稳定。一旦他们觉得一切顺利，他们就可以将其余的应用程序迁移到他们的 Kubernetes 集群中。</h5><p><strong>场景 2：</strong></p><h5 id="考虑一家拥有分布式系统的跨国公司，拥有大量数据中心，虚拟机和许多从事各种任务的员工。"><a href="#考虑一家拥有分布式系统的跨国公司，拥有大量数据中心，虚拟机和许多从事各种任务的员工。" class="headerlink" title="考虑一家拥有分布式系统的跨国公司，拥有大量数据中心，虚拟机和许多从事各种任务的员工。"></a>考虑一家拥有分布式系统的跨国公司，拥有大量数据中心，虚拟机和许多从事各种任务的员工。</h5><h5 id="您认为这样-的公司如何以与-Kubernetes-一致的方式管理所有任务？"><a href="#您认为这样-的公司如何以与-Kubernetes-一致的方式管理所有任务？" class="headerlink" title="您认为这样 的公司如何以与 Kubernetes 一致的方式管理所有任务？"></a>您认为这样 的公司如何以与 Kubernetes 一致的方式管理所有任务？</h5><p><strong>解：</strong></p><h5 id="正如我们所有人都知道-IT-部门推出了数千个容器，其任务在分布式系统中遍布全球众多节点。在这种情况下，公司可以使用能够为基于云的应用程序提供敏捷性，横向扩展功能和DevOps-实践的东西。因此，该公司可以使用-Kubernetes-来定制他们的调度架构并支持多种容器格式。这使得容器任务之间的亲和性成为可能，从而提供更高的效率，并为各种容器网络解决方案和容器存储提供广泛支持。"><a href="#正如我们所有人都知道-IT-部门推出了数千个容器，其任务在分布式系统中遍布全球众多节点。在这种情况下，公司可以使用能够为基于云的应用程序提供敏捷性，横向扩展功能和DevOps-实践的东西。因此，该公司可以使用-Kubernetes-来定制他们的调度架构并支持多种容器格式。这使得容器任务之间的亲和性成为可能，从而提供更高的效率，并为各种容器网络解决方案和容器存储提供广泛支持。" class="headerlink" title="正如我们所有人都知道 IT 部门推出了数千个容器，其任务在分布式系统中遍布全球众多节点。在这种情况下，公司可以使用能够为基于云的应用程序提供敏捷性，横向扩展功能和DevOps 实践的东西。因此，该公司可以使用 Kubernetes 来定制他们的调度架构并支持多种容器格式。这使得容器任务之间的亲和性成为可能，从而提供更高的效率，并为各种容器网络解决方案和容器存储提供广泛支持。"></a>正如我们所有人都知道 IT 部门推出了数千个容器，其任务在分布式系统中遍布全球众多节点。在这种情况下，公司可以使用能够为基于云的应用程序提供敏捷性，横向扩展功能和DevOps 实践的东西。因此，该公司可以使用 Kubernetes 来定制他们的调度架构并支持多种容器格式。这使得容器任务之间的亲和性成为可能，从而提供更高的效率，并为各种容器网络解决方案和容器存储提供广泛支持。</h5><p><strong>场景 3：</strong></p><h5 id="考虑一种情况，即公司希望通过维持最低成本来提高其效率和技术运营速度。您认为公司将如何实现这一目标？"><a href="#考虑一种情况，即公司希望通过维持最低成本来提高其效率和技术运营速度。您认为公司将如何实现这一目标？" class="headerlink" title="考虑一种情况，即公司希望通过维持最低成本来提高其效率和技术运营速度。您认为公司将如何实现这一目标？"></a>考虑一种情况，即公司希望通过维持最低成本来提高其效率和技术运营速度。您认为公司将如何实现这一目标？</h5><p><strong>解：</strong></p><h5 id="公司可以通过构建-CI-CD-管道来实现-DevOps-方法，但是这里可能出现的一个问题是配置可能需要一段时间才能启动并运行。因此，在实施-CI-CD-管道之后，公司的下一步应该是在云环境中工作。一旦他们开始处理云环境，他们就可以在集群上安排容器，并可以在-Kubernetes-的帮助下进行协调。这种方法将有助于公司缩短部署时间，并在各种环境中加快速度。"><a href="#公司可以通过构建-CI-CD-管道来实现-DevOps-方法，但是这里可能出现的一个问题是配置可能需要一段时间才能启动并运行。因此，在实施-CI-CD-管道之后，公司的下一步应该是在云环境中工作。一旦他们开始处理云环境，他们就可以在集群上安排容器，并可以在-Kubernetes-的帮助下进行协调。这种方法将有助于公司缩短部署时间，并在各种环境中加快速度。" class="headerlink" title="公司可以通过构建 CI&#x2F;CD 管道来实现 DevOps 方法，但是这里可能出现的一个问题是配置可能需要一段时间才能启动并运行。因此，在实施 CI&#x2F;CD 管道之后，公司的下一步应该是在云环境中工作。一旦他们开始处理云环境，他们就可以在集群上安排容器，并可以在 Kubernetes 的帮助下进行协调。这种方法将有助于公司缩短部署时间，并在各种环境中加快速度。"></a>公司可以通过构建 CI&#x2F;CD 管道来实现 DevOps 方法，但是这里可能出现的一个问题是配置可能需要一段时间才能启动并运行。因此，在实施 CI&#x2F;CD 管道之后，公司的下一步应该是在云环境中工作。一旦他们开始处理云环境，他们就可以在集群上安排容器，并可以在 Kubernetes 的帮助下进行协调。这种方法将有助于公司缩短部署时间，并在各种环境中加快速度。</h5><p><strong>场景 4：</strong></p><h5 id="假设一家公司想要修改它的部署方法，并希望建立一个更具可扩展性和响应性的平台。"><a href="#假设一家公司想要修改它的部署方法，并希望建立一个更具可扩展性和响应性的平台。" class="headerlink" title="假设一家公司想要修改它的部署方法，并希望建立一个更具可扩展性和响应性的平台。"></a>假设一家公司想要修改它的部署方法，并希望建立一个更具可扩展性和响应性的平台。</h5><h5 id="您如何看待这家公司能够实现这一目标以满足客户需求？"><a href="#您如何看待这家公司能够实现这一目标以满足客户需求？" class="headerlink" title="您如何看待这家公司能够实现这一目标以满足客户需求？"></a>您如何看待这家公司能够实现这一目标以满足客户需求？</h5><p><strong>解：</strong></p><h5 id="为了给数百万客户提供他们期望的数字体验，公司需要一个可扩展且响应迅速的平台，以便他们能够快速地将数据发送到客户网站。现在，要做到这一点，公司应该从他们的私有数据中心（如果他们使用任何）转移到任何云环境，如-AWS。不仅如此，他们还应该实现微服务架构，以便他们可以开始使用-Docker-容器。一旦他们准备好基础框架，他们就可以开始使用最好的编排平台，即-Kubernetes。这将使团队能够自主地构建应用程序并快速交付它们。"><a href="#为了给数百万客户提供他们期望的数字体验，公司需要一个可扩展且响应迅速的平台，以便他们能够快速地将数据发送到客户网站。现在，要做到这一点，公司应该从他们的私有数据中心（如果他们使用任何）转移到任何云环境，如-AWS。不仅如此，他们还应该实现微服务架构，以便他们可以开始使用-Docker-容器。一旦他们准备好基础框架，他们就可以开始使用最好的编排平台，即-Kubernetes。这将使团队能够自主地构建应用程序并快速交付它们。" class="headerlink" title="为了给数百万客户提供他们期望的数字体验，公司需要一个可扩展且响应迅速的平台，以便他们能够快速地将数据发送到客户网站。现在，要做到这一点，公司应该从他们的私有数据中心（如果他们使用任何）转移到任何云环境，如 AWS。不仅如此，他们还应该实现微服务架构，以便他们可以开始使用 Docker 容器。一旦他们准备好基础框架，他们就可以开始使用最好的编排平台，即 Kubernetes。这将使团队能够自主地构建应用程序并快速交付它们。"></a>为了给数百万客户提供他们期望的数字体验，公司需要一个可扩展且响应迅速的平台，以便他们能够快速地将数据发送到客户网站。现在，要做到这一点，公司应该从他们的私有数据中心（如果他们使用任何）转移到任何云环境，如 AWS。不仅如此，他们还应该实现微服务架构，以便他们可以开始使用 Docker 容器。一旦他们准备好基础框架，他们就可以开始使用最好的编排平台，即 Kubernetes。这将使团队能够自主地构建应用程序并快速交付它们。</h5><p><strong>场景 5：</strong></p><h5 id="考虑一家拥有非常分散的系统的跨国公司，期待解决整体代码库问题。"><a href="#考虑一家拥有非常分散的系统的跨国公司，期待解决整体代码库问题。" class="headerlink" title="考虑一家拥有非常分散的系统的跨国公司，期待解决整体代码库问题。"></a>考虑一家拥有非常分散的系统的跨国公司，期待解决整体代码库问题。</h5><h5 id="您认为公司如何解决他们的问题？"><a href="#您认为公司如何解决他们的问题？" class="headerlink" title="您认为公司如何解决他们的问题？"></a>您认为公司如何解决他们的问题？</h5><p><strong>解</strong></p><h5 id="那么，为了解决这个问题，我们可以将他们的单片代码库转移到微服务设计，然后每个微服务都可以被视为一个容器。因此，所有这些容器都可以在-Kubernetes-的帮助下进行部署和协调。"><a href="#那么，为了解决这个问题，我们可以将他们的单片代码库转移到微服务设计，然后每个微服务都可以被视为一个容器。因此，所有这些容器都可以在-Kubernetes-的帮助下进行部署和协调。" class="headerlink" title="那么，为了解决这个问题，我们可以将他们的单片代码库转移到微服务设计，然后每个微服务都可以被视为一个容器。因此，所有这些容器都可以在 Kubernetes 的帮助下进行部署和协调。"></a>那么，为了解决这个问题，我们可以将他们的单片代码库转移到微服务设计，然后每个微服务都可以被视为一个容器。因此，所有这些容器都可以在 Kubernetes 的帮助下进行部署和协调。</h5><p><strong>场景 6：</strong></p><h5 id="我们所有人都知道，从单片到微服务的转变解决了开发方面的问题，但却增加了部署方面的问题。"><a href="#我们所有人都知道，从单片到微服务的转变解决了开发方面的问题，但却增加了部署方面的问题。" class="headerlink" title="我们所有人都知道，从单片到微服务的转变解决了开发方面的问题，但却增加了部署方面的问题。"></a>我们所有人都知道，从单片到微服务的转变解决了开发方面的问题，但却增加了部署方面的问题。</h5><h5 id="公司如何解决部署方面的问题？"><a href="#公司如何解决部署方面的问题？" class="headerlink" title="公司如何解决部署方面的问题？"></a>公司如何解决部署方面的问题？</h5><p><strong>解</strong></p><h5 id="团队可以试验容器编排平台，例如-Kubernetes，并在数据中心运行。因此，通过这种方式，公司可以生成模板化应用程序，在五分钟内部署它，并在此时将实际实例集中在暂存环境中。这种-Kubernetes-项目将有数十个并行运行的微服务，以提高生产率，即使节点出现故障，也可以立即重新安排，而不会影响性能。"><a href="#团队可以试验容器编排平台，例如-Kubernetes，并在数据中心运行。因此，通过这种方式，公司可以生成模板化应用程序，在五分钟内部署它，并在此时将实际实例集中在暂存环境中。这种-Kubernetes-项目将有数十个并行运行的微服务，以提高生产率，即使节点出现故障，也可以立即重新安排，而不会影响性能。" class="headerlink" title="团队可以试验容器编排平台，例如 Kubernetes，并在数据中心运行。因此，通过这种方式，公司可以生成模板化应用程序，在五分钟内部署它，并在此时将实际实例集中在暂存环境中。这种 Kubernetes 项目将有数十个并行运行的微服务，以提高生产率，即使节点出现故障，也可以立即重新安排，而不会影响性能。"></a>团队可以试验容器编排平台，例如 Kubernetes，并在数据中心运行。因此，通过这种方式，公司可以生成模板化应用程序，在五分钟内部署它，并在此时将实际实例集中在暂存环境中。这种 Kubernetes 项目将有数十个并行运行的微服务，以提高生产率，即使节点出现故障，也可以立即重新安排，而不会影响性能。</h5><p><strong>场景 7：</strong></p><h5 id="假设一家公司希望通过采用新技术来优化其工作负载的分配。"><a href="#假设一家公司希望通过采用新技术来优化其工作负载的分配。" class="headerlink" title="假设一家公司希望通过采用新技术来优化其工作负载的分配。"></a>假设一家公司希望通过采用新技术来优化其工作负载的分配。</h5><h5 id="公司如何有效地实现这种资源分配？"><a href="#公司如何有效地实现这种资源分配？" class="headerlink" title="公司如何有效地实现这种资源分配？"></a>公司如何有效地实现这种资源分配？</h5><p><strong>解</strong></p><h5 id="这个问题的解决方案就是-Kubernetes。Kubernetes-确保资源得到有效优化，并且只使用特定应用程序所需的那些资源。因此，通过使用最佳容器编排工具，公司可以有效地实现资源分配"><a href="#这个问题的解决方案就是-Kubernetes。Kubernetes-确保资源得到有效优化，并且只使用特定应用程序所需的那些资源。因此，通过使用最佳容器编排工具，公司可以有效地实现资源分配" class="headerlink" title="这个问题的解决方案就是 Kubernetes。Kubernetes 确保资源得到有效优化，并且只使用特定应用程序所需的那些资源。因此，通过使用最佳容器编排工具，公司可以有效地实现资源分配"></a>这个问题的解决方案就是 Kubernetes。Kubernetes 确保资源得到有效优化，并且只使用特定应用程序所需的那些资源。因此，通过使用最佳容器编排工具，公司可以有效地实现资源分配</h5><p><strong>场景 8：</strong></p><h5 id="考虑一家拼车公司希望通过同时扩展其平台来增加服务器数量。"><a href="#考虑一家拼车公司希望通过同时扩展其平台来增加服务器数量。" class="headerlink" title="考虑一家拼车公司希望通过同时扩展其平台来增加服务器数量。"></a>考虑一家拼车公司希望通过同时扩展其平台来增加服务器数量。</h5><h5 id="您认为公司如何处理服务器及其安装？"><a href="#您认为公司如何处理服务器及其安装？" class="headerlink" title="您认为公司如何处理服务器及其安装？"></a>您认为公司如何处理服务器及其安装？</h5><p><strong>解</strong></p><h5 id="公司可以采用集装箱化的概念。一旦他们将所有应用程序部署到容器中，他们就可以使用-Kubernetes-进行编排，并使用像-Prometheus-这样的容器监视工具来监视容器中的操作。因此，利用容器的这种使用，在数据中心中为它们提供更好的容量规划，因为它们现在将受到更少的限制，因为服务和它们运行的硬件之间存在抽象。"><a href="#公司可以采用集装箱化的概念。一旦他们将所有应用程序部署到容器中，他们就可以使用-Kubernetes-进行编排，并使用像-Prometheus-这样的容器监视工具来监视容器中的操作。因此，利用容器的这种使用，在数据中心中为它们提供更好的容量规划，因为它们现在将受到更少的限制，因为服务和它们运行的硬件之间存在抽象。" class="headerlink" title="公司可以采用集装箱化的概念。一旦他们将所有应用程序部署到容器中，他们就可以使用 Kubernetes 进行编排，并使用像 Prometheus 这样的容器监视工具来监视容器中的操作。因此，利用容器的这种使用，在数据中心中为它们提供更好的容量规划，因为它们现在将受到更少的限制，因为服务和它们运行的硬件之间存在抽象。"></a>公司可以采用集装箱化的概念。一旦他们将所有应用程序部署到容器中，他们就可以使用 Kubernetes 进行编排，并使用像 Prometheus 这样的容器监视工具来监视容器中的操作。因此，利用容器的这种使用，在数据中心中为它们提供更好的容量规划，因为它们现在将受到更少的限制，因为服务和它们运行的硬件之间存在抽象。</h5><p><strong>场景 9：</strong></p><h5 id="考虑一种情况，公司希望向具有各种环境的客户提供所有必需的分发。您认为他们如何以动态的方式实现这一关键目标？"><a href="#考虑一种情况，公司希望向具有各种环境的客户提供所有必需的分发。您认为他们如何以动态的方式实现这一关键目标？" class="headerlink" title="考虑一种情况，公司希望向具有各种环境的客户提供所有必需的分发。您认为他们如何以动态的方式实现这一关键目标？"></a>考虑一种情况，公司希望向具有各种环境的客户提供所有必需的分发。您认为他们如何以动态的方式实现这一关键目标？</h5><p><strong>解</strong></p><h5 id="该公司可以使用-Docker-环境，组建一个横截面团队，使用-Kubernetes-构建-Web应用程序。这种框架将帮助公司实现在最短的时间内将所需产品投入生产的目标。因此，在这样的机器运行的情况下，公司可以向所有具有各种环境的客户发放电子邮件。"><a href="#该公司可以使用-Docker-环境，组建一个横截面团队，使用-Kubernetes-构建-Web应用程序。这种框架将帮助公司实现在最短的时间内将所需产品投入生产的目标。因此，在这样的机器运行的情况下，公司可以向所有具有各种环境的客户发放电子邮件。" class="headerlink" title="该公司可以使用 Docker 环境，组建一个横截面团队，使用 Kubernetes 构建 Web应用程序。这种框架将帮助公司实现在最短的时间内将所需产品投入生产的目标。因此，在这样的机器运行的情况下，公司可以向所有具有各种环境的客户发放电子邮件。"></a>该公司可以使用 Docker 环境，组建一个横截面团队，使用 Kubernetes 构建 Web应用程序。这种框架将帮助公司实现在最短的时间内将所需产品投入生产的目标。因此，在这样的机器运行的情况下，公司可以向所有具有各种环境的客户发放电子邮件。</h5><p><strong>场景 10：</strong></p><h5 id="假设公司希望在不同的云基础架构上运行各种工作负载，从裸机到公共云。"><a href="#假设公司希望在不同的云基础架构上运行各种工作负载，从裸机到公共云。" class="headerlink" title="假设公司希望在不同的云基础架构上运行各种工作负载，从裸机到公共云。"></a>假设公司希望在不同的云基础架构上运行各种工作负载，从裸机到公共云。</h5><h5 id="公司将如何在不同界面的存在下实现这一目标？"><a href="#公司将如何在不同界面的存在下实现这一目标？" class="headerlink" title="公司将如何在不同界面的存在下实现这一目标？"></a>公司将如何在不同界面的存在下实现这一目标？</h5><p><strong>解</strong></p><h5 id="该公司可以将其基础设施分解为微服务，然后采用-Kubernetes。这将使公司在不同的云基础架构上运行各种工作负载。"><a href="#该公司可以将其基础设施分解为微服务，然后采用-Kubernetes。这将使公司在不同的云基础架构上运行各种工作负载。" class="headerlink" title="该公司可以将其基础设施分解为微服务，然后采用 Kubernetes。这将使公司在不同的云基础架构上运行各种工作负载。"></a>该公司可以将其基础设施分解为微服务，然后采用 Kubernetes。这将使公司在不同的云基础架构上运行各种工作负载。</h5><p><strong>四、k8s 真实面试场景-面试题总结12 道面试题</strong></p><p><strong>1、deployment 创建 pod 流程？</strong></p><h5 id="1）、kubectl-提交创建-pod-命令-api-响应命令-通过一系列认证授权-把-pod-数据存储到etcd-创建-deployment-资源并初始化"><a href="#1）、kubectl-提交创建-pod-命令-api-响应命令-通过一系列认证授权-把-pod-数据存储到etcd-创建-deployment-资源并初始化" class="headerlink" title="1）、kubectl 提交创建 pod 命令,api 响应命令,通过一系列认证授权,把 pod 数据存储到etcd,创建 deployment 资源并初始化."></a>1）、kubectl 提交创建 pod 命令,api 响应命令,通过一系列认证授权,把 pod 数据存储到etcd,创建 deployment 资源并初始化.</h5><h5 id="2）、controller-通过-list-watch-机制-监测发现新的-deployment-将该资源加入到内部工作队列-发现该资源没有关联的-pod-和-replicaset-启用-deployment-controller-创建replicaset-资源-再启用-replicaset-controller-创建-pod"><a href="#2）、controller-通过-list-watch-机制-监测发现新的-deployment-将该资源加入到内部工作队列-发现该资源没有关联的-pod-和-replicaset-启用-deployment-controller-创建replicaset-资源-再启用-replicaset-controller-创建-pod" class="headerlink" title="2）、controller 通过 list-watch 机制,监测发现新的 deployment,将该资源加入到内部工作队列,发现该资源没有关联的 pod 和 replicaset,启用 deployment controller 创建replicaset 资源,再启用 replicaset controller 创建 pod."></a>2）、controller 通过 list-watch 机制,监测发现新的 deployment,将该资源加入到内部工作队列,发现该资源没有关联的 pod 和 replicaset,启用 deployment controller 创建replicaset 资源,再启用 replicaset controller 创建 pod.</h5><h5 id="3）、所有-controller-正常后-将-deployment-replicaset-pod-资源更新存储到etcd"><a href="#3）、所有-controller-正常后-将-deployment-replicaset-pod-资源更新存储到etcd" class="headerlink" title="3）、所有 controller 正常后.将 deployment,replicaset,pod 资源更新存储到etcd."></a>3）、所有 controller 正常后.将 deployment,replicaset,pod 资源更新存储到etcd.</h5><h5 id="4）、scheduler-通过-list-watch-机制-监测发现新的-pod-经过主机过滤主机打分规则-将pod-绑定-binding-到合适的主机"><a href="#4）、scheduler-通过-list-watch-机制-监测发现新的-pod-经过主机过滤主机打分规则-将pod-绑定-binding-到合适的主机" class="headerlink" title="4）、scheduler 通过 list-watch 机制,监测发现新的 pod,经过主机过滤主机打分规则,将pod 绑定(binding)到合适的主机."></a>4）、scheduler 通过 list-watch 机制,监测发现新的 pod,经过主机过滤主机打分规则,将pod 绑定(binding)到合适的主机.</h5><h5 id="5）、将绑定结果存储到-etcd"><a href="#5）、将绑定结果存储到-etcd" class="headerlink" title="5）、将绑定结果存储到 etcd"></a>5）、将绑定结果存储到 etcd</h5><h5 id="6）、kubelet-每隔-20s-可以自定义-向-kube-apiserver-通过-NodeName-获取自身Node-上所要运行的-pod-清单-通过与自己的内部缓存进行比较-新增加-pod"><a href="#6）、kubelet-每隔-20s-可以自定义-向-kube-apiserver-通过-NodeName-获取自身Node-上所要运行的-pod-清单-通过与自己的内部缓存进行比较-新增加-pod" class="headerlink" title="6）、kubelet 每隔 20s(可以自定义)向 kube-apiserver 通过 NodeName 获取自身Node 上所要运行的 pod 清单.通过与自己的内部缓存进行比较,新增加 pod."></a>6）、kubelet 每隔 20s(可以自定义)向 kube-apiserver 通过 NodeName 获取自身Node 上所要运行的 pod 清单.通过与自己的内部缓存进行比较,新增加 pod.</h5><h5 id="7）、启动-pod-启动容器"><a href="#7）、启动-pod-启动容器" class="headerlink" title="7）、启动 pod 启动容器"></a>7）、启动 pod 启动容器</h5><p><strong>2、你知道 HPA 吗？HPA 有什么缺点？</strong></p><h5 id="hpa-是-k8s-的自动扩缩容策略。v1-版可支持基于-cpu-的扩缩容，v2-版可支持基于-cpu、内存、自定义指标的扩容。默认情况下，pod-是一台台扩容的，所以类似于-微博热搜之类的-突然间-访问量剧增的情况下就显有些无力。所以需要修改-k8s-的调度规则。如果波动量比较大的服务容易造成业务抖动。主要是很被动，无法进行提前被容。"><a href="#hpa-是-k8s-的自动扩缩容策略。v1-版可支持基于-cpu-的扩缩容，v2-版可支持基于-cpu、内存、自定义指标的扩容。默认情况下，pod-是一台台扩容的，所以类似于-微博热搜之类的-突然间-访问量剧增的情况下就显有些无力。所以需要修改-k8s-的调度规则。如果波动量比较大的服务容易造成业务抖动。主要是很被动，无法进行提前被容。" class="headerlink" title="hpa 是 k8s 的自动扩缩容策略。v1 版可支持基于 cpu 的扩缩容，v2 版可支持基于 cpu、内存、自定义指标的扩容。默认情况下，pod 是一台台扩容的，所以类似于 微博热搜之类的 突然间 访问量剧增的情况下就显有些无力。所以需要修改 k8s 的调度规则。如果波动量比较大的服务容易造成业务抖动。主要是很被动，无法进行提前被容。"></a>hpa 是 k8s 的自动扩缩容策略。v1 版可支持基于 cpu 的扩缩容，v2 版可支持基于 cpu、内存、自定义指标的扩容。默认情况下，pod 是一台台扩容的，所以类似于 微博热搜之类的 突然间 访问量剧增的情况下就显有些无力。所以需要修改 k8s 的调度规则。如果波动量比较大的服务容易造成业务抖动。主要是很被动，无法进行提前被容。</h5><h5 id="HPA-可针对-cpu-和内存自定义参数弹性扩容，有-5-分钟的安全时间，不适合访问量波动大的业务。"><a href="#HPA-可针对-cpu-和内存自定义参数弹性扩容，有-5-分钟的安全时间，不适合访问量波动大的业务。" class="headerlink" title="HPA 可针对 cpu 和内存自定义参数弹性扩容，有 5 分钟的安全时间，不适合访问量波动大的业务。"></a>HPA 可针对 cpu 和内存自定义参数弹性扩容，有 5 分钟的安全时间，不适合访问量波动大的业务。</h5><p><strong>3、蓝绿发布和灰度发布 k8s 怎么实现的？</strong></p><h5 id="蓝绿发布通过-deployment-部署-pod，改变-service-或者-ingress-切换流量可以实现灰度发布通过-Ingress-Controller-或者-istio-可以实现"><a href="#蓝绿发布通过-deployment-部署-pod，改变-service-或者-ingress-切换流量可以实现灰度发布通过-Ingress-Controller-或者-istio-可以实现" class="headerlink" title="蓝绿发布通过 deployment 部署 pod，改变 service 或者 ingress 切换流量可以实现灰度发布通过 Ingress Controller 或者 istio 可以实现"></a>蓝绿发布通过 deployment 部署 pod，改变 service 或者 ingress 切换流量可以实现灰度发布通过 Ingress Controller 或者 istio 可以实现</h5><p><strong>4、如果一个 pod 创建过程中一直处于 pending 状态，你的处理思路是什么？</strong></p><h5 id="1）通过-describe-查看-pod-详细信息"><a href="#1）通过-describe-查看-pod-详细信息" class="headerlink" title="1）通过 describe 查看 pod 详细信息"></a>1）通过 describe 查看 pod 详细信息</h5><h5 id="2）通过-logs-查看-pod-日志"><a href="#2）通过-logs-查看-pod-日志" class="headerlink" title="2）通过 logs 查看 pod 日志"></a>2）通过 logs 查看 pod 日志</h5><h5 id="通过详细信息和日志基本就可以把问题定位出来了"><a href="#通过详细信息和日志基本就可以把问题定位出来了" class="headerlink" title="通过详细信息和日志基本就可以把问题定位出来了"></a>通过详细信息和日志基本就可以把问题定位出来了</h5><p><strong>5、k8s 如何实现持久化存储？有几种方式?</strong></p><h5 id="emptyDir、hostPath、pv、storageclass、ceph、nfs、gluster-等都可以实现-k8s数据持久化，也可以通过-storageclass-动态的从-nfs-provisioner-或者-cephprovisioner-等供应商动态的划分存储做成-pv"><a href="#emptyDir、hostPath、pv、storageclass、ceph、nfs、gluster-等都可以实现-k8s数据持久化，也可以通过-storageclass-动态的从-nfs-provisioner-或者-cephprovisioner-等供应商动态的划分存储做成-pv" class="headerlink" title="emptyDir、hostPath、pv、storageclass、ceph、nfs、gluster 等都可以实现 k8s数据持久化，也可以通过 storageclass 动态的从 nfs provisioner 或者 cephprovisioner 等供应商动态的划分存储做成 pv"></a>emptyDir、hostPath、pv、storageclass、ceph、nfs、gluster 等都可以实现 k8s数据持久化，也可以通过 storageclass 动态的从 nfs provisioner 或者 cephprovisioner 等供应商动态的划分存储做成 pv</h5><p><strong>6、service 的 type 类型有几种？</strong></p><h5 id="ClusterIp：集群内部相关访问"><a href="#ClusterIp：集群内部相关访问" class="headerlink" title="ClusterIp：集群内部相关访问"></a>ClusterIp：集群内部相关访问</h5><h5 id="NodePort：可以在物理机映射端口"><a href="#NodePort：可以在物理机映射端口" class="headerlink" title="NodePort：可以在物理机映射端口"></a>NodePort：可以在物理机映射端口</h5><h5 id="ExternalName：可以对-service-做软连接"><a href="#ExternalName：可以对-service-做软连接" class="headerlink" title="ExternalName：可以对 service 做软连接"></a>ExternalName：可以对 service 做软连接</h5><h5 id="LoadBalancer：使用的是云的-slb"><a href="#LoadBalancer：使用的是云的-slb" class="headerlink" title="LoadBalancer：使用的是云的 slb"></a>LoadBalancer：使用的是云的 slb</h5><p><strong>7、ceph 架构是什么？</strong></p><h5 id="Ceph-是统一存储系统，支持三种接口："><a href="#Ceph-是统一存储系统，支持三种接口：" class="headerlink" title="Ceph 是统一存储系统，支持三种接口："></a>Ceph 是统一存储系统，支持三种接口：</h5><p><strong>1）、Object：有原生的 API，而且也兼容 Swift 和 S3 的 API</strong></p><p><strong>2）、Block：支持精简配置、快照、克隆</strong></p><p><strong>3）、File：Posix 接口，支持快照</strong></p><p><img src="https://picture-base.oss-cn-hangzhou.aliyuncs.com/picture/202402221101104.png" alt="image-20240222110125980"></p><p><strong>Ceph 也是分布式存储系统，它的特点是：</strong></p><h5 id="高扩展性：使用普通-x86-服务器，支持-10-1000-台服务器，支持-TB-到-PB-级的扩展。"><a href="#高扩展性：使用普通-x86-服务器，支持-10-1000-台服务器，支持-TB-到-PB-级的扩展。" class="headerlink" title="高扩展性：使用普通 x86 服务器，支持 10~1000 台服务器，支持 TB 到 PB 级的扩展。"></a><strong>高扩展性：</strong>使用普通 x86 服务器，支持 10~1000 台服务器，支持 TB 到 PB 级的扩展。</h5><h5 id="高可靠性：没有单点故障，多数据副本，自动管理，自动修复。"><a href="#高可靠性：没有单点故障，多数据副本，自动管理，自动修复。" class="headerlink" title="高可靠性：没有单点故障，多数据副本，自动管理，自动修复。"></a><strong>高可靠性：</strong>没有单点故障，多数据副本，自动管理，自动修复。</h5><h5 id="高性能：数据分布均衡，并行化度高。对于-objects-storage-和-block-storage-不需要元数据服务器。"><a href="#高性能：数据分布均衡，并行化度高。对于-objects-storage-和-block-storage-不需要元数据服务器。" class="headerlink" title="高性能：数据分布均衡，并行化度高。对于 objects storage 和 block storage,不需要元数据服务器。"></a><strong>高性能：</strong>数据分布均衡，并行化度高。对于 objects storage 和 block storage,不需要元数据服务器。</h5><p><strong>8、k8s 怎么对接 ceph？</strong></p><h5 id="把-ceph-rbd-或者-cephfs-做成-pvStorageclass-可以动态从-ceph-provisioner-找到-ceph，然后生成-pv"><a href="#把-ceph-rbd-或者-cephfs-做成-pvStorageclass-可以动态从-ceph-provisioner-找到-ceph，然后生成-pv" class="headerlink" title="把 ceph rbd 或者 cephfs 做成 pvStorageclass 可以动态从 ceph provisioner 找到 ceph，然后生成 pv"></a>把 ceph rbd 或者 cephfs 做成 pvStorageclass 可以动态从 ceph provisioner 找到 ceph，然后生成 pv</h5><p><strong>9、k8s 挂载 cephfs 和 ceph rbd 适用场景分析</strong></p><h5 id="K8s-挂载-cephfs-可以支持跨-node-节点-pod-挂载"><a href="#K8s-挂载-cephfs-可以支持跨-node-节点-pod-挂载" class="headerlink" title="K8s 挂载 cephfs 可以支持跨 node 节点 pod 挂载"></a>K8s 挂载 cephfs 可以支持跨 node 节点 pod 挂载</h5><h5 id="K8s-挂载-ceph-rbd-不支持跨-node-节点-pod-挂载"><a href="#K8s-挂载-ceph-rbd-不支持跨-node-节点-pod-挂载" class="headerlink" title="K8s 挂载 ceph rbd 不支持跨 node 节点 pod 挂载"></a>K8s 挂载 ceph rbd 不支持跨 node 节点 pod 挂载</h5><p><strong>10、k8s 有几种探测方式，分别描述下具体作用？</strong></p><p><strong>livenessProbe：存活性探测</strong></p><h5 id="许多应用程序经过长时间运行，最终过渡到无法运行的状态，除了重启，无法恢复。通常情况下，K8S-会发现应用程序已经终止，然后重启应用程序-pod。有时应用程序可能因为某些原因（后端服务故障等）导致暂时无法对外提供服务，但应用软件没有终止，导致K8S-无法隔离有故障的-pod，调用者可能会访问到有故障的-pod，导致业务不稳定。K8S提供-livenessProbe-来检测容器是否正常运行，并且对相应状况进行相应的补救措施。"><a href="#许多应用程序经过长时间运行，最终过渡到无法运行的状态，除了重启，无法恢复。通常情况下，K8S-会发现应用程序已经终止，然后重启应用程序-pod。有时应用程序可能因为某些原因（后端服务故障等）导致暂时无法对外提供服务，但应用软件没有终止，导致K8S-无法隔离有故障的-pod，调用者可能会访问到有故障的-pod，导致业务不稳定。K8S提供-livenessProbe-来检测容器是否正常运行，并且对相应状况进行相应的补救措施。" class="headerlink" title="许多应用程序经过长时间运行，最终过渡到无法运行的状态，除了重启，无法恢复。通常情况下，K8S 会发现应用程序已经终止，然后重启应用程序 pod。有时应用程序可能因为某些原因（后端服务故障等）导致暂时无法对外提供服务，但应用软件没有终止，导致K8S 无法隔离有故障的 pod，调用者可能会访问到有故障的 pod，导致业务不稳定。K8S提供 livenessProbe 来检测容器是否正常运行，并且对相应状况进行相应的补救措施。"></a>许多应用程序经过长时间运行，最终过渡到无法运行的状态，除了重启，无法恢复。通常情况下，K8S 会发现应用程序已经终止，然后重启应用程序 pod。有时应用程序可能因为某些原因（后端服务故障等）导致暂时无法对外提供服务，但应用软件没有终止，导致K8S 无法隔离有故障的 pod，调用者可能会访问到有故障的 pod，导致业务不稳定。K8S提供 livenessProbe 来检测容器是否正常运行，并且对相应状况进行相应的补救措施。</h5><p><strong>readinessProbe：就绪性探测</strong></p><h5 id="在没有配置-readinessProbe-的资源对象中，pod-中的容器启动完成后，就认为-pod-中的应用程序可以对外提供服务，该-pod-就会加入相对应的-service，对外提供服务。但有时一些应用程序启动后，需要较长时间的加载才能对外服务，如果这时对外提供服务，执行结果必然无法达到预期效果，影响用户体验。比如使用-tomcat-的应用程序来说，并不是简单地说-tomcat-启动成功就可以对外提供服务的，还需要等待-spring-容器初始化，数据库连接上等等。"><a href="#在没有配置-readinessProbe-的资源对象中，pod-中的容器启动完成后，就认为-pod-中的应用程序可以对外提供服务，该-pod-就会加入相对应的-service，对外提供服务。但有时一些应用程序启动后，需要较长时间的加载才能对外服务，如果这时对外提供服务，执行结果必然无法达到预期效果，影响用户体验。比如使用-tomcat-的应用程序来说，并不是简单地说-tomcat-启动成功就可以对外提供服务的，还需要等待-spring-容器初始化，数据库连接上等等。" class="headerlink" title="在没有配置 readinessProbe 的资源对象中，pod 中的容器启动完成后，就认为 pod 中的应用程序可以对外提供服务，该 pod 就会加入相对应的 service，对外提供服务。但有时一些应用程序启动后，需要较长时间的加载才能对外服务，如果这时对外提供服务，执行结果必然无法达到预期效果，影响用户体验。比如使用 tomcat 的应用程序来说，并不是简单地说 tomcat 启动成功就可以对外提供服务的，还需要等待 spring 容器初始化，数据库连接上等等。"></a>在没有配置 readinessProbe 的资源对象中，pod 中的容器启动完成后，就认为 pod 中的应用程序可以对外提供服务，该 pod 就会加入相对应的 service，对外提供服务。但有时一些应用程序启动后，需要较长时间的加载才能对外服务，如果这时对外提供服务，执行结果必然无法达到预期效果，影响用户体验。比如使用 tomcat 的应用程序来说，并不是简单地说 tomcat 启动成功就可以对外提供服务的，还需要等待 spring 容器初始化，数据库连接上等等。</h5><p><strong>startupProbe:</strong> <strong>探测容器中的应用是否已经启动</strong>。</p><h5 id="如果提供了启动探测-startupprobe-，则禁用所有其他探测，直到它成功为止。如果启动探测失败，kubelet-将杀死容器，容器服从其重启策略进行重启。如果容器没有提供启动探测，则默认状态为成功Success。"><a href="#如果提供了启动探测-startupprobe-，则禁用所有其他探测，直到它成功为止。如果启动探测失败，kubelet-将杀死容器，容器服从其重启策略进行重启。如果容器没有提供启动探测，则默认状态为成功Success。" class="headerlink" title="如果提供了启动探测(startupprobe)，则禁用所有其他探测，直到它成功为止。如果启动探测失败，kubelet 将杀死容器，容器服从其重启策略进行重启。如果容器没有提供启动探测，则默认状态为成功Success。"></a>如果提供了启动探测(startupprobe)，则禁用所有其他探测，直到它成功为止。如果启动探测失败，kubelet 将杀死容器，容器服从其重启策略进行重启。如果容器没有提供启动探测，则默认状态为成功Success。</h5><p><strong>11、 k8s 的 service 与 Ingress 区别</strong></p><h5 id="Service-是四层代理，只能基于-ip-和端口代理后端服务Ingress-controller-是七层代理，可以通过-http-或者-https-的域名基于-cookie、地域、请求头、百分比进行代理转发。Ingress-controller-代理需要找到对应的-Service，由-Service-向后代理-Pod"><a href="#Service-是四层代理，只能基于-ip-和端口代理后端服务Ingress-controller-是七层代理，可以通过-http-或者-https-的域名基于-cookie、地域、请求头、百分比进行代理转发。Ingress-controller-代理需要找到对应的-Service，由-Service-向后代理-Pod" class="headerlink" title="Service 是四层代理，只能基于 ip 和端口代理后端服务Ingress controller 是七层代理，可以通过 http 或者 https 的域名基于 cookie、地域、请求头、百分比进行代理转发。Ingress controller 代理需要找到对应的 Service，由 Service 向后代理 Pod"></a>Service 是四层代理，只能基于 ip 和端口代理后端服务Ingress controller 是七层代理，可以通过 http 或者 https 的域名基于 cookie、地域、请求头、百分比进行代理转发。Ingress controller 代理需要找到对应的 Service，由 Service 向后代理 Pod</h5><p><strong>12、说几个 k8s 的网络插件，说一下他们的差异</strong></p><h5 id="flannel：支持地址分配，不支持网络策略-。"><a href="#flannel：支持地址分配，不支持网络策略-。" class="headerlink" title="flannel：支持地址分配，不支持网络策略 。"></a>flannel：支持地址分配，不支持网络策略 。</h5><h5 id="calico：支持地址分配，支持网络策略。"><a href="#calico：支持地址分配，支持网络策略。" class="headerlink" title="calico：支持地址分配，支持网络策略。"></a>calico：支持地址分配，支持网络策略。</h5><h5 id="flannel："><a href="#flannel：" class="headerlink" title="flannel："></a>flannel：</h5><h5 id="vxlan：-扩展的虚拟局域网"><a href="#vxlan：-扩展的虚拟局域网" class="headerlink" title="vxlan：#扩展的虚拟局域网"></a>vxlan：#扩展的虚拟局域网</h5><h5 id="V-虚拟的"><a href="#V-虚拟的" class="headerlink" title="V 虚拟的"></a>V 虚拟的</h5><h5 id="X-扩展的"><a href="#X-扩展的" class="headerlink" title="X 扩展的"></a>X 扩展的</h5><h5 id="lan-局域网"><a href="#lan-局域网" class="headerlink" title="lan 局域网"></a>lan 局域网</h5><p><strong>flannel 支持多种后端：</strong></p><p><strong>1、VxLAN</strong>：</p><h5 id="1-vxlan-叠加网络模式"><a href="#1-vxlan-叠加网络模式" class="headerlink" title="(1) vxlan 叠加网络模式"></a>(1) vxlan 叠加网络模式</h5><h5 id="2-Directrouting"><a href="#2-Directrouting" class="headerlink" title="(2) Directrouting"></a>(2) Directrouting</h5><p><strong>2、host-gw: Host Gateway</strong></p><h5 id="直接路由模式，不推荐，只能在二层网络中，不支持跨网络，如果有成千上万的-Pod，容易产生广播风暴"><a href="#直接路由模式，不推荐，只能在二层网络中，不支持跨网络，如果有成千上万的-Pod，容易产生广播风暴" class="headerlink" title="#直接路由模式，不推荐，只能在二层网络中，不支持跨网络，如果有成千上万的 Pod，容易产生广播风暴"></a>#直接路由模式，不推荐，只能在二层网络中，不支持跨网络，如果有成千上万的 Pod，容易产生广播风暴</h5><p><strong>3、UDP：一般不用这个模式，性能差</strong></p><h5 id="flannel-方案：需要在每个节点上把发向容器的数据包进行封装后，再用隧道将封装后的数据包发送到运行着目标-Pod-的-node-节点上。目标-node-节点再负责去掉封装，将去除封装的数据包发送到目标-Pod-上。数据通信性能则大受影响"><a href="#flannel-方案：需要在每个节点上把发向容器的数据包进行封装后，再用隧道将封装后的数据包发送到运行着目标-Pod-的-node-节点上。目标-node-节点再负责去掉封装，将去除封装的数据包发送到目标-Pod-上。数据通信性能则大受影响" class="headerlink" title="flannel 方案：需要在每个节点上把发向容器的数据包进行封装后，再用隧道将封装后的数据包发送到运行着目标 Pod 的 node 节点上。目标 node 节点再负责去掉封装，将去除封装的数据包发送到目标 Pod 上。数据通信性能则大受影响"></a>flannel 方案：需要在每个节点上把发向容器的数据包进行封装后，再用隧道将封装后的数据包发送到运行着目标 Pod 的 node 节点上。目标 node 节点再负责去掉封装，将去除封装的数据包发送到目标 Pod 上。数据通信性能则大受影响</h5><h5 id="4、calico-方案：在-k8s-多个网路解决方案中选择了延迟表现最好的-calico-方案，可以设置网络策略适用于大型集群。"><a href="#4、calico-方案：在-k8s-多个网路解决方案中选择了延迟表现最好的-calico-方案，可以设置网络策略适用于大型集群。" class="headerlink" title="4、calico 方案：在 k8s 多个网路解决方案中选择了延迟表现最好的-calico 方案，可以设置网络策略适用于大型集群。"></a><strong>4、calico 方案：</strong>在 k8s 多个网路解决方案中选择了延迟表现最好的-calico 方案，可以设置网络策略适用于大型集群。</h5>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>2024-8-1 linux运维面试题</title>
    <link href="/2024/12/05/2024-8-1%20%E5%B8%B8%E8%A7%81Linux%E8%BF%90%E7%BB%B4%E9%9D%A2%E8%AF%95%E9%A2%98%EF%BC%8C%E6%89%BE%E5%B7%A5%E4%BD%9C%E7%9A%84%E5%BF%85%E7%9C%8B%EF%BC%81/"/>
    <url>/2024/12/05/2024-8-1%20%E5%B8%B8%E8%A7%81Linux%E8%BF%90%E7%BB%B4%E9%9D%A2%E8%AF%95%E9%A2%98%EF%BC%8C%E6%89%BE%E5%B7%A5%E4%BD%9C%E7%9A%84%E5%BF%85%E7%9C%8B%EF%BC%81/</url>
    
    <content type="html"><![CDATA[<p>下面是一名运维人员求职数十家公司总结的 Linux运维面试题，给大家参考下~</p><p><strong>1、现在给你三百台服务器，你怎么对他们进行管理？</strong></p><p>管理3百台服务器的方式：<br>1）设定跳板机，使用统一账号登录，便于安全与登录的考量。<br>2）使用 salt、ansiable、puppet 进行系统的统一调度与配置的统一管理。<br>3）建立简单的服务器的系统、配置、应用的 cmdb 信息管理。便于查阅每台服务器上的各种信息记录。</p><p><strong>2、简述 raid0 raid1 raid5 三种工作模式的工作原理及特点</strong></p><p>RAID，可以把硬盘整合成一个大磁盘，还可以在大磁盘上再分区，放数据<br>还有一个大功能，多块盘放在一起可以有冗余（备份）<br>RAID整合方式有很多，常用的：0 1 5 10</p><p><strong>RAID 0，可以是一块盘和 N 个盘组合</strong></p><ul><li><strong>优点</strong>读写快，是 RAID 中最好的</li><li><strong>缺点</strong>：没有冗余，一块坏了数据就全没有了</li></ul><p><strong>RAID 1，只能2块盘，盘的大小可以不一样，以小的为准。</strong></p><ul><li>10G+10G只有10G，另一个做备份。它有100%的冗余，</li><li>缺点：浪费资源，成本高</li></ul><p><strong>RAID 5 ，3块盘，容量计算10 *（n-1），损失一块盘</strong></p><p>特点，读写性能一般，读还好一点，写不好</p><ul><li>冗余从好到坏：RAID1 RAID10 RAID 5 RAID0</li><li>性能从好到坏：RAID0 RAID10 RAID5 RAID1</li><li>成本从低到高：RAID0 RAID5 RAID1 RAID10</li></ul><p>单台服务器：很重要盘不多，系统盘，RAID1<br>数据库服务器：主库：RAID10 从库 RAID5\RAID0（为了维护成本，RAID10）<br>WEB服务器，如果没有太多的数据的话，RAID5,RAID0（单盘）<br>有多台，监控、应用服务器，RAID0 RAID5</p><p>我们会根据数据的存储和访问的需求，去匹配对应的RAID级别</p><p><strong>3、LVS、Nginx、HAproxy 有什么区别？工作中你怎么选择？</strong></p><ul><li><strong>LVS</strong>：是基于四层的转发</li><li><strong>HAproxy</strong>：是基于四层和七层的转发，是专业的代理服务器</li><li><strong>Nginx</strong>：是WEB服务器，缓存服务器，又是反向代理服务器，可以做七层的转发</li></ul><blockquote><p>区别：LVS由于是基于四层的转发所以只能做端口的转发，而基于URL的、基于目录的这种转发LVS就做不了。</p></blockquote><p>工作选择：</p><ul><li>HAproxy 和 Nginx 由于可以做七层的转发，所以 URL 和目录的转发都可以做</li><li>在很大并发量的时候我们就要选择LVS，像中小型公司的话并发量没那么大</li><li>选择 HAproxy 或者 Nginx 足已，由于 HAproxy 由是专业的代理服务器</li><li>配置简单，所以中小型企业推荐使用 HAproxy</li></ul><p><strong>4、Squid、Varinsh 和 Nginx 有什么区别，工作中你怎么选择？</strong></p><p>Squid、Varinsh 和 Nginx 都是代理服务器</p><p><strong>什么是代理服务器：</strong></p><p>能当替用户去访问公网，并且能把访问到的数据缓存到服务器本地，等用户下次再访问相同的资源的时候，代理服务器直接从本地回应给用户，当本地没有的时候，我代替你去访问公网，我接收你的请求，我先在我自已的本地缓存找，如果我本地缓存有，我直接从我本地的缓存里回复你；如果我在我本地没有找到你要访问的缓存的数据，那么代理服务器就会代替你去访问公网。</p><p><strong>区别：</strong></p><p>1）Nginx 本来是反向代理&#x2F;web服务器，用了插件可以做做这个副业，但是本身不支持特性挺多，只能缓存静态文件<br>2）从这些功能上。varnish 和 squid 是专业的 cache 服务，而 nginx 这些是第三方模块完成<br>3）varnish 本身的技术上优势要高于 squid，它采用了可视化页面缓存技术</p><p>在内存的利用上，Varnish 比 Squid 具有优势，性能要比 Squid 高。还有强大的通过 Varnish 管理端口，可以使用正则表达式快速、批量地清除部分缓存。它是内存缓存，速度一流，但是内存缓存也限制了其容量，缓存页面和图片一般是挺好的</p><p>4）squid 的优势在于完整的庞大的 cache 技术资料，和很多的应用生产环境</p><p>工作中选择：</p><p>要做 cache 服务的话，我们肯定是要选择专业的cache服务，优先选择squid或者varnish。</p><p><strong>5、Tomcat和Resin有什么区别，工作中你怎么选择？</strong></p><ul><li><strong>区别</strong>：Tomcat 用户数多，可参考文档多，Resin用户数少，可考虑文档少</li></ul><p>最主要区别则是 Tomcat 是标准的 java 容器，不过性能方面比 resin 的要差一些。但稳定性和 java 程序的兼容性，应该是比 resin 的要好</p><p>工作中选择：现在大公司都是用 resin，追求性能；而中小型公司都是用 Tomcat，追求稳定和程序的兼容</p><p><strong>6、什么是中间件？什么是jdk？</strong></p><p><strong>中间件介绍：</strong></p><blockquote><p>中间件是一种独立的系统软件或服务程序，分布式应用软件借助这种软件在不同的技术之间共享资源。中间件位于客户机&#x2F;服务器的操作系统之上，管理计算机资源和网络通讯，是连接两个独立应用程序或独立系统的软件。</p><p>相连接的系统，即使它们具有不同的接口，但通过中间件相互之间仍能交换信息。执行中间件的一个关键途径是信息传递，通过中间件，应用程序可以工作于多平台或OS环境。</p></blockquote><p>jdk：jdk是Java的开发工具包<br>它是一种用于构建在 Java 平台上发布的应用程序、applet 和组件的开发环境</p><p><strong>7、讲述一下Tomcat8005、8009、8080三个端口的含义？</strong></p><p>8005 -  关闭时使用<br>8009 -  为AJP端口，即容器使用，如Apache能通过AJP协议访问Tomcat的8009端口<br>8080 -  一般应用使用</p><p><strong>8、什么叫CDN？</strong></p><ul><li>即内容分发网络</li><li>其目的是通过在现有的 Internet 中增加一层新的网络架构，将网站的内容发布到最接近用户的网络边缘，使用户可就近取得所需的内容，提高用户访问网站的速度</li></ul><p><strong>9、什么叫网站灰度发布？</strong></p><p>灰度发布是指在黑与白之间，能够平滑过渡的一种发布方式。</p><p>AB test 就是一种灰度发布方式，让一部用户继续用A，一部分用户开始用B，如果用户对B没有什么反对意见，那么逐步扩大范围，把所有用户都迁移到B上面来，灰度发布可以保证整体系统的稳定，在初始灰度的时候就可以发现、调整问题，以保证其影响度。</p><p><strong>10、简述 DNS 进行域名解析的过程？</strong></p><p>用户要访问 <a href="http://www.baidu.com,会先找本机的/">www.baidu.com，会先找本机的</a> host 文件，再找本地设置的 DNS 服务器，如果也没有的话，就去网络中找根服务器，根服务器反馈结果，说只能提供一级域名服务器.cn，就去找一级域名服务器，一级域名服务器说只能提供二级域名服务器.com.cn，就去找二级域名服务器。</p><p>二级域服务器只能提供三级域名服务器.baidu.com.cn，就去找三级域名服务器，三级域名服务器正好有这个网站<a href="http://www.baidu.com,然后发给请求的服务器,保存一份之后,再发给客户端./">www.baidu.com，然后发给请求的服务器，保存一份之后，再发给客户端。</a></p><p><strong>11、RabbitMQ 是什么东西？</strong></p><p>RabbitMQ 也就是消息队列中间件，消息中间件是在消息的传息过程中保存消息的容器，消息中间件再将消息从它的源中到它的目标中标时充当中间人的作用。队列的主要目的是提供路由并保证消息的传递；如果发送消息时接收者不可用，消息队列不会保留消息，直到可以成功地传递为止，当然，消息队列保存消息也是有期限地。</p><p><strong>12、讲述一下LVS三种模式的工作过程？</strong></p><p>LVS 有三种负载均衡的模式，分别是 VS&#x2F;NAT（nat 模式）、VS&#x2F;DR(路由模式)、VS&#x2F;TUN（隧道模式）</p><p><strong>一、NAT模式（VS-NAT）</strong></p><blockquote><p>原理：就是把客户端发来的数据包的IP头的目的地址，在负载均衡器上换成其中一台RS的IP地址</p></blockquote><p>并发至此 RS 来处理，RS处理完后把数据交给负载均衡器，负载均衡器再把数据包原IP地址改为自己的IP</p><p>将目的地址改为客户端IP地址即可期间，无论是进来的流量,还是出去的流量,都必须经过负载均衡器</p><ul><li><strong>优点</strong>：集群中的物理服务器可以使用任何支持TCP&#x2F;IP操作系统，只有负载均衡器需要一个合法的IP地址</li><li><strong>缺点</strong>：扩展性有限。当服务器节点（普通PC服务器）增长过多时,负载均衡器将成为整个系统的瓶颈</li></ul><p>因为所有的请求包和应答包的流向都经过负载均衡器。当服务器节点过多时</p><p>大量的数据包都交汇在负载均衡器那，速度就会变慢！</p><p><strong>二、IP隧道模式（VS-TUN）</strong></p><blockquote><p>原理：首先要知道，互联网上的大多Internet服务的请求包很短小，而应答包通常很大</p></blockquote><p>那么隧道模式就是，把客户端发来的数据包，封装一个新的IP头标记(仅目的IP)发给RS</p><p>RS收到后，先把数据包的头解开,还原数据包,处理后,直接返回给客户端,不需要再经过</p><p>负载均衡器。注意,由于RS需要对负载均衡器发过来的数据包进行还原,所以说必须支持</p><p>IPTUNNEL协议，所以，在RS的内核中，必须编译支持IPTUNNEL这个选项</p><ul><li><strong>优点</strong>：负载均衡器只负责将请求包分发给后端节点服务器，而RS将应答包直接发给用户。所以，减少了负载均衡器的大量数据流动，负载均衡器不再是系统的瓶颈，就能处理很巨大的请求量。这种方式，一台负载均衡器能够为很多RS进行分发。而且跑在公网上就能进行不同地域的分发。</li><li><strong>缺点</strong>：隧道模式的RS节点需要合法IP，这种方式需要所有的服务器支持”IP Tunneling”(IP Encapsulation)协议，服务器可能只局限在部分 Linux 系统上</li></ul><p><strong>三、直接路由模式（VS-DR）</strong></p><blockquote><p>原理：负载均衡器和RS都使用同一个IP对外服务但只有DR对ARP请求进行响应</p></blockquote><p>所有RS对本身这个IP的ARP请求保持静默也就是说,网关会把对这个服务IP的请求全部定向给DR</p><p>而DR收到数据包后根据调度算法,找出对应的RS，把目的MAC地址改为RS的MAC（因为IP一致）</p><p>并将请求分发给这台RS这时RS收到这个数据包，处理完成之后，由于IP一致，可以直接将数据返给客户</p><p>则等于直接从客户端收到这个数据包无异，处理后直接返回给客户端</p><p>由于负载均衡器要对二层包头进行改换，所以负载均衡器和RS之间必须在一个广播域</p><p>也可以简单的理解为在同一台交换机上</p><p><strong>优点</strong>：和TUN（隧道模式）一样，负载均衡器也只是分发请求，应答包通过单独的路由方法返回给客户端</p><p>与VS-TUN相比，VS-DR这种实现方式不需要隧道结构，因此可以使用大多数操作系统做为物理服务器。</p><p><strong>缺点</strong>：（不能说缺点，只能说是不足）要求负载均衡器的网卡必须与物理网卡在一个物理段上。</p><p><strong>13、MySQL 的 innodb 如何定位锁问题，MySQL 如何减少主从复制延迟？</strong></p><p>MySQL 的 innodb 如何定位锁问题:</p><p>在使用 show engine innodb status检查引擎状态时，发现了死锁问题</p><p>在5.5中，information_schema 库中增加了三个关于锁的表（MEMORY引擎）</p><figure class="highlight clean"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs clean">innodb_trx         ## 当前运行的所有事务<br>innodb_locks     ## 当前出现的锁<br>innodb_lock_waits  ## 锁等待的对应关系<br></code></pre></td></tr></table></figure><p>MySQL 如何减少主从复制延迟：</p><p>如果延迟比较大，就先确认以下几个因素：</p><ol><li>从库硬件比主库差，导致复制延迟</li><li>主从复制单线程，如果主库写并发太大，来不及传送到从库，就会导致延迟。更高版本的mysql可以支持多线程复制</li><li>慢SQL语句过多</li><li>网络延迟</li><li>master负载；主库读写压力大，导致复制延迟，架构的前端要加buffer及缓存层</li><li>slave负载；一般的做法是，使用多台 slave 来分摊读请求，再从这些 slave 中取一台专用的服务器。只作为备份用，不进行其他任何操作.另外， 2个可以减少延迟的参数：–slave-net-timeout&#x3D;seconds 单位为秒 默认设置为 3600秒</li></ol><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs shell"><span class="hljs-meta prompt_">#</span><span class="language-bash">参数含义：当slave从主数据库读取<span class="hljs-built_in">log</span>数据失败后，等待多久重新建立连接并获取数据–master-connect-retry=seconds 单位为秒 默认设置为 60秒</span><br><span class="hljs-meta prompt_">#</span><span class="language-bash">参数含义：当重新建立主从连接时，如果连接建立失败，间隔多久后重试通常配置以上2个参数可以减少网络问题导致的主从数据同步延迟</span><br></code></pre></td></tr></table></figure><p>MySQL 数据库主从同步延迟解决方案</p><p>最简单的减少slave同步延时的方案就是在架构上做优化，尽量让主库的DDL快速执行</p><p>还有就是主库是写，对数据安全性较高，比如sync_binlog&#x3D;1，innodb_flush_log_at_trx_commit&#x3D; 1 之类的设置，而slave则不需要这么高的数据安全，完全可以讲sync_binlog设置为0或者关闭binlog</p><p>innodb_flushlog也可以设置为0来提高sql的执行效率。另外就是使用比主库更好的硬件设备作为slave</p><p><strong>14、如何重置 MySQL Root 密码？</strong></p><p><strong>一、 在已知MYSQL数据库的ROOT用户密码的情况下，修改密码的方法：</strong></p><p>1、 在SHELL环境下，使用 mysqladmin 命令设置：</p><p>- </p><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs css">mysqladmin –u root –<span class="hljs-selector-tag">p</span> password “新密码”   回车后要求输入旧密码<br></code></pre></td></tr></table></figure><p>2、 在mysql&gt;环境中,使用update命令，直接更新 MySQL 库 user 表的数据：</p><figure class="highlight pgsql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs pgsql"><span class="hljs-keyword">Update</span>  mysql.<span class="hljs-keyword">user</span>  <span class="hljs-keyword">set</span>  <span class="hljs-keyword">password</span>=<span class="hljs-keyword">password</span>(‘新密码’)  <span class="hljs-keyword">where</span>  <span class="hljs-keyword">user</span>=’root’;<br>flush   <span class="hljs-keyword">privileges</span>;<br>注意：mysql语句要以分号”；”结束<br></code></pre></td></tr></table></figure><p>3、在mysql&gt;环境中，使用 grant 命令，修改 root 用户的授权权限。</p><figure class="highlight pgsql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs pgsql"><span class="hljs-keyword">grant</span>  <span class="hljs-keyword">all</span>  <span class="hljs-keyword">on</span>  *.*  <span class="hljs-keyword">to</span>   root@’localhost’  identified  <span class="hljs-keyword">by</span>  ‘新密码’；<br></code></pre></td></tr></table></figure><p><strong>二、 如查忘记了mysql数据库的ROOT用户的密码，又如何做呢？方法如下：</strong></p><p>1、 关闭当前运行的mysqld服务程序：service  mysqld  stop（要先将mysqld添加为系统服务）</p><p>2、 使用mysqld_safe脚本以安全模式（不加载授权表）启动mysqld 服务</p><figure class="highlight applescript"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs applescript">/usr/<span class="hljs-keyword">local</span>/mysql/bin/mysqld_safe  <span class="hljs-comment">--skip-grant-table  &amp;</span><br></code></pre></td></tr></table></figure><p>3、 使用空密码的root用户登录数据库，重新设置ROOT用户的密码</p><figure class="highlight pgsql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs pgsql">＃mysql  -u   root<br>Mysql&gt; <span class="hljs-keyword">Update</span>  mysql.<span class="hljs-keyword">user</span>  <span class="hljs-keyword">set</span>  <span class="hljs-keyword">password</span>=<span class="hljs-keyword">password</span>(‘新密码’)  <span class="hljs-keyword">where</span>  <span class="hljs-keyword">user</span>=’root’;<br>Mysql&gt; flush   <span class="hljs-keyword">privileges</span>;<br></code></pre></td></tr></table></figure><p><strong>15、lvs&#x2F;nginx&#x2F;haproxy 优缺点</strong></p><ul><li><strong>Nginx 的优点是：</strong></li></ul><p>1、工作在网络的7层之上，可以针对http应用做一些分流的策略，比如针对域名、目录结构，它的正则规则比HAProxy更为强大和灵活，这也是它目前广泛流行的主要原因之一，Nginx单凭这点可利用的场合就远多于LVS了。</p><p>2、Nginx 对网络稳定性的依赖非常小，理论上能ping通就就能进行负载功能，这个也是它的优势之一，相反 LVS 对网络稳定性依赖比较大，这点本人深有体会；</p><p>3、Nginx 安装和配置比较简单，测试起来比较方便，它基本能把错误用日志打印出来，LVS 的配置、测试就要花比较长的时间了，LVS对网络依赖比较大。</p><p>4、可以承担高负载压力且稳定，在硬件不差的情况下一般能支撑几万次的并发量，负载度比LVS相对小些。</p><p>5、Nginx可以通过端口检测到服务器内部的故障，比如根据服务器处理网页返回的状态码、超时等等，并且会把返回错误的请求重新提交到另一个节点，不过其中缺点就是不支持url来检测。比如用户正在上传一个文件，而处理该上传的节点刚好在上传过程中出现故障，Nginx会把上传切到另一台服务器重新处理，而LVS就直接断掉了。如果是上传一个很大的文件或者很重要的文件的话，用户可能会因此而不满。</p><p>6、Nginx 不仅仅是一款优秀的负载均衡器&#x2F;反向代理软件，它同时也是功能强大的Web应用服务器，LNMP也是近几年非常流行的web架构，在高流量的环境中稳定性也很好。</p><p>7、Nginx现在作为Web反向加速缓存越来越成熟了，速度比传统的Squid服务器更快，可考虑用其作为反向代理加速器</p><p>8、Nginx可作为中层反向代理使用，这一层面Nginx基本上无对手，唯一可以对比Nginx的就只有lighttpd了。不过lighttpd目前还没有做到Nginx完全的功能，配置也不那么清晰易读，社区资料也远远没Nginx活跃</p><p>9、Nginx也可作为静态网页和图片服务器，这方面的性能也无对手。还有Nginx社区非常活跃，第三方模块也很多</p><ul><li><strong>Nginx 的缺点是：</strong></li></ul><p>1、Nginx仅能支持http、https和Email协议，这样就在适用范围上面小些，这个是它的缺点</p><p>2、对后端服务器的健康检查，只支持通过端口来检测，不支持通过url来检测，不支持Session的直接保持，但能通过ip_hash来解决。</p><p>LVS：使用Linux内核集群实现一个高性能、高可用的负载均衡服务器</p><p>它具有很好的可伸缩性（Scalability)、可靠性（Reliability)和可管理性（Manageability)</p><ul><li><strong>LVS的优点是：</strong></li></ul><p>1、抗负载能力强、是工作在网络4层之上仅作分发之用，没有流量的产生，这个特点也决定了它在负载均衡软件里的性能最强的，对内存和cpu资源消耗比较低。</p><p>2、配置性比较低，这是一个缺点也是一个优点，因为没有可太多配置的东西，所以并不需要太多接触，大大减少了人为出错的几率</p><p>3、工作稳定，因为其本身抗负载能力很强，自身有完整的双机热备方案，如LVS+Keepalived，不过我们在项目实施中用得最多的还是LVS&#x2F;DR+Keepalived</p><p>4、无流量，LVS只分发请求，而流量并不从它本身出去，这点保证了均衡器IO的性能不会收到大流量的影响。</p><p>5、应用范围较广，因为LVS工作在4层，所以它几乎可对所有应用做负载均衡，包括http、数据库、在线聊天室等</p><ul><li><strong>LVS的缺点是：</strong></li></ul><p>1、软件本身不支持正则表达式处理，不能做动静分离</p><p>而现在许多网站在这方面都有较强的需求，这个是Nginx&#x2F;HAProxy+Keepalived的优势所在</p><p>2、如果是网站应用比较庞大的话，LVS&#x2F;DR+Keepalived 实施起来就比较复杂了</p><p>特别后面有Windows Server的机器的话，如果实施及配置还有维护过程就比较复杂了</p><p>相对而言，Nginx&#x2F;HAProxy+Keepalived 就简单多了。</p><ul><li><strong>HAProxy的特点是：</strong></li></ul><p>1、HAProxy也是支持虚拟主机的。</p><p>2、HAProxy 的优点能够补充 Nginx的 一些缺点，比如支持 Session 的保持，Cookie 的引导</p><figure class="highlight qml"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs qml">同时支持通过获取指定的<span class="hljs-built_in">url</span>来检测后端服务器的状态<br></code></pre></td></tr></table></figure><p>3、HAProxy跟LVS类似，本身就只是一款负载均衡软件</p><figure class="highlight"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs">单纯从效率上来讲HAProxy会比Nginx有更出色的负载均衡速度，在并发处理上也是优于Nginx的<br></code></pre></td></tr></table></figure><p>4、HAProxy支持TCP协议的负载均衡转发，可以对MySQL读进行负载均衡</p><figure class="highlight"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs">对后端的 MySQL 节点进行检测和负载均衡，大家可以用 LVS+Keepalived 对 MySQL 主从做负载均衡<br></code></pre></td></tr></table></figure><p>5、HAProxy负载均衡策略非常多，HAProxy的负载均衡算法现在具体有如下8种：</p><p>①roundrobin，表示简单的轮询，这个不多说，这个是负载均衡基本都具备的；</p><p>② static-rr，表示根据权重，建议关注；</p><p>③leastconn，表示最少连接者先处理，建议关注；</p><p>④ source，表示根据请求源IP，这个跟Nginx的IP_hash机制类似</p><figure class="highlight pgsql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs pgsql">我们用其作为解决<span class="hljs-keyword">session</span>问题的一种方法，建议关注；<br></code></pre></td></tr></table></figure><p>⑤ri，表示根据请求的URI；</p><p>⑥rl_param，表示根据请求的URl参数’balance url_param’ requires an URL parameter name；</p><p>⑦hdr(name)，表示根据HTTP请求头来锁定每一次HTTP请求；</p><p>⑧rdp-cookie(name)，表示根据据cookie(name)来锁定并哈希每一次TCP请求。</p><p><strong>16、mysql数据备份工具</strong></p><ul><li>mysqldump工具</li></ul><p>mysqldump是mysql自带的备份工具，目录在bin目录下面：&#x2F;usr&#x2F;local&#x2F;mysql&#x2F;bin&#x2F;mysqldump</p><p>支持基于innodb的热备份，但是由于是逻辑备份，所以速度不是很快，适合备份数据比较小的场景</p><p>Mysqldump完全备份+二进制日志可以实现基于时间点的恢复。</p><ul><li>基于LVM快照备份</li></ul><p>在物理备份中，有基于文件系统的物理备份（LVM的快照），也可以直接用tar之类的命令对整个数据库目录</p><p>进行打包备份，但是这些只能进行泠备份，不同的存储引擎备份的也不一样，myisam自动备份到表级别</p><p>而innodb不开启独立表空间的话只能备份整个数据库。</p><ul><li>tar包备份</li></ul><p>percona提供的xtrabackup工具</p><p>支持innodb的物理热备份，支持完全备份，增量备份，而且速度非常快，支持innodb存储引起的数据在不同</p><p>数据库之间迁移，支持复制模式下的从机备份恢复备份恢复，为了让xtrabackup支持更多的功能扩展</p><p>可以设立独立表空间，打开 innodb_file_per_table功能，启用之后可以支持单独的表备份</p><p><strong>17、keepalive的工作原理和如何做到健康检查</strong></p><p>keepalived是以VRRP协议为实现基础的，VRRP全称Virtual Router Redundancy Protocol，即虚拟路由冗余协议。</p><p>虚拟路由冗余协议，可以认为是实现路由器高可用的协议，即将N台提供相同功能的路由器组成一个路由器组</p><p>这个组里面有一个master和多个backup，master上面有一个对外提供服务的vip（该路由器所在局域网内</p><p>其他机器的默认路由为该vip），master会发组播，当backup收不到vrrp包时就认为master宕掉了</p><p>这时就需要根据VRRP的优先级来选举一个backup当master。这样就可以保证路由器的高可用了</p><p>keepalived主要有三个模块，分别是core、check和vrrp。core模块为keepalived的核心，负责主进程的启动、维护</p><p>及全局配置文件的加载和解析。check负责健康检查，包括常见的各种检查方式，vrrp模块是来实现VRRP协议的</p><p>Keepalived健康检查方式配置：</p><figure class="highlight tap"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><code class="hljs tap"><br>HTTP_GET|SSL_GET<br>HTTP_GET | SSL_GET<br>&#123;<br>url &#123;<br>path /<span class="hljs-comment"># HTTP/SSL 检查的url可以是多个</span><br>digest &lt;STRING&gt; <span class="hljs-comment"># HTTP/SSL 检查后的摘要信息用工具genhash生成</span><br>status_code 200<span class="hljs-comment"># HTTP/SSL 检查返回的状态码</span><br>&#125;<br>connect_port<span class="hljs-number"> 80 </span><span class="hljs-comment"># 连接端口</span><br>bindto&lt;IPADD&gt;<br>connect_timeout<span class="hljs-number"> 3 </span><span class="hljs-comment"># 连接超时时间</span><br>nb_get_retry<span class="hljs-number"> 3 </span><span class="hljs-comment"># 重连次数</span><br>delay_before_retry<span class="hljs-number"> 2 </span><span class="hljs-comment">#连接间隔时间</span><br>&#125;<br></code></pre></td></tr></table></figure><p><strong>18、统计ip访问情况，要求分析nginx访问日志，找出访问页面数量在前十位的ip</strong></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">cat</span> access.log | awk <span class="hljs-string">&#x27;&#123;print $1&#125;&#x27;</span> | <span class="hljs-built_in">uniq</span> -c | <span class="hljs-built_in">sort</span> -rn | <span class="hljs-built_in">head</span> -10<br></code></pre></td></tr></table></figure><p><strong>19、使用 tcpdump 监听主机为192.168.1.1，tcp端口为80的数据，同时将输出结果保存输出到 tcpdump.log</strong></p><figure class="highlight 1c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs 1c">tcpdump &#x27;host 192.168.1.1 and port 80&#x27; &gt; tcpdump.<span class="hljs-built_in">log</span><br></code></pre></td></tr></table></figure><p><strong>20、如何将本地80 端口的请求转发到8080 端口，当前主机IP 为192.168.2.1</strong></p><figure class="highlight apache"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs apache"><span class="hljs-attribute">iptables</span> -A PREROUTING -d <span class="hljs-number">192.168.2.1</span> -p tcp -m tcp -dport <span class="hljs-number">80</span> -j DNAT-to-destination <span class="hljs-number">192.168.2.1:8080</span><br></code></pre></td></tr></table></figure><p><strong>21、你对现在运维工程师的理解和以及对其工作的认识</strong></p><p>运维工程师在公司当中责任重大，需要保证时刻为公司及客户提供最高、最快、最稳定、最安全的服务</p><p>运维工程师的一个小小的失误，很有可能会对公司及客户造成重大损失</p><p>因此运维工程师的工作需要严谨及富有创新精神</p><p><strong>22、实时抓取并显示当前系统中tcp 80端口的网络数据信息，请写出完整操作命令</strong></p><figure class="highlight apache"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs apache"><span class="hljs-attribute">tcpdump</span> -nn tcp port <span class="hljs-number">80</span><br></code></pre></td></tr></table></figure><p><strong>23、服务器开不了机怎么解决一步步的排查</strong></p><p><img src="https://picture-base.oss-cn-hangzhou.aliyuncs.com/picture/202408011532620.png" alt="image-20240801153227472"></p><p><strong>24、Linux 系统中病毒怎么解决</strong></p><p>1）最简单有效的方法就是重装系统<br>2）要查的话就是找到病毒文件然后删除</p><p>中毒之后一般机器 CPU、内存使用率会比较高</p><p>机器向外发包等异常情况，排查方法简单介绍下</p><p>top 命令找到 CPU 使用率最高的进程</p><p>一般病毒文件命名都比较乱，可以用 ps aux 找到病毒文件位置</p><p>rm -f 命令删除病毒文件</p><p>检查计划任务、开机启动项和病毒文件目录有无其他可以文件等</p><p>3）由于即使删除病毒文件不排除有潜伏病毒，所以最好是把机器备份数据之后重装一下</p><p><strong>25、发现一个病毒文件你删了他又自动创建怎么解决</strong></p><p>公司的内网某台linux服务器流量莫名其妙的剧增，用 iftop 查看有连接外网的情况</p><p>针对这种情况一般重点查看 netstat 连接的外网 ip 和端口。</p><p>用 lsof -p pid 可以查看到具体是那些进程，哪些文件</p><p>经查勘发现&#x2F;root下有相关的配置conf.n hhe两个可疑文件，rm -rf后不到一分钟就自动生成了</p><p>由此推断是某个母进程产生的这些文件。所以找到母进程就是找到罪魁祸首</p><p>查杀病毒最好断掉外网访问，还好是内网服务器，可以通过内网访问</p><p>断了内网，病毒就失去外联的能力，杀掉它就容易的多</p><p>怎么找到呢，找了半天也没有看到蛛丝马迹，没办法只有ps axu一个个排查</p><p>方法是查看可以的用户和和系统相似而又不是的冒牌货，果然，看到了如下进程可疑</p><p>看不到图片就是&#x2F;usr&#x2F;bin&#x2F;.sshd</p><p>于是我杀掉所有.sshd相关的进程，然后直接删掉.sshd这个可执行文件</p><p>然后才删掉了文章开头提到的自动复活的文件</p><p>总结一下，遇到这种问题，如果不是太严重，尽量不要重装系统</p><p>一般就是先断外网，然后利用iftop,ps,netstat,chattr,lsof,pstree这些工具顺藤摸瓜</p><p>一般都能找到元凶。但是如果遇到诸如此类的问题</p><p>&#x2F;boot&#x2F;efi&#x2F;EFI&#x2F;redhat&#x2F;grub.efi: Heuristics.Broken.Executable FOUND，个人觉得就要重装系统了</p><p><strong>26、说说TCP&#x2F;IP的七层模型</strong></p><ul><li><strong>应用层 (Application)：</strong></li></ul><p>网络服务与最终用户的一个接口。</p><p>协议有：HTTP FTP TFTP SMTP SNMP DNS TELNET HTTPS POP3 DHCP</p><ul><li><strong>表示层（Presentation Layer）：</strong></li></ul><p>数据的表示、安全、压缩。（在五层模型里面已经合并到了应用层）</p><p>格式有，JPEG、ASCll、DECOIC、加密格式等</p><ul><li><strong>会话层（Session Layer）：</strong></li></ul><p>建立、管理、终止会话。（在五层模型里面已经合并到了应用层）</p><p>对应主机进程，指本地主机与远程主机正在进行的会话</p><ul><li><strong>传输层 (Transport)：</strong></li></ul><p>定义传输数据的协议端口号，以及流控和差错校验。</p><p>协议有：TCP UDP，数据包一旦离开网卡即进入网络传输层</p><ul><li><strong>网络层 (Network)：</strong></li></ul><p>进行逻辑地址寻址，实现不同网络之间的路径选择。</p><p>协议有：ICMP IGMP IP（IPV4 IPV6） ARP RARP</p><ul><li><strong>数据链路层 (Link)：</strong></li></ul><p>建立逻辑连接、进行硬件地址寻址、差错校验等功能。（由底层网络定义协议）</p><p>将比特组合成字节进而组合成帧，用MAC地址访问介质，错误发现但不能纠正</p><ul><li><strong>物理层（Physical Layer）：</strong></li></ul><p>是计算机网络OSI模型中最低的一层</p><p>物理层规定：为传输数据所需要的物理链路创建、维持、拆除</p><p>而提供具有机械的，电子的，功能的和规范的特性</p><p>简单的说，物理层确保原始的数据可在各种物理媒体上传输。局域网与广域网皆属第1、2层</p><p>物理层是OSI的第一层，它虽然处于最底层，却是整个开放系统的基础</p><p>物理层为设备之间的数据通信提供传输媒体及互连设备，为数据传输提供可靠的环境</p><p>如果您想要用尽量少的词来记住这个第一层，那就是“信号和介质”。</p><p><strong>27、你常用的 Nginx 模块，用来做什么</strong></p><figure class="highlight ada"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs ada"><br>rewrite模块，实现重写功能<br><span class="hljs-keyword">access</span>模块：来源控制<br>ssl模块：安全加密<br>ngx_http_gzip_module：网络传输压缩模块<br>ngx_http_proxy_module 模块实现代理<br>ngx_http_upstream_module模块实现定义后端服务器列表<br>ngx_cache_purge实现缓存清除功能<br></code></pre></td></tr></table></figure><p><strong>28、请列出你了解的web服务器负载架构</strong></p><p>Nginx<br>Haproxy<br>Keepalived<br>LVS</p><p><strong>29、查看http的并发请求数与其TCP连接状态</strong></p><figure class="highlight cos"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cos">netstat -n | awk &#x27;/<span class="hljs-symbol">^tcp</span>/ &#123;++<span class="hljs-keyword">S</span>[<span class="hljs-built_in">$NF</span>]&#125; END &#123;<span class="hljs-keyword">for</span>(a in <span class="hljs-keyword">S</span>) <span class="hljs-keyword">print</span> a, <span class="hljs-keyword">S</span>[a]&#125;&#x27;<br></code></pre></td></tr></table></figure><p>还有ulimit -n 查看linux系统打开最大的文件描述符，这里默认1024</p><p>不修改这里web服务器修改再大也没用，若要用就修改很几个办法，这里说其中一个：</p><p>修改&#x2F;etc&#x2F;security&#x2F;limits.conf</p><figure class="highlight asciidoc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs asciidoc"><br><span class="hljs-bullet">* </span>soft nofile 10240<br><br><span class="hljs-bullet">* </span>hard nofile 10240<br></code></pre></td></tr></table></figure><p>重启后生效</p><p><strong>30、用tcpdump嗅探80端口的访问看看谁最高</strong></p><figure class="highlight apache"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs apache"><span class="hljs-attribute">tcpdump</span> -i eth0 -tnn dst port <span class="hljs-number">80</span> -c <span class="hljs-number">1000</span> | awk -F<span class="hljs-string">&quot;.&quot;</span> &#x27;&#123;print $<span class="hljs-number">1</span><span class="hljs-string">&quot;.&quot;</span>$<span class="hljs-number">2</span><span class="hljs-string">&quot;.&quot;</span>$<span class="hljs-number">3</span><span class="hljs-string">&quot;.&quot;</span>$<span class="hljs-number">4</span>&#125;&#x27;| sort | uniq -c | sort -nr |head -<span class="hljs-number">20</span><br></code></pre></td></tr></table></figure><p><strong>31、写一个脚本，实现判断192.168.1.0&#x2F;24网络里，当前在线的IP有哪些，能ping通则认为在线</strong></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><code class="hljs bash"><br><span class="hljs-comment">#!/bin/bash</span><br><span class="hljs-keyword">for</span> ip <span class="hljs-keyword">in</span> `<span class="hljs-built_in">seq</span> 1 255`<br><span class="hljs-keyword">do</span><br>&#123;<br><br>ping -c 1 192.168.1.<span class="hljs-variable">$ip</span> &gt; /dev/null 2&gt;&amp;1<br><span class="hljs-keyword">if</span> [ $? -eq 0 ]; <span class="hljs-keyword">then</span><br><span class="hljs-built_in">echo</span> 192.168.1.<span class="hljs-variable">$ip</span> UP<br><span class="hljs-keyword">else</span><br><span class="hljs-built_in">echo</span> 192.168.1.<span class="hljs-variable">$ip</span> DOWN<br><span class="hljs-keyword">fi</span><br>&#125;&amp;<br><span class="hljs-keyword">done</span><br><span class="hljs-built_in">wait</span><br></code></pre></td></tr></table></figure><p><strong>32、已知 apache 服务的访问日志按天记录在服务器本地目录&#x2F;app&#x2F;logs 下，由于磁盘空间紧张现在要求只能保留最近 7 天的访问日志！请问如何解决？请给出解决办法或配置或处理命令</strong></p><p>创建文件脚本：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs bash"><br><span class="hljs-comment">#!/bin/bash</span><br><span class="hljs-keyword">for</span> n <span class="hljs-keyword">in</span> `<span class="hljs-built_in">seq</span> 14`<br><span class="hljs-keyword">do</span>        <br><br><span class="hljs-built_in">date</span> -s <span class="hljs-string">&quot;11/0<span class="hljs-variable">$n</span>/14&quot;</span><br><span class="hljs-built_in">touch</span> access_www_`(<span class="hljs-built_in">date</span> +%F)`.<span class="hljs-built_in">log</span><br><span class="hljs-keyword">done</span><br></code></pre></td></tr></table></figure><p>解决方法：</p><figure class="highlight tap"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br></pre></td><td class="code"><pre><code class="hljs tap"><br><span class="hljs-comment"># pwd/application/logs</span><br><span class="hljs-comment"># ll</span><br>-rw-r--r--.<span class="hljs-number"> 1 </span>root root<span class="hljs-number"> 0 </span>Jan <span class="hljs-number"> 1 </span>00:00 access_www_2015-01-01.log<br>-rw-r--r--.<span class="hljs-number"> 1 </span>root root<span class="hljs-number"> 0 </span>Jan <span class="hljs-number"> 2 </span>00:00 access_www_2015-01-02.log<br>-rw-r--r--.<span class="hljs-number"> 1 </span>root root<span class="hljs-number"> 0 </span>Jan <span class="hljs-number"> 3 </span>00:00 access_www_2015-01-03.log<br>-rw-r--r--.<span class="hljs-number"> 1 </span>root root<span class="hljs-number"> 0 </span>Jan <span class="hljs-number"> 4 </span>00:00 access_www_2015-01-04.log<br>-rw-r--r--.<span class="hljs-number"> 1 </span>root root<span class="hljs-number"> 0 </span>Jan <span class="hljs-number"> 5 </span>00:00 access_www_2015-01-05.log<br>-rw-r--r--.<span class="hljs-number"> 1 </span>root root<span class="hljs-number"> 0 </span>Jan <span class="hljs-number"> 6 </span>00:00 access_www_2015-01-06.log<br>-rw-r--r--.<span class="hljs-number"> 1 </span>root root<span class="hljs-number"> 0 </span>Jan <span class="hljs-number"> 7 </span>00:00 access_www_2015-01-07.log<br>-rw-r--r--.<span class="hljs-number"> 1 </span>root root<span class="hljs-number"> 0 </span>Jan <span class="hljs-number"> 8 </span>00:00 access_www_2015-01-08.log<br>-rw-r--r--.<span class="hljs-number"> 1 </span>root root<span class="hljs-number"> 0 </span>Jan <span class="hljs-number"> 9 </span>00:00 access_www_2015-01-09.log<br>-rw-r--r--.<span class="hljs-number"> 1 </span>root root<span class="hljs-number"> 0 </span>Jan<span class="hljs-number"> 10 </span>00:00 access_www_2015-01-10.log<br>-rw-r--r--.<span class="hljs-number"> 1 </span>root root<span class="hljs-number"> 0 </span>Jan<span class="hljs-number"> 11 </span>00:00 access_www_2015-01-11.log<br>-rw-r--r--.<span class="hljs-number"> 1 </span>root root<span class="hljs-number"> 0 </span>Jan<span class="hljs-number"> 12 </span>00:00 access_www_2015-01-12.log<br>-rw-r--r--.<span class="hljs-number"> 1 </span>root root<span class="hljs-number"> 0 </span>Jan<span class="hljs-number"> 13 </span>00:00 access_www_2015-01-13.log<br><br>-rw-r--r--.<span class="hljs-number"> 1 </span>root root<span class="hljs-number"> 0 </span>Jan<span class="hljs-number"> 14 </span>00:00 access_www_2015-01-14.log<br><span class="hljs-comment"># find /application/logs/ -type f -mtime +7 -name &quot;*.log&quot;|xargs rm –f  </span><br><br><span class="hljs-comment">##也可以使用-exec rm -f &#123;&#125; \;进行删除</span><br><span class="hljs-comment"># ll</span><br>-rw-r--r--.<span class="hljs-number"> 1 </span>root root<span class="hljs-number"> 0 </span>Jan <span class="hljs-number"> 7 </span>00:00 access_www_2015-01-07.log<br>-rw-r--r--.<span class="hljs-number"> 1 </span>root root<span class="hljs-number"> 0 </span>Jan <span class="hljs-number"> 8 </span>00:00 access_www_2015-01-08.log<br>-rw-r--r--.<span class="hljs-number"> 1 </span>root root<span class="hljs-number"> 0 </span>Jan <span class="hljs-number"> 9 </span>00:00 access_www_2015-01-09.log<br>-rw-r--r--.<span class="hljs-number"> 1 </span>root root<span class="hljs-number"> 0 </span>Jan<span class="hljs-number"> 10 </span>00:00 access_www_2015-01-10.log<br>-rw-r--r--.<span class="hljs-number"> 1 </span>root root<span class="hljs-number"> 0 </span>Jan<span class="hljs-number"> 11 </span>00:00 access_www_2015-01-11.log<br>-rw-r--r--.<span class="hljs-number"> 1 </span>root root<span class="hljs-number"> 0 </span>Jan<span class="hljs-number"> 12 </span>00:00 access_www_2015-01-12.log<br>-rw-r--r--.<span class="hljs-number"> 1 </span>root root<span class="hljs-number"> 0 </span>Jan<span class="hljs-number"> 13 </span>00:00 access_www_2015-01-13.log<br><br>-rw-r--r--.<span class="hljs-number"> 1 </span>root root<span class="hljs-number"> 0 </span>Jan<span class="hljs-number"> 14 </span>00:00 access_www_2015-01-14.log<br></code></pre></td></tr></table></figure><p><strong>33、如何优化 Linux系统（可以不说太具体）？</strong></p><p>不用root，添加普通用户，通过sudo授权管理<br>更改默认的远程连接SSH服务端口及禁止root用户远程连接<br>定时自动更新服务器时间<br>配置国内yum源<br>关闭selinux及iptables（iptables工作场景如果有外网IP一定要打开，高并发除外）<br>调整文件描述符的数量<br>精简开机启动服务（crond rsyslog network sshd）<br>内核参数优化（&#x2F;etc&#x2F;sysctl.conf）<br>更改字符集，支持中文，但建议还是用英文字符集，防止乱码<br>锁定关键系统文件<br>清空&#x2F;etc&#x2F;issue，去除系统及内核版本登录前的屏幕显示</p><p><strong>34、请执行命令取出 linux 中 eth0 的 IP 地址(请用 cut，有能力者也可分别用 awk,sed 命令答）</strong></p><p>cut方法1：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs shell"><span class="hljs-meta prompt_"></span><br><span class="hljs-meta prompt_"># </span><span class="language-bash">ifconfig eth0|sed -n <span class="hljs-string">&#x27;2p&#x27;</span>|<span class="hljs-built_in">cut</span> -d <span class="hljs-string">&quot;:&quot;</span> -f2|<span class="hljs-built_in">cut</span> -d <span class="hljs-string">&quot; &quot;</span> -f1</span><br>192.168.20.130<br></code></pre></td></tr></table></figure><p>awk方法2：</p><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs dart"><br># ifconfig eth0|awk <span class="hljs-string">&#x27;NR==2&#x27;</span>|awk -F <span class="hljs-string">&quot;:&quot;</span> <span class="hljs-string">&#x27;&#123;print <span class="hljs-subst">$2</span>&#125;&#x27;</span>|awk <span class="hljs-string">&#x27;&#123;print <span class="hljs-subst">$1</span>&#125;&#x27;</span><br><span class="hljs-number">192.168</span><span class="hljs-number">.20</span><span class="hljs-number">.130</span><br></code></pre></td></tr></table></figure><p>awk多分隔符方法3：</p><figure class="highlight accesslog"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs accesslog"><br># ifconfig eth0|awk &#x27;NR==<span class="hljs-number">2</span>&#x27;|awk -F <span class="hljs-string">&quot;[: ]+&quot;</span> &#x27;&#123;print $<span class="hljs-number">4</span>&#125;&#x27;<br><span class="hljs-number">192.168.20.130</span><br></code></pre></td></tr></table></figure><p>sed方法4:</p><figure class="highlight accesslog"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs accesslog"><br># ifconfig eth0|sed -n &#x27;/inet addr/p&#x27;|sed -r &#x27;s#^.*ddr:(.*)Bc.*$#\<span class="hljs-number">1</span>#g&#x27;<br><span class="hljs-number">192.168.20.130</span><br></code></pre></td></tr></table></figure><p><strong>35、每天晚上 12 点，打包站点目录&#x2F;var&#x2F;www&#x2F;html 备份到&#x2F;data 目录下（最好每次备份按时间生成不同的备份包）</strong></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs bash"><br><span class="hljs-comment"># cat a.sh </span><br><br><span class="hljs-comment">#/bin/bash</span><br><span class="hljs-built_in">cd</span> /var/www/ &amp;&amp; /bin/tar zcf /data/html-`<span class="hljs-built_in">date</span> +%m-%d%H`.tar.gz html/<br><span class="hljs-comment"># crontab –e</span><br>00 00 * * * /bin/sh /root/a.sh<br></code></pre></td></tr></table></figure>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>2024-3-8 面试</title>
    <link href="/2024/12/05/2024-3-8%20%E9%9D%A2%E8%AF%950308/"/>
    <url>/2024/12/05/2024-3-8%20%E9%9D%A2%E8%AF%950308/</url>
    
    <content type="html"><![CDATA[<h3 id="根据nginx日志-过滤nginx前十访问量的ip"><a href="#根据nginx日志-过滤nginx前十访问量的ip" class="headerlink" title="根据nginx日志  过滤nginx前十访问量的ip"></a>根据nginx日志  过滤nginx前十访问量的ip</h3><h5 id="要根据Nginx日志过滤出前十访问量的IP，你可以使用awk、sort和head命令组合来完成这个任务。假设你的Nginx访问日志格式如下（这是Nginx的默认格式）："><a href="#要根据Nginx日志过滤出前十访问量的IP，你可以使用awk、sort和head命令组合来完成这个任务。假设你的Nginx访问日志格式如下（这是Nginx的默认格式）：" class="headerlink" title="要根据Nginx日志过滤出前十访问量的IP，你可以使用awk、sort和head命令组合来完成这个任务。假设你的Nginx访问日志格式如下（这是Nginx的默认格式）："></a>要根据Nginx日志过滤出前十访问量的IP，你可以使用<code>awk</code>、<code>sort</code>和<code>head</code>命令组合来完成这个任务。假设你的Nginx访问日志格式如下（这是Nginx的默认格式）：</h5><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs bash">bash复制代码<br><br>127.0.0.1 - - [10/Oct/2023:14:05:01 +0000] <span class="hljs-string">&quot;GET / HTTP/1.1&quot;</span> 200 612 <span class="hljs-string">&quot;-&quot;</span> <span class="hljs-string">&quot;curl/7.68.0&quot;</span><br></code></pre></td></tr></table></figure><h5 id="在这个日志中，第一个字段是客户端的IP地址。"><a href="#在这个日志中，第一个字段是客户端的IP地址。" class="headerlink" title="在这个日志中，第一个字段是客户端的IP地址。"></a>在这个日志中，第一个字段是客户端的IP地址。</h5><h5 id="你可以使用以下命令来过滤出前十访问量的IP："><a href="#你可以使用以下命令来过滤出前十访问量的IP：" class="headerlink" title="你可以使用以下命令来过滤出前十访问量的IP："></a>你可以使用以下命令来过滤出前十访问量的IP：</h5><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs bash">bash复制代码<br><br>awk <span class="hljs-string">&#x27;&#123;print $1&#125;&#x27;</span> access.log | <span class="hljs-built_in">sort</span> | <span class="hljs-built_in">uniq</span> -c | <span class="hljs-built_in">sort</span> -nr | <span class="hljs-built_in">head</span> -n 10<br><br><span class="hljs-built_in">cat</span> /var/log/nginx/access.log |awk <span class="hljs-string">&#x27;&#123;print $1&#125;&#x27;</span>|<span class="hljs-built_in">sort</span>|<span class="hljs-built_in">uniq</span> -c |<span class="hljs-built_in">sort</span> -nr|<span class="hljs-built_in">head</span> -n 10<br></code></pre></td></tr></table></figure><h5 id="这个命令的解释如下："><a href="#这个命令的解释如下：" class="headerlink" title="这个命令的解释如下："></a>这个命令的解释如下：</h5><ol><li><h5 id="awk-print-1-access-log：使用awk命令从access-log文件中提取每行的第一个字段（即IP地址）。"><a href="#awk-print-1-access-log：使用awk命令从access-log文件中提取每行的第一个字段（即IP地址）。" class="headerlink" title="awk &#39;{print $1}&#39; access.log：使用awk命令从access.log文件中提取每行的第一个字段（即IP地址）。"></a><code>awk &#39;&#123;print $1&#125;&#39; access.log</code>：使用<code>awk</code>命令从<code>access.log</code>文件中提取每行的第一个字段（即IP地址）。</h5></li><li><h5 id="sort：对提取出的IP地址进行排序。"><a href="#sort：对提取出的IP地址进行排序。" class="headerlink" title="sort：对提取出的IP地址进行排序。"></a><code>sort</code>：对提取出的IP地址进行排序。</h5></li><li><h5 id="uniq-c：统计每个唯一IP地址的出现次数。"><a href="#uniq-c：统计每个唯一IP地址的出现次数。" class="headerlink" title="uniq -c：统计每个唯一IP地址的出现次数。"></a><code>uniq -c</code>：统计每个唯一IP地址的出现次数。</h5></li><li><h5 id="sort-nr：按数字进行逆序排序，这样访问量最多的IP会排在最前面。"><a href="#sort-nr：按数字进行逆序排序，这样访问量最多的IP会排在最前面。" class="headerlink" title="sort -nr：按数字进行逆序排序，这样访问量最多的IP会排在最前面。"></a><code>sort -nr</code>：按数字进行逆序排序，这样访问量最多的IP会排在最前面。</h5></li><li><h5 id="head-n-10：只显示前10行，即访问量最多的前10个IP。"><a href="#head-n-10：只显示前10行，即访问量最多的前10个IP。" class="headerlink" title="head -n 10：只显示前10行，即访问量最多的前10个IP。"></a><code>head -n 10</code>：只显示前10行，即访问量最多的前10个IP。</h5></li></ol><h5 id="请注意，这个命令假设你的Nginx日志文件名是access-log。如果你的日志文件名不同，请相应地替换文件名。此外，这个命令也假设你的日志格式与上面所示的默认格式相匹配。如果你的日志格式不同，你可能需要调整awk命令中的字段选择器。"><a href="#请注意，这个命令假设你的Nginx日志文件名是access-log。如果你的日志文件名不同，请相应地替换文件名。此外，这个命令也假设你的日志格式与上面所示的默认格式相匹配。如果你的日志格式不同，你可能需要调整awk命令中的字段选择器。" class="headerlink" title="请注意，这个命令假设你的Nginx日志文件名是access.log。如果你的日志文件名不同，请相应地替换文件名。此外，这个命令也假设你的日志格式与上面所示的默认格式相匹配。如果你的日志格式不同，你可能需要调整awk命令中的字段选择器。"></a>请注意，这个命令假设你的Nginx日志文件名是<code>access.log</code>。如果你的日志文件名不同，请相应地替换文件名。此外，这个命令也假设你的日志格式与上面所示的默认格式相匹配。如果你的日志格式不同，你可能需要调整<code>awk</code>命令中的字段选择器。</h5><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">cat</span> access.log|awk <span class="hljs-string">&#x27;&#123;print$1&#125;&#x27;</span>|<span class="hljs-built_in">sort</span>|<span class="hljs-built_in">sort</span> -nr|<span class="hljs-built_in">uniq</span> -c|<span class="hljs-built_in">head</span> -n 10<br></code></pre></td></tr></table></figure><h3 id="awk的-nf啥意思"><a href="#awk的-nf啥意思" class="headerlink" title="awk的 $nf啥意思"></a>awk的 $nf啥意思</h3><h5 id="在awk命令中，-n是一个字段引用，其中n是一个数字，表示当前行的第n个字段。awk默认使用空白字符（通常是空格或制表符）作为字段分隔符，因此-1、-2、-3等分别代表每行的第一个、第二个、第三个字段，依此类推。"><a href="#在awk命令中，-n是一个字段引用，其中n是一个数字，表示当前行的第n个字段。awk默认使用空白字符（通常是空格或制表符）作为字段分隔符，因此-1、-2、-3等分别代表每行的第一个、第二个、第三个字段，依此类推。" class="headerlink" title="在awk命令中，$n是一个字段引用，其中n是一个数字，表示当前行的第n个字段。awk默认使用空白字符（通常是空格或制表符）作为字段分隔符，因此$1、$2、$3等分别代表每行的第一个、第二个、第三个字段，依此类推。"></a>在<code>awk</code>命令中，<code>$n</code>是一个字段引用，其中<code>n</code>是一个数字，表示当前行的第<code>n</code>个字段。<code>awk</code>默认使用空白字符（通常是空格或制表符）作为字段分隔符，因此<code>$1</code>、<code>$2</code>、<code>$3</code>等分别代表每行的第一个、第二个、第三个字段，依此类推。</h5><h5 id="在处理Nginx的访问日志时，通常每个字段都是由特定的分隔符（如空格或特殊字符）分隔的。例如，在Nginx的默认日志格式中："><a href="#在处理Nginx的访问日志时，通常每个字段都是由特定的分隔符（如空格或特殊字符）分隔的。例如，在Nginx的默认日志格式中：" class="headerlink" title="在处理Nginx的访问日志时，通常每个字段都是由特定的分隔符（如空格或特殊字符）分隔的。例如，在Nginx的默认日志格式中："></a>在处理Nginx的访问日志时，通常每个字段都是由特定的分隔符（如空格或特殊字符）分隔的。例如，在Nginx的默认日志格式中：</h5><figure class="highlight accesslog"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs accesslog">复制代码<br><br><span class="hljs-number">127.0.0.1</span> - - <span class="hljs-string">[10/Oct/2023:14:05:01 +0000]</span> <span class="hljs-string">&quot;<span class="hljs-keyword">GET</span> / HTTP/1.1&quot;</span> <span class="hljs-number">200</span> <span class="hljs-number">612</span> <span class="hljs-string">&quot;-&quot;</span> <span class="hljs-string">&quot;curl/7.68.0&quot;</span><br></code></pre></td></tr></table></figure><ul><li><h5 id="1-通常是客户端的IP地址。"><a href="#1-通常是客户端的IP地址。" class="headerlink" title="$1 通常是客户端的IP地址。"></a><code>$1</code> 通常是客户端的IP地址。</h5></li><li><h5 id="2-通常是身份验证的用户名（在这个例子中是-，表示没有用户名）。"><a href="#2-通常是身份验证的用户名（在这个例子中是-，表示没有用户名）。" class="headerlink" title="$2 通常是身份验证的用户名（在这个例子中是 -，表示没有用户名）。"></a><code>$2</code> 通常是身份验证的用户名（在这个例子中是 <code>-</code>，表示没有用户名）。</h5></li><li><h5 id="3-通常是用户的身份（通常也是-）。"><a href="#3-通常是用户的身份（通常也是-）。" class="headerlink" title="$3 通常是用户的身份（通常也是 -）。"></a><code>$3</code> 通常是用户的身份（通常也是 <code>-</code>）。</h5></li><li><h5 id="4-是请求时间。"><a href="#4-是请求时间。" class="headerlink" title="$4 是请求时间。"></a><code>$4</code> 是请求时间。</h5></li><li><h5 id="5-是请求行（例如-GET-HTTP-1-1-）。"><a href="#5-是请求行（例如-GET-HTTP-1-1-）。" class="headerlink" title="$5 是请求行（例如 &quot;GET / HTTP/1.1&quot;）。"></a><code>$5</code> 是请求行（例如 <code>&quot;GET / HTTP/1.1&quot;</code>）。</h5></li><li><h5 id="6-是响应状态码（例如-200）。"><a href="#6-是响应状态码（例如-200）。" class="headerlink" title="$6 是响应状态码（例如 200）。"></a><code>$6</code> 是响应状态码（例如 <code>200</code>）。</h5></li><li><h5 id="7-是响应体的大小（例如-612）。"><a href="#7-是响应体的大小（例如-612）。" class="headerlink" title="$7 是响应体的大小（例如 612）。"></a><code>$7</code> 是响应体的大小（例如 <code>612</code>）。</h5></li><li><h5 id="后续的字段可能包括引用页、用户代理等。"><a href="#后续的字段可能包括引用页、用户代理等。" class="headerlink" title="后续的字段可能包括引用页、用户代理等。"></a>后续的字段可能包括引用页、用户代理等。</h5></li></ul><h5 id="当你使用awk-print-1-access-log时，你正在告诉awk打印出每一行的第一个字段，即客户端的IP地址。"><a href="#当你使用awk-print-1-access-log时，你正在告诉awk打印出每一行的第一个字段，即客户端的IP地址。" class="headerlink" title="当你使用awk &#39;{print $1}&#39; access.log时，你正在告诉awk打印出每一行的第一个字段，即客户端的IP地址。"></a>当你使用<code>awk &#39;&#123;print $1&#125;&#39; access.log</code>时，你正在告诉<code>awk</code>打印出每一行的第一个字段，即客户端的IP地址。</h5><h5 id="awk也允许你通过-F选项来指定一个不同的字段分隔符。例如，如果Nginx日志使用不同的分隔符，你可以这样指定："><a href="#awk也允许你通过-F选项来指定一个不同的字段分隔符。例如，如果Nginx日志使用不同的分隔符，你可以这样指定：" class="headerlink" title="awk也允许你通过-F选项来指定一个不同的字段分隔符。例如，如果Nginx日志使用不同的分隔符，你可以这样指定："></a><code>awk</code>也允许你通过<code>-F</code>选项来指定一个不同的字段分隔符。例如，如果Nginx日志使用不同的分隔符，你可以这样指定：</h5><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs bash">bash复制代码<br><br>awk -F<span class="hljs-string">&#x27; &#x27;</span> <span class="hljs-string">&#x27;&#123;print $1&#125;&#x27;</span> access.log<br></code></pre></td></tr></table></figure><p>这里的<code>-F&#39; &#39;</code>告诉<code>awk</code>使用空格作为字段分隔符。但是，请注意，Nginx日志中的时间戳通常包含在方括号内，这可能会影响字段的计数。如果时间戳是一个字段，并且你想跳过它，你可能需要使用更复杂的<code>awk</code>脚本或者不同的方法来处理日志。</p><p>如果你需要处理更复杂的日志格式，你可能需要调整<code>awk</code>脚本，以正确解析和提取所需的字段。例如，你可以使用正则表达式来匹配和提取特定的字段，或者调整字段分隔符以匹配日志中的实际格式。</p><h3 id="bash-正常返回啥"><a href="#bash-正常返回啥" class="headerlink" title="bash 正常返回啥"></a>bash 正常返回啥</h3><h5 id="在-Bash-脚本或命令行中，一个命令“正常返回”通常意味着该命令执行成功并且没有错误。当命令成功执行时，它会返回一个退出状态码（exit-status-code），这个状态码通常是一个介于-0-到-255-之间的整数。在-Unix-和-Linux-系统中，约定俗成的是："><a href="#在-Bash-脚本或命令行中，一个命令“正常返回”通常意味着该命令执行成功并且没有错误。当命令成功执行时，它会返回一个退出状态码（exit-status-code），这个状态码通常是一个介于-0-到-255-之间的整数。在-Unix-和-Linux-系统中，约定俗成的是：" class="headerlink" title="在 Bash 脚本或命令行中，一个命令“正常返回”通常意味着该命令执行成功并且没有错误。当命令成功执行时，它会返回一个退出状态码（exit status code），这个状态码通常是一个介于 0 到 255 之间的整数。在 Unix 和 Linux 系统中，约定俗成的是："></a>在 Bash 脚本或命令行中，一个命令“正常返回”通常意味着该命令执行成功并且没有错误。当命令成功执行时，它会返回一个退出状态码（exit status code），这个状态码通常是一个介于 0 到 255 之间的整数。在 Unix 和 Linux 系统中，约定俗成的是：</h5><ul><li><h5 id="退出状态码-0-表示命令执行成功。"><a href="#退出状态码-0-表示命令执行成功。" class="headerlink" title="退出状态码 0 表示命令执行成功。"></a><strong>退出状态码 0</strong> 表示命令执行成功。</h5></li><li><h5 id="非零的退出状态码表示命令执行出错或遇到了某种问题。不同的非零状态码通常用于指示不同类型的错误。"><a href="#非零的退出状态码表示命令执行出错或遇到了某种问题。不同的非零状态码通常用于指示不同类型的错误。" class="headerlink" title="非零的退出状态码表示命令执行出错或遇到了某种问题。不同的非零状态码通常用于指示不同类型的错误。"></a>非零的退出状态码表示命令执行出错或遇到了某种问题。不同的非零状态码通常用于指示不同类型的错误。</h5></li></ul><h5 id="你可以使用-这个特殊变量来获取上一个命令的退出状态码。例如："><a href="#你可以使用-这个特殊变量来获取上一个命令的退出状态码。例如：" class="headerlink" title="你可以使用 $? 这个特殊变量来获取上一个命令的退出状态码。例如："></a>你可以使用 <code>$?</code> 这个特殊变量来获取上一个命令的退出状态码。例如：</h5><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-meta">#!/bin/bash  </span><br>  <br><span class="hljs-built_in">echo</span> <span class="hljs-string">&quot;Running a command...&quot;</span>  <br><span class="hljs-built_in">ls</span> /nonexistentdirectory  <br><span class="hljs-built_in">echo</span> <span class="hljs-string">&quot;Exit status: $?&quot;</span><br></code></pre></td></tr></table></figure><h5 id="在这个脚本中，ls-nonexistentdirectory-命令会尝试列出一个不存在的目录的内容，这将导致一个错误，并返回一个非零的退出状态码。然后，echo-Exit-status-会打印出这个状态码。"><a href="#在这个脚本中，ls-nonexistentdirectory-命令会尝试列出一个不存在的目录的内容，这将导致一个错误，并返回一个非零的退出状态码。然后，echo-Exit-status-会打印出这个状态码。" class="headerlink" title="在这个脚本中，ls /nonexistentdirectory 命令会尝试列出一个不存在的目录的内容，这将导致一个错误，并返回一个非零的退出状态码。然后，echo &quot;Exit status: $?&quot; 会打印出这个状态码。"></a>在这个脚本中，<code>ls /nonexistentdirectory</code> 命令会尝试列出一个不存在的目录的内容，这将导致一个错误，并返回一个非零的退出状态码。然后，<code>echo &quot;Exit status: $?&quot;</code> 会打印出这个状态码。</h5><h5 id="通常，你不需要手动检查每个命令的退出状态码，因为-Bash-脚本会在遇到非零退出状态码时停止执行（除非你在脚本中使用了错误处理机制，如-set-e）。但是，在编写需要处理错误或根据不同退出状态码采取不同动作的复杂脚本时，了解和使用退出状态码是非常重要的。"><a href="#通常，你不需要手动检查每个命令的退出状态码，因为-Bash-脚本会在遇到非零退出状态码时停止执行（除非你在脚本中使用了错误处理机制，如-set-e）。但是，在编写需要处理错误或根据不同退出状态码采取不同动作的复杂脚本时，了解和使用退出状态码是非常重要的。" class="headerlink" title="通常，你不需要手动检查每个命令的退出状态码，因为 Bash 脚本会在遇到非零退出状态码时停止执行（除非你在脚本中使用了错误处理机制，如 set -e）。但是，在编写需要处理错误或根据不同退出状态码采取不同动作的复杂脚本时，了解和使用退出状态码是非常重要的。"></a>通常，你不需要手动检查每个命令的退出状态码，因为 Bash 脚本会在遇到非零退出状态码时停止执行（除非你在脚本中使用了错误处理机制，如 <code>set -e</code>）。但是，在编写需要处理错误或根据不同退出状态码采取不同动作的复杂脚本时，了解和使用退出状态码是非常重要的。</h5><h5 id="此外，有些命令可能会返回特定的非零状态码来表示不同类型的错误。例如，grep-命令在找不到匹配项时会返回-1，而在遇到其他错误时会返回不同的值。因此，理解你正在使用的命令的文档，以了解它们返回的不同状态码的含义，也是非常重要的。"><a href="#此外，有些命令可能会返回特定的非零状态码来表示不同类型的错误。例如，grep-命令在找不到匹配项时会返回-1，而在遇到其他错误时会返回不同的值。因此，理解你正在使用的命令的文档，以了解它们返回的不同状态码的含义，也是非常重要的。" class="headerlink" title="此外，有些命令可能会返回特定的非零状态码来表示不同类型的错误。例如，grep 命令在找不到匹配项时会返回 1，而在遇到其他错误时会返回不同的值。因此，理解你正在使用的命令的文档，以了解它们返回的不同状态码的含义，也是非常重要的。"></a>此外，有些命令可能会返回特定的非零状态码来表示不同类型的错误。例如，<code>grep</code> 命令在找不到匹配项时会返回 1，而在遇到其他错误时会返回不同的值。因此，理解你正在使用的命令的文档，以了解它们返回的不同状态码的含义，也是非常重要的。</h5><h3 id="k8s创建pod的流程"><a href="#k8s创建pod的流程" class="headerlink" title="k8s创建pod的流程"></a>k8s创建pod的流程</h3><figure class="highlight markdown"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs markdown"><span class="hljs-bullet">1.</span> 客户端提交 Pod 的配置信息(可以是 yaml 文件定义的信息)到 kube-apiserver.<br><br><span class="hljs-bullet">2.</span> Apiserver 收到指令后,通知 controllr-manager 创建一个资源对象<br><br><span class="hljs-bullet">3.</span> controller-manager 通过 apiserver 将 pod 的配置信息存储到 ETCD 数据中心中<br><br><span class="hljs-bullet">4.</span> kube-scheduler 检查到 pod 信息会开始调度预选,会先过滤不符合 Pod 资源配置要求的节点,然后开始调度调优,主要是挑选出更适合运行的 pod 节点,然后将 pod 的资源配置单发送到 node 节点上的 kubelet 组件上<br><br><span class="hljs-bullet">5.</span>  kubelet 根据 scheduler 发来的资源配置单运行 pod,运行成功后,将 pod 的运行的信息返回 scheduler, scheduler 将返回的 pod 运行状况的信息存储到 etcd 数据中心<br></code></pre></td></tr></table></figure><h3 id="dockerfile的copy和add的区别"><a href="#dockerfile的copy和add的区别" class="headerlink" title="dockerfile的copy和add的区别"></a>dockerfile的copy和add的区别</h3><figure class="highlight oxygene"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><code class="hljs oxygene">Dockerfile中的<span class="hljs-keyword">COPY</span>和<span class="hljs-keyword">ADD</span>指令都用于将文件或目录从构建上下文复制到容器中，但它们之间存在一些关键区别：<br><br>解压缩功能：<br><span class="hljs-keyword">COPY</span>指令仅用于复制本地文件到镜像，不涉及解压缩操作。<br><span class="hljs-keyword">ADD</span>指令除了复制文件外，还具有自动解压缩的功能。如果源路径为URL地址或压缩文件，<span class="hljs-keyword">ADD</span>会尝试自动解压缩文件到目标路径。这意味着使用<span class="hljs-keyword">ADD</span>时，如果源文件是压缩格式（如tar.gz），它会在复制过程中自动解压。<br>源文件来源：<br><span class="hljs-keyword">COPY</span>指令只能从执行docker build所在的主机上读取资源并复制到镜像中。<br><span class="hljs-keyword">ADD</span>指令除了能够复制本地文件和目录外，还支持通过URL从远程服务器读取资源并复制到镜像中。这使得<span class="hljs-keyword">ADD</span>在需要从外部源获取文件时非常有用。<br>目录创建：<br><span class="hljs-keyword">ADD</span>指令在复制文件时，如果目标路径不存在，则会自动创建目标路径。而<span class="hljs-keyword">COPY</span>指令没有这个自动创建路径的功能。<br>最佳实践：<br>由于<span class="hljs-keyword">COPY</span>指令更简单且直接，通常推荐在只需要复制本地文件到容器中的情况下使用<span class="hljs-keyword">COPY</span>，这样可以避免意外的解压缩行为，也更符合直觉。<br>multistage构建：<br>在multistage构建的场景中，<span class="hljs-keyword">COPY</span>指令具有特定的用法。它可以将前一阶段构建的产物拷贝到另一个镜像中。这是<span class="hljs-keyword">ADD</span>指令所不具备的功能。<br>综上所述，<span class="hljs-keyword">COPY</span>和<span class="hljs-keyword">ADD</span>指令在Dockerfile中各有其独特之处，选择使用哪个指令取决于具体的需求和场景。在大多数情况下，如果只需要复制本地文件且不需要解压缩或远程获取文件，<span class="hljs-keyword">COPY</span>指令是更好的选择。然而，如果需要从远程URL获取文件或自动解压缩压缩文件，那么<span class="hljs-keyword">ADD</span>指令将更为合适。<br></code></pre></td></tr></table></figure><h4 id="helm-chart的包怎么打？"><a href="#helm-chart的包怎么打？" class="headerlink" title="helm chart的包怎么打？"></a>helm chart的包怎么打？</h4><figure class="highlight vim"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs vim">Helm Chart包的打包过程可以通过以下步骤完成：<br><br>安装Helm工具。<br>创建一个新的目录，作为Helm包的根目录。<br>在根目录中创建一个Chart.yaml文件，用于存储Helm包的元数据。<br>在根目录中创建一个templates目录，用于存储Kubernetes资源的模板文件。这些模板文件可以包括各种Kubernetes资源的定义，例如Deployment、Service等。<br>在templates目录中编写需要的Kubernetes资源模板文件。这些文件使用Go模板语言编写，可以根据需要引用<span class="hljs-built_in">values</span>.yaml文件中的变量。<br>使用Helm工具将模板文件打包成Chart包。这可以通过执行helm package <span class="hljs-symbol">&lt;chart_path&gt;</span>命令完成，其中<span class="hljs-symbol">&lt;chart_path&gt;</span>是包含Chart.yaml和templates目录的Helm包路径。<br>打包完成后，你就得到了一个Helm Chart包，它包含了所有用于部署Kubernetes应用的必要文件和模板。你可以将这个包发布到Helm Chart仓库中，以便其他人使用，或者使用Helm工具将其安装到Kubernetes集群中。<br><br>请注意，这只是一个基本的打包过程，实际的步骤可能会根据你的具体需求和Helm的版本有所不同。因此，在打包Helm Chart包之前，建议查阅Helm的官方文档或相关教程以获取更详细和准确的信息。<br></code></pre></td></tr></table></figure><h3 id="http的504错误"><a href="#http的504错误" class="headerlink" title="http的504错误"></a>http的504错误</h3><figure class="highlight awk"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><code class="hljs awk"><span class="hljs-number">504</span>错误代表网关超时 （Gateway timeout），是指服务器作为网关或代理，但是没有及时从上游服务器收到请求。这通常意味着上游服务器已关闭（不响应网关 <span class="hljs-regexp">/ 代理），而不是上游服务器和网关/</span>代理在交换数据的协议上不一致。<br><br>首先，了解什么是网关。<br><br>网络的基本概念：<br><br>客户端:应用 C<span class="hljs-regexp">/S（客户端/</span>服务器） B<span class="hljs-regexp">/S（浏览器/</span>服务器）<br><br>服务器：为客户端提供服务、数据、资源的机器<br><br>请求：客户端向服务器索取数据<br><br>响应：服务器对客户端请求作出反应，一般是返回给客户端数据<br><br>在这之中，把nginx或Apache作为网关。一般服务的架构是：用PHP则是nginx+php的一系列进程，Apache+tomcat+JVM。<br><br>网关超时就与nginx或Apache配置的超时时间，和与php线程、java线程的响应时间有关。以nginx与PHP为例：它的超时配置fastcgi_connect_timeout、fastcgi_send_timeout、fastcgi_read_timeout。nginx将请求丢给PHP来处理，某个PHP的线程响应时间假如是<span class="hljs-number">10</span>s，在<span class="hljs-number">10</span>s内没有响应给nginx就报超时。这时可以打开PHP慢日志记录，然后排查之。<br><br>另外，数据库的慢查询也会导致<span class="hljs-number">504</span> 。nginx只要进程没有死，一般不是nginx的问题。假如场景是：确定程序执行是正确的，比如向数据库插入大量数据，需要<span class="hljs-number">5</span>分钟，nginx设置的超时时间是<span class="hljs-number">3</span>分钟。这时候可以将超时时间临时设置为大于<span class="hljs-number">5</span>分钟。<br><br></code></pre></td></tr></table></figure>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>2024-2-26 面试</title>
    <link href="/2024/12/05/2024-2-26%20%E9%9D%A2%E8%AF%95/"/>
    <url>/2024/12/05/2024-2-26%20%E9%9D%A2%E8%AF%95/</url>
    
    <content type="html"><![CDATA[<h3 id="k8s-有哪些组件？"><a href="#k8s-有哪些组件？" class="headerlink" title="k8s 有哪些组件？"></a>k8s 有哪些组件？</h3><h5 id="1、etcd保存了集群的状态"><a href="#1、etcd保存了集群的状态" class="headerlink" title="1、etcd保存了集群的状态"></a>1、etcd保存了集群的状态</h5><h5 id="2、apiserver提供了资源操作的唯一入口，还提供认证，授权，访问控制，api注册和服务发现等"><a href="#2、apiserver提供了资源操作的唯一入口，还提供认证，授权，访问控制，api注册和服务发现等" class="headerlink" title="2、apiserver提供了资源操作的唯一入口，还提供认证，授权，访问控制，api注册和服务发现等"></a>2、apiserver提供了资源操作的唯一入口，还提供认证，授权，访问控制，api注册和服务发现等</h5><h5 id="3、controller-manager负责维护集群的状态，比如自动扩展，滚动更新，故障检测等"><a href="#3、controller-manager负责维护集群的状态，比如自动扩展，滚动更新，故障检测等" class="headerlink" title="3、controller manager负责维护集群的状态，比如自动扩展，滚动更新，故障检测等"></a>3、controller manager负责维护集群的状态，比如自动扩展，滚动更新，故障检测等</h5><h5 id="4、scheduler-负责集群资源的调度，安照预期的调度策略将pod调度到相应的node上"><a href="#4、scheduler-负责集群资源的调度，安照预期的调度策略将pod调度到相应的node上" class="headerlink" title="4、scheduler 负责集群资源的调度，安照预期的调度策略将pod调度到相应的node上"></a>4、scheduler 负责集群资源的调度，安照预期的调度策略将pod调度到相应的node上</h5><h5 id="5、kubelet负责管理容器的生命周期，还负责共享存储卷和网络的管理"><a href="#5、kubelet负责管理容器的生命周期，还负责共享存储卷和网络的管理" class="headerlink" title="5、kubelet负责管理容器的生命周期，还负责共享存储卷和网络的管理"></a>5、kubelet负责管理容器的生命周期，还负责共享存储卷和网络的管理</h5><h5 id="6、Container-runtime-容器运行时，负责管理镜像的管理，pod和容器的真正运行"><a href="#6、Container-runtime-容器运行时，负责管理镜像的管理，pod和容器的真正运行" class="headerlink" title="6、Container runtime 容器运行时，负责管理镜像的管理，pod和容器的真正运行"></a>6、Container runtime 容器运行时，负责管理镜像的管理，pod和容器的真正运行</h5><h5 id="7、kube-proxy负责管理集群的服务发现和负载均衡"><a href="#7、kube-proxy负责管理集群的服务发现和负载均衡" class="headerlink" title="7、kube-proxy负责管理集群的服务发现和负载均衡"></a>7、kube-proxy负责管理集群的服务发现和负载均衡</h5><p>Kubernetes（K8s）是一个功能强大的容器编排平台，它由多个组件构成，这些组件共同协作来实现集群的管理、调度、监控等功能。下面是 Kubernetes 主要组件的简要介绍：</p><h3 id="1-Kubernetes-核心组件"><a href="#1-Kubernetes-核心组件" class="headerlink" title="1. Kubernetes 核心组件"></a><strong>1. Kubernetes 核心组件</strong></h3><h4 id="1-1-API-Server-kube-apiserver"><a href="#1-1-API-Server-kube-apiserver" class="headerlink" title="1.1 API Server (kube-apiserver)"></a>1.1 <strong>API Server (<code>kube-apiserver</code>)</strong></h4><ul><li><p><strong>职责</strong>：API Server 是 Kubernetes 控制平面的核心组件，它暴露了 Kubernetes 集群的 REST API，所有的命令和操作（如创建、删除、更新 Pod 等）都通过 API Server 来进行。</p></li><li><p>功能</p><p>：</p><ul><li>接收来自用户、控制器、调度器等的请求。</li><li>负责验证和处理请求。</li><li>提供 REST API 接口，供客户端和其他组件使用。</li><li>存储集群状态数据（通过与 etcd 交互）。</li></ul></li></ul><h4 id="1-2-Controller-Manager-kube-controller-manager"><a href="#1-2-Controller-Manager-kube-controller-manager" class="headerlink" title="1.2 Controller Manager (kube-controller-manager)"></a>1.2 <strong>Controller Manager (<code>kube-controller-manager</code>)</strong></h4><ul><li><p><strong>职责</strong>：Controller Manager 负责运行集群的控制循环（controller loops），确保集群的实际状态与期望状态相一致。</p></li><li><p>功能</p><p>：</p><ul><li>启动各种控制器，如 Deployment Controller、ReplicaSet Controller、Node Controller 等。</li><li>处理集群资源的自我修复任务，如创建新的 Pod 来替代已死 Pod，扩缩容等。</li></ul></li></ul><h4 id="1-3-Scheduler-kube-scheduler"><a href="#1-3-Scheduler-kube-scheduler" class="headerlink" title="1.3 Scheduler (kube-scheduler)"></a>1.3 <strong>Scheduler (<code>kube-scheduler</code>)</strong></h4><ul><li><p><strong>职责</strong>：调度器负责将 Pod 分配到集群中的合适节点上。</p></li><li><p>功能</p><p>：</p><ul><li>根据集群资源情况和 Pod 的要求，选择最佳的节点来运行 Pod。</li><li>支持不同的调度策略，如资源请求、节点亲和性（Node Affinity）等。</li></ul></li></ul><h4 id="1-4-etcd"><a href="#1-4-etcd" class="headerlink" title="1.4 etcd"></a>1.4 <strong>etcd</strong></h4><ul><li><p><strong>职责</strong>：etcd 是 Kubernetes 的分布式键值存储系统，用于存储集群的所有状态数据，包括 Pod、节点、Service 等所有资源对象的配置和元数据。</p></li><li><p>功能</p><p>：</p><ul><li>持久化存储集群的所有状态信息。</li><li>提供强一致性保障，确保所有集群组件共享最新的状态数据。</li></ul></li></ul><h4 id="1-5-Cloud-Controller-Manager-kube-cloud-controller-manager"><a href="#1-5-Cloud-Controller-Manager-kube-cloud-controller-manager" class="headerlink" title="1.5 Cloud Controller Manager (kube-cloud-controller-manager)"></a>1.5 <strong>Cloud Controller Manager (<code>kube-cloud-controller-manager</code>)</strong></h4><ul><li><p><strong>职责</strong>：Cloud Controller Manager 是为了支持云环境的 Kubernetes 扩展，它使得 Kubernetes 集群能够与云提供商的 API 集成。</p></li><li><p>功能</p><p>：</p><ul><li>负责与云提供商的控制平面交互，管理负载均衡器、存储卷等云资源。</li><li>让 Kubernetes 集群能够在多云环境中管理资源。</li></ul></li></ul><hr><h3 id="2-Kubernetes-节点组件"><a href="#2-Kubernetes-节点组件" class="headerlink" title="2. Kubernetes 节点组件"></a><strong>2. Kubernetes 节点组件</strong></h3><h4 id="2-1-Kubelet"><a href="#2-1-Kubelet" class="headerlink" title="2.1 Kubelet"></a>2.1 <strong>Kubelet</strong></h4><ul><li><p><strong>职责</strong>：Kubelet 是集群中每个节点上的代理，负责管理节点上的容器（Pod）。</p></li><li><p>功能</p><p>：</p><ul><li>监控和管理节点上的容器，确保 Pod 中的容器处于正常状态。</li><li>从 API Server 获取 Pod 的规格，并执行容器的启动、停止等操作。</li><li>收集和报告节点的资源使用情况（如 CPU、内存）到 API Server。</li></ul></li></ul><h4 id="2-2-Kube-Proxy"><a href="#2-2-Kube-Proxy" class="headerlink" title="2.2 Kube Proxy"></a>2.2 <strong>Kube Proxy</strong></h4><ul><li><p><strong>职责</strong>：Kube Proxy 是一个网络代理，它负责维护集群中的网络规则，以确保网络流量能够正确地转发到 Pod。</p></li><li><p>功能</p><p>：</p><ul><li>管理每个节点的网络流量，基于 iptables 或 IPVS 实现负载均衡。</li><li>确保 Service 的 DNS 名称能够被解析，并且流量能够正确地路由到后端的 Pod。</li></ul></li></ul><h4 id="2-3-Container-Runtime"><a href="#2-3-Container-Runtime" class="headerlink" title="2.3 Container Runtime"></a>2.3 <strong>Container Runtime</strong></h4><ul><li><p><strong>职责</strong>：容器运行时（如 Docker、containerd）负责运行容器并管理容器生命周期。</p></li><li><p>功能</p><p>：</p><ul><li>启动、停止容器。</li><li>拉取容器镜像并管理容器资源。</li></ul></li></ul><hr><h3 id="3-高层组件和工具"><a href="#3-高层组件和工具" class="headerlink" title="3. 高层组件和工具"></a><strong>3. 高层组件和工具</strong></h3><h4 id="3-1-Kubernetes-Dashboard"><a href="#3-1-Kubernetes-Dashboard" class="headerlink" title="3.1 Kubernetes Dashboard"></a>3.1 <strong>Kubernetes Dashboard</strong></h4><ul><li><p><strong>职责</strong>：Kubernetes Dashboard 是一个基于 Web 的 UI 工具，用于管理和监控 Kubernetes 集群。</p></li><li><p>功能</p><p>：</p><ul><li>提供图形化的界面，方便用户查看集群状态、Pod、Deployment、Service 等资源。</li><li>支持用户创建、更新、删除集群资源。</li></ul></li></ul><h4 id="3-2-Helm"><a href="#3-2-Helm" class="headerlink" title="3.2 Helm"></a>3.2 <strong>Helm</strong></h4><ul><li><p><strong>职责</strong>：Helm 是 Kubernetes 的包管理工具，类似于 Linux 的 <code>apt</code> 或 <code>yum</code>。</p></li><li><p>功能</p><p>：</p><ul><li>通过 Helm Charts 来管理应用程序的部署。</li><li>Helm Charts 是预配置的 Kubernetes 资源文件集合，允许用户方便地部署和管理应用。</li></ul></li></ul><h4 id="3-3-Ingress-Controller"><a href="#3-3-Ingress-Controller" class="headerlink" title="3.3 Ingress Controller"></a>3.3 <strong>Ingress Controller</strong></h4><ul><li><p><strong>职责</strong>：Ingress Controller 是一种专门处理 HTTP&#x2F;HTTPS 请求的 Kubernetes 组件，它根据 Ingress 资源来路由流量。</p></li><li><p>功能</p><p>：</p><ul><li>控制外部流量进入集群。</li><li>配置负载均衡、SSL&#x2F;TLS 终端和 URL 路由等。</li><li>常见的 Ingress Controller 有 Nginx Ingress Controller、Traefik 等。</li></ul></li></ul><hr><h3 id="4-Kubernetes-核心资源"><a href="#4-Kubernetes-核心资源" class="headerlink" title="4. Kubernetes 核心资源"></a><strong>4. Kubernetes 核心资源</strong></h3><p>除了上述组件，Kubernetes 还包括一些重要的资源对象，用于管理集群和应用程序：</p><h4 id="4-1-Pod"><a href="#4-1-Pod" class="headerlink" title="4.1 Pod"></a>4.1 <strong>Pod</strong></h4><ul><li><strong>职责</strong>：Pod 是 Kubernetes 中最小的可调度单元，包含一个或多个容器，所有容器共享同一网络和存储。</li></ul><h4 id="4-2-Service"><a href="#4-2-Service" class="headerlink" title="4.2 Service"></a>4.2 <strong>Service</strong></h4><ul><li><strong>职责</strong>：Service 提供了一种稳定的访问方式，用于暴露一组 Pod。它抽象了 Pod 的 IP 地址，并可以提供负载均衡。</li></ul><h4 id="4-3-ReplicaSet"><a href="#4-3-ReplicaSet" class="headerlink" title="4.3 ReplicaSet"></a>4.3 <strong>ReplicaSet</strong></h4><ul><li><strong>职责</strong>：ReplicaSet 用于保证在任何时候都维持指定数量的 Pod 副本。它常常与 Deployment 一起使用。</li></ul><h4 id="4-4-Deployment"><a href="#4-4-Deployment" class="headerlink" title="4.4 Deployment"></a>4.4 <strong>Deployment</strong></h4><ul><li><strong>职责</strong>：Deployment 是一种高级控制器，负责管理 Pod 的副本、更新和滚动更新等操作。</li></ul><h4 id="4-5-StatefulSet"><a href="#4-5-StatefulSet" class="headerlink" title="4.5 StatefulSet"></a>4.5 <strong>StatefulSet</strong></h4><ul><li><strong>职责</strong>：StatefulSet 是 Kubernetes 中用于管理有状态应用的控制器，支持持久化存储和 Pod 的稳定标识符。</li></ul><h4 id="4-6-ConfigMap-和-Secret"><a href="#4-6-ConfigMap-和-Secret" class="headerlink" title="4.6 ConfigMap 和 Secret"></a>4.6 <strong>ConfigMap 和 Secret</strong></h4><ul><li><p>职责</p><p>：</p><ul><li><strong>ConfigMap</strong>：用于存储非敏感的配置数据。</li><li><strong>Secret</strong>：用于存储敏感数据，如密码、OAuth token 等。</li></ul></li></ul><h4 id="4-7-PersistentVolume-PV-和-PersistentVolumeClaim-PVC"><a href="#4-7-PersistentVolume-PV-和-PersistentVolumeClaim-PVC" class="headerlink" title="4.7 PersistentVolume (PV) 和 PersistentVolumeClaim (PVC)"></a>4.7 <strong>PersistentVolume (PV) 和 PersistentVolumeClaim (PVC)</strong></h4><ul><li><strong>职责</strong>：PV 和 PVC 用于管理 Kubernetes 中的持久存储。PV 是集群中的存储资源，PVC 是 Pod 请求存储资源的方式。</li></ul><hr><h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a><strong>总结</strong></h3><p>Kubernetes 是由多个组件组成的复杂系统，每个组件都在集群的不同部分起着关键作用。<strong>控制平面</strong> 主要负责管理集群的整体状态，如 API Server、调度器、控制器等；而 <strong>节点组件</strong> 则在每个工作节点上执行容器的管理和调度任务，确保容器能够按照预期正常运行。通过这些组件的协作，Kubernetes 提供了强大的容器管理能力，支持大规模的容器化应用部署和运维。</p><h3 id="创建-Pod的主要流程"><a href="#创建-Pod的主要流程" class="headerlink" title="创建 Pod的主要流程?"></a>创建 Pod的主要流程?</h3><h5 id="1-客户端提交pod的配置信息-可以是yaml-到kube-apiserver"><a href="#1-客户端提交pod的配置信息-可以是yaml-到kube-apiserver" class="headerlink" title="1.客户端提交pod的配置信息(可以是yaml)到kube-apiserver"></a>1.客户端提交pod的配置信息(可以是yaml)到kube-apiserver</h5><h5 id="2-apiserver收到信息后kube-controller-manager创建资源对象"><a href="#2-apiserver收到信息后kube-controller-manager创建资源对象" class="headerlink" title="2.apiserver收到信息后kube-controller-manager创建资源对象"></a>2.apiserver收到信息后kube-controller-manager创建资源对象</h5><h5 id="3-kubecontroller将yaml信息存到etcd"><a href="#3-kubecontroller将yaml信息存到etcd" class="headerlink" title="3.kubecontroller将yaml信息存到etcd"></a>3.kubecontroller将yaml信息存到etcd</h5><h5 id="4-kubescheduler调度pod到某个节点"><a href="#4-kubescheduler调度pod到某个节点" class="headerlink" title="4.kubescheduler调度pod到某个节点"></a>4.kubescheduler调度pod到某个节点</h5><h5 id="5-kubelet根据kube-scheduler发来的配置信息来运行pod，运行成功将pod信息返回给schedule，scheduler将信息返回并存储到到etcd"><a href="#5-kubelet根据kube-scheduler发来的配置信息来运行pod，运行成功将pod信息返回给schedule，scheduler将信息返回并存储到到etcd" class="headerlink" title="5.kubelet根据kube-scheduler发来的配置信息来运行pod，运行成功将pod信息返回给schedule，scheduler将信息返回并存储到到etcd"></a>5.kubelet根据kube-scheduler发来的配置信息来运行pod，运行成功将pod信息返回给schedule，scheduler将信息返回并存储到到etcd</h5><p>在 Kubernetes 中，<strong>Pod</strong> 是最小的部署单元，通常用于部署一个或多个容器。创建 Pod 的主要流程包括从用户发起请求到容器在节点上运行的各个步骤。以下是创建 Pod 的主要流程：</p><hr><h3 id="1-用户请求创建-Pod"><a href="#1-用户请求创建-Pod" class="headerlink" title="1. 用户请求创建 Pod"></a><strong>1. 用户请求创建 Pod</strong></h3><p>创建 Pod 的第一步是用户通过 <code>kubectl</code> 或其他 Kubernetes API 客户端发起创建请求。</p><ul><li><p><strong>通过 kubectl 命令</strong>： 用户可以通过 <code>kubectl run</code> 或 <code>kubectl apply -f pod.yaml</code> 命令创建 Pod。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">kubectl apply -f pod.yaml<br></code></pre></td></tr></table></figure><p>或者通过 <code>kubectl run</code> 创建 Pod（通常用于临时 Pod）：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">kubectl run mypod --image=nginx<br></code></pre></td></tr></table></figure></li><li><p><strong>通过 Kubernetes API</strong>： 用户也可以通过 Kubernetes API 发起创建请求（通常由控制器自动发起，如 Deployment 或 StatefulSet）。这时，Pod 描述会通过 API 发送到 API Server。</p></li></ul><hr><h3 id="2-API-Server-接收并验证请求"><a href="#2-API-Server-接收并验证请求" class="headerlink" title="2. API Server 接收并验证请求"></a><strong>2. API Server 接收并验证请求</strong></h3><p>Kubernetes 中的所有操作都会通过 <strong>API Server</strong> 来进行。无论是通过 <code>kubectl</code> 还是 API 发起的请求，都会到达 API Server。</p><ul><li><strong>验证请求</strong>：API Server 对 Pod 请求进行验证，确保所请求的 Pod 描述符合 Kubernetes 的规范。例如，确保资源请求正确，标签、容忍等参数合法。</li><li><strong>存储请求</strong>：验证通过后，API Server 会将 Pod 的描述（通常是 YAML 或 JSON 格式）存储到 Kubernetes 的 <strong>etcd</strong> 数据存储中，作为集群的当前状态。</li></ul><hr><h3 id="3-调度器选择节点（调度）"><a href="#3-调度器选择节点（调度）" class="headerlink" title="3. 调度器选择节点（调度）"></a><strong>3. 调度器选择节点（调度）</strong></h3><p>当 API Server 成功接收到 Pod 创建请求并将其存储在 etcd 中后，接下来的任务是将 Pod 调度到一个合适的节点上。这是由 <strong>调度器（kube-scheduler）</strong> 完成的。</p><h4 id="调度过程："><a href="#调度过程：" class="headerlink" title="调度过程："></a><strong>调度过程</strong>：</h4><ol><li><strong>过滤阶段</strong>：<ul><li>调度器会根据 Pod 的要求（如资源请求、亲和性&#x2F;反亲和性等），对所有节点进行过滤，排除不符合条件的节点。</li><li>例如，某些节点的资源可能不足，或者节点与 Pod 的亲和性要求不匹配。</li></ul></li><li><strong>优选阶段</strong>：<ul><li>对剩下的符合条件的节点，调度器会根据节点的负载、拓扑结构等因素打分，选择一个最合适的节点。</li><li>例如，调度器会考虑节点的 CPU 和内存使用情况，选择资源最空闲的节点。</li></ul></li><li><strong>绑定</strong>：<ul><li>一旦选定了节点，调度器会将 Pod 与节点绑定，向 API Server 通知这个 Pod 已经成功调度到指定节点。</li></ul></li></ol><hr><h3 id="4-节点上的-Kubelet-处理-Pod"><a href="#4-节点上的-Kubelet-处理-Pod" class="headerlink" title="4. 节点上的 Kubelet 处理 Pod"></a><strong>4. 节点上的 Kubelet 处理 Pod</strong></h3><p>调度器将 Pod 绑定到特定节点后，<strong>Kubelet</strong> 会在该节点上进行具体的操作。</p><ul><li><strong>拉取容器镜像</strong>：Kubelet 会根据 Pod 配置中的容器镜像信息，拉取相应的容器镜像到本地。如果镜像已经存在，则会跳过这一步。</li><li><strong>创建容器</strong>：Kubelet 使用容器运行时（如 Docker、containerd）在节点上启动容器。Pod 可以包含一个或多个容器，Kubelet 会逐个启动它们。</li><li><strong>设置网络和存储</strong>：<ul><li>Kubelet 为 Pod 配置网络，使得容器之间可以相互通信，并且可以访问外部网络。</li><li>如果 Pod 定义了持久存储（如卷），Kubelet 会挂载相关的存储资源。</li></ul></li></ul><hr><h3 id="5-Pod-运行与健康检查"><a href="#5-Pod-运行与健康检查" class="headerlink" title="5. Pod 运行与健康检查"></a><strong>5. Pod 运行与健康检查</strong></h3><p>当容器启动后，Kubelet 会持续监控 Pod 和容器的状态。Kubelet 会定期执行以下任务：</p><ul><li><strong>健康检查（Liveness &#x2F; Readiness）</strong>：<ul><li><strong>Liveness Probe</strong>：检查容器是否仍然存活。如果 Pod 容器挂掉，Kubelet 会重启容器。</li><li><strong>Readiness Probe</strong>：检查容器是否已准备好接受流量。如果容器不准备好，Kubelet 会将其从服务的负载均衡池中移除，直到它准备好。</li></ul></li><li><strong>资源监控</strong>： Kubelet 会报告节点和容器的资源使用情况（如 CPU、内存等）到 API Server。</li><li><strong>日志与事件</strong>： Kubelet 会将容器的日志和事件收集起来，供用户查看。</li></ul><hr><h3 id="6-控制器的反馈（如-Deployment、StatefulSet）"><a href="#6-控制器的反馈（如-Deployment、StatefulSet）" class="headerlink" title="6. 控制器的反馈（如 Deployment、StatefulSet）"></a><strong>6. 控制器的反馈（如 Deployment、StatefulSet）</strong></h3><p>如果 Pod 是由某个控制器（如 <strong>Deployment</strong>、<strong>StatefulSet</strong>、<strong>DaemonSet</strong>）管理的，那么这些控制器会继续监视 Pod 的状态，确保集群的实际状态符合所期望的状态。如果有 Pod 创建失败，控制器会重新调度 Pod。</p><ul><li>例如，<strong>Deployment</strong> 会确保它管理的 Pod 数量始终保持一致。如果某个 Pod 不健康或不可用，Deployment 会创建一个新的 Pod 来替代它。</li></ul><hr><h3 id="7-更新集群状态"><a href="#7-更新集群状态" class="headerlink" title="7. 更新集群状态"></a><strong>7. 更新集群状态</strong></h3><p>最终，Kubernetes 集群的状态（包括 Pod、节点、容器等信息）会更新到 <strong>etcd</strong> 中，以确保集群的最终状态一致。</p><h3 id="总结：创建-Pod-的主要流程"><a href="#总结：创建-Pod-的主要流程" class="headerlink" title="总结：创建 Pod 的主要流程"></a><strong>总结：创建 Pod 的主要流程</strong></h3><ol><li><strong>用户请求</strong>：用户通过 <code>kubectl</code> 或 API 创建 Pod。</li><li><strong>API Server 验证和存储</strong>：API Server 验证请求并将 Pod 存储到 etcd 中。</li><li><strong>调度器调度</strong>：调度器选择合适的节点，并将 Pod 绑定到该节点。</li><li><strong>Kubelet 启动容器</strong>：Kubelet 在节点上拉取容器镜像并启动容器。</li><li><strong>健康检查与资源监控</strong>：Kubelet 执行容器的健康检查，并持续报告资源使用情况。</li><li><strong>控制器管理</strong>：控制器监控 Pod 的状态，确保集群的期望状态与实际状态一致。</li><li><strong>集群状态更新</strong>：Pod 创建成功后，集群状态存储在 etcd 中。</li></ol><p>通过这些步骤，Kubernetes 确保 Pod 在集群中得到高效、可靠的管理和调度。</p><h3 id="k8s的调度机制"><a href="#k8s的调度机制" class="headerlink" title="k8s的调度机制"></a>k8s的调度机制</h3><h5 id="1-节点预选"><a href="#1-节点预选" class="headerlink" title="1.节点预选"></a>1.节点预选</h5><h5 id="2-节点打分排序"><a href="#2-节点打分排序" class="headerlink" title="2.节点打分排序"></a>2.节点打分排序</h5><h5 id="3-节点选优"><a href="#3-节点选优" class="headerlink" title="3.节点选优"></a>3.节点选优</h5><p><strong>Kubernetes (K8s)</strong> 的调度机制是其核心功能之一，用于决定容器应该在集群中的哪个节点上运行。K8s 调度器（Scheduler）负责根据节点的资源可用性、负载、拓扑、约束条件等因素，智能地将 Pod 分配到集群中的不同节点。调度是一个多阶段的过程，并且涉及到多个重要的调度策略和插件。</p><h3 id="Kubernetes-调度流程概述"><a href="#Kubernetes-调度流程概述" class="headerlink" title="Kubernetes 调度流程概述"></a><strong>Kubernetes 调度流程概述</strong></h3><ol><li><strong>Pod 创建</strong><ul><li>用户通过 <code>kubectl</code> 或其他 API 创建一个 Pod 请求，或者在某个 Deployment、StatefulSet 等控制器中声明了新的 Pod。</li><li>Kubernetes 控制平面通过 API Server 接收这个请求，并将 Pod 交给调度器来决定放置在哪个节点上。</li></ul></li><li><strong>调度器决定节点</strong><ul><li>调度器会检查集群中所有节点的资源使用情况，并根据一系列规则来选择最合适的节点。</li></ul></li><li><strong>Pod 绑定到节点</strong><ul><li>调度器选择合适的节点后，将 Pod 绑定到节点上，并将信息更新到 API Server。然后，节点上的 kubelet 负责拉取容器镜像、启动容器。</li></ul></li></ol><hr><h3 id="Kubernetes-调度的关键机制"><a href="#Kubernetes-调度的关键机制" class="headerlink" title="Kubernetes 调度的关键机制"></a><strong>Kubernetes 调度的关键机制</strong></h3><ol><li><strong>调度阶段</strong> Kubernetes 调度器通常按以下顺序工作：<ul><li><strong>过滤阶段 (Filter)</strong><ul><li>调度器通过节点的 <strong>节点选择器</strong>（NodeSelector）、资源限制、硬件&#x2F;网络要求等，过滤出不符合条件的节点。</li><li>过滤阶段的目标是排除掉那些不适合运行该 Pod 的节点，通常根据一些基本的约束条件，如 CPU、内存等资源。</li></ul></li><li><strong>优选阶段 (Score)</strong><ul><li>对于剩下的候选节点，调度器通过 <strong>调度策略</strong> 进行排序（比如节点的负载、拓扑结构等），选择一个最优的节点。</li><li>在优选阶段，调度器会基于 <code>Affinity</code>（亲和性）和 <code>Anti-Affinity</code>（反亲和性）规则、Pod 的资源请求与限制、Pod 的拓扑要求等因素对节点进行评分。</li></ul></li><li><strong>绑定阶段 (Bind)</strong><ul><li>调度器最终选择一个节点并将 Pod 绑定到该节点上。这时，节点上的 <code>kubelet</code> 会负责启动容器。</li></ul></li></ul></li></ol><hr><h3 id="Kubernetes-调度策略"><a href="#Kubernetes-调度策略" class="headerlink" title="Kubernetes 调度策略"></a><strong>Kubernetes 调度策略</strong></h3><ol><li><p><strong>资源请求和限制</strong></p><ul><li>每个 Pod 可以定义 <strong>资源请求</strong>（request）和 <strong>资源限制</strong>（limit）。资源请求是调度器决定在哪里调度 Pod 的基础，而资源限制则是容器运行时能使用的最大资源量。</li><li>调度器会根据节点的可用资源（CPU、内存等）和 Pod 的资源请求来选择节点。</li></ul></li><li><p><strong>Node Affinity（节点亲和性）</strong></p><ul><li>通过 <code>nodeAffinity</code> 规则，可以控制 Pod 只调度到符合特定标签的节点上。</li><li><strong>硬亲和性</strong>（必需条件）和 <strong>软亲和性</strong>（优先条件）都可以通过 <code>affinity</code> 配置来指定。</li></ul><p>示例：让 Pod 只调度到具有特定标签的节点上：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs yaml"><span class="hljs-attr">affinity:</span><br>  <span class="hljs-attr">nodeAffinity:</span><br>    <span class="hljs-attr">requiredDuringSchedulingIgnoredDuringExecution:</span><br>      <span class="hljs-attr">nodeSelectorTerms:</span><br>      <span class="hljs-bullet">-</span> <span class="hljs-attr">matchExpressions:</span><br>          <span class="hljs-bullet">-</span> <span class="hljs-attr">key:</span> <span class="hljs-string">&quot;disktype&quot;</span><br>            <span class="hljs-attr">operator:</span> <span class="hljs-string">In</span><br>            <span class="hljs-attr">values:</span><br>            <span class="hljs-bullet">-</span> <span class="hljs-string">ssd</span><br></code></pre></td></tr></table></figure></li><li><p><strong>Pod Affinity 和 Anti-Affinity（Pod亲和性和反亲和性）</strong></p><ul><li><strong>Pod Affinity</strong>：控制 Pod 应该尽可能地调度到与其他 Pod 相同或相邻的节点上。</li><li><strong>Pod Anti-Affinity</strong>：控制 Pod 不应调度到与特定 Pod 相同或相邻的节点上。</li></ul><p>示例：让 Pod 调度到与另一个特定 Pod 在同一个节点上：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs yaml"><span class="hljs-attr">affinity:</span><br>  <span class="hljs-attr">podAffinity:</span><br>    <span class="hljs-attr">requiredDuringSchedulingIgnoredDuringExecution:</span><br>      <span class="hljs-attr">labelSelector:</span><br>        <span class="hljs-attr">matchLabels:</span><br>          <span class="hljs-attr">app:</span> <span class="hljs-string">frontend</span><br>      <span class="hljs-attr">topologyKey:</span> <span class="hljs-string">&quot;kubernetes.io/hostname&quot;</span><br></code></pre></td></tr></table></figure></li><li><p><strong>Taints 和 Tolerations（污点和容忍）</strong></p><ul><li><strong>Taints</strong>：允许节点标记为“污点”，即节点会拒绝调度任何没有适当容忍的 Pod。</li><li><strong>Tolerations</strong>：Pod 可以设置容忍，允许其调度到有污点的节点上。</li></ul><p>示例：一个节点加上污点，只有具有特定容忍的 Pod 才能调度到该节点。</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs yaml"><span class="hljs-string">kubectl</span> <span class="hljs-string">taint</span> <span class="hljs-string">nodes</span> <span class="hljs-string">node1</span> <span class="hljs-string">key=value:NoSchedule</span><br></code></pre></td></tr></table></figure><p>在 Pod 配置中添加容忍：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs yaml"><span class="hljs-attr">tolerations:</span><br><span class="hljs-bullet">-</span> <span class="hljs-attr">key:</span> <span class="hljs-string">&quot;key&quot;</span><br>  <span class="hljs-attr">operator:</span> <span class="hljs-string">&quot;Equal&quot;</span><br>  <span class="hljs-attr">value:</span> <span class="hljs-string">&quot;value&quot;</span><br>  <span class="hljs-attr">effect:</span> <span class="hljs-string">&quot;NoSchedule&quot;</span><br></code></pre></td></tr></table></figure></li><li><p><strong>资源负载均衡</strong></p><ul><li>在选择节点时，调度器也会考虑节点的负载，确保集群资源的合理利用。调度器会避免某些节点资源过载，而其他节点资源闲置。</li><li>调度器会根据节点的 CPU、内存等资源的剩余情况来选择最合适的节点。</li></ul></li></ol><hr><h3 id="Kubernetes-默认调度器和自定义调度器"><a href="#Kubernetes-默认调度器和自定义调度器" class="headerlink" title="Kubernetes 默认调度器和自定义调度器"></a><strong>Kubernetes 默认调度器和自定义调度器</strong></h3><ol><li><strong>默认调度器</strong>：Kubernetes 使用 <code>kube-scheduler</code> 作为默认调度器，处理所有 Pod 的调度请求。</li><li><strong>自定义调度器</strong>：如果有特殊的调度需求，可以使用自定义调度器。可以在集群中运行多个调度器，指定特定的调度策略和工作负载。</li></ol><hr><h3 id="Kubernetes-调度器插件"><a href="#Kubernetes-调度器插件" class="headerlink" title="Kubernetes 调度器插件"></a><strong>Kubernetes 调度器插件</strong></h3><p>Kubernetes 调度器支持插件架构，允许在调度过程中使用不同的策略和插件来定制行为。常见的插件包括：</p><ul><li><strong>预调度插件</strong>（如 <code>NodeAffinity</code>、<code>TaintToleration</code> 等）</li><li><strong>调度策略插件</strong>（如 <code>PodAffinity</code>、<code>ResourceLimits</code> 等）</li><li><strong>后调度插件</strong>（如调度后监听 Pod 状态的变化）</li></ul><p>这些插件使得 Kubernetes 调度器能够灵活地满足各种业务场景的需求。</p><hr><h3 id="总结-1"><a href="#总结-1" class="headerlink" title="总结"></a><strong>总结</strong></h3><p>Kubernetes 调度器负责根据节点的资源、拓扑、亲和性和反亲和性、污点和容忍等规则，智能地将 Pod 分配到最合适的节点上。调度过程包括 <strong>过滤</strong>、<strong>优选</strong> 和 <strong>绑定</strong> 阶段。通过使用 <strong>Node Affinity</strong>、<strong>Pod Affinity</strong>、<strong>Taints 和 Tolerations</strong> 等策略，可以精细控制 Pod 的调度行为，确保集群资源的高效利用和满足业务需求。</p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>2024-8-1 linux面试题</title>
    <link href="/2024/12/05/2024-8-1%20linux%E8%BF%90%E7%BB%B4%E5%B7%A5%E7%A8%8B%E5%B8%8850%E4%B8%AA%E5%B8%B8%E8%A7%81%E9%9D%A2%E8%AF%95%E9%A2%98/"/>
    <url>/2024/12/05/2024-8-1%20linux%E8%BF%90%E7%BB%B4%E5%B7%A5%E7%A8%8B%E5%B8%8850%E4%B8%AA%E5%B8%B8%E8%A7%81%E9%9D%A2%E8%AF%95%E9%A2%98/</url>
    
    <content type="html"><![CDATA[<p><strong>1、请简述OSI七层网络模型有哪些层及各自的含义?</strong></p><ul><li><strong>物理层：底层数据传输，比如网线、网卡标准</strong></li><li><strong>数据链路层：定义数据的基本格式，如何传输，如何标识。比如网卡MAC地址</strong></li><li><strong>网络层：定义IP编码，定义路由功能，比如不同设备的数据转发</strong></li><li><strong>传输层：端到端传输数据的基本功能，比如TCP、UDP</strong></li><li><strong>会话层：控制应用程序之间会话能力，比如不同软件数据分发给不停软件</strong></li><li><strong>表示层：数据格式标识，基本压缩加密功能。</strong></li><li><strong>应用层：各种应用软件，包括 Web 应用。</strong></li></ul><h3 id="2、在Linux的LVM分区格式下，请简述给根分区磁盘扩容的步骤"><a href="#2、在Linux的LVM分区格式下，请简述给根分区磁盘扩容的步骤" class="headerlink" title="2、在Linux的LVM分区格式下，请简述给根分区磁盘扩容的步骤?"></a><strong>2、在Linux的LVM分区格式下，请简述给根分区磁盘扩容的步骤?</strong></h3><p><strong>这个分3种</strong></p><p><strong>第一种方法:</strong></p><figure class="highlight awk"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs awk">growpart <span class="hljs-regexp">/dev/</span>vda <span class="hljs-number">1</span><br>resize2fs <span class="hljs-regexp">/dev/</span>vda1    <br></code></pre></td></tr></table></figure><p><strong>第二种方法:</strong></p><figure class="highlight awk"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs awk">partpeobe <span class="hljs-regexp">/dev/</span>sda<br>resize2fs <span class="hljs-regexp">/dev/</span>vda1    <br></code></pre></td></tr></table></figure><p><strong>第三种方法:</strong></p><figure class="highlight awk"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs awk">fdisk <span class="hljs-regexp">/dev/</span>sdb   <span class="hljs-comment"># n p 1 1 回车 回车 t 8e w</span><br>pvcreate <span class="hljs-regexp">/dev/</span>sdb1<br>vgextend datavg <span class="hljs-regexp">/dev/</span>sdb1<br>lvextend -r -L +<span class="hljs-number">100</span>%free <span class="hljs-regexp">/dev/m</span>apper/datavg-lv01<br></code></pre></td></tr></table></figure><h3 id="3、讲述一下Tomcat8005、8009、8080三个端口的含义？"><a href="#3、讲述一下Tomcat8005、8009、8080三个端口的含义？" class="headerlink" title="3、讲述一下Tomcat8005、8009、8080三个端口的含义？"></a><strong>3、讲述一下Tomcat8005、8009、8080三个端口的含义？</strong></h3><ul><li><strong>8005 关闭时使用</strong></li><li><strong>8009为AJP端口，即容器使用，如Apache能通过AJP协议访问Tomcat的8009端口来实现功能</strong></li><li><strong>8080 一般应用使用</strong></li></ul><h3 id="4、简述DNS进行域名解析的过程？"><a href="#4、简述DNS进行域名解析的过程？" class="headerlink" title="4、简述DNS进行域名解析的过程？"></a><strong>4、简述DNS进行域名解析的过程？</strong></h3><p><strong>迭代查询（返回最优结果）、递归查询（本地找DNS）用户要访问 <a href="http://www.baidu.com,会先找本机的host文件,再找本地设置的dns服务器,如果也没有找到,就去网络中找根服务器,根服务器反馈结果,说只能提供一级域名服务器.cn,就去找一级域名服务器,一级域名服务器说只能提供二级域名服务器.com.cn,就去找二级域名服务器,二级域服务器只能提供三级域名服务器.baidu.com.cn,就去找三级域名服务器,三级域名服务器正好有这个网站www.baidu.com,然后发给请求的服务器,保存一份之后,再发给客户端./">www.baidu.com，会先找本机的host文件，再找本地设置的DNS服务器，如果也没有找到，就去网络中找根服务器，根服务器反馈结果，说只能提供一级域名服务器.cn，就去找一级域名服务器，一级域名服务器说只能提供二级域名服务器.com.cn,就去找二级域名服务器，二级域服务器只能提供三级域名服务器.baidu.com.cn，就去找三级域名服务器，三级域名服务器正好有这个网站www.baidu.com，然后发给请求的服务器，保存一份之后，再发给客户端。</a></strong></p><h3 id="5、讲一下Keepalived的工作原理？"><a href="#5、讲一下Keepalived的工作原理？" class="headerlink" title="5、讲一下Keepalived的工作原理？"></a><strong>5、讲一下Keepalived的工作原理？</strong></h3><p><strong>在一个虚拟路由器中，只有作为MASTER的VRRP(虚拟路由冗余协议)路由器会一直发送VRRP通告信息，BACKUP不会抢占MASTER，除非它的优先级更高。当MASTER不可用时(BACKUP收不到通告信息)多台BACKUP中优先级最高的这台会被抢占为MASTER。这种抢占是非常快速的(&lt;1s)，以保证服务的连续性由于安全性考虑，VRRP包使用了加密协议进行加密。BACKUP不会发送通告信息，只会接收通告信息。</strong></p><h3 id="6、LVS、Nginx、HAproxy有什么区别？工作中你怎么选择？"><a href="#6、LVS、Nginx、HAproxy有什么区别？工作中你怎么选择？" class="headerlink" title="6、LVS、Nginx、HAproxy有什么区别？工作中你怎么选择？"></a><strong>6、LVS、Nginx、HAproxy有什么区别？工作中你怎么选择？</strong></h3><p><strong>LVS：</strong></p><ul><li><strong>抗负载能力强、工作在第4层仅作分发之用，没有流量的产生，这个特点也决定了它在负载均衡软件里的性能最强的；无流量，同时保证了均衡器IO的性能不会受到大流量的影响；</strong></li><li><strong>工作稳定，自身有完整的双机热备方案，如LVS+Keepalived和LVS+Heartbeat；</strong></li><li><strong>应用范围比较广，可以对所有应用做负载均衡；</strong></li><li><strong>配置简单，因为没有可太多配置的东西，所以并不需要太多接触，大大减少了人为出错的几率；</strong></li></ul><p><strong>LVS的缺点：</strong></p><ul><li><strong>软件本身不支持正则处理，不能做动静分离，这就凸显了Nginx&#x2F;HAProxy+Keepalived的优势。</strong></li><li><strong>如果网站应用比较庞大，LVS&#x2F;DR+Keepalived就比较复杂了，特别是后面有Windows Server应用的机器，实施及配置还有维护过程就比较麻烦，相对而言，Nginx&#x2F;HAProxy+Keepalived就简单多了。</strong></li></ul><p><strong>Nginx：</strong></p><ul><li><strong>工作在第7层，应用层，可以针对http应用做一些分流的策略。比如针对域名、目录结构。它的正则比HAProxy更为强大和灵活；</strong></li><li><strong>Nginx对网络的依赖非常小，理论上能ping通就就能进行负载功能</strong></li><li><strong>Nginx安装和配置简单</strong></li><li><strong>可以承担高的负载压力且稳定，一般能支撑超过几万次的并发量；</strong></li><li><strong>Nginx不仅仅是一款优秀的负载均衡器&#x2F;反向代理软件，它同时也是功能强大的Web应用服务器。Nginx在处理静态页面、特别是抗高并发方面相对apache有优势；</strong></li><li><strong>Nginx作为Web反向代理加速缓存越来越成熟，速度比传统的Squid服务器更快</strong></li></ul><p><strong>Nginx的缺点：</strong></p><ul><li><strong>Nginx不支持url来检测。</strong></li><li><strong>Nginx仅能支持http、https和Email协议</strong></li><li><strong>Nginx的Session的保持，Cookie的引导能力相对欠缺。</strong></li></ul><p><strong>HAProxy：</strong></p><ul><li><strong>HAProxy是支持虚拟主机的，可以工作在4、7层(支持多网段)；</strong></li><li><strong>能够补充Nginx的一些缺点比如Session的保持，Cookie的引导等工作；</strong></li><li><strong>支持url检测后端的服务器；</strong></li><li><strong>它跟LVS一样，本身仅仅就只是一款负载均衡软件；单纯从效率上来讲HAProxy更会比Nginx有更出色的负载均衡速度，在并发处理上也是优于Nginx的；</strong></li><li><strong>HAProxy可以对Mysql读进行负载均衡，对后端的MySQL节点进行检测和负载均衡，不过在后端的MySQL slaves数量超过10台时性能不如LVS；</strong></li><li><strong>HAProxy的算法较多，达到8种；</strong></li></ul><p><strong>工作选择：</strong></p><p><strong>HAproxy和Nginx由于可以做七层的转发，所以URL和目录的转发都可以做在很大并发量的时候我们就要选择LVS，像中小型公司的话并发量没那么大选择HAproxy或者Nginx足已，由于HAproxy由是专业的代理服务器配置简单，所以中小型企业推荐使用HAproxy。</strong></p><h3 id="7、docker的工作原理是什么，讲一下"><a href="#7、docker的工作原理是什么，讲一下" class="headerlink" title="7、docker的工作原理是什么，讲一下"></a><strong>7、docker的工作原理是什么，讲一下</strong></h3><p><strong>docker是一个Client-Server结构的系统，docker守护进程运行在宿主机上，守护进程从客户端接受命令并管理运行在主机上的容器，容器是一个运行时环境，这就是我们说的集装箱。</strong></p><h3 id="8、docker的组成包含哪几大部分"><a href="#8、docker的组成包含哪几大部分" class="headerlink" title="8、docker的组成包含哪几大部分"></a><strong>8、docker的组成包含哪几大部分</strong></h3><p><strong>一个完整的docker有以下几个部分组成：</strong></p><ul><li><strong>docker client，客户端，为用户提供一系列可执行命令，用户用这些命令实现跟 docker daemon 交互；</strong></li><li><strong>docker daemon，守护进程，一般在宿主主机后台运行，等待接收来自客户端的请求消息；</strong></li><li><strong>docker image，镜像，镜像run之后就生成为docker容器；</strong></li><li><strong>docker container，容器，一个系统级别的服务，拥有自己的ip和系统目录结构；运行容器前需要本地存在对应的镜像，如果本地不存在该镜像则就去镜像仓库下载。</strong></li></ul><p><strong>docker 使用客户端-服务器 (C&#x2F;S) 架构模式，使用远程api来管理和创建docker容器。docker 容器通过 docker 镜像来创建。容器与镜像的关系类似于面向对象编程中的对象与类。</strong></p><h3 id="9、docker与传统虚拟机的区别什么？"><a href="#9、docker与传统虚拟机的区别什么？" class="headerlink" title="9、docker与传统虚拟机的区别什么？"></a><strong>9、docker与传统虚拟机的区别什么？</strong></h3><ul><li><strong>传统虚拟机是需要安装整个操作系统的，然后再在上面安装业务应用，启动应用，通常需要几分钟去启动应用，而docker是直接使用镜像来运行业务容器的，其容器启动属于秒级别；</strong></li><li><strong>Docker需要的资源更少，Docker在操作系统级别进行虚拟化，Docker容器和内核交互，几乎没有性能损耗，而虚拟机运行着整个操作系统，占用物理机的资源就比较多;</strong></li><li><strong>Docker更轻量，Docker的架构可以共用一个内核与共享应用程序库，所占内存极小;同样的硬件环境，Docker运行的镜像数远多于虚拟机数量，对系统的利用率非常高;</strong></li><li><strong>与虚拟机相比，Docker隔离性更弱，Docker属于进程之间的隔离，虚拟机可实现系统级别隔离;</strong></li><li><strong>Docker的安全性也更弱，Docker的租户root和宿主机root相同，一旦容器内的用户从普通用户权限提升为root权限，它就直接具备了宿主机的root权限，进而可进行无限制的操作。虚拟机租户root权限和宿主机的root虚拟机权限是分离的，并且虚拟机利用如Intel的VT-d和VT-x的ring-1硬件隔离技术，这种技术可以防止虚拟机突破和彼此交互，而容器至今还没有任何形式的硬件隔离;</strong></li><li><strong>Docker的集中化管理工具还不算成熟，各种虚拟化技术都有成熟的管理工具，比如：VMware vCenter提供完备的虚拟机管理能力;</strong></li><li><strong>Docker对业务的高可用支持是通过快速重新部署实现的，虚拟化具备负载均衡，高可用、容错、迁移和数据保护等经过生产实践检验的成熟保障机制，Vmware可承诺虚拟机99.999%高可用，保证业务连续性;</strong></li><li><strong>虚拟化创建是分钟级别的，Docker容器创建是秒级别的，Docker的快速迭代性，决定了无论是开发、测试、部署都可以节省大量时间;</strong></li><li><strong>虚拟机可以通过镜像实现环境交付的一致性，但镜像分发无法体系化，Docker在Dockerfile中记录了容器构建过程，可在集群中实现快速分发和快速部署。from wljslmz</strong></li></ul><h3 id="10、docker技术的三大核心概念是什么？"><a href="#10、docker技术的三大核心概念是什么？" class="headerlink" title="10、docker技术的三大核心概念是什么？"></a><strong>10、docker技术的三大核心概念是什么？</strong></h3><ul><li><strong>镜像：镜像是一种轻量级、可执行的独立软件包，它包含运行某个软件所需的所有内容，我们把应用程序和配置依赖打包好形成一个可交付的运行环境(包括代码、运行时需要的库、环境变量和配置文件等)，这个打包好的运行环境就是image镜像文件。</strong></li><li><strong>容器：容器是基于镜像创建的，是镜像运行起来之后的一个实例，容器才是真正运行业务程序的地方。如果把镜像比作程序里面的类，那么容器就是对象。</strong></li><li><strong>镜像仓库：存放镜像的地方，研发工程师打包好镜像之后需要把镜像上传到镜像仓库中去，然后就可以运行有仓库权限的人拉取镜像来运行容器了。</strong></li></ul><h3 id="11、centos镜像几个G，但是docker-centos镜像才几百兆，这是为什么？"><a href="#11、centos镜像几个G，但是docker-centos镜像才几百兆，这是为什么？" class="headerlink" title="11、centos镜像几个G，但是docker centos镜像才几百兆，这是为什么？"></a><strong>11、centos镜像几个G，但是docker centos镜像才几百兆，这是为什么？</strong></h3><p><strong>一个完整的Linux操作系统包含Linux内核和rootfs根文件系统，即我们熟悉的&#x2F;dev、&#x2F;proc&#x2F;、&#x2F;bin等目录。我们平时看到的centOS除了rootfs，还会选装很多软件，服务，图形桌面等，所以centOS镜像有好几个G也不足为奇。</strong></p><p><strong>而对于容器镜像而言，所有容器都是共享宿主机的Linux 内核的，而对于docker镜像而言，docker镜像只需要提供一个很小的rootfs即可，只需要包含最基本的命令，工具，程序库即可，所有docker镜像才会这么小。</strong></p><h3 id="12、讲一下镜像的分层结构以及为什么要使用镜像的分层结构？"><a href="#12、讲一下镜像的分层结构以及为什么要使用镜像的分层结构？" class="headerlink" title="12、讲一下镜像的分层结构以及为什么要使用镜像的分层结构？"></a><strong>12、讲一下镜像的分层结构以及为什么要使用镜像的分层结构？</strong></h3><p><strong>一个新的镜像其实是从 base 镜像一层一层叠加生成的。每安装一个软件，dockerfile中使用RUM命令，就会在现有镜像的基础上增加一层，这样一层一层的叠加最后构成整个镜像。所以我们docker pull拉取一个镜像的时候会看到docker是一层层拉去的。</strong></p><p><strong>分层机构最大的一个好处就是 ：共享资源。比如：有多个镜像都从相同的 base 镜像构建而来，那么 Docker Host 只需在磁盘上保存一份 base 镜像；同时内存中也只需加载一份 base 镜像，就可以为所有容器服务了。而且镜像的每一层都可以被共享。</strong></p><h3 id="13、讲一下容器的copy-on-write特性，修改容器里面的内容会修改镜像吗？"><a href="#13、讲一下容器的copy-on-write特性，修改容器里面的内容会修改镜像吗？" class="headerlink" title="13、讲一下容器的copy-on-write特性，修改容器里面的内容会修改镜像吗？"></a><strong>13、讲一下容器的copy-on-write特性，修改容器里面的内容会修改镜像吗？</strong></h3><p><strong>我们知道，镜像是分层的，镜像的每一层都可以被共享，同时，镜像是只读的。当一个容器启动时，一个新的可写层被加载到镜像的顶部，这一层通常被称作“容器层”，“容器层”之下的都叫“镜像层”。</strong></p><p><strong>所有对容器的改动 - 无论添加、删除、还是修改文件，都只会发生在容器层中，因为只有容器层是可写的，容器层下面的所有镜像层都是只读的。镜像层数量可能会很多，所有镜像层会联合在一起组成一个统一的文件系统。如果不同层中有一个相同路径的文件，比如 &#x2F;a，上层的 &#x2F;a 会覆盖下层的 &#x2F;a，也就是说用户只能访问到上层中的文件 &#x2F;a。在容器层中，用户看到的是一个叠加之后的文件系统。</strong></p><ul><li><strong>添加文件：在容器中创建文件时，新文件被添加到容器层中。</strong></li><li><strong>读取文件：在容器中读取某个文件时，Docker 会从上往下依次在各镜像层中查找此文件。一旦找到，立即将其复制到容器层，然后打开并读入内存。</strong></li><li><strong>修改文件：在容器中修改已存在的文件时，Docker 会从上往下依次在各镜像层中查找此文件。一旦找到，立即将其复制到容器层，然后修改之。</strong></li><li><strong><a href="http://mp.weixin.qq.com/s?__biz=MzIyMzIwNzAxMQ==&mid=2649443640&idx=1&sn=fbb5b3d069dbda4539a5fa8ac6be73a0&chksm=f03e2634c749af22340637e6aa81d4a8f5973b528833abfcc1540a7259f2e898df823550b16d&scene=21#wechat_redirect">删除文件</a>：在容器中删除文件时，Docker 也是从上往下依次在镜像层中查找此文件。找到后，会在容器层中记录下此删除操作。</strong></li></ul><p><strong>只有当需要修改时才复制一份数据，这种特性被称作 Copy-on-Write。可见，容器层保存的是镜像变化的部分，不会对镜像本身进行任何修改。</strong></p><h3 id="14、简单描述一下Dockerfile的整个构建镜像过程"><a href="#14、简单描述一下Dockerfile的整个构建镜像过程" class="headerlink" title="14、简单描述一下Dockerfile的整个构建镜像过程"></a><strong>14、简单描述一下Dockerfile的整个构建镜像过程</strong></h3><ul><li><strong>首先，创建一个目录用于存放应用程序以及构建过程中使用到的各个文件等；</strong></li><li><strong>然后，在这个目录下创建一个Dockerfile文件，一般建议Dockerfile的文件名就是Dockerfile；</strong></li><li><strong>编写Dockerfile文件，编写指令，如，使用FORM指令指定基础镜像，COPY指令复制文件，RUN指令指定要运行的命令，ENV设置环境变量，EXPOSE指定容器要暴露的端口，WORKDIR设置当前工作目录，CMD容器启动时运行命令，等等指令构建镜像；</strong></li><li><strong>Dockerfile编写完成就可以构建镜像了，使用docker build -t 镜像名:tag . 命令来构建镜像，最后一个点是表示当前目录，docker会默认寻找当前目录下的Dockerfile文件来构建镜像，如果不使用默认，可以使用-f参数来指定dockerfile文件，如：docker build -t 镜像名:tag -f &#x2F;xx&#x2F;xxx&#x2F;Dockerfile ；</strong></li><li><strong>使用docker build命令构建之后，docker就会将当前目录下所有的文件发送给docker daemon，顺序执行Dockerfile文件里的指令，在这过程中会生成临时容器，在临时容器里面安装RUN指定的命令，安装成功后，docker底层会使用类似于docker commit命令来将容器保存为镜像，然后删除临时容器，以此类推，一层层的构建镜像，运行临时容器安装软件，直到最后的镜像构建成功。</strong></li></ul><h3 id="15、Dockerfile构建镜像出现异常，如何排查？"><a href="#15、Dockerfile构建镜像出现异常，如何排查？" class="headerlink" title="15、Dockerfile构建镜像出现异常，如何排查？"></a><strong>15、Dockerfile构建镜像出现异常，如何排查？</strong></h3><p><strong>首先，Dockerfile是一层一层的构建镜像，期间会产生一个或多个临时容器，构建过程中其实就是在临时容器里面安装应用，如果因为临时容器安装应用出现异常导致镜像构建失败，这时容器虽然被清理掉了，但是期间构建的中间镜像还在，那么我们可以根据异常时上一层已经构建好的临时镜像，将临时镜像运行为容器，然后在容器里面运行安装命令来定位具体的异常。</strong></p><h3 id="16、Dockerfile的基本指令有哪些？"><a href="#16、Dockerfile的基本指令有哪些？" class="headerlink" title="16、Dockerfile的基本指令有哪些？"></a><strong>16、Dockerfile的基本指令有哪些？</strong></h3><ul><li><strong>FROM 指定基础镜像（必须为第一个指令，因为需要指定使用哪个基础镜像来构建镜像）；</strong></li><li><strong>MAINTAINER 设置镜像作者相关信息，如作者名字，日期，邮件，联系方式等；</strong></li><li><strong>COPY 复制文件到镜像；</strong></li><li><strong>ADD 复制文件到镜像（ADD与COPY的区别在于，ADD会自动解压tar、zip、tgz、xz等归档文件，而COPY不会，同时ADD指令还可以接一个url下载文件地址，一般建议使用COPY复制文件即可，文件在宿主机上是什么样子复制到镜像里面就是什么样子这样比较好）；</strong></li><li><strong>ENV 设置环境变量；</strong></li><li><strong>EXPOSE 暴露容器进程的端口，仅仅是提示别人容器使用的哪个端口，没有过多作用；</strong></li><li><strong>VOLUME 数据卷持久化，挂载一个目录；</strong></li><li><strong>WORKDIR 设置工作目录，如果目录不在，则会自动创建目录；</strong></li><li><strong>RUN 在容器中运行命令，RUN指令会创建新的镜像层，RUN指令经常被用于安装软件包；</strong></li><li><strong>CMD 指定容器启动时默认运行哪些命令，如果有多个CMD，则只有最后一个生效，另外，CMD指令可以被docker run之后的参数替换；</strong></li><li><strong>ENTRYOINT 指定容器启动时运行哪些命令，如果有多个ENTRYOINT，则只有最后一个生效，另外，如果Dockerfile中同时存在CMD和ENTRYOINT，那么CMD或docker run之后的参数将被当做参数传递给ENTRYOINT；</strong></li></ul><h3 id="17、如何进入容器？使用哪个命令"><a href="#17、如何进入容器？使用哪个命令" class="headerlink" title="17、如何进入容器？使用哪个命令"></a><strong>17、如何进入容器？使用哪个命令</strong></h3><p><strong>进入容器有两种方法：<code>docker attach</code>、<code>docker exec</code>。</strong></p><h3 id="18、什么是k8s？说出你的理解"><a href="#18、什么是k8s？说出你的理解" class="headerlink" title="18、什么是k8s？说出你的理解"></a><strong>18、什么是k8s？说出你的理解</strong></h3><p><strong>K8s是kubernetes的简称，其本质是一个开源的容器编排系统，主要用于管理容器化的应用，其目标是让部署容器化的应用简单并且高效（powerful）,Kubernetes提供了应用部署，规划，更新，维护的一种机制。</strong></p><p><strong>说简单点：k8s就是一个编排容器的系统，一个可以管理容器应用全生命周期的工具，从创建应用，应用的部署，应用提供服务，扩容缩容应用，应用更新，都非常的方便，而且还可以做到故障自愈，所以，k8s是一个非常强大的容器编排系统。</strong></p><h3 id="19、k8s的组件有哪些，作用分别是什么？"><a href="#19、k8s的组件有哪些，作用分别是什么？" class="headerlink" title="19、k8s的组件有哪些，作用分别是什么？"></a><strong>19、k8s的组件有哪些，作用分别是什么？</strong></h3><p><strong>k8s主要由master节点和node节点构成。master节点负责管理集群，node节点是容器应用真正运行的地方。</strong></p><ul><li><strong>master节点包含的组件有：kube-api-server、kube-controller-manager、kube-scheduler、etcd。</strong></li><li><strong>node节点包含的组件有：kubelet、kube-proxy、container-runtime。</strong></li></ul><p><strong>kube-api-server：以下简称api-server，api-server是k8s最重要的核心组件之一，它是k8s集群管理的统一访问入口，提供了RESTful API接口, 实现了认证、授权和准入控制等安全功能；api-server还是其他组件之间的数据交互和通信的枢纽，其他组件彼此之间并不会直接通信，其他组件对资源对象的增、删、改、查和监听操作都是交由api-server处理后，api-server再提交给etcd数据库做持久化存储，只有api-server才能直接操作etcd数据库，其他组件都不能直接操作etcd数据库，其他组件都是通过api-server间接的读取，写入数据到etcd。</strong></p><p><strong>kube-controller-manager：以下简称controller-manager，controller-manager是k8s中各种控制器的的管理者，是k8s集群内部的管理控制中心，也是k8s自动化功能的核心；controller-manager内部包含replication controller、node controller、deployment controller、endpoint controller等各种资源对象的控制器，每种控制器都负责一种特定资源的控制流程，而controller-manager正是这些controller的核心管理者。</strong></p><p><strong>kube-scheduler：以下简称scheduler，scheduler负责集群资源调度，其作用是将待调度的pod通过一系列复杂的调度算法计算出最合适的node节点，然后将pod绑定到目标节点上。shceduler会根据pod的信息（关注微信公众号：网络技术联盟站），全部节点信息列表，过滤掉不符合要求的节点，过滤出一批候选节点，然后给候选节点打分，选分最高的就是最佳节点，scheduler就会把目标pod安置到该节点。</strong></p><p><strong>Etcd：etcd是一个分布式的键值对存储数据库，主要是用于保存k8s集群状态数据，比如，pod，service等资源对象的信息；etcd可以是单个也可以有多个，多个就是etcd数据库集群，etcd通常部署奇数个实例，在大规模集群中，etcd有5个或7个节点就足够了；另外说明一点，etcd本质上可以不与master节点部署在一起，只要master节点能通过网络连接etcd数据库即可。</strong></p><p><strong>kubelet：每个node节点上都有一个kubelet服务进程，kubelet作为连接master和各node之间的桥梁，负责维护pod和容器的生命周期，当监听到master下发到本节点的任务时，比如创建、更新、终止pod等任务，kubelet 即通过控制docker来创建、更新、销毁容器；每个kubelet进程都会在api-server上注册本节点自身的信息，用于定期向master汇报本节点资源的使用情况。</strong></p><p><strong>kube-proxy：kube-proxy运行在node节点上，在Node节点上实现Pod网络代理，维护网络规则和四层负载均衡工作，kube-proxy会监听api-server中从而获取service和endpoint的变化情况，创建并维护路由规则以提供服务IP和负载均衡功能。简单理解此进程是Service的透明代理兼负载均衡器，其核心功能是将到某个Service的访问请求转发到后端的多个Pod实例上。</strong></p><p><strong>container-runtime：容器运行时环境，即运行容器所需要的一系列程序，目前k8s支持的容器运行时有很多，如docker、rkt或其他，比较受欢迎的是docker，但是新版的k8s已经宣布弃用docker。</strong></p><h3 id="20、kubelet的功能、作用是什么？（重点，经常会问）"><a href="#20、kubelet的功能、作用是什么？（重点，经常会问）" class="headerlink" title="20、kubelet的功能、作用是什么？（重点，经常会问）"></a><strong>20、kubelet的功能、作用是什么？（重点，经常会问）</strong></h3><p><strong>kubelet部署在每个node节点上的，它主要有4个功能：</strong></p><ul><li><strong>节点管理。kubelet启动时会向api-server进行注册，然后会定时的向api-server汇报本节点信息状态，资源使用状态等，这样master就能够知道node节点的资源剩余，节点是否失联等等相关的信息了。master知道了整个集群所有节点的资源情况，这对于 pod 的调度和正常运行至关重要。</strong></li><li><strong>pod管理。kubelet负责维护node节点上pod的生命周期，当kubelet监听到master的下发到自己节点的任务时，比如要创建、更新、删除一个pod，kubelet 就会通过CRI（容器运行时接口）插件来调用不同的容器运行时来创建、更新、删除容器；常见的容器运行时有docker、containerd、rkt等等这些容器运行时，我们最熟悉的就是docker了，但在新版本的k8s已经弃用docker了，k8s1.24版本中已经使用containerd作为容器运行时了。</strong></li><li><strong>容器健康检查。pod中可以定义启动探针、存活探针、就绪探针等3种，我们最常用的就是存活探针、就绪探针，kubelet 会定期调用容器中的探针来检测容器是否存活，是否就绪，如果是存活探针，则会根据探测结果对检查失败的容器进行相应的重启策略；</strong></li><li><strong><a href="http://mp.weixin.qq.com/s?__biz=MzIyMzIwNzAxMQ==&mid=2649443701&idx=1&sn=fccddcdc2362b974fda61974aca2d669&chksm=f03e2679c749af6f28d61f5d9317f0ef69ae2b909be12567cdfd24c140c0c930f0347043b425&scene=21#wechat_redirect">Metrics Server资源监控</a>。在node节点上部署Metrics Server用于监控node节点、pod的CPU、内存、文件系统、网络使用等资源使用情况，而kubelet则通过Metrics Server获取所在节点及容器的上的数据。</strong></li></ul><h3 id="21、kube-api-server的端口是多少？各个pod是如何访问kube-api-server的？"><a href="#21、kube-api-server的端口是多少？各个pod是如何访问kube-api-server的？" class="headerlink" title="21、kube-api-server的端口是多少？各个pod是如何访问kube-api-server的？"></a><strong>21、kube-api-server的端口是多少？各个pod是如何访问kube-api-server的？</strong></h3><p><strong>kube-api-server的端口是8080和6443，前者是http的端口，后者是https的端口，以我本机使用kubeadm安装的k8s为例：</strong></p><p><strong>在命名空间的kube-system命名空间里，有一个名称为kube-api-master的pod，这个pod就是运行着kube-api-server进程，它绑定了master主机的ip地址和6443端口，但是在default命名空间下，存在一个叫kubernetes的服务，该服务对外暴露端口为443，目标端口6443，这个服务的ip地址是clusterip地址池里面的第一个地址，同时这个服务的yaml定义里面并没有指定标签选择器，也就是说这个kubernetes服务所对应的endpoint是手动创建的，该endpoint也是名称叫做kubernetes，该endpoint的yaml定义里面代理到master节点的6443端口，也就是kube-api-server的IP和端口。这样一来，其他pod访问kube-api-server的整个流程就是：pod创建后嵌入了环境变量，pod获取到了kubernetes这个服务的ip和443端口，请求到kubernetes这个服务其实就是转发到了master节点上的6443端口的kube-api-server这个pod里面。</strong></p><h3 id="22、k8s中命名空间的作用是什么？"><a href="#22、k8s中命名空间的作用是什么？" class="headerlink" title="22、k8s中命名空间的作用是什么？"></a><strong>22、k8s中命名空间的作用是什么？</strong></h3><p><strong>amespace是kubernetes系统中的一种非常重要的资源，namespace的主要作用是用来实现多套环境的资源隔离，或者说是多租户的资源隔离。</strong></p><p><strong>k8s通过将集群内部的资源分配到不同的namespace中，可以形成逻辑上的隔离，以方便不同的资源进行隔离使用和管理。不同的命名空间可以存在同名的资源，命名空间为资源提供了一个作用域。</strong></p><p><strong>可以通过k8s的授权机制，将不同的namespace交给不同的租户进行管理，这样就实现了多租户的资源隔离，还可以结合k8s的资源配额机制，限定不同的租户能占用的资源，例如CPU使用量、内存使用量等等来实现租户可用资源的管理。</strong></p><h3 id="23、pod资源控制器类型有哪些"><a href="#23、pod资源控制器类型有哪些" class="headerlink" title="23、pod资源控制器类型有哪些?"></a><strong>23、pod资源控制器类型有哪些?</strong></h3><ul><li><strong>Deployments：Deployment为Pod和ReplicaSet提供声明式的更新能力。</strong></li><li><strong>ReplicaSet：ReplicaSet的目的是维护一组在任何时候都处于运行状态的Pod副本的稳定集合。因此，它通常用来保证给定数量的、完全相同的Pod的可用性。</strong></li><li><strong>StatefulSets：和Deployment类似，StatefulSet管理基于相同容器规约的一组Pod。但和Deployment不同的是，StatefulSet为它们的每个Pod维护了一个有粘性的ID。这些Pod是基于相同的规约来创建的，但是不能相互替换：无论怎么调度，每个Pod都有一个永久不变的ID。</strong></li><li><strong>DaemonSet：DaemonSet确保全部（或者某些）节点上运行一个Pod的副本。当有节点加入集群时，也会为他们新增一个Pod。当有节点从集群移除时，这些Pod也会被回收。删除DaemonSet将会删除它创建的所有Pod。</strong></li><li><strong>Jobs：Job会创建一个或者多个Pod，并将继续重试Pod的执行，直到指定数量的Pod成功终止。随着Pod成功结束，Job跟踪记录成功完成的Pod个数。当数量达到指定的成功个数阈值时，任务（即Job）结束。删除Job的操作会清除所创建的全部Pod。挂起Job的操作会删除Job的所有活跃Pod，直到Job被再次恢复执行。</strong></li><li><strong>Automatic Clean-up for Finished Jobs：TTL-after-finished控制器提供了一种TTL机制来限制已完成执行的资源对象的生命周期。TTL控制器目前只处理Job。</strong></li><li><strong>CronJob：一个CronJob对象就像crontab(crontable)文件中的一行。它用Cron格式进行编写，并周期性地在给定的调度时间执行Job。</strong></li><li><strong>ReplicationController：ReplicationController确保在任何时候都有特定数量的Pod副本处于运行状态。换句话说，ReplicationController确保一个Pod或一组同类的Pod总是可用的。</strong></li></ul><h3 id="24、nginx算法策略"><a href="#24、nginx算法策略" class="headerlink" title="24、nginx算法策略"></a><strong>24、nginx算法策略</strong></h3><p><strong>轮询（默认）</strong></p><p><strong>加权轮询（轮询+weight）</strong></p><p><strong>ip_hash</strong></p><p><strong>每一个请求的访问IP，都会映射成一个hash，再通过hash算法（hash值%node_count），分配到不同的后端服务器，访问ip相同的请求会固定访问同一个后端服务器，这样可以做到会话保持，解决session同步问题。</strong></p><p><strong>least_conn（最少连接）</strong></p><p><strong>使用最少连接的负载平衡，nginx将尝试不会使繁忙的应用程序服务器超载请求过多，而是将新请求分发给不太繁忙的服务器。</strong></p><h3 id="25、nignx常用模块"><a href="#25、nignx常用模块" class="headerlink" title="25、nignx常用模块"></a><strong>25、nignx常用模块</strong></h3><ul><li><strong>upstream</strong></li><li><strong>rewrite</strong></li><li><strong>location</strong></li><li><strong>proxy_pass</strong></li></ul><h3 id="26、如何查看并且杀死僵尸进程？"><a href="#26、如何查看并且杀死僵尸进程？" class="headerlink" title="26、如何查看并且杀死僵尸进程？"></a><strong>26、如何查看并且杀死僵尸进程？</strong></h3><p><strong>top —&gt; task (line)—&gt; zombie.</strong></p><p><strong>把父进程杀掉，父进程死后，过继给1号进程init，init 始终负责清理僵尸进程，它产生的所有僵尸进程跟着消失；如果你使用kill ，一般都不能杀掉 defunct进程.。用了kill -15,kill -9以后 之后反而会多出更多的僵尸进程。</strong></p><h3 id="27、搜索某个用户运行的进程"><a href="#27、搜索某个用户运行的进程" class="headerlink" title="27、搜索某个用户运行的进程"></a><strong>27、搜索某个用户运行的进程</strong></h3><figure class="highlight ebnf"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs ebnf"><span class="hljs-attribute">pgrep -au neteagle</span><br></code></pre></td></tr></table></figure><h3 id="28、查看某个端口正在被哪个进程使用"><a href="#28、查看某个端口正在被哪个进程使用" class="headerlink" title="28、查看某个端口正在被哪个进程使用"></a><strong>28、查看某个端口正在被哪个进程使用</strong></h3><figure class="highlight stylus"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs stylus">lsof -<span class="hljs-selector-tag">i</span> :<span class="hljs-selector-attr">[port]</span><br></code></pre></td></tr></table></figure><h3 id="29、端口转发"><a href="#29、端口转发" class="headerlink" title="29、端口转发"></a><strong>29、端口转发</strong></h3><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs css">iptables -t nat -<span class="hljs-selector-tag">A</span> PREROUTING -d <span class="hljs-number">10.0</span>.<span class="hljs-number">0.8</span> -<span class="hljs-selector-tag">p</span> tcp <span class="hljs-attr">--dport</span> <span class="hljs-number">80</span> -j REDIRECT <span class="hljs-attr">--to-ports</span> <span class="hljs-number">8080</span><br></code></pre></td></tr></table></figure><h3 id="30、查看http的并发请求数与其TCP连接状态"><a href="#30、查看http的并发请求数与其TCP连接状态" class="headerlink" title="30、查看http的并发请求数与其TCP连接状态"></a><strong>30、查看http的并发请求数与其TCP连接状态</strong></h3><figure class="highlight cos"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cos">etstat-n|awk&#x27;/<span class="hljs-symbol">^tcp</span>/&#123;++b[<span class="hljs-built_in">$NF</span>]&#125;END&#123;<span class="hljs-keyword">for</span>(ainb)printa,b[a]&#125;&#x27;<br></code></pre></td></tr></table></figure><h3 id="31、查看-var-log目录下文件数"><a href="#31、查看-var-log目录下文件数" class="headerlink" title="31、查看&#x2F;var&#x2F;log目录下文件数"></a><strong>31、查看&#x2F;var&#x2F;log目录下文件数</strong></h3><figure class="highlight stata"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs stata"><span class="hljs-keyword">ls</span>/<span class="hljs-keyword">var</span>/<span class="hljs-keyword">log</span>/-lR|grep<span class="hljs-string">&quot;^-&quot;</span>|wc-<span class="hljs-keyword">l</span><br></code></pre></td></tr></table></figure><h3 id="32、linux系统启动流程"><a href="#32、linux系统启动流程" class="headerlink" title="32、linux系统启动流程"></a><strong>32、linux系统启动流程</strong></h3><ul><li><strong>第一步：开机自检，加载BIOS</strong></li><li><strong>第二步：读取ＭＢＲ</strong></li><li><strong>第三步：Boot Loader　grub引导菜单</strong></li><li><strong>第四步：加载kernel内核</strong></li><li><strong>第五步：init进程依据inittab文件夹来设定运行级别</strong></li><li><strong>第六步：init进程执行rc.sysinit</strong></li><li><strong>第七步：启动内核模块</strong></li><li><strong>第八步：执行不同运行级别的脚本程序</strong></li><li><strong>第九步：执行&#x2F;etc&#x2F;rc.d&#x2F;rc.lo</strong></li></ul><h3 id="33、Linux文件类型"><a href="#33、Linux文件类型" class="headerlink" title="33、Linux文件类型"></a><strong>33、Linux文件类型</strong></h3><ul><li><strong>-：常规文件，即file</strong></li><li><strong>d：目录文件</strong></li><li><strong>b：block device 即块设备文件，如硬盘;支持以block为单位进行随机访问</strong></li><li><strong>c：character device 即字符设备文件，如键盘支持以character为单位进行线性访问</strong></li><li><strong>l：symbolic link 即符号链接文件（关注微信公众号：网络技术联盟站），又称软链接文件</strong></li><li><strong>p：pipe 即命名管道文件</strong></li><li><strong>s：socket 即套接字文件，用于实现两个进程进行通信</strong></li></ul><h3 id="34、简述lvm，如何给使用lvm的-分区扩容？"><a href="#34、简述lvm，如何给使用lvm的-分区扩容？" class="headerlink" title="34、简述lvm，如何给使用lvm的&#x2F;分区扩容？"></a><strong>34、简述lvm，如何给使用lvm的&#x2F;分区扩容？</strong></h3><p><strong>功能：可以对磁盘进行动态管理。动态按需调整大小</strong></p><p><strong>概念：</strong></p><ul><li><strong><a href="http://mp.weixin.qq.com/s?__biz=MzIyMzIwNzAxMQ==&mid=2649443790&idx=1&sn=b99e3741b03c03eca4705616e28fe75a&chksm=f03e26c2c749afd42d23d4f1855e18d365a9a402e89896c62a906633ab95a3bcf4a188e2ec3d&scene=21#wechat_redirect">PV 物理卷</a>：物理卷在逻辑卷管理中处于最底层，它可以是实际物理硬盘上的分区，也可以是整个物理硬盘，也可以是raid设备。</strong></li><li><strong>VG 卷组：卷组建立在物理卷之上，一个卷组中至少要包括一个物理卷，在卷组建立之后可动态添加物理卷到卷组中。一个逻辑卷管理系统工程中可以只有一个卷组，也可以拥有多个卷组。</strong></li><li><strong>LV 逻辑卷：逻辑卷建立在卷组之上，卷组中的未分配空间可以用于建立新的逻辑卷，逻辑卷建立后可以动态地扩展和缩小空间。系统中的多个逻辑卷可以属于同一个卷组，也可以属于不同的多个卷组。</strong></li></ul><p><strong>给&#x2F;分区扩容步骤：</strong></p><ul><li><strong>添加磁盘</strong></li><li><strong>使用fdisk命令对新增加的磁盘进行分区</strong></li><li><strong>分区完成后修改分区类型为lvm</strong></li><li><strong>使用pvcreate创建物理卷</strong></li><li><strong>使用vgextend命令将新增加的分区加入到根目录分区中</strong></li><li><strong>使用lvextend命令进行扩容</strong></li><li><strong>使用xfs_growfs调整卷分区大小</strong></li></ul><h3 id="35、如何在文本里面进行复制、粘贴，删除行，删除全部，按行查找和按字母查找。"><a href="#35、如何在文本里面进行复制、粘贴，删除行，删除全部，按行查找和按字母查找。" class="headerlink" title="35、如何在文本里面进行复制、粘贴，删除行，删除全部，按行查找和按字母查找。"></a><strong>35、如何在文本里面进行复制、粘贴，删除行，删除全部，按行查找和按字母查找。</strong></h3><p><strong>以下操作全部在vi&#x2F;vim命令行状态操作，不要在编辑状态操作：</strong></p><ul><li><strong>在文本里 移动到想要复制的行按yy想复制到哪就移动到哪，然后按P就黏贴了</strong></li><li><strong>删除行  移动到改行 按dd</strong></li><li><strong>删除全部dG这里注意G一定要大写</strong></li><li><strong>按行查找  :90 这样就是找到第90行</strong></li><li><strong>按字母查找 &#x2F;path 这样就是找到path这个单词所在的位置，文本里可能存在多个，多次查找会显示在不同的位置。</strong></li></ul><h3 id="36、符号链接与硬链接的区别"><a href="#36、符号链接与硬链接的区别" class="headerlink" title="36、符号链接与硬链接的区别"></a><strong>36、符号链接与硬链接的区别</strong></h3><ul><li><strong>我们可以把符号链接，也就是软连接 当做是 windows系统里的 快捷方式。</strong></li><li><strong>硬链接 就好像是 又复制了一份.</strong></li><li><strong>ln 3.txt 4.txt  这是硬链接，相当于复制，不可以跨分区，但修改3,4会跟着变，若删除3,4不受任何影响。</strong></li><li><strong>ln -s 3.txt 4.txt  这是软连接，相当于快捷方式。修改4,3也会跟着变，若删除3,4就坏掉了。不可以用了。</strong></li></ul><h3 id="37、什么是正向代理？"><a href="#37、什么是正向代理？" class="headerlink" title="37、什么是正向代理？"></a><strong>37、什么是正向代理？</strong></h3><p><strong>一个位于客户端和原始服务器(origin server)之间的服务器，为了从原始服务器取得内容，客户端向代理发送一个请求并指定目标(原始服务器)，然后代理向原始服务器转交请求并将获得的内容返回给客户端。</strong></p><p><strong>客户端才能使用正向代理。正向代理总结就一句话：代理端代理的是客户端。例如说：我们使用的OpenVPN 等等。</strong></p><h3 id="38、什么是反向代理？"><a href="#38、什么是反向代理？" class="headerlink" title="38、什么是反向代理？"></a><strong>38、什么是反向代理？</strong></h3><p><strong>反向代理（Reverse Proxy）方式，是指以代理服务器来接受 Internet上的连接请求，然后将请求，发给内部网络上的服务器并将从服务器上得到的结果返回给 Internet 上请求连接的客户端，此时代理服务器对外就表现为一个反向代理服务器。</strong></p><p><strong>反向代理总结就一句话：代理端代理的是服务端。</strong></p><h3 id="39、什么是动态资源、静态资源分离？"><a href="#39、什么是动态资源、静态资源分离？" class="headerlink" title="39、什么是动态资源、静态资源分离？"></a><strong>39、什么是动态资源、静态资源分离？</strong></h3><p><strong>动态资源、静态资源分离，是让动态网站里的动态网页根据一定规则把不变的资源和经常变的资源区分开来，动静资源做好了拆分以后我们就可以根据静态资源的特点将其做缓存操作，这就是网站静态化处理的核心思路。</strong></p><p><strong>动态资源、静态资源分离简单的概括是：动态文件与静态文件的分离。</strong></p><p><strong>在我们的软件开发中，有些请求是需要后台处理的（如：.jsp,.do 等等），有些请求是不需要经过后台处理的（如：css、html、jpg、js 等等文件），这些不需要经过后台处理的文件称为静态文件，否则动态文件。</strong></p><p><strong>因此我们后台处理忽略静态文件。这会有人又说那我后台忽略静态文件不就完了吗？当然这是可以的，但是这样后台的请求次数就明显增多了。在我们对资源的响应速度有要求的时候，我们应该使用这种动静分离的策略去解决动、静分离将网站静态资源（HTML，JavaScript，CSS，img等文件）与后台应用分开部署，提高用户访问静态代码的速度，降低对后台应用访问</strong></p><p><strong>这里我们将静态资源放到 Nginx 中，动态资源转发到 Tomcat 服务器中去。</strong></p><p><strong>当然，因为现在七牛、阿里云等 CDN 服务已经很成熟，主流的做法，是把静态资源缓存到 CDN 服务中，从而提升访问速度。</strong></p><p><strong>相比本地的 Nginx 来说，CDN 服务器由于在国内有更多的节点，可以实现用户的就近访问。并且，CDN 服务可以提供更大的带宽，不像我们自己的应用服务，提供的带宽是有限的。</strong></p><h3 id="40、网站登陆缓慢是什么原因"><a href="#40、网站登陆缓慢是什么原因" class="headerlink" title="40、网站登陆缓慢是什么原因?"></a><strong>40、网站登陆缓慢是什么原因?</strong></h3><ul><li><strong>网络带宽，这是一个很常见的瓶颈。</strong></li><li><strong>cpu、硬盘、内存配置过低，服务器负载不起来。</strong></li><li><strong>网站的开发代码不够完善，例如mysql语句没有进行优化，导致数据库的读写相当耗费时间。</strong></li><li><strong>数据库的瓶颈。当我们的数据库的数据变得越来越多的时候，那么对于数据库的读写压力肯定会变大。</strong></li></ul><h3 id="41、a与b服务器不在同一网段怎么设置-设置完还ping不通怎么排查"><a href="#41、a与b服务器不在同一网段怎么设置-设置完还ping不通怎么排查" class="headerlink" title="41、a与b服务器不在同一网段怎么设置?设置完还ping不通怎么排查?"></a><strong>41、a与b服务器不在同一网段怎么设置?设置完还ping不通怎么排查?</strong></h3><ul><li><strong>AB服务器不在同一个网段</strong></li><li><strong>首先把不同IP段的服务器划分给不同的vlan</strong></li><li><strong>在通过通过三层交换机添加虚拟IP路由实在不同网段的vlan的连接</strong></li></ul><h3 id="42、在AB两台服务器之间通过一个服务器c做软路由使用给路由器c配置两块网卡并开启自身的路由功能"><a href="#42、在AB两台服务器之间通过一个服务器c做软路由使用给路由器c配置两块网卡并开启自身的路由功能" class="headerlink" title="42、在AB两台服务器之间通过一个服务器c做软路由使用给路由器c配置两块网卡并开启自身的路由功能"></a><strong>42、在AB两台服务器之间通过一个服务器c做软路由使用给路由器c配置两块网卡并开启自身的路由功能</strong></h3><figure class="highlight awk"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs awk">vi <span class="hljs-regexp">/etc/</span>sysconfig<span class="hljs-regexp">/network-scripts/i</span>fcfg-eth0<br></code></pre></td></tr></table></figure><p><strong>查看网卡状况IP -a -s 网卡的名字</strong></p><p><strong>A服务器设置相关网卡信息</strong></p><figure class="highlight routeros"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs routeros">子网掩码：255.255.255.0<br><span class="hljs-attribute">IP</span>=10.0.0.1<br>网关=10.0.0.254<br>重启网卡生效<br>查看路由信息<span class="hljs-built_in"></span><br><span class="hljs-built_in">route </span>-n<br>添加对应路由<span class="hljs-built_in"></span><br><span class="hljs-built_in">route </span><span class="hljs-built_in">add</span> -net 10.0.1.0/24 gw 10.0.0.11<br></code></pre></td></tr></table></figure><p><strong>B服务器的设置相关信息</strong></p><figure class="highlight routeros"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs routeros"><span class="hljs-attribute">IP</span>=10.0.1.10<br>网关10.0.1.254<br>重启网卡生效<span class="hljs-built_in"></span><br><span class="hljs-built_in">route </span>-n<br>添加对应的路由<span class="hljs-built_in"></span><br><span class="hljs-built_in">route </span><span class="hljs-built_in">add</span> -net 10.0.0.0/24 gw 10.0.1.11<br></code></pre></td></tr></table></figure><p><strong>C服务器的两块网卡</strong></p><figure class="highlight abnf"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs abnf">网卡<span class="hljs-number">1</span><br><span class="hljs-attribute">IP</span><span class="hljs-operator">=</span><span class="hljs-number">10.0</span>.<span class="hljs-number">0.11</span><br>网关<span class="hljs-operator">=</span><span class="hljs-number">10.0</span>.<span class="hljs-number">0.254</span><br><br><br>网卡<span class="hljs-number">2</span><br><span class="hljs-attribute">IP</span><span class="hljs-operator">=</span><span class="hljs-number">10.0</span>.<span class="hljs-number">1.11</span><br>网关<span class="hljs-operator">=</span><span class="hljs-number">10.0</span>.<span class="hljs-number">1.254</span><br>重启网卡生效<br>route -n<br>vi /etc/sysctl.conf<br>net.ipv4.ip_forword <span class="hljs-operator">=</span> <span class="hljs-number">1</span><br></code></pre></td></tr></table></figure><h3 id="43、如果PING不通怎么排查"><a href="#43、如果PING不通怎么排查" class="headerlink" title="43、如果PING不通怎么排查"></a><strong>43、如果PING不通怎么排查</strong></h3><ul><li><strong>首先先看看是不是网路接口故障水晶头或是网卡接口接触不良造成，其次检查交换机和路由等网络设备是有故障</strong></li><li><strong>是否关闭了防火墙和selinux机制</strong></li><li><strong>然后查看网卡和路由和网关是否配置正确</strong></li></ul><h3 id="44、docker容器ping不通是什么原因"><a href="#44、docker容器ping不通是什么原因" class="headerlink" title="44、docker容器ping不通是什么原因?"></a><strong>44、docker容器ping不通是什么原因?</strong></h3><p><strong>ifconfig 查看一下docker0网桥，ping一下网桥看看是否通，有可能是网桥配置问题</strong></p><p><strong>weave路由器端口6783</strong></p><ul><li><strong>安装docker容器的服务器没有关闭防火墙(访问一下安装docker物理机的，是否能访问，如果不能访问就变不能访问docker)</strong></li><li><strong>docker在创建镜像的时候没有做端口映射(出现这种情况能访问物理机不能访问docker)使用dockers ps 查看镜像的端口映射情况</strong></li><li><strong>端口映射不正确</strong></li><li><strong>查看网络配置ping网桥看是否能ping通，有可能是网桥的原因</strong></li></ul><h3 id="45、如果一台办公室内主机无法上网-打不开网站-，请给出你的排查步骤"><a href="#45、如果一台办公室内主机无法上网-打不开网站-，请给出你的排查步骤" class="headerlink" title="45、如果一台办公室内主机无法上网(打不开网站)，请给出你的排查步骤?"></a><strong>45、如果一台办公室内主机无法上网(打不开网站)，请给出你的排查步骤?</strong></h3><ul><li><strong>首先确定物理链路是否联通正常。</strong></li><li><strong>查看本机IP，路由，DNS的设置情况是否达标。</strong></li><li><strong>telnet检查服务器的WEB有没有开启以及防火墙是否阻拦。</strong></li><li><strong>ping一下网关，进行最基础的检查，通了，表示能够到达服务器。</strong></li><li><strong>测试到网关或路由器的通常情况，先测网关，然后再测路由器一级一级的测试。</strong></li><li><strong>测试ping公网ip的通常情况(记住几个外部IP)，</strong></li><li><strong>测试DNS的通畅。ping出对应IP。</strong></li><li><strong>通过以上检查后，还在网管的路由器上进行检查。</strong></li></ul><h3 id="46、如果我们的网站打开速度慢请说下您的排查思路"><a href="#46、如果我们的网站打开速度慢请说下您的排查思路" class="headerlink" title="46、如果我们的网站打开速度慢请说下您的排查思路?"></a><strong>46、如果我们的网站打开速度慢请说下您的排查思路?</strong></h3><p><strong>判断原因</strong></p><p><strong>首先我要以用户的身份登录我们的网站，判断问题出现在我们自身原因，还是用户那边的原因。</strong></p><p><strong>如果是用户问题有以下几个原因：</strong></p><ul><li><strong>用户那边的带宽</strong></li><li><strong>用户的浏览器器版本低，安装插件太多</strong></li><li><strong>中毒和电脑里的垃圾文件过多</strong></li><li><strong>用户主机的主机的性能和操作系统</strong></li></ul><p><strong><a href="http://mp.weixin.qq.com/s?__biz=MzIyMzIwNzAxMQ==&mid=2649443950&idx=1&sn=e6cc92566a0ebcf1186327a870485c3f&chksm=f03e2562c749ac74452aaabdacfbaf9ec704ed1f006cb13ccbb0eaa6ecdf3f5855e8d6f69e0b&scene=21#wechat_redirect">如果是我们的网站自身问题有一下几个原因</a></strong></p><ul><li><strong>网络带宽</strong></li><li><strong>服务器的cpu、硬盘、内存过低服务器负载不起来也就是说服务器自身的性能方面</strong></li><li><strong>网站代码不够完善。如mysql语句没有进行优化导致数据库读写耗时</strong></li><li><strong>服务器未开启图片压缩</strong></li><li><strong>网页台下</strong></li><li><strong>死连接过多插件使用及js文件调用频繁网站服务器的速度或是租用空间所在的服务器速度</strong></li></ul><p><strong>解决思路</strong></p><p><strong>1、检测服务器速度的快慢</strong></p><ul><li><strong>ping命令查看连接到服务器的时间和丢包情况(ping 测试网址的)</strong></li><li><strong>查看丢包率(1000个包没有丢一个是最理想的、一般一个速度好的机房丢包率不超过1%)</strong></li><li><strong>ping值要小同城电信adsl ping平均值绝对不能超过20，一般都在10，跨省的平均值20-40属于正常</strong></li><li><strong>ping值要均匀最小值和最大值相差太大说明路由不稳定的表现</strong></li></ul><p><strong>2、查看服务器自身性能</strong></p><p><strong>查看cpu的使用率<code>uptime</code></strong></p><p><strong>查看内存情况 <code>free -m</code></strong></p><p><strong>查看I&#x2F;O读写iostat 磁盘I&#x2F;O读写等看看是那个进程大量占用系统资源导致我的服务器变慢</strong></p><p><strong>3、看看访问最多的URL和IP有什么特征，如果是恶意URL和IP就把他屏蔽掉如果是善意的就限流有可能是CDN回源量大造成网站无法访问</strong></p><p><strong>4、查看同台服务器上其他网站的打开速度，可以通过查询工具查看和自己在同一台服务器上的网站个数和网址可以看他们打开快慢</strong></p><p><strong>5、电信和联通互访的问题</strong></p><p><strong>如果是空间打开时快时慢，有时打不开那就是空间不稳定找空间商解决或是换空间伤，如果是有的地方快有的地方慢应该是网络线路问题，比如电信用户访问放在联通服务器上的网站，联通用户访问放在电信服务器上的网站，解决办吧是：使用双线空间或是多线空间</strong></p><p><strong>6、从网站自身的原因</strong></p><ul><li><strong>网站的程序设计结构是否合理是否由于幻灯片代码影响网站打开速度(找程序设计相关人士解决)</strong></li><li><strong>网页的设计结构和代码错误(请专业人士进行修改)</strong></li><li><strong>网页的内容如：大尺寸图片、大尺寸flash、过多的引用其他网站内容，如果被引用内容的网站速度慢，也影响自身网站把。譬如友情连接可以把对方 的图片放到自己网站上</strong></li></ul><p><strong>解决办法</strong></p><ul><li><strong>优化图片，限制图片大小尺寸，降低图片质量，减少图片数量</strong></li><li><strong>限定图片的格式：jpg，png，gif</strong></li><li><strong>减少http的请求数(当打开网页时浏览器会发出很多对象请求，每个对象的加载都会有所延时，如果网页上的对象很多就会花费大量的时间，去除不必要的对象，将临近的图片合成一张，合并css文件) f r o m ：w l j s l m z</strong></li></ul><h3 id="46、如何查看二进制文件的内容"><a href="#46、如何查看二进制文件的内容" class="headerlink" title="46、如何查看二进制文件的内容"></a><strong>46、如何查看二进制文件的内容</strong></h3><p><strong>我们一般通过 hexdump 命令 来查看二进制文件的内容。</strong></p><p><strong>hexdump -C XXX(文件名)  -C 是参数 不同的参数有不同的意义</strong></p><p><strong>-C  是比较规范的 十六进制和 ASCII 码显示</strong></p><p><strong>-c  是单字节字符显示</strong></p><p><strong>-b  单字节八进制显示</strong></p><p><strong>-o  是双字节八进制显示</strong></p><p><strong>-d  是双字节十进制显示</strong></p><p><strong>-x  是双字节十六进制显示</strong></p><p><strong>等等等等</strong></p><h3 id="47、你是怎么备份数据的，包括数据库备份？"><a href="#47、你是怎么备份数据的，包括数据库备份？" class="headerlink" title="47、你是怎么备份数据的，包括数据库备份？"></a><strong>47、你是怎么备份数据的，包括数据库备份？</strong></h3><p><strong>在生产环境下，不管是应用数据、还是数据库数据首先在部署的时候就会有主从架构、或者集群，这本身就是属于数据的热备份；其实考虑冷备份，用专门一台服务器做为备份服务器，比如可以用rsync+inotify配合计划任务来实现数据的冷备份，如果是发版的包备份，正常情况下有台发布服务器，每次发版都会保存好发版的包。</strong></p><h3 id="48、zabbix常用术语你知道几个？"><a href="#48、zabbix常用术语你知道几个？" class="headerlink" title="48、zabbix常用术语你知道几个？"></a><strong>48、zabbix常用术语你知道几个？</strong></h3><ul><li><strong>主机（host）：要监控的网络设备，可由IP或DNS名称指定；</strong></li><li><strong>主机组（hostgroup）：主机的逻辑容器，可以包含主机和模板，但同一个组织内的主机和模板不能互相链接；主机组通常在给用户或用户组指派监控权限时使用；</strong></li><li><strong>监控项（item）：一个特定监控指标的相关的数据；这些数据来自于被监控对象；item是zabbix进行数据收集的核心，相对某个监控对象，每个item都由”key”标识；</strong></li><li><strong>触发器（trigger）：一个表达式，用于评估某监控对象的特定item内接收到的数据是否在合理范围内，也就是阈值；接收的数据量大于阈值时，触发器状态将从”OK”转变为”Problem”，当数据再次恢复到合理范围，又转变为”OK”；</strong></li><li><strong>事件（event）：触发一个值得关注的事情，比如触发器状态转变，新的agent或重新上线的agent的自动注册等；</strong></li><li><strong>动作（action）：指对于特定事件事先定义的处理方法，如发送通知，何时执行操作；</strong></li><li><strong>报警升级（escalation）：发送警报或者执行远程命令的自定义方案，如每隔5分钟发送一次警报，共发送5次等；</strong></li><li><strong>媒介（media）：发送通知的手段或者通道，如Email、Jabber或者SMS等；</strong></li><li><strong>通知（notification）：通过选定的媒介向用户发送的有关某事件的信息；远程命令（remote command）：预定义的命令，可在被监控主机处于某特定条件下时自动执行；</strong></li><li><strong>模板（template）：用于快速定义被监控主机的预设条目集合，通常包含了item、trigger、graph、screen、application以及low-level</strong></li><li><strong>discovery rule；模板可以直接链接至某个主机；应用（application）：一组item的集合；</strong></li><li><strong>web场景（webscennario）：用于检测web站点可用性的一个活多个HTTP请求；前端（frontend）：Zabbix的web接口；</strong></li></ul><h3 id="49、虚拟化技术有哪些表现形式"><a href="#49、虚拟化技术有哪些表现形式" class="headerlink" title="49、虚拟化技术有哪些表现形式"></a><strong>49、虚拟化技术有哪些表现形式</strong></h3><ul><li><strong><a href="http://mp.weixin.qq.com/s?__biz=MzIyMzIwNzAxMQ==&mid=2649444120&idx=2&sn=6b60d6aa055a5acfbbc60c2141e95a35&chksm=f03e2414c749ad021c7f078b4b676ac6cc2f18236e31ecb205792277572a23980854821bab4a&scene=21#wechat_redirect">完全拟化技术</a>：通过软件实现对操作系统的资源再分配，比较成熟，完全虚拟化代表技术：KVM、ESXI、Hyper-V。</strong></li><li><strong><a href="http://mp.weixin.qq.com/s?__biz=MzIyMzIwNzAxMQ==&mid=2649444104&idx=2&sn=b8d9c7eb1bcf5cad54b1d94122a4e49e&chksm=f03e2404c749ad12407b298a7c5f2572fa9fc62fcdf4b6003d306b687971a61b30cf4fc106ff&scene=21#wechat_redirect">半虚拟化技术</a>：通过代码修改已有的系统，形成一种新的可虚拟化的系统，调用硬件资源去安装多个系统，整体速度上相对高一点，半虚拟化代表技术：Xen。</strong></li><li><strong><a href="http://mp.weixin.qq.com/s?__biz=MzIyMzIwNzAxMQ==&mid=2649443950&idx=1&sn=e6cc92566a0ebcf1186327a870485c3f&chksm=f03e2562c749ac74452aaabdacfbaf9ec704ed1f006cb13ccbb0eaa6ecdf3f5855e8d6f69e0b&scene=21#wechat_redirect">轻量级虚拟化</a>：介于完全虚拟化、半虚拟化之间，轻量级虚拟化代表技术：Docker。</strong></li></ul><h3 id="50、修改线上业务配置文件流程"><a href="#50、修改线上业务配置文件流程" class="headerlink" title="50、修改线上业务配置文件流程"></a><strong>50、修改线上业务配置文件流程</strong></h3><ul><li><strong>先告知运维经理和业务相关开发人员</strong></li><li><strong>在测试环境测试，并备份之前的配置文件</strong></li><li><strong>测试无误后修改生产环境配置</strong></li><li><strong>观察生产环境是否正常，是否有报警</strong></li><li><strong>完成配置文件更改</strong></li></ul>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>2024-8-1 k8s面试题</title>
    <link href="/2024/12/05/2024-8-1%20kubernetes%E9%9D%A2%E8%AF%95%E9%A2%98/"/>
    <url>/2024/12/05/2024-8-1%20kubernetes%E9%9D%A2%E8%AF%95%E9%A2%98/</url>
    
    <content type="html"><![CDATA[<h2 id="简述ETCD及其特点？"><a href="#简述ETCD及其特点？" class="headerlink" title="简述ETCD及其特点？"></a><strong>简述ETCD及其特点？</strong></h2><p><strong>etcd 是 CoreOS 团队发起的开源项目，是一个管理配置信息和服务发现（service discovery）的项目，它的目标是构建一个高可用的分布式键值（key-value）数据库，基于 Go 语言实现。</strong></p><ul><li><p><strong>特点：</strong></p></li><li><ul><li><strong>简单：支持 REST 风格的 HTTP+JSON API</strong></li><li><strong>安全：支持 HTTPS 方式的访问</strong></li><li><strong>快速：支持并发 1k&#x2F;s 的写操作</strong></li><li><strong>可靠：支持分布式结构，基于 Raft 的一致性算法，Raft 是一套通过选举主节点来实现分布式系统一致性的算法。</strong></li></ul></li></ul><h2 id="简述ETCD适应的场景？"><a href="#简述ETCD适应的场景？" class="headerlink" title="简述ETCD适应的场景？"></a><strong>简述ETCD适应的场景？</strong></h2><p><strong>etcd基于其优秀的特点，可广泛的应用于以下场景：</strong></p><p><strong>服务发现(Service Discovery)：服务发现主要解决在同一个分布式集群中的进程或服务，要如何才能找到对方并建立连接。本质上来说，服务发现就是想要了解集群中是否有进程在监听udp或tcp端口，并且通过名字就可以查找和连接。</strong></p><p><strong>消息发布与订阅：在分布式系统中，最适用的一种组件间通信方式就是消息发布与订阅。即构建一个配置共享中心，数据提供者在这个配置中心发布消息，而消息使用者则订阅他们关心的主题，一旦主题有消息发布，就会实时通知订阅者。通过这种方式可以做到分布式系统配置的集中式管理与动态更新。应用中用到的一些配置信息放到etcd上进行集中管理。</strong></p><p><strong>负载均衡：在分布式系统中，为了保证服务的高可用以及数据的一致性，通常都会把数据和服务部署多份，以此达到对等服务，即使其中的某一个服务失效了，也不影响使用。etcd本身分布式架构存储的信息访问支持负载均衡。etcd集群化以后，每个etcd的核心节点都可以处理用户的请求。所以，把数据量小但是访问频繁的消息数据直接存储到etcd中也可以实现负载均衡的效果。</strong></p><p><strong>分布式通知与协调：与消息发布和订阅类似，都用到了etcd中的Watcher机制，通过注册与异步通知机制，实现分布式环境下不同系统之间的通知与协调，从而对数据变更做到实时处理。</strong></p><p><strong>分布式锁：因为etcd使用Raft算法保持了数据的强一致性，某次操作存储到集群中的值必然是全局一致的，所以很容易实现分布式锁。锁服务有两种使用方式，一是保持独占，二是控制时序。</strong></p><p><strong>集群监控与Leader竞选：通过etcd来进行监控实现起来非常简单并且实时性强。</strong></p><h2 id="简述什么是Kubernetes？"><a href="#简述什么是Kubernetes？" class="headerlink" title="简述什么是Kubernetes？"></a><strong>简述什么是Kubernetes？</strong></h2><p><strong>Kubernetes是一个全新的基于容器技术的分布式系统支撑平台。是Google开源的容器集群管理系统（谷歌内部:Borg）。在Docker技术的基础上，为容器化的应用提供部署运行、资源调度、服务发现和动态伸缩等一系列完整功能，提高了大规模容器集群管理的便捷性。并且具有完备的集群管理能力，多层次的安全防护和准入机制、多租户应用支撑能力、透明的服务注册和发现机制、內建智能负载均衡器、强大的故障发现和自我修复能力、服务滚动升级和在线扩容能力、可扩展的资源自动调度机制以及多粒度的资源配额管理能力。</strong></p><h2 id="简述Kubernetes和Docker的关系？"><a href="#简述Kubernetes和Docker的关系？" class="headerlink" title="简述Kubernetes和Docker的关系？"></a><strong>简述Kubernetes和Docker的关系？</strong></h2><p><strong>Docker 提供容器的生命周期管理和，Docker 镜像构建运行时容器。它的主要优点是将将软件&#x2F;应用程序运行所需的设置和依赖项打包到一个容器中，从而实现了可移植性等优点。</strong></p><p><strong>Kubernetes 用于关联和编排在多个主机上运行的容器。</strong></p><h2 id="简述Kubernetes中什么是Minikube、Kubectl、Kubelet？"><a href="#简述Kubernetes中什么是Minikube、Kubectl、Kubelet？" class="headerlink" title="简述Kubernetes中什么是Minikube、Kubectl、Kubelet？"></a><strong>简述Kubernetes中什么是Minikube、Kubectl、Kubelet？</strong></h2><p><strong>Minikube 是一种可以在本地轻松运行一个单节点 Kubernetes 群集的工具。</strong></p><p><strong>Kubectl 是一个命令行工具，可以使用该工具控制Kubernetes集群管理器，如检查群集资源，创建、删除和更新组件，查看应用程序。</strong></p><p><strong>Kubelet 是一个代理服务，它在每个节点上运行，并使从服务器与主服务器通信。</strong></p><h2 id="简述Kubernetes常见的部署方式？"><a href="#简述Kubernetes常见的部署方式？" class="headerlink" title="简述Kubernetes常见的部署方式？"></a><strong>简述Kubernetes常见的部署方式？</strong></h2><p><strong>常见的Kubernetes部署方式有：</strong></p><ul><li><strong>kubeadm：也是推荐的一种部署方式；</strong></li><li><strong>二进制：网页上很多教程，未来我也会写一个</strong></li><li><strong>minikube：在本地轻松运行一个单节点 Kubernetes 群集的工具。</strong></li><li></li></ul><h2 id="简述Kubernetes如何实现集群管理？"><a href="#简述Kubernetes如何实现集群管理？" class="headerlink" title="简述Kubernetes如何实现集群管理？"></a><strong>简述Kubernetes如何实现集群管理？</strong></h2><p><strong>在集群管理方面，Kubernetes将集群中的机器划分为一个Master节点和一群工作节点Node。其中，在Master节点运行着集群管理相关的一组进程kube-apiserver、kube-controller-manager和kube-scheduler，这些进程实现了整个集群的资源管理、Pod调度、弹性伸缩、安全控制、系统监控和纠错等管理能力，并且都是全自动完成的。</strong></p><p><strong>简述Kubernetes的优势、适应场景及其特点？</strong></p><p><strong>Kubernetes作为一个完备的分布式系统支撑平台，其主要优势：</strong></p><ul><li><strong>容器编排</strong></li><li><strong>轻量级</strong></li><li><strong>开源</strong></li><li><strong>弹性伸缩</strong></li><li><strong>负载均衡</strong></li></ul><p><strong>Kubernetes常见场景：</strong></p><ul><li><strong>快速部署应用</strong></li><li><strong>快速扩展应用</strong></li><li><strong>无缝对接新的应用功能</strong></li><li><strong>节省资源，优化硬件资源的使用</strong></li></ul><p><strong>Kubernetes相关特点：</strong></p><ul><li><strong>可移植: 支持公有云、私有云、混合云、多重云（multi-cloud）。</strong></li><li><strong>可扩展: 模块化,、插件化、可挂载、可组合。</strong></li><li><strong>自动化: 自动部署、自动重启、自动复制、自动伸缩&#x2F;扩展。</strong></li></ul><h2 id="简述Kubernetes的缺点或当前的不足之处？"><a href="#简述Kubernetes的缺点或当前的不足之处？" class="headerlink" title="简述Kubernetes的缺点或当前的不足之处？"></a><strong>简述Kubernetes的缺点或当前的不足之处？</strong></h2><p><strong>Kubernetes当前存在的缺点（不足）如下：</strong></p><ul><li><strong>安装过程和配置相对困难复杂。</strong></li><li><strong>管理服务相对繁琐。</strong></li><li><strong>运行和编译需要很多时间。</strong></li><li><strong>它比其他替代品更昂贵。</strong></li><li><strong>对于简单的应用程序来说，可能不需要涉及Kubernetes即可满足。</strong></li></ul><h2 id="简述Kubernetes相关基础概念？"><a href="#简述Kubernetes相关基础概念？" class="headerlink" title="简述Kubernetes相关基础概念？"></a><strong>简述Kubernetes相关基础概念？</strong></h2><p><strong>master：k8s集群的管理节点，负责管理集群，提供集群的资源数据访问入口。拥有Etcd存储服务（可选），运行Api Server进程，Controller Manager服务进程及Scheduler服务进程。</strong></p><p><strong>node（worker）：Node（worker）是Kubernetes集群架构中运行Pod的服务节点，是Kubernetes集群操作的单元，用来承载被分配Pod的运行，是Pod运行的宿主机。运行docker eninge服务，守护进程kunelet及负载均衡器kube-proxy。</strong></p><p><strong>pod：运行于Node节点上，若干相关容器的组合。Pod内包含的容器运行在同一宿主机上，使用相同的网络命名空间、IP地址和端口，能够通过localhost进行通信。Pod是Kurbernetes进行创建、调度和管理的最小单位，它提供了比容器更高层次的抽象，使得部署和管理更加灵活。一个Pod可以包含一个容器或者多个相关容器。</strong></p><p><strong>label：Kubernetes中的Label实质是一系列的Key&#x2F;Value键值对，其中key与value可自定义。Label可以附加到各种资源对象上，如Node、Pod、Service、RC等。一个资源对象可以定义任意数量的Label，同一个Label也可以被添加到任意数量的资源对象上去。Kubernetes通过Label Selector（标签选择器）查询和筛选资源对象。</strong></p><p><strong>Replication Controller：Replication Controller用来管理Pod的副本，保证集群中存在指定数量的Pod副本。集群中副本的数量大于指定数量，则会停止指定数量之外的多余容器数量。反之，则会启动少于指定数量个数的容器，保证数量不变。Replication Controller是实现弹性伸缩、动态扩容和滚动升级的核心。</strong></p><p><strong>Deployment：Deployment在内部使用了RS来实现目的，Deployment相当于RC的一次升级，其最大的特色为可以随时获知当前Pod的部署进度。</strong></p><p><strong>HPA（Horizontal Pod Autoscaler）：Pod的横向自动扩容，也是Kubernetes的一种资源，通过追踪分析RC控制的所有Pod目标的负载变化情况，来确定是否需要针对性的调整Pod副本数量。</strong></p><p><strong>Service：Service定义了Pod的逻辑集合和访问该集合的策略，是真实服务的抽象。Service提供了一个统一的服务访问入口以及服务代理和发现机制，关联多个相同Label的Pod，用户不需要了解后台Pod是如何运行。</strong></p><p><strong>Volume：Volume是Pod中能够被多个容器访问的共享目录，Kubernetes中的Volume是定义在Pod上，可以被一个或多个Pod中的容器挂载到某个目录下。</strong></p><p><strong>Namespace：Namespace用于实现多租户的资源隔离，可将集群内部的资源对象分配到不同的Namespace中，形成逻辑上的不同项目、小组或用户组，便于不同的Namespace在共享使用整个集群的资源的同时还能被分别管理。</strong></p><h2 id="简述Kubernetes集群相关组件？"><a href="#简述Kubernetes集群相关组件？" class="headerlink" title="简述Kubernetes集群相关组件？"></a><strong>简述Kubernetes集群相关组件？</strong></h2><p><strong>Kubernetes Master控制组件，调度管理整个系统（集群），包含如下组件:</strong></p><p><strong>Kubernetes API Server：作为Kubernetes系统的入口，其封装了核心对象的增删改查操作，以RESTful API接口方式提供给外部客户和内部组件调用，集群内各个功能模块之间数据交互和通信的中心枢纽。</strong></p><p><strong>Kubernetes Scheduler：为新建立的Pod进行节点(node)选择(即分配机器)，负责集群的资源调度。</strong></p><p><strong>Kubernetes Controller：负责执行各种控制器，目前已经提供了很多控制器来保证Kubernetes的正常运行。</strong></p><p><strong>Replication Controller：管理维护Replication Controller，关联Replication Controller和Pod，保证Replication Controller定义的副本数量与实际运行Pod数量一致。</strong></p><p><strong>Node Controller：管理维护Node，定期检查Node的健康状态，标识出(失效|未失效)的Node节点。</strong></p><p><strong>Namespace Controller：管理维护Namespace，定期清理无效的Namespace，包括Namesapce下的API对象，比如Pod、Service等。</strong></p><p><strong>Service Controller：管理维护Service，提供负载以及服务代理。</strong></p><p><strong>EndPoints Controller：管理维护Endpoints，关联Service和Pod，创建Endpoints为Service的后端，当Pod发生变化时，实时更新Endpoints。</strong></p><p><strong>Service Account Controller：管理维护Service Account，为每个Namespace创建默认的Service Account，同时为Service Account创建Service Account Secret。</strong></p><p><strong>Persistent Volume Controller：管理维护Persistent Volume和Persistent Volume Claim，为新的Persistent Volume Claim分配Persistent Volume进行绑定，为释放的Persistent Volume执行清理回收。</strong></p><p><strong>Daemon Set Controller：管理维护Daemon Set，负责创建Daemon Pod，保证指定的Node上正常的运行Daemon Pod。</strong></p><p><strong>Deployment Controller：管理维护Deployment，关联Deployment和Replication Controller，保证运行指定数量的Pod。当Deployment更新时，控制实现Replication Controller和Pod的更新。</strong></p><p><strong>Job Controller：管理维护Job，为Jod创建一次性任务Pod，保证完成Job指定完成的任务数目</strong></p><p><strong>Pod Autoscaler Controller：实现Pod的自动伸缩，定时获取监控数据，进行策略匹配，当满足条件时执行Pod的伸缩动作。</strong></p><h2 id="简述Kubernetes-RC的机制？"><a href="#简述Kubernetes-RC的机制？" class="headerlink" title="简述Kubernetes RC的机制？"></a><strong>简述Kubernetes RC的机制？</strong></h2><p><strong>Replication Controller用来管理Pod的副本，保证集群中存在指定数量的Pod副本。当定义了RC并提交至Kubernetes集群中之后，Master节点上的Controller Manager组件获悉，并同时巡检系统中当前存活的目标Pod，并确保目标Pod实例的数量刚好等于此RC的期望值，若存在过多的Pod副本在运行，系统会停止一些Pod，反之则自动创建一些Pod。</strong></p><p><strong>简述Kubernetes Replica Set 和 Replication Controller 之间有什么区别？</strong></p><p><strong>Replica Set 和 Replication Controller 类似，都是确保在任何给定时间运行指定数量的 Pod 副本。不同之处在于RS 使用基于集合的选择器，而 Replication Controller 使用基于权限的选择器。</strong></p><h2 id="简述kube-proxy作用？"><a href="#简述kube-proxy作用？" class="headerlink" title="简述kube-proxy作用？"></a><strong>简述kube-proxy作用？</strong></h2><p><strong>kube-proxy 运行在所有节点上，它监听 apiserver 中 service 和 endpoint 的变化情况，创建路由规则以提供服务 IP 和负载均衡功能。简单理解此进程是Service的透明代理兼负载均衡器，其核心功能是将到某个Service的访问请求转发到后端的多个Pod实例上。</strong></p><h2 id="简述kube-proxy-iptables原理？"><a href="#简述kube-proxy-iptables原理？" class="headerlink" title="简述kube-proxy iptables原理？"></a><strong>简述kube-proxy iptables原理？</strong></h2><p><strong>Kubernetes从1.2版本开始，将iptables作为kube-proxy的默认模式。iptables模式下的kube-proxy不再起到Proxy的作用，其核心功能：通过API Server的Watch接口实时跟踪Service与Endpoint的变更信息，并更新对应的iptables规则，Client的请求流量则通过iptables的NAT机制“直接路由”到目标Pod。</strong></p><h2 id="简述kube-proxy-ipvs原理？"><a href="#简述kube-proxy-ipvs原理？" class="headerlink" title="简述kube-proxy ipvs原理？"></a><strong>简述kube-proxy ipvs原理？</strong></h2><p><strong>IPVS在Kubernetes1.11中升级为GA稳定版。IPVS则专门用于高性能负载均衡，并使用更高效的数据结构（Hash表），允许几乎无限的规模扩张，因此被kube-proxy采纳为最新模式。</strong></p><p><strong>在IPVS模式下，使用iptables的扩展ipset，而不是直接调用iptables来生成规则链。iptables规则链是一个线性的数据结构，ipset则引入了带索引的数据结构，因此当规则很多时，也可以很高效地查找和匹配。</strong></p><p><strong>可以将ipset简单理解为一个IP（段）的集合，这个集合的内容可以是IP地址、IP网段、端口等，iptables可以直接添加规则对这个“可变的集合”进行操作，这样做的好处在于可以大大减少iptables规则的数量，从而减少性能损耗。</strong></p><h2 id="简述kube-proxy-ipvs和iptables的异同？"><a href="#简述kube-proxy-ipvs和iptables的异同？" class="headerlink" title="简述kube-proxy ipvs和iptables的异同？"></a><strong>简述kube-proxy ipvs和iptables的异同？</strong></h2><p><strong>iptables与IPVS都是基于Netfilter实现的，但因为定位不同，二者有着本质的差别：iptables是为防火墙而设计的；IPVS则专门用于高性能负载均衡，并使用更高效的数据结构（Hash表），允许几乎无限的规模扩张。</strong></p><p><strong>与iptables相比，IPVS拥有以下明显优势：</strong></p><ul><li><strong>1、为大型集群提供了更好的可扩展性和性能；</strong></li><li><strong>2、支持比iptables更复杂的复制均衡算法（最小负载、最少连接、加权等）；</strong></li><li><strong>3、支持服务器健康检查和连接重试等功能；</strong></li><li><strong>4、可以动态修改ipset的集合，即使iptables的规则正在使用这个集合。</strong></li></ul><h2 id="简述Kubernetes中什么是静态Pod？"><a href="#简述Kubernetes中什么是静态Pod？" class="headerlink" title="简述Kubernetes中什么是静态Pod？"></a><strong>简述Kubernetes中什么是静态Pod？</strong></h2><p><strong>静态pod是由kubelet进行管理的仅存在于特定Node的Pod上，他们不能通过API Server进行管理，无法与ReplicationController、Deployment或者DaemonSet进行关联，并且kubelet无法对他们进行健康检查。静态Pod总是由kubelet进行创建，并且总是在kubelet所在的Node上运行。</strong></p><h2 id="简述Kubernetes中Pod可能位于的状态？"><a href="#简述Kubernetes中Pod可能位于的状态？" class="headerlink" title="简述Kubernetes中Pod可能位于的状态？"></a><strong>简述Kubernetes中Pod可能位于的状态？</strong></h2><p><strong>Pending：API Server已经创建该Pod，且Pod内还有一个或多个容器的镜像没有创建，包括正在下载镜像的过程。</strong></p><p><strong>Running：Pod内所有容器均已创建，且至少有一个容器处于运行状态、正在启动状态或正在重启状态。</strong></p><p><strong>Succeeded：Pod内所有容器均成功执行退出，且不会重启。</strong></p><p><strong>Failed：Pod内所有容器均已退出，但至少有一个容器退出为失败状态。</strong></p><p><strong>Unknown：由于某种原因无法获取该Pod状态，可能由于网络通信不畅导致。</strong></p><h2 id="简述Kubernetes创建一个Pod的主要流程？"><a href="#简述Kubernetes创建一个Pod的主要流程？" class="headerlink" title="简述Kubernetes创建一个Pod的主要流程？"></a><strong>简述Kubernetes创建一个Pod的主要流程？</strong></h2><p><strong>Kubernetes中创建一个Pod涉及多个组件之间联动，主要流程如下：</strong></p><ul><li><strong>1、客户端提交Pod的配置信息（可以是yaml文件定义的信息）到kube-apiserver。</strong></li><li><strong>2、Apiserver收到指令后，通知给controller-manager创建一个资源对象。</strong></li><li><strong>3、Controller-manager通过api-server将pod的配置信息存储到ETCD数据中心中。</strong></li><li><strong>4、Kube-scheduler检测到pod信息会开始调度预选，会先过滤掉不符合Pod资源配置要求的节点，然后开始调度调优，主要是挑选出更适合运行pod的节点，然后将pod的资源配置单发送到node节点上的kubelet组件上。</strong></li><li><strong>5、Kubelet根据scheduler发来的资源配置单运行pod，运行成功后，将pod的运行信息返回给scheduler，scheduler将返回的pod运行状况的信息存储到etcd数据中心。</strong></li></ul><h2 id="简述Kubernetes中Pod的重启策略？"><a href="#简述Kubernetes中Pod的重启策略？" class="headerlink" title="简述Kubernetes中Pod的重启策略？"></a><strong>简述Kubernetes中Pod的重启策略？</strong></h2><p><strong>Pod重启策略（RestartPolicy）应用于Pod内的所有容器，并且仅在Pod所处的Node上由kubelet进行判断和重启操作。当某个容器异常退出或者健康检查失败时，kubelet将根据RestartPolicy的设置来进行相应操作。</strong></p><p><strong>Pod的重启策略包括Always、OnFailure和Never，默认值为Always。</strong></p><ul><li><strong>Always：当容器失效时，由kubelet自动重启该容器；</strong></li><li><strong>OnFailure：当容器终止运行且退出码不为0时，由kubelet自动重启该容器；</strong></li><li><strong>Never：不论容器运行状态如何，kubelet都不会重启该容器。</strong></li></ul><p><strong>同时Pod的重启策略与控制方式关联，当前可用于管理Pod的控制器包括ReplicationController、Job、DaemonSet及直接管理kubelet管理（静态Pod）。</strong></p><p><strong>不同控制器的重启策略限制如下：</strong></p><ul><li><strong>RC和DaemonSet：必须设置为Always，需要保证该容器持续运行；</strong></li><li><strong>Job：OnFailure或Never，确保容器执行完成后不再重启；</strong></li><li><strong>kubelet：在Pod失效时重启，不论将RestartPolicy设置为何值，也不会对Pod进行健康检查。</strong></li></ul><h2 id="简述Kubernetes中Pod的健康检查方式？"><a href="#简述Kubernetes中Pod的健康检查方式？" class="headerlink" title="简述Kubernetes中Pod的健康检查方式？"></a><strong>简述Kubernetes中Pod的健康检查方式？</strong></h2><p><strong>对Pod的健康检查可以通过两类探针来检查：LivenessProbe和ReadinessProbe。</strong></p><p><strong>LivenessProbe探针：用于判断容器是否存活（running状态），如果LivenessProbe探针探测到容器不健康，则kubelet将杀掉该容器，并根据容器的重启策略做相应处理。若一个容器不包含LivenessProbe探针，kubelet认为该容器的LivenessProbe探针返回值用于是“Success”。</strong></p><p><strong>ReadineeProbe探针：用于判断容器是否启动完成（ready状态）。如果ReadinessProbe探针探测到失败，则Pod的状态将被修改。Endpoint Controller将从Service的Endpoint中删除包含该容器所在Pod的Eenpoint。</strong></p><p><strong>startupProbe探针：启动检查机制，应用一些启动缓慢的业务，避免业务长时间启动而被上面两类探针kill掉。</strong></p><h2 id="简述Kubernetes-Pod的LivenessProbe探针的常见方式？"><a href="#简述Kubernetes-Pod的LivenessProbe探针的常见方式？" class="headerlink" title="简述Kubernetes Pod的LivenessProbe探针的常见方式？"></a><strong>简述Kubernetes Pod的LivenessProbe探针的常见方式？</strong></h2><p><strong>kubelet定期执行LivenessProbe探针来诊断容器的健康状态，通常有以下三种方式：</strong></p><p><strong>ExecAction：在容器内执行一个命令，若返回码为0，则表明容器健康。</strong></p><p><strong>TCPSocketAction：通过容器的IP地址和端口号执行TCP检查，若能建立TCP连接，则表明容器健康。</strong></p><p><strong>HTTPGetAction：通过容器的IP地址、端口号及路径调用HTTP Get方法，若响应的状态码大于等于200且小于400，则表明容器健康。</strong></p><h2 id="简述Kubernetes-Pod的常见调度方式？"><a href="#简述Kubernetes-Pod的常见调度方式？" class="headerlink" title="简述Kubernetes Pod的常见调度方式？"></a><strong>简述Kubernetes Pod的常见调度方式？</strong></h2><p><strong>Kubernetes中，Pod通常是容器的载体，主要有如下常见调度方式：</strong></p><ul><li><strong>Deployment或RC：该调度策略主要功能就是自动部署一个容器应用的多份副本，以及持续监控副本的数量，在集群内始终维持用户指定的副本数量。</strong></li><li><strong>NodeSelector：定向调度，当需要手动指定将Pod调度到特定Node上，可以通过Node的标签（Label）和Pod的nodeSelector属性相匹配。</strong></li><li><strong>NodeAffinity亲和性调度：亲和性调度机制极大的扩展了Pod的调度能力，目前有两种节点亲和力表达：</strong></li><li><strong>requiredDuringSchedulingIgnoredDuringExecution：硬规则，必须满足指定的规则，调度器才可以调度Pod至Node上（类似nodeSelector，语法不同）。</strong></li><li><strong>preferredDuringSchedulingIgnoredDuringExecution：软规则，优先调度至满足的Node的节点，但不强求，多个优先级规则还可以设置权重值。</strong></li><li><strong>Taints和Tolerations（污点和容忍）：</strong></li><li><strong>Taint：使Node拒绝特定Pod运行；</strong></li><li><strong>Toleration：为Pod的属性，表示Pod能容忍（运行）标注了Taint的Node。</strong></li></ul><h2 id="简述Kubernetes初始化容器（init-container）？"><a href="#简述Kubernetes初始化容器（init-container）？" class="headerlink" title="简述Kubernetes初始化容器（init container）？"></a><strong>简述Kubernetes初始化容器（init container）？</strong></h2><p><strong>init container的运行方式与应用容器不同，它们必须先于应用容器执行完成，当设置了多个init container时，将按顺序逐个运行，并且只有前一个init container运行成功后才能运行后一个init container。当所有init container都成功运行后，Kubernetes才会初始化Pod的各种信息，并开始创建和运行应用容器。</strong></p><h2 id="简述Kubernetes-deployment升级过程？"><a href="#简述Kubernetes-deployment升级过程？" class="headerlink" title="简述Kubernetes deployment升级过程？"></a><strong>简述Kubernetes deployment升级过程？</strong></h2><ul><li><strong>初始创建Deployment时，系统创建了一个ReplicaSet，并按用户的需求创建了对应数量的Pod副本。</strong></li><li><strong>当更新Deployment时，系统创建了一个新的ReplicaSet，并将其副本数量扩展到1，然后将旧ReplicaSet缩减为2。</strong></li><li><strong>之后，系统继续按照相同的更新策略对新旧两个ReplicaSet进行逐个调整。</strong></li><li><strong>最后，新的ReplicaSet运行了对应个新版本Pod副本，旧的ReplicaSet副本数量则缩减为0。</strong></li></ul><h2 id="简述Kubernetes-deployment升级策略？"><a href="#简述Kubernetes-deployment升级策略？" class="headerlink" title="简述Kubernetes deployment升级策略？"></a><strong>简述Kubernetes deployment升级策略？</strong></h2><p><strong>在Deployment的定义中，可以通过spec.strategy指定Pod更新的策略，目前支持两种策略：Recreate（重建）和RollingUpdate（滚动更新），默认值为RollingUpdate。</strong></p><p><strong>Recreate：设置spec.strategy.type&#x3D;Recreate，表示Deployment在更新Pod时，会先杀掉所有正在运行的Pod，然后创建新的Pod。</strong></p><p><strong>RollingUpdate：设置spec.strategy.type&#x3D;RollingUpdate，表示Deployment会以滚动更新的方式来逐个更新Pod。同时，可以通过设置spec.strategy.rollingUpdate下的两个参数（maxUnavailable和maxSurge）来控制滚动更新的过程。</strong></p><h2 id="简述Kubernetes-DaemonSet类型的资源特性？"><a href="#简述Kubernetes-DaemonSet类型的资源特性？" class="headerlink" title="简述Kubernetes DaemonSet类型的资源特性？"></a><strong>简述Kubernetes DaemonSet类型的资源特性？</strong></h2><p><strong>DaemonSet资源对象会在每个Kubernetes集群中的节点上运行，并且每个节点只能运行一个pod，这是它和deployment资源对象的最大也是唯一的区别。因此，在定义yaml文件中，不支持定义replicas。</strong></p><p><strong>它的一般使用场景如下：</strong></p><ul><li><strong>在去做每个节点的日志收集工作。</strong></li><li><strong>监控每个节点的的运行状态。</strong></li></ul><h2 id="简述Kubernetes自动扩容机制？"><a href="#简述Kubernetes自动扩容机制？" class="headerlink" title="简述Kubernetes自动扩容机制？"></a><strong>简述Kubernetes自动扩容机制？</strong></h2><p><strong>Kubernetes使用Horizontal Pod Autoscaler（HPA）的控制器实现基于CPU使用率进行自动Pod扩缩容的功能。HPA控制器周期性地监测目标Pod的资源性能指标，并与HPA资源对象中的扩缩容条件进行对比，在满足条件时对Pod副本数量进行调整。</strong></p><p><strong>HPA原理</strong></p><p><strong>Kubernetes中的某个Metrics Server（Heapster或自定义Metrics Server）持续采集所有Pod副本的指标数据。HPA控制器通过Metrics Server的API（Heapster的API或聚合API）获取这些数据，基于用户定义的扩缩容规则进行计算，得到目标Pod副本数量。</strong></p><p><strong>当目标Pod副本数量与当前副本数量不同时，HPA控制器就向Pod的副本控制器（Deployment、RC或ReplicaSet）发起scale操作，调整Pod的副本数量，完成扩缩容操作。</strong></p><h2 id="简述Kubernetes-Service类型？"><a href="#简述Kubernetes-Service类型？" class="headerlink" title="简述Kubernetes Service类型？"></a><strong>简述Kubernetes Service类型？</strong></h2><p><strong>通过创建Service，可以为一组具有相同功能的容器应用提供一个统一的入口地址，并且将请求负载分发到后端的各个容器应用上。其主要类型有：</strong></p><ul><li><strong>ClusterIP：虚拟的服务IP地址，该地址用于Kubernetes集群内部的Pod访问，在Node上kube-proxy通过设置的iptables规则进行转发；</strong></li><li><strong>NodePort：使用宿主机的端口，使能够访问各Node的外部客户端通过Node的IP地址和端口号就能访问服务；</strong></li><li><strong>LoadBalancer：使用外接负载均衡器完成到服务的负载分发，需要在spec.status.loadBalancer字段指定外部负载均衡器的IP地址，通常用于公有云。</strong></li></ul><h2 id="简述Kubernetes-Service分发后端的策略？"><a href="#简述Kubernetes-Service分发后端的策略？" class="headerlink" title="简述Kubernetes Service分发后端的策略？"></a><strong>简述Kubernetes Service分发后端的策略？</strong></h2><p><strong>Service负载分发的策略有：RoundRobin和SessionAffinity</strong></p><ul><li><strong>RoundRobin：默认为轮询模式，即轮询将请求转发到后端的各个Pod上。</strong></li><li><strong>SessionAffinity：基于客户端IP地址进行会话保持的模式，即第1次将某个客户端发起的请求转发到后端的某个Pod上，之后从相同的客户端发起的请求都将被转发到后端相同的Pod上。</strong></li></ul><h2 id="简述Kubernetes-Headless-Service？"><a href="#简述Kubernetes-Headless-Service？" class="headerlink" title="简述Kubernetes Headless Service？"></a><strong>简述Kubernetes Headless Service？</strong></h2><p><strong>在某些应用场景中，若需要人为指定负载均衡器，不使用Service提供的默认负载均衡的功能，或者应用程序希望知道属于同组服务的其他实例。Kubernetes提供了Headless Service来实现这种功能，即不为Service设置ClusterIP（入口IP地址），仅通过Label Selector将后端的Pod列表返回给调用的客户端。</strong></p><h2 id="简述Kubernetes外部如何访问集群内的服务？"><a href="#简述Kubernetes外部如何访问集群内的服务？" class="headerlink" title="简述Kubernetes外部如何访问集群内的服务？"></a><strong>简述Kubernetes外部如何访问集群内的服务？</strong></h2><p><strong>对于Kubernetes，集群外的客户端默认情况，无法通过Pod的IP地址或者Service的虚拟IP地址:虚拟端口号进行访问。通常可以通过以下方式进行访问Kubernetes集群内的服务：</strong></p><p><strong>映射Pod到物理机：将Pod端口号映射到宿主机，即在Pod中采用hostPort方式，以使客户端应用能够通过物理机访问容器应用。</strong></p><p><strong>映射Service到物理机：将Service端口号映射到宿主机，即在Service中采用nodePort方式，以使客户端应用能够通过物理机访问容器应用。</strong></p><p><strong>映射Sercie到LoadBalancer：通过设置LoadBalancer映射到云服务商提供的LoadBalancer地址。这种用法仅用于在公有云服务提供商的云平台上设置Service的场景。</strong></p><h2 id="简述Kubernetes-ingress？"><a href="#简述Kubernetes-ingress？" class="headerlink" title="简述Kubernetes ingress？"></a><strong>简述Kubernetes ingress？</strong></h2><p><strong>Kubernetes的Ingress资源对象，用于将不同URL的访问请求转发到后端不同的Service，以实现HTTP层的业务路由机制。</strong></p><p><strong>Kubernetes使用了Ingress策略和Ingress Controller，两者结合并实现了一个完整的Ingress负载均衡器。使用Ingress进行负载分发时，Ingress Controller基于Ingress规则将客户端请求直接转发到Service对应的后端Endpoint（Pod）上，从而跳过kube-proxy的转发功能，kube-proxy不再起作用，全过程为：ingress controller + ingress 规则 —-&gt; services。</strong></p><p><strong>同时当Ingress Controller提供的是对外服务，则实际上实现的是边缘路由器的功能。</strong></p><h2 id="简述Kubernetes镜像的下载策略？"><a href="#简述Kubernetes镜像的下载策略？" class="headerlink" title="简述Kubernetes镜像的下载策略？"></a><strong>简述Kubernetes镜像的下载策略？</strong></h2><p><strong>K8s的镜像下载策略有三种：Always、Never、IFNotPresent。</strong></p><ul><li><strong>Always：镜像标签为latest时，总是从指定的仓库中获取镜像。</strong></li><li><strong>Never：禁止从仓库中下载镜像，也就是说只能使用本地镜像。</strong></li><li><strong>IfNotPresent：仅当本地没有对应镜像时，才从目标仓库中下载。默认的镜像下载策略是：当镜像标签是latest时，默认策略是Always；当镜像标签是自定义时（也就是标签不是latest），那么默认策略是IfNotPresent。</strong></li></ul><h2 id="简述Kubernetes的负载均衡器？"><a href="#简述Kubernetes的负载均衡器？" class="headerlink" title="简述Kubernetes的负载均衡器？"></a><strong>简述Kubernetes的负载均衡器？</strong></h2><p><strong>负载均衡器是暴露服务的最常见和标准方式之一。</strong></p><p><strong>根据工作环境使用两种类型的负载均衡器，即内部负载均衡器或外部负载均衡器。内部负载均衡器自动平衡负载并使用所需配置分配容器，而外部负载均衡器将流量从外部负载引导至后端容器。</strong></p><h2 id="简述Kubernetes各模块如何与API-Server通信？"><a href="#简述Kubernetes各模块如何与API-Server通信？" class="headerlink" title="简述Kubernetes各模块如何与API Server通信？"></a><strong>简述Kubernetes各模块如何与API Server通信？</strong></h2><p><strong>Kubernetes API Server作为集群的核心，负责集群各功能模块之间的通信。集群内的各个功能模块通过API Server将信息存入etcd，当需要获取和操作这些数据时，则通过API Server提供的REST接口（用GET、LIST或WATCH方法）来实现，从而实现各模块之间的信息交互。</strong></p><p><strong>如kubelet进程与API Server的交互：每个Node上的kubelet每隔一个时间周期，就会调用一次API Server的REST接口报告自身状态，API Server在接收到这些信息后，会将节点状态信息更新到etcd中。</strong></p><p><strong>如kube-controller-manager进程与API Server的交互：kube-controller-manager中的Node Controller模块通过API Server提供的Watch接口实时监控Node的信息，并做相应处理。</strong></p><p><strong>如kube-scheduler进程与API Server的交互：Scheduler通过API Server的Watch接口监听到新建Pod副本的信息后，会检索所有符合该Pod要求的Node列表，开始执行Pod调度逻辑，在调度成功后将Pod绑定到目标节点上。</strong></p><h2 id="简述Kubernetes-Scheduler作用及实现原理？"><a href="#简述Kubernetes-Scheduler作用及实现原理？" class="headerlink" title="简述Kubernetes Scheduler作用及实现原理？"></a><strong>简述Kubernetes Scheduler作用及实现原理？</strong></h2><p><strong>Kubernetes Scheduler是负责Pod调度的重要功能模块，Kubernetes Scheduler在整个系统中承担了“承上启下”的重要功能，“承上”是指它负责接收Controller Manager创建的新Pod，为其调度至目标Node；“启下”是指调度完成后，目标Node上的kubelet服务进程接管后继工作，负责Pod接下来生命周期。</strong></p><p><strong>Kubernetes Scheduler的作用是将待调度的Pod（API新创建的Pod、Controller Manager为补足副本而创建的Pod等）按照特定的调度算法和调度策略绑定（Binding）到集群中某个合适的Node上，并将绑定信息写入etcd中。</strong></p><p><strong>在整个调度过程中涉及三个对象，分别是待调度Pod列表、可用Node列表，以及调度算法和策略。</strong></p><p><strong>Kubernetes Scheduler通过调度算法调度为待调度Pod列表中的每个Pod从Node列表中选择一个最适合的Node来实现Pod的调度。随后，目标节点上的kubelet通过API Server监听到Kubernetes Scheduler产生的Pod绑定事件，然后获取对应的Pod清单，下载Image镜像并启动容器。</strong></p><h2 id="简述Kubernetes-Scheduler使用哪两种算法将Pod绑定到worker节点？"><a href="#简述Kubernetes-Scheduler使用哪两种算法将Pod绑定到worker节点？" class="headerlink" title="简述Kubernetes Scheduler使用哪两种算法将Pod绑定到worker节点？"></a><strong>简述Kubernetes Scheduler使用哪两种算法将Pod绑定到worker节点？</strong></h2><p><strong>Kubernetes Scheduler根据如下两种调度算法将 Pod 绑定到最合适的工作节点：</strong></p><p><strong>预选（Predicates）：输入是所有节点，输出是满足预选条件的节点。kube-scheduler根据预选策略过滤掉不满足策略的Nodes。如果某节点的资源不足或者不满足预选策略的条件则无法通过预选。如“Node的label必须与Pod的Selector一致”。</strong></p><p><strong>优选（Priorities）：输入是预选阶段筛选出的节点，优选会根据优先策略为通过预选的Nodes进行打分排名，选择得分最高的Node。例如，资源越富裕、负载越小的Node可能具有越高的排名。</strong></p><h2 id="简述Kubernetes-kubelet的作用？"><a href="#简述Kubernetes-kubelet的作用？" class="headerlink" title="简述Kubernetes kubelet的作用？"></a><strong>简述Kubernetes kubelet的作用？</strong></h2><p><strong>在Kubernetes集群中，在每个Node（又称Worker）上都会启动一个kubelet服务进程。该进程用于处理Master下发到本节点的任务，管理Pod及Pod中的容器。每个kubelet进程都会在API Server上注册节点自身的信息，定期向Master汇报节点资源的使用情况，并通过cAdvisor监控容器和节点资源。</strong></p><h2 id="简述Kubernetes-kubelet监控Worker节点资源是使用什么组件来实现的？"><a href="#简述Kubernetes-kubelet监控Worker节点资源是使用什么组件来实现的？" class="headerlink" title="简述Kubernetes kubelet监控Worker节点资源是使用什么组件来实现的？"></a><strong>简述Kubernetes kubelet监控Worker节点资源是使用什么组件来实现的？</strong></h2><p><strong>kubelet使用cAdvisor对worker节点资源进行监控。在 Kubernetes 系统中，cAdvisor 已被默认集成到 kubelet 组件内，当 kubelet 服务启动时，它会自动启动 cAdvisor 服务，然后 cAdvisor 会实时采集所在节点的性能指标及在节点上运行的容器的性能指标。</strong></p><h2 id="简述Kubernetes如何保证集群的安全性？"><a href="#简述Kubernetes如何保证集群的安全性？" class="headerlink" title="简述Kubernetes如何保证集群的安全性？"></a><strong>简述Kubernetes如何保证集群的安全性？</strong></h2><p><strong>Kubernetes通过一系列机制来实现集群的安全控制，主要有如下不同的维度：</strong></p><ul><li><p><strong>基础设施方面：保证容器与其所在宿主机的隔离；</strong></p></li><li><p><strong>权限方面：</strong></p></li><li><ul><li><strong>最小权限原则：合理限制所有组件的权限，确保组件只执行它被授权的行为，通过限制单个组件的能力来限制它的权限范围。</strong></li><li><strong>用户权限：划分普通用户和管理员的角色。</strong></li></ul></li><li><p><strong>集群方面：</strong></p></li><li><ul><li><strong>API Server的认证授权：Kubernetes集群中所有资源的访问和变更都是通过Kubernetes API Server来实现的，因此需要建议采用更安全的HTTPS或Token来识别和认证客户端身份（Authentication），以及随后访问权限的授权（Authorization）环节。</strong></li><li><strong>API Server的授权管理：通过授权策略来决定一个API调用是否合法。对合法用户进行授权并且随后在用户访问时进行鉴权，建议采用更安全的RBAC方式来提升集群安全授权。</strong></li><li><strong>敏感数据引入Secret机制：对于集群敏感数据建议使用Secret方式进行保护。</strong></li><li><strong>AdmissionControl（准入机制）：对kubernetes api的请求过程中，顺序为：先经过认证 &amp; 授权，然后执行准入操作，最后对目标对象进行操作。</strong></li></ul></li></ul><h2 id="简述Kubernetes准入机制？"><a href="#简述Kubernetes准入机制？" class="headerlink" title="简述Kubernetes准入机制？"></a><strong>简述Kubernetes准入机制？</strong></h2><p><strong>在对集群进行请求时，每个准入控制代码都按照一定顺序执行。如果有一个准入控制拒绝了此次请求，那么整个请求的结果将会立即返回，并提示用户相应的error信息。</strong></p><p><strong>准入控制（AdmissionControl）准入控制本质上为一段准入代码，在对kubernetes api的请求过程中，顺序为：先经过认证 &amp; 授权，然后执行准入操作，最后对目标对象进行操作。常用组件（控制代码）如下：</strong></p><ul><li><strong>AlwaysAdmit：允许所有请求</strong></li><li><strong>AlwaysDeny：禁止所有请求，多用于测试环境。</strong></li><li><strong>ServiceAccount：它将serviceAccounts实现了自动化，它会辅助serviceAccount做一些事情，比如如果pod没有serviceAccount属性，它会自动添加一个default，并确保pod的serviceAccount始终存在。</strong></li><li><strong>LimitRanger：观察所有的请求，确保没有违反已经定义好的约束条件，这些条件定义在namespace中LimitRange对象中。</strong></li><li><strong>NamespaceExists：观察所有的请求，如果请求尝试创建一个不存在的namespace，则这个请求被拒绝。</strong></li></ul><h2 id="简述Kubernetes-RBAC及其特点（优势）？"><a href="#简述Kubernetes-RBAC及其特点（优势）？" class="headerlink" title="简述Kubernetes RBAC及其特点（优势）？"></a><strong>简述Kubernetes RBAC及其特点（优势）？</strong></h2><p><strong>RBAC是基于角色的访问控制，是一种基于个人用户的角色来管理对计算机或网络资源的访问的方法。</strong></p><p><strong>相对于其他授权模式，RBAC具有如下优势：</strong></p><ol><li><strong>对集群中的资源和非资源权限均有完整的覆盖。</strong></li><li><strong>整个RBAC完全由几个API对象完成， 同其他API对象一样， 可以用kubectl或API进行操作。</strong></li><li><strong>可以在运行时进行调整，无须重新启动API Server。</strong></li></ol><h2 id="简述Kubernetes-Secret作用？"><a href="#简述Kubernetes-Secret作用？" class="headerlink" title="简述Kubernetes Secret作用？"></a><strong>简述Kubernetes Secret作用？</strong></h2><p><strong>Secret对象，主要作用是保管私密数据，比如密码、OAuth Tokens、SSH Keys等信息。将这些私密信息放在Secret对象中比直接放在Pod或Docker Image中更安全，也更便于使用和分发。</strong></p><h2 id="简述Kubernetes-Secret有哪些使用方式？"><a href="#简述Kubernetes-Secret有哪些使用方式？" class="headerlink" title="简述Kubernetes Secret有哪些使用方式？"></a><strong>简述Kubernetes Secret有哪些使用方式？</strong></h2><p><strong>创建完secret之后，可通过如下三种方式使用：</strong></p><ol><li><strong>在创建Pod时，通过为Pod指定Service Account来自动使用该Secret。</strong></li><li><strong>通过挂载该Secret到Pod来使用它。</strong></li><li><strong>在Docker镜像下载时使用，通过指定Pod的spc.ImagePullSecrets来引用它。</strong></li></ol><h2 id="简述Kubernetes-PodSecurityPolicy机制？"><a href="#简述Kubernetes-PodSecurityPolicy机制？" class="headerlink" title="简述Kubernetes PodSecurityPolicy机制？"></a><strong>简述Kubernetes PodSecurityPolicy机制？</strong></h2><p><strong>Kubernetes PodSecurityPolicy是为了更精细地控制Pod对资源的使用方式以及提升安全策略。在开启PodSecurityPolicy准入控制器后，Kubernetes默认不允许创建任何Pod，需要创建PodSecurityPolicy策略和相应的RBAC授权策略（Authorizing Policies），Pod才能创建成功。</strong></p><h2 id="简述Kubernetes-PodSecurityPolicy机制能实现哪些安全策略？"><a href="#简述Kubernetes-PodSecurityPolicy机制能实现哪些安全策略？" class="headerlink" title="简述Kubernetes PodSecurityPolicy机制能实现哪些安全策略？"></a><strong>简述Kubernetes PodSecurityPolicy机制能实现哪些安全策略？</strong></h2><p><strong>在PodSecurityPolicy对象中可以设置不同字段来控制Pod运行时的各种安全策略，常见的有：</strong></p><ul><li><strong>特权模式：privileged是否允许Pod以特权模式运行。</strong></li><li><strong>宿主机资源：控制Pod对宿主机资源的控制，如hostPID：是否允许Pod共享宿主机的进程空间。</strong></li><li><strong>用户和组：设置运行容器的用户ID（范围）或组（范围）。</strong></li><li><strong>提升权限：AllowPrivilegeEscalation：设置容器内的子进程是否可以提升权限，通常在设置非root用户（MustRunAsNonRoot）时进行设置。</strong></li><li><strong>SELinux：进行SELinux的相关配置。</strong></li></ul><h2 id="简述Kubernetes网络模型？"><a href="#简述Kubernetes网络模型？" class="headerlink" title="简述Kubernetes网络模型？"></a><strong>简述Kubernetes网络模型？</strong></h2><p><strong>Kubernetes网络模型中每个Pod都拥有一个独立的IP地址，并假定所有Pod都在一个可以直接连通的、扁平的网络空间中。所以不管它们是否运行在同一个Node（宿主机）中，都要求它们可以直接通过对方的IP进行访问。设计这个原则的原因是，用户不需要额外考虑如何建立Pod之间的连接，也不需要考虑如何将容器端口映射到主机端口等问题。</strong></p><p><strong>同时为每个Pod都设置一个IP地址的模型使得同一个Pod内的不同容器会共享同一个网络命名空间，也就是同一个Linux网络协议栈。这就意味着同一个Pod内的容器可以通过localhost来连接对方的端口。</strong></p><p><strong>在Kubernetes的集群里，IP是以Pod为单位进行分配的。一个Pod内部的所有容器共享一个网络堆栈（相当于一个网络命名空间，它们的IP地址、网络设备、配置等都是共享的）。</strong></p><h2 id="简述Kubernetes-CNI模型？"><a href="#简述Kubernetes-CNI模型？" class="headerlink" title="简述Kubernetes CNI模型？"></a><strong>简述Kubernetes CNI模型？</strong></h2><p><strong>CNI提供了一种应用容器的插件化网络解决方案，定义对容器网络进行操作和配置的规范，通过插件的形式对CNI接口进行实现。CNI仅关注在创建容器时分配网络资源，和在销毁容器时删除网络资源。在CNI模型中只涉及两个概念：容器和网络。</strong></p><p><strong>容器（Container）：是拥有独立Linux网络命名空间的环境，例如使用Docker或rkt创建的容器。容器需要拥有自己的Linux网络命名空间，这是加入网络的必要条件。</strong></p><p><strong>网络（Network）：表示可以互连的一组实体，这些实体拥有各自独立、唯一的IP地址，可以是容器、物理机或者其他网络设备（比如路由器）等。</strong></p><p><strong>对容器网络的设置和操作都通过插件（Plugin）进行具体实现，CNI插件包括两种类型：CNI Plugin和IPAM（IP Address Management）Plugin。CNI Plugin负责为容器配置网络资源，IPAM Plugin负责对容器的IP地址进行分配和管理。IPAM Plugin作为CNI Plugin的一部分，与CNI Plugin协同工作。</strong></p><h2 id="简述Kubernetes网络策略？"><a href="#简述Kubernetes网络策略？" class="headerlink" title="简述Kubernetes网络策略？"></a><strong>简述Kubernetes网络策略？</strong></h2><p><strong>为实现细粒度的容器间网络访问隔离策略，Kubernetes引入Network Policy。</strong></p><p><strong>Network Policy的主要功能是对Pod间的网络通信进行限制和准入控制，设置允许访问或禁止访问的客户端Pod列表。Network Policy定义网络策略，配合策略控制器（Policy Controller）进行策略的实现。</strong></p><h2 id="简述Kubernetes网络策略原理？"><a href="#简述Kubernetes网络策略原理？" class="headerlink" title="简述Kubernetes网络策略原理？"></a><strong>简述Kubernetes网络策略原理？</strong></h2><p><strong>Network Policy的工作原理主要为：policy controller需要实现一个API Listener，监听用户设置的Network Policy定义，并将网络访问规则通过各Node的Agent进行实际设置（Agent则需要通过CNI网络插件实现）。</strong></p><h2 id="简述Kubernetes中flannel的作用？"><a href="#简述Kubernetes中flannel的作用？" class="headerlink" title="简述Kubernetes中flannel的作用？"></a><strong>简述Kubernetes中flannel的作用？</strong></h2><p><strong>Flannel可以用于Kubernetes底层网络的实现，主要作用有：</strong></p><ul><li><strong>它能协助Kubernetes，给每一个Node上的Docker容器都分配互相不冲突的IP地址。</strong></li><li><strong>它能在这些IP地址之间建立一个覆盖网络（Overlay Network），通过这个覆盖网络，将数据包原封不动地传递到目标容器内。</strong></li></ul><h2 id="简述Kubernetes-Calico网络组件实现原理？"><a href="#简述Kubernetes-Calico网络组件实现原理？" class="headerlink" title="简述Kubernetes Calico网络组件实现原理？"></a><strong>简述Kubernetes Calico网络组件实现原理？</strong></h2><p><strong>Calico是一个基于BGP的纯三层的网络方案，与OpenStack、Kubernetes、AWS、GCE等云平台都能够良好地集成。</strong></p><p><strong>Calico在每个计算节点都利用Linux Kernel实现了一个高效的vRouter来负责数据转发。每个vRouter都通过BGP协议把在本节点上运行的容器的路由信息向整个Calico网络广播，并自动设置到达其他节点的路由转发规则。</strong></p><p><strong>Calico保证所有容器之间的数据流量都是通过IP路由的方式完成互联互通的。Calico节点组网时可以直接利用数据中心的网络结构（L2或者L3），不需要额外的NAT、隧道或者Overlay Network，没有额外的封包解包，能够节约CPU运算，提高网络效率。</strong></p><h2 id="简述Kubernetes共享存储的作用？"><a href="#简述Kubernetes共享存储的作用？" class="headerlink" title="简述Kubernetes共享存储的作用？"></a><strong>简述Kubernetes共享存储的作用？</strong></h2><p><strong>Kubernetes对于有状态的容器应用或者对数据需要持久化的应用，因此需要更加可靠的存储来保存应用产生的重要数据，以便容器应用在重建之后仍然可以使用之前的数据。因此需要使用共享存储。</strong></p><h2 id="简述Kubernetes数据持久化的方式有哪些？"><a href="#简述Kubernetes数据持久化的方式有哪些？" class="headerlink" title="简述Kubernetes数据持久化的方式有哪些？"></a><strong>简述Kubernetes数据持久化的方式有哪些？</strong></h2><p><strong>Kubernetes 通过数据持久化来持久化保存重要数据，常见的方式有：</strong></p><p><strong>EmptyDir（空目录）：没有指定要挂载宿主机上的某个目录，直接由Pod内保部映射到宿主机上。类似于docker中的manager volume。</strong></p><ul><li><p><strong>场景：</strong></p></li><li><ul><li><strong>只需要临时将数据保存在磁盘上，比如在合并&#x2F;排序算法中；</strong></li><li><strong>作为两个容器的共享存储。</strong></li></ul></li><li><p><strong>特性：</strong></p></li><li><ul><li><strong>同个pod里面的不同容器，共享同一个持久化目录，当pod节点删除时，volume的数据也会被删除。</strong></li><li><strong>emptyDir的数据持久化的生命周期和使用的pod一致，一般是作为临时存储使用。</strong></li></ul></li></ul><p><strong>Hostpath：将宿主机上已存在的目录或文件挂载到容器内部。类似于docker中的bind mount挂载方式。</strong></p><ul><li><strong>特性：增加了pod与节点之间的耦合。</strong></li></ul><p><strong>PersistentVolume（简称PV）：如基于NFS服务的PV，也可以基于GFS的PV。它的作用是统一数据持久化目录，方便管理。</strong></p><h2 id="简述Kubernetes-PV和PVC？"><a href="#简述Kubernetes-PV和PVC？" class="headerlink" title="简述Kubernetes PV和PVC？"></a><strong>简述Kubernetes PV和PVC？</strong></h2><p><strong>PV是对底层网络共享存储的抽象，将共享存储定义为一种“资源”。</strong></p><p><strong>PVC则是用户对存储资源的一个“申请”。</strong></p><h2 id="简述Kubernetes-PV生命周期内的阶段？"><a href="#简述Kubernetes-PV生命周期内的阶段？" class="headerlink" title="简述Kubernetes PV生命周期内的阶段？"></a><strong>简述Kubernetes PV生命周期内的阶段？</strong></h2><p><strong>某个PV在生命周期中可能处于以下4个阶段（Phaes）之一。</strong></p><ul><li><strong>Available：可用状态，还未与某个PVC绑定。</strong></li><li><strong>Bound：已与某个PVC绑定。</strong></li><li><strong>Released：绑定的PVC已经删除，资源已释放，但没有被集群回收。</strong></li><li><strong>Failed：自动资源回收失败。</strong></li></ul><h2 id="简述Kubernetes所支持的存储供应模式？"><a href="#简述Kubernetes所支持的存储供应模式？" class="headerlink" title="简述Kubernetes所支持的存储供应模式？"></a><strong>简述Kubernetes所支持的存储供应模式？</strong></h2><p><strong>Kubernetes支持两种资源的存储供应模式：静态模式（Static）和动态模式（Dynamic）。</strong></p><p><strong>静态模式：集群管理员手工创建许多PV，在定义PV时需要将后端存储的特性进行设置。</strong></p><p><strong>动态模式：集群管理员无须手工创建PV，而是通过StorageClass的设置对后端存储进行描述，标记为某种类型。此时要求PVC对存储的类型进行声明，系统将自动完成PV的创建及与PVC的绑定。</strong></p><h2 id="简述Kubernetes-CSI模型？"><a href="#简述Kubernetes-CSI模型？" class="headerlink" title="简述Kubernetes CSI模型？"></a><strong>简述Kubernetes CSI模型？</strong></h2><p><strong>Kubernetes CSI是Kubernetes推出与容器对接的存储接口标准，存储提供方只需要基于标准接口进行存储插件的实现，就能使用Kubernetes的原生存储机制为容器提供存储服务。CSI使得存储提供方的代码能和Kubernetes代码彻底解耦，部署也与Kubernetes核心组件分离，显然，存储插件的开发由提供方自行维护，就能为Kubernetes用户提供更多的存储功能，也更加安全可靠。</strong></p><p><strong>CSI包括CSI Controller和CSI Node：</strong></p><ul><li><strong>CSI Controller的主要功能是提供存储服务视角对存储资源和存储卷进行管理和操作。</strong></li><li><strong>CSI Node的主要功能是对主机（Node）上的Volume进行管理和操作。</strong></li></ul><h2 id="简述Kubernetes-Worker节点加入集群的过程？"><a href="#简述Kubernetes-Worker节点加入集群的过程？" class="headerlink" title="简述Kubernetes Worker节点加入集群的过程？"></a><strong>简述Kubernetes Worker节点加入集群的过程？</strong></h2><p><strong>通常需要对Worker节点进行扩容，从而将应用系统进行水平扩展。主要过程如下：</strong></p><ul><li><strong>1、在该Node上安装Docker、kubelet和kube-proxy服务；</strong></li><li><strong>2、然后配置kubelet和kubeproxy的启动参数，将Master URL指定为当前Kubernetes集群Master的地址，最后启动这些服务；</strong></li><li><strong>3、通过kubelet默认的自动注册机制，新的Worker将会自动加入现有的Kubernetes集群中；</strong></li><li><strong>4、Kubernetes Master在接受了新Worker的注册之后，会自动将其纳入当前集群的调度范围。</strong></li></ul><h2 id="简述Kubernetes-Pod如何实现对节点的资源控制？"><a href="#简述Kubernetes-Pod如何实现对节点的资源控制？" class="headerlink" title="简述Kubernetes Pod如何实现对节点的资源控制？"></a><strong>简述Kubernetes Pod如何实现对节点的资源控制？</strong></h2><p><strong>Kubernetes集群里的节点提供的资源主要是计算资源，计算资源是可计量的能被申请、分配和使用的基础资源。当前Kubernetes集群中的计算资源主要包括CPU、GPU及Memory。CPU与Memory是被Pod使用的，因此在配置Pod时可以通过参数CPU Request及Memory Request为其中的每个容器指定所需使用的CPU与Memory量，Kubernetes会根据Request的值去查找有足够资源的Node来调度此Pod。</strong></p><p><strong>通常，一个程序所使用的CPU与Memory是一个动态的量，确切地说，是一个范围，跟它的负载密切相关：负载增加时，CPU和Memory的使用量也会增加。</strong></p><h2 id="简述Kubernetes-Requests和Limits如何影响Pod的调度？"><a href="#简述Kubernetes-Requests和Limits如何影响Pod的调度？" class="headerlink" title="简述Kubernetes Requests和Limits如何影响Pod的调度？"></a><strong>简述Kubernetes Requests和Limits如何影响Pod的调度？</strong></h2><p><strong>当一个Pod创建成功时，Kubernetes调度器（Scheduler）会为该Pod选择一个节点来执行。对于每种计算资源（CPU和Memory）而言，每个节点都有一个能用于运行Pod的最大容量值。调度器在调度时，首先要确保调度后该节点上所有Pod的CPU和内存的Requests总和，不超过该节点能提供给Pod使用的CPU和Memory的最大容量值。</strong></p><h2 id="简述Kubernetes-Metric-Service？"><a href="#简述Kubernetes-Metric-Service？" class="headerlink" title="简述Kubernetes Metric Service？"></a><strong>简述Kubernetes Metric Service？</strong></h2><p><strong>在Kubernetes从1.10版本后采用Metrics Server作为默认的性能数据采集和监控，主要用于提供核心指标（Core Metrics），包括Node、Pod的CPU和内存使用指标。</strong></p><p><strong>对其他自定义指标（Custom Metrics）的监控则由Prometheus等组件来完成。</strong></p><h2 id="简述Kubernetes中，如何使用EFK实现日志的统一管理？"><a href="#简述Kubernetes中，如何使用EFK实现日志的统一管理？" class="headerlink" title="简述Kubernetes中，如何使用EFK实现日志的统一管理？"></a><strong>简述Kubernetes中，如何使用EFK实现日志的统一管理？</strong></h2><p><strong>在Kubernetes集群环境中，通常一个完整的应用或服务涉及组件过多，建议对日志系统进行集中化管理，通常采用EFK实现。</strong></p><p><strong>EFK是 Elasticsearch、Fluentd 和 Kibana 的组合，其各组件功能如下：</strong></p><ul><li><strong>Elasticsearch：是一个搜索引擎，负责存储日志并提供查询接口；</strong></li><li><strong>Fluentd：负责从 Kubernetes 搜集日志，每个node节点上面的fluentd监控并收集该节点上面的系统日志，并将处理过后的日志信息发送给Elasticsearch；</strong></li><li><strong>Kibana：提供了一个 Web GUI，用户可以浏览和搜索存储在 Elasticsearch 中的日志。</strong></li></ul><p><strong>通过在每台node上部署一个以DaemonSet方式运行的fluentd来收集每台node上的日志。Fluentd将docker日志目录&#x2F;var&#x2F;lib&#x2F;docker&#x2F;containers和&#x2F;var&#x2F;log目录挂载到Pod中，然后Pod会在node节点的&#x2F;var&#x2F;log&#x2F;pods目录中创建新的目录，可以区别不同的容器日志输出，该目录下有一个日志文件链接到&#x2F;var&#x2F;lib&#x2F;docker&#x2F;contianers目录下的容器日志输出。</strong></p><h2 id="简述Kubernetes如何进行优雅的节点关机维护？"><a href="#简述Kubernetes如何进行优雅的节点关机维护？" class="headerlink" title="简述Kubernetes如何进行优雅的节点关机维护？"></a><strong>简述Kubernetes如何进行优雅的节点关机维护？</strong></h2><p><strong>由于Kubernetes节点运行大量Pod，因此在进行关机维护之前，建议先使用kubectl drain将该节点的Pod进行驱逐，然后进行关机维护。</strong></p><h2 id="简述Kubernetes集群联邦？"><a href="#简述Kubernetes集群联邦？" class="headerlink" title="简述Kubernetes集群联邦？"></a><strong>简述Kubernetes集群联邦？</strong></h2><p><strong>Kubernetes集群联邦可以将多个Kubernetes集群作为一个集群进行管理。因此，可以在一个数据中心&#x2F;云中创建多个Kubernetes集群，并使用集群联邦在一个地方控制&#x2F;管理所有集群。</strong></p><h2 id="简述Helm及其优势？"><a href="#简述Helm及其优势？" class="headerlink" title="简述Helm及其优势？"></a><strong>简述Helm及其优势？</strong></h2><p><strong>Helm 是 Kubernetes 的软件包管理工具。类似 Ubuntu 中使用的apt、Centos中使用的yum 或者Python中的 pip 一样。</strong></p><p><strong>Helm能够将一组K8S资源打包统一管理, 是查找、共享和使用为Kubernetes构建的软件的最佳方式。</strong></p><p><strong>Helm中通常每个包称为一个Chart，一个Chart是一个目录（一般情况下会将目录进行打包压缩，形成name-version.tgz格式的单一文件，方便传输和存储）。</strong></p><h3 id="Helm优势"><a href="#Helm优势" class="headerlink" title="Helm优势"></a><strong>Helm优势</strong></h3><p><strong>在 Kubernetes中部署一个可以使用的应用，需要涉及到很多的 Kubernetes 资源的共同协作。使用helm则具有如下优势：</strong></p><ul><li><strong>统一管理、配置和更新这些分散的 k8s 的应用资源文件；</strong></li><li><strong>分发和复用一套应用模板；</strong></li><li><strong>将应用的一系列资源当做一个软件包管理。</strong></li><li><strong>对于应用发布者而言，可以通过 Helm 打包应用、管理应用依赖关系、管理应用版本并发布应用到软件仓库。</strong></li><li><strong>对于使用者而言，使用 Helm 后不用需要编写复杂的应用部署文件，可以以简单的方式在 Kubernetes 上查找、安装、升级、回滚、卸载应用程序。</strong></li></ul>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>2024-2-22 k8s面试宝典</title>
    <link href="/2024/12/05/2024-2-22%20k8s%E9%9D%A2%E8%AF%95%E5%AE%9D%E5%85%B8/"/>
    <url>/2024/12/05/2024-2-22%20k8s%E9%9D%A2%E8%AF%95%E5%AE%9D%E5%85%B8/</url>
    
    <content type="html"><![CDATA[<h2 id="创建-Pod的主要流程"><a href="#创建-Pod的主要流程" class="headerlink" title="创建 Pod的主要流程?"></a>创建 Pod的主要流程?</h2><ol><li><h5 id="客户端提交-Pod-的配置信息-可以是-yaml-文件定义的信息-到-kube-apiserver"><a href="#客户端提交-Pod-的配置信息-可以是-yaml-文件定义的信息-到-kube-apiserver" class="headerlink" title="客户端提交 Pod 的配置信息(可以是 yaml 文件定义的信息)到 kube-apiserver."></a>客户端提交 Pod 的配置信息(可以是 yaml 文件定义的信息)到 kube-apiserver.</h5></li><li><h5 id="Apiserver-收到指令后-通知-controllr-manager-创建一个资源对象"><a href="#Apiserver-收到指令后-通知-controllr-manager-创建一个资源对象" class="headerlink" title="Apiserver 收到指令后,通知 controllr-manager 创建一个资源对象"></a>Apiserver 收到指令后,通知 controllr-manager 创建一个资源对象</h5></li><li><h5 id="controller-manager-通过-apiserver-将-pod-的配置信息存储到-ETCD-数据中心中"><a href="#controller-manager-通过-apiserver-将-pod-的配置信息存储到-ETCD-数据中心中" class="headerlink" title="controller-manager 通过 apiserver 将 pod 的配置信息存储到 ETCD 数据中心中"></a>controller-manager 通过 apiserver 将 pod 的配置信息存储到 ETCD 数据中心中</h5></li><li><h5 id="kube-scheduler-检查到-pod-信息会开始调度预选-会先过滤不符合-Pod-资源配置要求的节点-然后开始调度调优-主要是挑选出更适合运行的-pod-节点-然后将-pod-的资源配置单发送到-node-节点上的-kubelet-组件上"><a href="#kube-scheduler-检查到-pod-信息会开始调度预选-会先过滤不符合-Pod-资源配置要求的节点-然后开始调度调优-主要是挑选出更适合运行的-pod-节点-然后将-pod-的资源配置单发送到-node-节点上的-kubelet-组件上" class="headerlink" title="kube-scheduler 检查到 pod 信息会开始调度预选,会先过滤不符合 Pod 资源配置要求的节点,然后开始调度调优,主要是挑选出更适合运行的 pod 节点,然后将 pod 的资源配置单发送到 node 节点上的 kubelet 组件上"></a>kube-scheduler 检查到 pod 信息会开始调度预选,会先过滤不符合 Pod 资源配置要求的节点,然后开始调度调优,主要是挑选出更适合运行的 pod 节点,然后将 pod 的资源配置单发送到 node 节点上的 kubelet 组件上</h5></li><li><h5 id="kubelet-根据-scheduler-发来的资源配置单运行-pod-运行成功后-将-pod-的运行的信息返回-scheduler-scheduler-将返回的-pod-运行状况的信息存储到-etcd-数据中心"><a href="#kubelet-根据-scheduler-发来的资源配置单运行-pod-运行成功后-将-pod-的运行的信息返回-scheduler-scheduler-将返回的-pod-运行状况的信息存储到-etcd-数据中心" class="headerlink" title="kubelet 根据 scheduler 发来的资源配置单运行 pod,运行成功后,将 pod 的运行的信息返回 scheduler, scheduler 将返回的 pod 运行状况的信息存储到 etcd 数据中心"></a>kubelet 根据 scheduler 发来的资源配置单运行 pod,运行成功后,将 pod 的运行的信息返回 scheduler, scheduler 将返回的 pod 运行状况的信息存储到 etcd 数据中心</h5></li></ol><h2 id="Pod-的重启策略"><a href="#Pod-的重启策略" class="headerlink" title="Pod 的重启策略"></a>Pod 的重启策略</h2><ul><li><h5 id="•-Pod-重启策略-RestartPolicy-应用于-Pod-内的所有容器-并且仅再-Pod-所处的-Node-上由-Kubelet-进行判断和重启操作-当某个容器异常退出或健康检查失败时-kubele-将根据-RestartPolicy-的设置来进行相应操作"><a href="#•-Pod-重启策略-RestartPolicy-应用于-Pod-内的所有容器-并且仅再-Pod-所处的-Node-上由-Kubelet-进行判断和重启操作-当某个容器异常退出或健康检查失败时-kubele-将根据-RestartPolicy-的设置来进行相应操作" class="headerlink" title="• Pod 重启策略(RestartPolicy)应用于 Pod 内的所有容器,并且仅再 Pod 所处的 Node 上由 Kubelet 进行判断和重启操作.当某个容器异常退出或健康检查失败时,kubele 将根据 RestartPolicy 的设置来进行相应操作"></a>• Pod 重启策略(RestartPolicy)应用于 Pod 内的所有容器,并且仅再 Pod 所处的 Node 上由 Kubelet 进行判断和重启操作.当某个容器异常退出或健康检查失败时,kubele 将根据 RestartPolicy 的设置来进行相应操作</h5></li><li><h5 id="•-pod-的重启策略包括-Always-OnFaliure-和-Never-默认值为-Always"><a href="#•-pod-的重启策略包括-Always-OnFaliure-和-Never-默认值为-Always" class="headerlink" title="• pod 的重启策略包括 Always,OnFaliure 和 Never,默认值为 Always"></a>• pod 的重启策略包括 Always,OnFaliure 和 Never,默认值为 Always</h5></li><li><ul><li><h5 id="•-Always-当容器失效时由-kubelet-自动重启该容器"><a href="#•-Always-当容器失效时由-kubelet-自动重启该容器" class="headerlink" title="• Always: 当容器失效时由 kubelet 自动重启该容器"></a>• Always: 当容器失效时由 kubelet 自动重启该容器</h5></li><li><h5 id="•-OnFailure-当容器终止运行且退出不为-0-时-由-kubelet-自动重启该容器"><a href="#•-OnFailure-当容器终止运行且退出不为-0-时-由-kubelet-自动重启该容器" class="headerlink" title="• OnFailure:当容器终止运行且退出不为 0 时, 由 kubelet 自动重启该容器"></a>• OnFailure:当容器终止运行且退出不为 0 时, 由 kubelet 自动重启该容器</h5></li><li><h5 id="•-Nerve-不论容器运行状态如何-kubelet-都不会重启该容器"><a href="#•-Nerve-不论容器运行状态如何-kubelet-都不会重启该容器" class="headerlink" title="• Nerve: 不论容器运行状态如何,kubelet 都不会重启该容器"></a>• Nerve: 不论容器运行状态如何,kubelet 都不会重启该容器</h5></li></ul></li><li><h5 id="•-同时-pod-的容器策略与控制方式关联-当前可用于管理-Pod-的控制器包括-RelicatonController"><a href="#•-同时-pod-的容器策略与控制方式关联-当前可用于管理-Pod-的控制器包括-RelicatonController" class="headerlink" title="• 同时 pod 的容器策略与控制方式关联,当前可用于管理 Pod 的控制器包括 RelicatonController"></a>• 同时 pod 的容器策略与控制方式关联,当前可用于管理 Pod 的控制器包括 RelicatonController</h5></li></ul><h2 id="Pod-的健康检查方式"><a href="#Pod-的健康检查方式" class="headerlink" title="Pod 的健康检查方式"></a>Pod 的健康检查方式</h2><ul><li><h5 id="•-LivenessProbe探针：用于判断容器是否存活（running状态），如果LivenessProbe探针探测到容器不健康，则kubelet将杀掉该容器，并根据容器的重启策略做相应处理。若一个容器不包含LivenessProbe探针，kubelet认为该容器的LivenessProbe探针返回值用于是“Success”。"><a href="#•-LivenessProbe探针：用于判断容器是否存活（running状态），如果LivenessProbe探针探测到容器不健康，则kubelet将杀掉该容器，并根据容器的重启策略做相应处理。若一个容器不包含LivenessProbe探针，kubelet认为该容器的LivenessProbe探针返回值用于是“Success”。" class="headerlink" title="• LivenessProbe探针：用于判断容器是否存活（running状态），如果LivenessProbe探针探测到容器不健康，则kubelet将杀掉该容器，并根据容器的重启策略做相应处理。若一个容器不包含LivenessProbe探针，kubelet认为该容器的LivenessProbe探针返回值用于是“Success”。"></a>• LivenessProbe探针：用于判断容器是否存活（running状态），如果LivenessProbe探针探测到容器不健康，则kubelet将杀掉该容器，并根据容器的重启策略做相应处理。若一个容器不包含LivenessProbe探针，kubelet认为该容器的LivenessProbe探针返回值用于是“Success”。</h5></li><li><h5 id="•-ReadineeProbe探针：用于判断容器是否启动完成（ready状态）。如果ReadinessProbe探针探测到失败，则Pod的状态将被修改。Endpoint-Controller将从Service的Endpoint中删除包含该容器所在Pod的Eenpoint。"><a href="#•-ReadineeProbe探针：用于判断容器是否启动完成（ready状态）。如果ReadinessProbe探针探测到失败，则Pod的状态将被修改。Endpoint-Controller将从Service的Endpoint中删除包含该容器所在Pod的Eenpoint。" class="headerlink" title="• ReadineeProbe探针：用于判断容器是否启动完成（ready状态）。如果ReadinessProbe探针探测到失败，则Pod的状态将被修改。Endpoint Controller将从Service的Endpoint中删除包含该容器所在Pod的Eenpoint。"></a>• ReadineeProbe探针：用于判断容器是否启动完成（ready状态）。如果ReadinessProbe探针探测到失败，则Pod的状态将被修改。Endpoint Controller将从Service的Endpoint中删除包含该容器所在Pod的Eenpoint。</h5></li><li><h5 id="•-startupProbe探针：启动检查机制，应用一些启动缓慢的业务，避免业务长时间启动而被上面两类探针kill掉。"><a href="#•-startupProbe探针：启动检查机制，应用一些启动缓慢的业务，避免业务长时间启动而被上面两类探针kill掉。" class="headerlink" title="• startupProbe探针：启动检查机制，应用一些启动缓慢的业务，避免业务长时间启动而被上面两类探针kill掉。"></a>• startupProbe探针：启动检查机制，应用一些启动缓慢的业务，避免业务长时间启动而被上面两类探针kill掉。</h5></li></ul><h2 id="Pod-探针常见方式"><a href="#Pod-探针常见方式" class="headerlink" title="Pod 探针常见方式"></a>Pod 探针常见方式</h2><ul><li><h5 id="•-ExecAction：在容器内执行一个命令，若返回码为0，则表明容器健康。"><a href="#•-ExecAction：在容器内执行一个命令，若返回码为0，则表明容器健康。" class="headerlink" title="• ExecAction：在容器内执行一个命令，若返回码为0，则表明容器健康。"></a>• ExecAction：在容器内执行一个命令，若返回码为0，则表明容器健康。</h5></li><li><h5 id="•-TCPSocketAction：通过容器的IP地址和端口号执行TCP检查，若能建立TCP连接，则表明容器健康。"><a href="#•-TCPSocketAction：通过容器的IP地址和端口号执行TCP检查，若能建立TCP连接，则表明容器健康。" class="headerlink" title="• TCPSocketAction：通过容器的IP地址和端口号执行TCP检查，若能建立TCP连接，则表明容器健康。"></a>• TCPSocketAction：通过容器的IP地址和端口号执行TCP检查，若能建立TCP连接，则表明容器健康。</h5></li><li><h5 id="•-HTTPGetAction：通过容器的IP地址、端口号及路径调用HTTP-Get方法，若响应的状态码大于等于200且小于400，则表明容器健康。"><a href="#•-HTTPGetAction：通过容器的IP地址、端口号及路径调用HTTP-Get方法，若响应的状态码大于等于200且小于400，则表明容器健康。" class="headerlink" title="• HTTPGetAction：通过容器的IP地址、端口号及路径调用HTTP Get方法，若响应的状态码大于等于200且小于400，则表明容器健康。"></a>• HTTPGetAction：通过容器的IP地址、端口号及路径调用HTTP Get方法，若响应的状态码大于等于200且小于400，则表明容器健康。</h5></li></ul><h2 id="Pod-常见的调度方式"><a href="#Pod-常见的调度方式" class="headerlink" title="Pod 常见的调度方式"></a>Pod 常见的调度方式</h2><ul><li><h5 id="•-Deployment或RC：该调度策略主要功能就是自动部署一个容器应用的多份副本，以及持续监控副本的数量，在集群内始终维持用户指定的副本数量。"><a href="#•-Deployment或RC：该调度策略主要功能就是自动部署一个容器应用的多份副本，以及持续监控副本的数量，在集群内始终维持用户指定的副本数量。" class="headerlink" title="• Deployment或RC：该调度策略主要功能就是自动部署一个容器应用的多份副本，以及持续监控副本的数量，在集群内始终维持用户指定的副本数量。"></a>• Deployment或RC：该调度策略主要功能就是自动部署一个容器应用的多份副本，以及持续监控副本的数量，在集群内始终维持用户指定的副本数量。</h5></li><li><h5 id="•-NodeSelector：定向调度，当需要手动指定将Pod调度到特定Node上，可以通过Node的标签（Label）和Pod的nodeSelector属性相匹配。"><a href="#•-NodeSelector：定向调度，当需要手动指定将Pod调度到特定Node上，可以通过Node的标签（Label）和Pod的nodeSelector属性相匹配。" class="headerlink" title="• NodeSelector：定向调度，当需要手动指定将Pod调度到特定Node上，可以通过Node的标签（Label）和Pod的nodeSelector属性相匹配。"></a>• NodeSelector：定向调度，当需要手动指定将Pod调度到特定Node上，可以通过Node的标签（Label）和Pod的nodeSelector属性相匹配。</h5></li><li><h5 id="•-NodeAffinity亲和性调度：亲和性调度机制极大的扩展了Pod的调度能力，目前有两种节点亲和力表达："><a href="#•-NodeAffinity亲和性调度：亲和性调度机制极大的扩展了Pod的调度能力，目前有两种节点亲和力表达：" class="headerlink" title="• NodeAffinity亲和性调度：亲和性调度机制极大的扩展了Pod的调度能力，目前有两种节点亲和力表达："></a>• NodeAffinity亲和性调度：亲和性调度机制极大的扩展了Pod的调度能力，目前有两种节点亲和力表达：</h5></li><li><h5 id="•-requiredDuringSchedulingIgnoredDuringExecution：硬规则，必须满足指定的规则，调度器才可以调度Pod至Node上（类似nodeSelector，语法不同）。"><a href="#•-requiredDuringSchedulingIgnoredDuringExecution：硬规则，必须满足指定的规则，调度器才可以调度Pod至Node上（类似nodeSelector，语法不同）。" class="headerlink" title="• requiredDuringSchedulingIgnoredDuringExecution：硬规则，必须满足指定的规则，调度器才可以调度Pod至Node上（类似nodeSelector，语法不同）。"></a>• requiredDuringSchedulingIgnoredDuringExecution：硬规则，必须满足指定的规则，调度器才可以调度Pod至Node上（类似nodeSelector，语法不同）。</h5></li><li><h5 id="•-preferredDuringSchedulingIgnoredDuringExecution：软规则，优先调度至满足的Node的节点，但不强求，多个优先级规则还可以设置权重值。"><a href="#•-preferredDuringSchedulingIgnoredDuringExecution：软规则，优先调度至满足的Node的节点，但不强求，多个优先级规则还可以设置权重值。" class="headerlink" title="• preferredDuringSchedulingIgnoredDuringExecution：软规则，优先调度至满足的Node的节点，但不强求，多个优先级规则还可以设置权重值。"></a>• preferredDuringSchedulingIgnoredDuringExecution：软规则，优先调度至满足的Node的节点，但不强求，多个优先级规则还可以设置权重值。</h5></li><li><h5 id="•-Taints和Tolerations（污点和容忍）："><a href="#•-Taints和Tolerations（污点和容忍）：" class="headerlink" title="• Taints和Tolerations（污点和容忍）："></a>• Taints和Tolerations（污点和容忍）：</h5></li><li><ul><li><h5 id="•-Taint：使Node拒绝特定Pod运行；"><a href="#•-Taint：使Node拒绝特定Pod运行；" class="headerlink" title="• Taint：使Node拒绝特定Pod运行；"></a>• Taint：使Node拒绝特定Pod运行；</h5></li><li><h5 id="•-Toleration：为Pod的属性，表示Pod能容忍（运行）标注了Taint的Node。"><a href="#•-Toleration：为Pod的属性，表示Pod能容忍（运行）标注了Taint的Node。" class="headerlink" title="• Toleration：为Pod的属性，表示Pod能容忍（运行）标注了Taint的Node。"></a>• Toleration：为Pod的属性，表示Pod能容忍（运行）标注了Taint的Node。</h5></li></ul></li></ul><h2 id="deployment升级策略"><a href="#deployment升级策略" class="headerlink" title="deployment升级策略?"></a>deployment升级策略?</h2><ul><li><h5 id="•-在Deployment的定义中，可以通过spec-strategy指定Pod更新的策略，目前支持两种策略：Recreate（重建）和RollingUpdate（滚动更新），默认值为RollingUpdate。"><a href="#•-在Deployment的定义中，可以通过spec-strategy指定Pod更新的策略，目前支持两种策略：Recreate（重建）和RollingUpdate（滚动更新），默认值为RollingUpdate。" class="headerlink" title="• 在Deployment的定义中，可以通过spec.strategy指定Pod更新的策略，目前支持两种策略：Recreate（重建）和RollingUpdate（滚动更新），默认值为RollingUpdate。"></a>• 在Deployment的定义中，可以通过spec.strategy指定Pod更新的策略，目前支持两种策略：Recreate（重建）和RollingUpdate（滚动更新），默认值为RollingUpdate。</h5></li><li><h5 id="•-Recreate：设置spec-strategy-type-Recreate，表示Deployment在更新Pod时，会先杀掉所有正在运行的Pod，然后创建新的Pod。"><a href="#•-Recreate：设置spec-strategy-type-Recreate，表示Deployment在更新Pod时，会先杀掉所有正在运行的Pod，然后创建新的Pod。" class="headerlink" title="• Recreate：设置spec.strategy.type&#x3D;Recreate，表示Deployment在更新Pod时，会先杀掉所有正在运行的Pod，然后创建新的Pod。"></a>• Recreate：设置spec.strategy.type&#x3D;Recreate，表示Deployment在更新Pod时，会先杀掉所有正在运行的Pod，然后创建新的Pod。</h5></li><li><h5 id="•-RollingUpdate：设置spec-strategy-type-RollingUpdate，表示Deployment会以滚动更新的方式来逐个更新Pod。同时，可以通过设置spec-strategy-rollingUpdate下的两个参数（maxUnavailable和maxSurge）来控制滚动更新的过程"><a href="#•-RollingUpdate：设置spec-strategy-type-RollingUpdate，表示Deployment会以滚动更新的方式来逐个更新Pod。同时，可以通过设置spec-strategy-rollingUpdate下的两个参数（maxUnavailable和maxSurge）来控制滚动更新的过程" class="headerlink" title="• RollingUpdate：设置spec.strategy.type&#x3D;RollingUpdate，表示Deployment会以滚动更新的方式来逐个更新Pod。同时，可以通过设置spec.strategy.rollingUpdate下的两个参数（maxUnavailable和maxSurge）来控制滚动更新的过程"></a>• RollingUpdate：设置spec.strategy.type&#x3D;RollingUpdate，表示Deployment会以滚动更新的方式来逐个更新Pod。同时，可以通过设置spec.strategy.rollingUpdate下的两个参数（maxUnavailable和maxSurge）来控制滚动更新的过程</h5></li></ul><h2 id="Kubernetes-Service类型"><a href="#Kubernetes-Service类型" class="headerlink" title="Kubernetes Service类型?"></a>Kubernetes Service类型?</h2><h5 id="通过创建Service，可以为一组具有相同功能的容器应用提供一个统一的入口地址，并且将请求负载分发到后端的各个容器应用上。其主要类型有："><a href="#通过创建Service，可以为一组具有相同功能的容器应用提供一个统一的入口地址，并且将请求负载分发到后端的各个容器应用上。其主要类型有：" class="headerlink" title="通过创建Service，可以为一组具有相同功能的容器应用提供一个统一的入口地址，并且将请求负载分发到后端的各个容器应用上。其主要类型有："></a>通过创建Service，可以为一组具有相同功能的容器应用提供一个统一的入口地址，并且将请求负载分发到后端的各个容器应用上。其主要类型有：</h5><ul><li><h5 id="•-ClusterIP：虚拟的服务IP地址，该地址用于Kubernetes集群内部的Pod访问，在Node上kube-proxy通过设置的iptables规则进行转发；"><a href="#•-ClusterIP：虚拟的服务IP地址，该地址用于Kubernetes集群内部的Pod访问，在Node上kube-proxy通过设置的iptables规则进行转发；" class="headerlink" title="• ClusterIP：虚拟的服务IP地址，该地址用于Kubernetes集群内部的Pod访问，在Node上kube-proxy通过设置的iptables规则进行转发；"></a>• ClusterIP：虚拟的服务IP地址，该地址用于Kubernetes集群内部的Pod访问，在Node上kube-proxy通过设置的iptables规则进行转发；</h5></li><li><h5 id="•-NodePort：使用宿主机的端口，使能够访问各Node的外部客户端通过Node的IP地址和端口号就能访问服务；"><a href="#•-NodePort：使用宿主机的端口，使能够访问各Node的外部客户端通过Node的IP地址和端口号就能访问服务；" class="headerlink" title="• NodePort：使用宿主机的端口，使能够访问各Node的外部客户端通过Node的IP地址和端口号就能访问服务；"></a>• NodePort：使用宿主机的端口，使能够访问各Node的外部客户端通过Node的IP地址和端口号就能访问服务；</h5></li><li><h5 id="•-LoadBalancer：使用外接负载均衡器完成到服务的负载分发，需要在spec-status-loadBalancer字段指定外部负载均衡器的IP地址，通常用于公有云。"><a href="#•-LoadBalancer：使用外接负载均衡器完成到服务的负载分发，需要在spec-status-loadBalancer字段指定外部负载均衡器的IP地址，通常用于公有云。" class="headerlink" title="• LoadBalancer：使用外接负载均衡器完成到服务的负载分发，需要在spec.status.loadBalancer字段指定外部负载均衡器的IP地址，通常用于公有云。"></a>• LoadBalancer：使用外接负载均衡器完成到服务的负载分发，需要在spec.status.loadBalancer字段指定外部负载均衡器的IP地址，通常用于公有云。</h5></li></ul><h2 id="Service分发后端的策略"><a href="#Service分发后端的策略" class="headerlink" title="Service分发后端的策略?"></a>Service分发后端的策略?</h2><h5 id="Service负载分发的策略有：RoundRobin和SessionAffinity"><a href="#Service负载分发的策略有：RoundRobin和SessionAffinity" class="headerlink" title="Service负载分发的策略有：RoundRobin和SessionAffinity"></a>Service负载分发的策略有：RoundRobin和SessionAffinity</h5><ul><li><h5 id="•-RoundRobin：默认为轮询模式，即轮询将请求转发到后端的各个Pod上。"><a href="#•-RoundRobin：默认为轮询模式，即轮询将请求转发到后端的各个Pod上。" class="headerlink" title="• RoundRobin：默认为轮询模式，即轮询将请求转发到后端的各个Pod上。"></a>• RoundRobin：默认为轮询模式，即轮询将请求转发到后端的各个Pod上。</h5></li><li><h5 id="•-SessionAffinity：基于客户端IP地址进行会话保持的模式，即第1次将某个客户端发起的请求转发到后端的某个Pod上，之后从相同的客户端发起的请求都将被转发到后端相同的Pod上。"><a href="#•-SessionAffinity：基于客户端IP地址进行会话保持的模式，即第1次将某个客户端发起的请求转发到后端的某个Pod上，之后从相同的客户端发起的请求都将被转发到后端相同的Pod上。" class="headerlink" title="• SessionAffinity：基于客户端IP地址进行会话保持的模式，即第1次将某个客户端发起的请求转发到后端的某个Pod上，之后从相同的客户端发起的请求都将被转发到后端相同的Pod上。"></a>• SessionAffinity：基于客户端IP地址进行会话保持的模式，即第1次将某个客户端发起的请求转发到后端的某个Pod上，之后从相同的客户端发起的请求都将被转发到后端相同的Pod上。</h5></li></ul><h2 id="Kubernetes外部如何访问集群内的服务"><a href="#Kubernetes外部如何访问集群内的服务" class="headerlink" title="Kubernetes外部如何访问集群内的服务?"></a>Kubernetes外部如何访问集群内的服务?</h2><ul><li><h5 id="•-映射Pod到物理机：将Pod端口号映射到宿主机，即在Pod中采用hostPort方式，以使客户端应用能够通过物理机访问容器应用。"><a href="#•-映射Pod到物理机：将Pod端口号映射到宿主机，即在Pod中采用hostPort方式，以使客户端应用能够通过物理机访问容器应用。" class="headerlink" title="• 映射Pod到物理机：将Pod端口号映射到宿主机，即在Pod中采用hostPort方式，以使客户端应用能够通过物理机访问容器应用。"></a>• 映射Pod到物理机：将Pod端口号映射到宿主机，即在Pod中采用hostPort方式，以使客户端应用能够通过物理机访问容器应用。</h5></li><li><h5 id="•-映射Service到物理机：将Service端口号映射到宿主机，即在Service中采用nodePort方式，以使客户端应用能够通过物理机访问容器应用。"><a href="#•-映射Service到物理机：将Service端口号映射到宿主机，即在Service中采用nodePort方式，以使客户端应用能够通过物理机访问容器应用。" class="headerlink" title="• 映射Service到物理机：将Service端口号映射到宿主机，即在Service中采用nodePort方式，以使客户端应用能够通过物理机访问容器应用。"></a>• 映射Service到物理机：将Service端口号映射到宿主机，即在Service中采用nodePort方式，以使客户端应用能够通过物理机访问容器应用。</h5></li><li><h5 id="•-映射Sercie到LoadBalancer：通过设置LoadBalancer映射到云服务商提供的LoadBalancer地址。这种用法仅用于在公有云服务提供商的云平台上设置Service的场景。"><a href="#•-映射Sercie到LoadBalancer：通过设置LoadBalancer映射到云服务商提供的LoadBalancer地址。这种用法仅用于在公有云服务提供商的云平台上设置Service的场景。" class="headerlink" title="• 映射Sercie到LoadBalancer：通过设置LoadBalancer映射到云服务商提供的LoadBalancer地址。这种用法仅用于在公有云服务提供商的云平台上设置Service的场景。"></a>• 映射Sercie到LoadBalancer：通过设置LoadBalancer映射到云服务商提供的LoadBalancer地址。这种用法仅用于在公有云服务提供商的云平台上设置Service的场景。</h5></li></ul><h2 id="Kubernetes-ingress"><a href="#Kubernetes-ingress" class="headerlink" title="Kubernetes ingress?"></a>Kubernetes ingress?</h2><ul><li><h5 id="•-Kubernetes的Ingress资源对象，用于将不同URL的访问请求转发到后端不同的Service，以实现HTTP层的业务路由机制。"><a href="#•-Kubernetes的Ingress资源对象，用于将不同URL的访问请求转发到后端不同的Service，以实现HTTP层的业务路由机制。" class="headerlink" title="• Kubernetes的Ingress资源对象，用于将不同URL的访问请求转发到后端不同的Service，以实现HTTP层的业务路由机制。"></a>• Kubernetes的Ingress资源对象，用于将不同URL的访问请求转发到后端不同的Service，以实现HTTP层的业务路由机制。</h5></li><li><h5 id="•-Kubernetes使用了Ingress策略和Ingress-Controller，两者结合并实现了一个完整的Ingress负载均衡器。使用Ingress进行负载分发时，Ingress-Controller基于Ingress规则将客户端请求直接转发到Service对应的后端Endpoint（Pod）上，从而跳过kube-proxy的转发功能，kube-proxy不再起作用，全过程为：ingress-controller-ingress-规则-—-services。"><a href="#•-Kubernetes使用了Ingress策略和Ingress-Controller，两者结合并实现了一个完整的Ingress负载均衡器。使用Ingress进行负载分发时，Ingress-Controller基于Ingress规则将客户端请求直接转发到Service对应的后端Endpoint（Pod）上，从而跳过kube-proxy的转发功能，kube-proxy不再起作用，全过程为：ingress-controller-ingress-规则-—-services。" class="headerlink" title="• Kubernetes使用了Ingress策略和Ingress Controller，两者结合并实现了一个完整的Ingress负载均衡器。使用Ingress进行负载分发时，Ingress Controller基于Ingress规则将客户端请求直接转发到Service对应的后端Endpoint（Pod）上，从而跳过kube-proxy的转发功能，kube-proxy不再起作用，全过程为：ingress controller + ingress 规则 —-&gt; services。"></a>• Kubernetes使用了Ingress策略和Ingress Controller，两者结合并实现了一个完整的Ingress负载均衡器。使用Ingress进行负载分发时，Ingress Controller基于Ingress规则将客户端请求直接转发到Service对应的后端Endpoint（Pod）上，从而跳过kube-proxy的转发功能，kube-proxy不再起作用，全过程为：ingress controller + ingress 规则 —-&gt; services。</h5></li><li><h5 id="•-同时当Ingress-Controller提供的是对外服务，则实际上实现的是边缘路由器的功能。"><a href="#•-同时当Ingress-Controller提供的是对外服务，则实际上实现的是边缘路由器的功能。" class="headerlink" title="• 同时当Ingress Controller提供的是对外服务，则实际上实现的是边缘路由器的功能。"></a>• 同时当Ingress Controller提供的是对外服务，则实际上实现的是边缘路由器的功能。</h5></li></ul><h2 id="Kubernetes镜像的下载策略"><a href="#Kubernetes镜像的下载策略" class="headerlink" title="Kubernetes镜像的下载策略?"></a>Kubernetes镜像的下载策略?</h2><h5 id="K8s的镜像下载策略有三种：Always、Never、IFNotPresent。"><a href="#K8s的镜像下载策略有三种：Always、Never、IFNotPresent。" class="headerlink" title="K8s的镜像下载策略有三种：Always、Never、IFNotPresent。"></a>K8s的镜像下载策略有三种：Always、Never、IFNotPresent。</h5><ul><li><h5 id="•-Always：镜像标签为latest时，总是从指定的仓库中获取镜像。"><a href="#•-Always：镜像标签为latest时，总是从指定的仓库中获取镜像。" class="headerlink" title="• Always：镜像标签为latest时，总是从指定的仓库中获取镜像。"></a>• Always：镜像标签为latest时，总是从指定的仓库中获取镜像。</h5></li><li><h5 id="•-Never：禁止从仓库中下载镜像，也就是说只能使用本地镜像。"><a href="#•-Never：禁止从仓库中下载镜像，也就是说只能使用本地镜像。" class="headerlink" title="• Never：禁止从仓库中下载镜像，也就是说只能使用本地镜像。"></a>• Never：禁止从仓库中下载镜像，也就是说只能使用本地镜像。</h5></li><li><h5 id="•-IfNotPresent：仅当本地没有对应镜像时，才从目标仓库中下载。"><a href="#•-IfNotPresent：仅当本地没有对应镜像时，才从目标仓库中下载。" class="headerlink" title="• IfNotPresent：仅当本地没有对应镜像时，才从目标仓库中下载。"></a>• IfNotPresent：仅当本地没有对应镜像时，才从目标仓库中下载。</h5></li></ul><h5 id="默认的镜像下载策略是：当镜像标签是latest时，默认策略是Always；当镜像标签是自定义时（也就是标签不是latest），那么默认策略是IfNotPresent。"><a href="#默认的镜像下载策略是：当镜像标签是latest时，默认策略是Always；当镜像标签是自定义时（也就是标签不是latest），那么默认策略是IfNotPresent。" class="headerlink" title="默认的镜像下载策略是：当镜像标签是latest时，默认策略是Always；当镜像标签是自定义时（也就是标签不是latest），那么默认策略是IfNotPresent。"></a>默认的镜像下载策略是：当镜像标签是latest时，默认策略是Always；当镜像标签是自定义时（也就是标签不是latest），那么默认策略是IfNotPresent。</h5><h2 id="Kubernetes-kubelet的作用"><a href="#Kubernetes-kubelet的作用" class="headerlink" title="Kubernetes kubelet的作用?"></a>Kubernetes kubelet的作用?</h2><ul><li><h5 id="•-在Kubernetes集群中，在每个Node（又称Worker）上都会启动一个kubelet服务进程。该进程用于处理Master下发到本节点的任务，管理Pod及Pod中的容器。每个kubelet进程都会在API-Server上注册节点自身的信息，定期向Master汇报节点资源的使用情况，并通过cAdvisor监控容器和节点资源。"><a href="#•-在Kubernetes集群中，在每个Node（又称Worker）上都会启动一个kubelet服务进程。该进程用于处理Master下发到本节点的任务，管理Pod及Pod中的容器。每个kubelet进程都会在API-Server上注册节点自身的信息，定期向Master汇报节点资源的使用情况，并通过cAdvisor监控容器和节点资源。" class="headerlink" title="• 在Kubernetes集群中，在每个Node（又称Worker）上都会启动一个kubelet服务进程。该进程用于处理Master下发到本节点的任务，管理Pod及Pod中的容器。每个kubelet进程都会在API Server上注册节点自身的信息，定期向Master汇报节点资源的使用情况，并通过cAdvisor监控容器和节点资源。"></a>• 在Kubernetes集群中，在每个Node（又称Worker）上都会启动一个kubelet服务进程。该进程用于处理Master下发到本节点的任务，管理Pod及Pod中的容器。每个kubelet进程都会在API Server上注册节点自身的信息，定期向Master汇报节点资源的使用情况，并通过cAdvisor监控容器和节点资源。</h5></li></ul><h2 id="Secret有哪些使用方式"><a href="#Secret有哪些使用方式" class="headerlink" title="Secret有哪些使用方式?"></a>Secret有哪些使用方式?</h2><ul><li><h5 id="•-创建完secret之后，可通过如下三种方式使用："><a href="#•-创建完secret之后，可通过如下三种方式使用：" class="headerlink" title="• 创建完secret之后，可通过如下三种方式使用："></a>• 创建完secret之后，可通过如下三种方式使用：</h5></li><li><h5 id="•-在创建Pod时，通过为Pod指定Service-Account来自动使用该Secret。"><a href="#•-在创建Pod时，通过为Pod指定Service-Account来自动使用该Secret。" class="headerlink" title="• 在创建Pod时，通过为Pod指定Service Account来自动使用该Secret。"></a>• 在创建Pod时，通过为Pod指定Service Account来自动使用该Secret。</h5></li><li><h5 id="•-通过挂载该Secret到Pod来使用它。"><a href="#•-通过挂载该Secret到Pod来使用它。" class="headerlink" title="• 通过挂载该Secret到Pod来使用它。"></a>• 通过挂载该Secret到Pod来使用它。</h5></li><li><h5 id="•-在Docker镜像下载时使用，通过指定Pod的spc-ImagePullSecrets来引用它。"><a href="#•-在Docker镜像下载时使用，通过指定Pod的spc-ImagePullSecrets来引用它。" class="headerlink" title="• 在Docker镜像下载时使用，通过指定Pod的spc.ImagePullSecrets来引用它。"></a>• 在Docker镜像下载时使用，通过指定Pod的spc.ImagePullSecrets来引用它。</h5></li></ul><h2 id="Kubernetes-CNI模型"><a href="#Kubernetes-CNI模型" class="headerlink" title="Kubernetes CNI模型?"></a>Kubernetes CNI模型?</h2><ul><li><h5 id="•-CNI提供了一种应用容器的插件化网络解决方案，定义对容器网络进行操作和配置的规范，通过插件的形式对CNI接口进行实现。CNI仅关注在创建容器时分配网络资源，和在销毁容器时删除网络资源。在CNI模型中只涉及两个概念：容器和网络。"><a href="#•-CNI提供了一种应用容器的插件化网络解决方案，定义对容器网络进行操作和配置的规范，通过插件的形式对CNI接口进行实现。CNI仅关注在创建容器时分配网络资源，和在销毁容器时删除网络资源。在CNI模型中只涉及两个概念：容器和网络。" class="headerlink" title="• CNI提供了一种应用容器的插件化网络解决方案，定义对容器网络进行操作和配置的规范，通过插件的形式对CNI接口进行实现。CNI仅关注在创建容器时分配网络资源，和在销毁容器时删除网络资源。在CNI模型中只涉及两个概念：容器和网络。"></a>• CNI提供了一种应用容器的插件化网络解决方案，定义对容器网络进行操作和配置的规范，通过插件的形式对CNI接口进行实现。CNI仅关注在创建容器时分配网络资源，和在销毁容器时删除网络资源。在CNI模型中只涉及两个概念：容器和网络。</h5></li><li><h5 id="•-容器（Container）：是拥有独立Linux网络命名空间的环境，例如使用Docker或rkt创建的容器。容器需要拥有自己的Linux网络命名空间，这是加入网络的必要条件。"><a href="#•-容器（Container）：是拥有独立Linux网络命名空间的环境，例如使用Docker或rkt创建的容器。容器需要拥有自己的Linux网络命名空间，这是加入网络的必要条件。" class="headerlink" title="• 容器（Container）：是拥有独立Linux网络命名空间的环境，例如使用Docker或rkt创建的容器。容器需要拥有自己的Linux网络命名空间，这是加入网络的必要条件。"></a>• 容器（Container）：是拥有独立Linux网络命名空间的环境，例如使用Docker或rkt创建的容器。容器需要拥有自己的Linux网络命名空间，这是加入网络的必要条件。</h5></li><li><h5 id="•-网络（Network）：表示可以互连的一组实体，这些实体拥有各自独立、唯一的IP地址，可以是容器、物理机或者其他网络设备（比如路由器）等。"><a href="#•-网络（Network）：表示可以互连的一组实体，这些实体拥有各自独立、唯一的IP地址，可以是容器、物理机或者其他网络设备（比如路由器）等。" class="headerlink" title="• 网络（Network）：表示可以互连的一组实体，这些实体拥有各自独立、唯一的IP地址，可以是容器、物理机或者其他网络设备（比如路由器）等。"></a>• 网络（Network）：表示可以互连的一组实体，这些实体拥有各自独立、唯一的IP地址，可以是容器、物理机或者其他网络设备（比如路由器）等。</h5></li><li><h5 id="•-对容器网络的设置和操作都通过插件（Plugin）进行具体实现，CNI插件包括两种类型：CNI-Plugin和IPAM（IP-Address-Management）Plugin。CNI-Plugin负责为容器配置网络资源，IPAM-Plugin负责对容器的IP地址进行分配和管理。IPAM-Plugin作为CNI-Plugin的一部分，与CNI-Plugin协同工作。"><a href="#•-对容器网络的设置和操作都通过插件（Plugin）进行具体实现，CNI插件包括两种类型：CNI-Plugin和IPAM（IP-Address-Management）Plugin。CNI-Plugin负责为容器配置网络资源，IPAM-Plugin负责对容器的IP地址进行分配和管理。IPAM-Plugin作为CNI-Plugin的一部分，与CNI-Plugin协同工作。" class="headerlink" title="• 对容器网络的设置和操作都通过插件（Plugin）进行具体实现，CNI插件包括两种类型：CNI Plugin和IPAM（IP Address Management）Plugin。CNI Plugin负责为容器配置网络资源，IPAM Plugin负责对容器的IP地址进行分配和管理。IPAM Plugin作为CNI Plugin的一部分，与CNI Plugin协同工作。"></a>• 对容器网络的设置和操作都通过插件（Plugin）进行具体实现，CNI插件包括两种类型：CNI Plugin和IPAM（IP Address Management）Plugin。CNI Plugin负责为容器配置网络资源，IPAM Plugin负责对容器的IP地址进行分配和管理。IPAM Plugin作为CNI Plugin的一部分，与CNI Plugin协同工作。</h5></li></ul><h2 id="Kubernetes-PV和PVC"><a href="#Kubernetes-PV和PVC" class="headerlink" title="Kubernetes PV和PVC?"></a>Kubernetes PV和PVC?</h2><ul><li><h5 id="•-PV是对底层网络共享存储的抽象，将共享存储定义为一种“资源”。"><a href="#•-PV是对底层网络共享存储的抽象，将共享存储定义为一种“资源”。" class="headerlink" title="• PV是对底层网络共享存储的抽象，将共享存储定义为一种“资源”。"></a>• PV是对底层网络共享存储的抽象，将共享存储定义为一种“资源”。</h5></li><li><h5 id="•-PVC则是用户对存储资源的一个“申请”。"><a href="#•-PVC则是用户对存储资源的一个“申请”。" class="headerlink" title="• PVC则是用户对存储资源的一个“申请”。"></a>• PVC则是用户对存储资源的一个“申请”。</h5></li></ul><h2 id="PV生命周期内的阶段"><a href="#PV生命周期内的阶段" class="headerlink" title="PV生命周期内的阶段?"></a>PV生命周期内的阶段?</h2><h5 id="某个PV在生命周期中可能处于以下4个阶段（Phaes）之一。"><a href="#某个PV在生命周期中可能处于以下4个阶段（Phaes）之一。" class="headerlink" title="某个PV在生命周期中可能处于以下4个阶段（Phaes）之一。"></a>某个PV在生命周期中可能处于以下4个阶段（Phaes）之一。</h5><ul><li><h5 id="•-Available：可用状态，还未与某个PVC绑定。"><a href="#•-Available：可用状态，还未与某个PVC绑定。" class="headerlink" title="• Available：可用状态，还未与某个PVC绑定。"></a>• Available：可用状态，还未与某个PVC绑定。</h5></li><li><h5 id="•-Bound：已与某个PVC绑定。"><a href="#•-Bound：已与某个PVC绑定。" class="headerlink" title="• Bound：已与某个PVC绑定。"></a>• Bound：已与某个PVC绑定。</h5></li><li><h5 id="•-Released：绑定的PVC已经删除，资源已释放，但没有被集群回收。"><a href="#•-Released：绑定的PVC已经删除，资源已释放，但没有被集群回收。" class="headerlink" title="• Released：绑定的PVC已经删除，资源已释放，但没有被集群回收。"></a>• Released：绑定的PVC已经删除，资源已释放，但没有被集群回收。</h5></li><li><h5 id="•-Failed：自动资源回收失败。"><a href="#•-Failed：自动资源回收失败。" class="headerlink" title="• Failed：自动资源回收失败。"></a>• Failed：自动资源回收失败。</h5></li></ul><h2 id="calico-网络模式"><a href="#calico-网络模式" class="headerlink" title="calico 网络模式"></a>calico 网络模式</h2><table><thead><tr><th><strong>模式</strong></th><th><strong>数据包封包</strong></th><th><strong>优点</strong></th><th><strong>缺点</strong></th></tr></thead><tbody><tr><td>vxlan</td><td>封包， 在vxlan设备上将pod发来的数据包源、目的mac替换为本机vxlan网卡和对端节点vxlan网卡的mac。外层udp目的ip地址根据路由和对端vxlan的mac查fdb表获取</td><td>只要k8s节点间三层互通， 可以跨网段， 对主机网关路由没有特殊要求。各个node节点通过vxlan设备实现基于三层的”二层”互通, 三层即vxlan包封装在udp数据包中， 要求udp在k8s节点间三层可达；二层即vxlan封包的源mac地址和目的mac地址是自己的vxlan设备mac和对端vxlan设备mac。</td><td>需要进行vxlan的数据包封包和解包会存在一定的性能损耗</td></tr><tr><td>ipip</td><td>封包，在tunl0设备上将pod发来的数据包的mac层去掉，留下ip层封包。 外层数据包目的ip地址根据路由得到。</td><td>只要k8s节点间三层互通， 可以跨网段， 对主机网关路由没有特殊要求。</td><td>需要进行ipip的数据包封包和解包会存在一定的性能损耗</td></tr><tr><td>bgp</td><td>不需要进行数据包封包</td><td>不用封包解包，通过bgp协议可实现pod网络在主机间的三层可达， k8s节点不跨网段时和flannel的host-gw相似；</td><td></td></tr><tr><td>支持跨网段， 满足复杂的网络架构</td><td>跨网段时，需要主机网关路由也充当BGP Speaker能够学习到pod子网路由并实现pod子网路由的转发</td><td></td><td></td></tr></tbody></table><h2 id="fannel三种模式"><a href="#fannel三种模式" class="headerlink" title="fannel三种模式"></a>fannel三种模式</h2><table><thead><tr><th><strong>fannel三种模式</strong></th><th><strong>效率</strong></th><th><strong>calico 模式</strong></th></tr></thead><tbody><tr><td>UDP</td><td>性能较差，封包解包涉及到多次用户态和内核态交互</td><td>类似 IPIP</td></tr><tr><td>VXLAN</td><td>性能较好，封包解包在内核态实现，内核转发数据，flanneld负责动态配置ARP和FDB（转发数据库）表项更新</td><td>类似VXLAN</td></tr><tr><td>host-gw</td><td>性能最好，不需要再次封包，正常发包，目的容器所在的主机充当网关</td><td></td></tr><tr><td>flanneld 负责主机上路由表的刷新</td><td>类似 BGP</td><td></td></tr></tbody></table><h2 id="你知道的几种CNI网络插件，并详述其工作原理。K8s常用的CNI网络插件-（calico-flannel），简述一下它们的工作原理和区别。"><a href="#你知道的几种CNI网络插件，并详述其工作原理。K8s常用的CNI网络插件-（calico-flannel），简述一下它们的工作原理和区别。" class="headerlink" title="你知道的几种CNI网络插件，并详述其工作原理。K8s常用的CNI网络插件 （calico &amp;&amp; flannel），简述一下它们的工作原理和区别。"></a>你知道的几种CNI网络插件，并详述其工作原理。K8s常用的CNI网络插件 （calico &amp;&amp; flannel），简述一下它们的工作原理和区别。</h2><ol><li><h5 id="1-calico根据iptables规则进行路由转发，并没有进行封包，解包的过程，这和flannel比起来效率就会快多-calico包括如下重要组件：Felix，etcd，BGP-Client，BGP-Route-Reflector。下面分别说明一下这些组件。"><a href="#1-calico根据iptables规则进行路由转发，并没有进行封包，解包的过程，这和flannel比起来效率就会快多-calico包括如下重要组件：Felix，etcd，BGP-Client，BGP-Route-Reflector。下面分别说明一下这些组件。" class="headerlink" title="\1. calico根据iptables规则进行路由转发，并没有进行封包，解包的过程，这和flannel比起来效率就会快多 calico包括如下重要组件：Felix，etcd，BGP Client，BGP Route Reflector。下面分别说明一下这些组件。"></a>\1. calico根据iptables规则进行路由转发，并没有进行封包，解包的过程，这和flannel比起来效率就会快多 calico包括如下重要组件：Felix，etcd，BGP Client，BGP Route Reflector。下面分别说明一下这些组件。</h5></li></ol><h5 id="Felix：主要负责路由配置以及ACLS规则的配置以及下发，它存在在每个node节点上。-etcd：分布式键值存储，主要负责网络元数据一致性，确保Calico网络状态的准确性，可以与kubernetes共用；-BGPClient-BIRD-主要负责把-Felix写入-kernel的路由信息分发到当前-Calico网络，确保-workload间的通信的有效性；-BGPRoute-Reflector-BIRD-大规模部署时使用，摒弃所有节点互联的mesh模式，通过一个或者多个-BGPRoute-Reflector-来完成集中式的路由分发-通过将整个互联网的可扩展-IP网络原则压缩到数据中心级别，Calico在每一个计算节点利用-Linuxkernel-实现了一个高效的-vRouter来负责数据转发，而每个vRouter通过-BGP协议负责把自己上运行的-workload的路由信息向整个Calico网络内传播，小规模部署可以直接互联，大规模下可通过指定的BGProute-reflector-来完成。这样保证最终所有的workload之间的数据流量都是通过-IP包的方式完成互联的。"><a href="#Felix：主要负责路由配置以及ACLS规则的配置以及下发，它存在在每个node节点上。-etcd：分布式键值存储，主要负责网络元数据一致性，确保Calico网络状态的准确性，可以与kubernetes共用；-BGPClient-BIRD-主要负责把-Felix写入-kernel的路由信息分发到当前-Calico网络，确保-workload间的通信的有效性；-BGPRoute-Reflector-BIRD-大规模部署时使用，摒弃所有节点互联的mesh模式，通过一个或者多个-BGPRoute-Reflector-来完成集中式的路由分发-通过将整个互联网的可扩展-IP网络原则压缩到数据中心级别，Calico在每一个计算节点利用-Linuxkernel-实现了一个高效的-vRouter来负责数据转发，而每个vRouter通过-BGP协议负责把自己上运行的-workload的路由信息向整个Calico网络内传播，小规模部署可以直接互联，大规模下可通过指定的BGProute-reflector-来完成。这样保证最终所有的workload之间的数据流量都是通过-IP包的方式完成互联的。" class="headerlink" title="Felix：主要负责路由配置以及ACLS规则的配置以及下发，它存在在每个node节点上。 etcd：分布式键值存储，主要负责网络元数据一致性，确保Calico网络状态的准确性，可以与kubernetes共用； BGPClient(BIRD), 主要负责把 Felix写入 kernel的路由信息分发到当前 Calico网络，确保 workload间的通信的有效性； BGPRoute Reflector(BIRD), 大规模部署时使用，摒弃所有节点互联的mesh模式，通过一个或者多个 BGPRoute Reflector 来完成集中式的路由分发 通过将整个互联网的可扩展 IP网络原则压缩到数据中心级别，Calico在每一个计算节点利用 Linuxkernel 实现了一个高效的 vRouter来负责数据转发，而每个vRouter通过 BGP协议负责把自己上运行的 workload的路由信息向整个Calico网络内传播，小规模部署可以直接互联，大规模下可通过指定的BGProute reflector 来完成。这样保证最终所有的workload之间的数据流量都是通过 IP包的方式完成互联的。"></a>Felix：主要负责路由配置以及ACLS规则的配置以及下发，它存在在每个node节点上。 etcd：分布式键值存储，主要负责网络元数据一致性，确保Calico网络状态的准确性，可以与kubernetes共用； BGPClient(BIRD), 主要负责把 Felix写入 kernel的路由信息分发到当前 Calico网络，确保 workload间的通信的有效性； BGPRoute Reflector(BIRD), 大规模部署时使用，摒弃所有节点互联的mesh模式，通过一个或者多个 BGPRoute Reflector 来完成集中式的路由分发 通过将整个互联网的可扩展 IP网络原则压缩到数据中心级别，Calico在每一个计算节点利用 Linuxkernel 实现了一个高效的 vRouter来负责数据转发，而每个vRouter通过 BGP协议负责把自己上运行的 workload的路由信息向整个Calico网络内传播，小规模部署可以直接互联，大规模下可通过指定的BGProute reflector 来完成。这样保证最终所有的workload之间的数据流量都是通过 IP包的方式完成互联的。</h5><ol><li><h5 id="1-Flannel的工作原理：-Flannel实质上是一种“覆盖网络-overlay-network-”，也就是将TCP数据包装在另一种网络包里面进行路由转发和通信，目前已经支持UDP、VxLAN、AWS-VPC和GCE路由等数据转发方式。"><a href="#1-Flannel的工作原理：-Flannel实质上是一种“覆盖网络-overlay-network-”，也就是将TCP数据包装在另一种网络包里面进行路由转发和通信，目前已经支持UDP、VxLAN、AWS-VPC和GCE路由等数据转发方式。" class="headerlink" title="\1. Flannel的工作原理： Flannel实质上是一种“覆盖网络(overlay network)”，也就是将TCP数据包装在另一种网络包里面进行路由转发和通信，目前已经支持UDP、VxLAN、AWS VPC和GCE路由等数据转发方式。"></a>\1. Flannel的工作原理： Flannel实质上是一种“覆盖网络(overlay network)”，也就是将TCP数据包装在另一种网络包里面进行路由转发和通信，目前已经支持UDP、VxLAN、AWS VPC和GCE路由等数据转发方式。</h5></li></ol><h5 id="默认的节点间数据通信方式是UDP转发。-工作原理：-数据从源容器中发出后，经由所在主机的docker0虚拟网卡转发到flannel0虚拟网卡（先可以不经过docker0网卡，使用cni模式），这是个P2P的虚拟网卡，flanneld服务监听在网卡的另外一端。-Flannel通过Etcd服务维护了一张节点间的路由表，详细记录了各节点子网网段-。-源主机的flanneld服务将原本的数据内容UDP封装后根据自己的路由表投递给目的节点的flanneld服务，数据到达以后被解包，然后直接进入目的节点的flannel0虚拟网卡，然后被转发到目的主机的docker0虚拟网卡，最后就像本机容器通信一下的有docker0路由到达目标容器。-flannel在进行路由转发的基础上进行了封包解包的操作，这样浪费了CPU的计算资源。"><a href="#默认的节点间数据通信方式是UDP转发。-工作原理：-数据从源容器中发出后，经由所在主机的docker0虚拟网卡转发到flannel0虚拟网卡（先可以不经过docker0网卡，使用cni模式），这是个P2P的虚拟网卡，flanneld服务监听在网卡的另外一端。-Flannel通过Etcd服务维护了一张节点间的路由表，详细记录了各节点子网网段-。-源主机的flanneld服务将原本的数据内容UDP封装后根据自己的路由表投递给目的节点的flanneld服务，数据到达以后被解包，然后直接进入目的节点的flannel0虚拟网卡，然后被转发到目的主机的docker0虚拟网卡，最后就像本机容器通信一下的有docker0路由到达目标容器。-flannel在进行路由转发的基础上进行了封包解包的操作，这样浪费了CPU的计算资源。" class="headerlink" title="默认的节点间数据通信方式是UDP转发。 工作原理： 数据从源容器中发出后，经由所在主机的docker0虚拟网卡转发到flannel0虚拟网卡（先可以不经过docker0网卡，使用cni模式），这是个P2P的虚拟网卡，flanneld服务监听在网卡的另外一端。 Flannel通过Etcd服务维护了一张节点间的路由表，详细记录了各节点子网网段 。 源主机的flanneld服务将原本的数据内容UDP封装后根据自己的路由表投递给目的节点的flanneld服务，数据到达以后被解包，然后直接进入目的节点的flannel0虚拟网卡，然后被转发到目的主机的docker0虚拟网卡，最后就像本机容器通信一下的有docker0路由到达目标容器。 flannel在进行路由转发的基础上进行了封包解包的操作，这样浪费了CPU的计算资源。"></a>默认的节点间数据通信方式是UDP转发。 工作原理： 数据从源容器中发出后，经由所在主机的docker0虚拟网卡转发到flannel0虚拟网卡（先可以不经过docker0网卡，使用cni模式），这是个P2P的虚拟网卡，flanneld服务监听在网卡的另外一端。 Flannel通过Etcd服务维护了一张节点间的路由表，详细记录了各节点子网网段 。 源主机的flanneld服务将原本的数据内容UDP封装后根据自己的路由表投递给目的节点的flanneld服务，数据到达以后被解包，然后直接进入目的节点的flannel0虚拟网卡，然后被转发到目的主机的docker0虚拟网卡，最后就像本机容器通信一下的有docker0路由到达目标容器。 flannel在进行路由转发的基础上进行了封包解包的操作，这样浪费了CPU的计算资源。</h5><h2 id="Worker节点宕机，简述Pods驱逐流程。"><a href="#Worker节点宕机，简述Pods驱逐流程。" class="headerlink" title="Worker节点宕机，简述Pods驱逐流程。"></a>Worker节点宕机，简述Pods驱逐流程。</h2><ol><li><h5 id="1-在-Kubernetes-集群中，当节点由于某些原因（网络、宕机等）不能正常工作时会被认定为不可用状态（Unknown-或者-False-状态），当时间超过了-pod-eviction-timeout-值时，那么节点上的所有-Pod-都会被节点控制器计划删除。"><a href="#1-在-Kubernetes-集群中，当节点由于某些原因（网络、宕机等）不能正常工作时会被认定为不可用状态（Unknown-或者-False-状态），当时间超过了-pod-eviction-timeout-值时，那么节点上的所有-Pod-都会被节点控制器计划删除。" class="headerlink" title="\1. 在 Kubernetes 集群中，当节点由于某些原因（网络、宕机等）不能正常工作时会被认定为不可用状态（Unknown 或者 False 状态），当时间超过了 pod-eviction-timeout 值时，那么节点上的所有 Pod 都会被节点控制器计划删除。"></a>\1. 在 Kubernetes 集群中，当节点由于某些原因（网络、宕机等）不能正常工作时会被认定为不可用状态（Unknown 或者 False 状态），当时间超过了 pod-eviction-timeout 值时，那么节点上的所有 Pod 都会被节点控制器计划删除。</h5></li><li><h5 id="2-Kubernetes-集群中有一个节点生命周期控制器：node-lifecycle-controller-go。它会与每一个节点上的-kubelet-进行通信，以收集各个节点已经节点上容器的相关状态信息。当超出一定时间后不能与-kubelet-通信，那么就会标记该节点为-Unknown-状态。并且节点生命周期控制器会自动创建代表状况的污点，用于防止调度器调度-pod-到该节点。"><a href="#2-Kubernetes-集群中有一个节点生命周期控制器：node-lifecycle-controller-go。它会与每一个节点上的-kubelet-进行通信，以收集各个节点已经节点上容器的相关状态信息。当超出一定时间后不能与-kubelet-通信，那么就会标记该节点为-Unknown-状态。并且节点生命周期控制器会自动创建代表状况的污点，用于防止调度器调度-pod-到该节点。" class="headerlink" title="\2. Kubernetes 集群中有一个节点生命周期控制器：node_lifecycle_controller.go。它会与每一个节点上的 kubelet 进行通信，以收集各个节点已经节点上容器的相关状态信息。当超出一定时间后不能与 kubelet 通信，那么就会标记该节点为 Unknown 状态。并且节点生命周期控制器会自动创建代表状况的污点，用于防止调度器调度 pod 到该节点。"></a>\2. Kubernetes 集群中有一个节点生命周期控制器：node_lifecycle_controller.go。它会与每一个节点上的 kubelet 进行通信，以收集各个节点已经节点上容器的相关状态信息。当超出一定时间后不能与 kubelet 通信，那么就会标记该节点为 Unknown 状态。并且节点生命周期控制器会自动创建代表状况的污点，用于防止调度器调度 pod 到该节点。</h5></li><li><h5 id="3-那么-Unknown-状态的节点上已经运行的-pod-会怎么处理呢？节点上的所有-Pod-都会被污点管理器（taint-manager-go）计划删除。而在节点被认定为不可用状态到删除节点上的-Pod-之间是有一段时间的，这段时间被称为容忍度。如果在不配置的情况下，Kubernetes-会自动给-Pod-添加一个-key-为-node-kubernetes-io-not-ready-的容忍度-并配置-tolerationSeconds-300，同样，Kubernetes-会给-Pod-添加一个-key-为-node-kubernetes-io-unreachable-的容忍度-并配置-tolerationSeconds-300。"><a href="#3-那么-Unknown-状态的节点上已经运行的-pod-会怎么处理呢？节点上的所有-Pod-都会被污点管理器（taint-manager-go）计划删除。而在节点被认定为不可用状态到删除节点上的-Pod-之间是有一段时间的，这段时间被称为容忍度。如果在不配置的情况下，Kubernetes-会自动给-Pod-添加一个-key-为-node-kubernetes-io-not-ready-的容忍度-并配置-tolerationSeconds-300，同样，Kubernetes-会给-Pod-添加一个-key-为-node-kubernetes-io-unreachable-的容忍度-并配置-tolerationSeconds-300。" class="headerlink" title="\3. 那么 Unknown 状态的节点上已经运行的 pod 会怎么处理呢？节点上的所有 Pod 都会被污点管理器（taint_manager.go）计划删除。而在节点被认定为不可用状态到删除节点上的 Pod 之间是有一段时间的，这段时间被称为容忍度。如果在不配置的情况下，Kubernetes 会自动给 Pod 添加一个 key 为 node.kubernetes.io&#x2F;not-ready 的容忍度 并配置 tolerationSeconds&#x3D;300，同样，Kubernetes 会给 Pod 添加一个 key 为 node.kubernetes.io&#x2F;unreachable 的容忍度 并配置 tolerationSeconds&#x3D;300。"></a>\3. 那么 Unknown 状态的节点上已经运行的 pod 会怎么处理呢？节点上的所有 Pod 都会被污点管理器（taint_manager.go）计划删除。而在节点被认定为不可用状态到删除节点上的 Pod 之间是有一段时间的，这段时间被称为容忍度。如果在不配置的情况下，Kubernetes 会自动给 Pod 添加一个 key 为 node.kubernetes.io&#x2F;not-ready 的容忍度 并配置 tolerationSeconds&#x3D;300，同样，Kubernetes 会给 Pod 添加一个 key 为 node.kubernetes.io&#x2F;unreachable 的容忍度 并配置 tolerationSeconds&#x3D;300。</h5></li><li><h5 id="4-当到了删除-Pod-时，污点管理器会创建污点标记事件，然后驱逐-pod-。这里需要注意的是由于已经不能与-kubelet-通信，所以该节点上的-Pod-在管理后台看到的是处于灰色标记，但是此时如果去获取-pod-的状态其实还是处于-Running-状态。每种类型的资源都有相应的资源控制器（Controller），例如：deployment-controller-go、stateful-set-control-go。每种控制器都在监听资源变化，从而做出相应的动作执行。deployment-控制器在监听到-Pod-被驱逐后会创建一个新的-Pod-出来，但是-Statefulset-控制器并不会创建出新的-Pod，原因是因为它可能会违反-StatefulSet-固有的至多一个的语义，可能出现具有相同身份的多个成员，这将可能是灾难性的，并且可能导致数据丢失。"><a href="#4-当到了删除-Pod-时，污点管理器会创建污点标记事件，然后驱逐-pod-。这里需要注意的是由于已经不能与-kubelet-通信，所以该节点上的-Pod-在管理后台看到的是处于灰色标记，但是此时如果去获取-pod-的状态其实还是处于-Running-状态。每种类型的资源都有相应的资源控制器（Controller），例如：deployment-controller-go、stateful-set-control-go。每种控制器都在监听资源变化，从而做出相应的动作执行。deployment-控制器在监听到-Pod-被驱逐后会创建一个新的-Pod-出来，但是-Statefulset-控制器并不会创建出新的-Pod，原因是因为它可能会违反-StatefulSet-固有的至多一个的语义，可能出现具有相同身份的多个成员，这将可能是灾难性的，并且可能导致数据丢失。" class="headerlink" title="\4. 当到了删除 Pod 时，污点管理器会创建污点标记事件，然后驱逐 pod 。这里需要注意的是由于已经不能与 kubelet 通信，所以该节点上的 Pod 在管理后台看到的是处于灰色标记，但是此时如果去获取 pod 的状态其实还是处于 Running 状态。每种类型的资源都有相应的资源控制器（Controller），例如：deployment_controller.go、stateful_set_control.go。每种控制器都在监听资源变化，从而做出相应的动作执行。deployment 控制器在监听到 Pod 被驱逐后会创建一个新的 Pod 出来，但是 Statefulset 控制器并不会创建出新的 Pod，原因是因为它可能会违反 StatefulSet 固有的至多一个的语义，可能出现具有相同身份的多个成员，这将可能是灾难性的，并且可能导致数据丢失。"></a>\4. 当到了删除 Pod 时，污点管理器会创建污点标记事件，然后驱逐 pod 。这里需要注意的是由于已经不能与 kubelet 通信，所以该节点上的 Pod 在管理后台看到的是处于灰色标记，但是此时如果去获取 pod 的状态其实还是处于 Running 状态。每种类型的资源都有相应的资源控制器（Controller），例如：deployment_controller.go、stateful_set_control.go。每种控制器都在监听资源变化，从而做出相应的动作执行。deployment 控制器在监听到 Pod 被驱逐后会创建一个新的 Pod 出来，但是 Statefulset 控制器并不会创建出新的 Pod，原因是因为它可能会违反 StatefulSet 固有的至多一个的语义，可能出现具有相同身份的多个成员，这将可能是灾难性的，并且可能导致数据丢失。</h5></li></ol><h2 id="你知道的K8s中几种Controller控制器，并详述其工作原理"><a href="#你知道的K8s中几种Controller控制器，并详述其工作原理" class="headerlink" title="你知道的K8s中几种Controller控制器，并详述其工作原理"></a>你知道的K8s中几种Controller控制器，并详述其工作原理</h2><ol><li><h5 id="1-deployment：适合无状态的服务部署-适合部署无状态的应用服务，用来管理pod和replicaset，具有上线部署、副本设定、滚动更新、回滚等功能，还可提供声明式更新，例如只更新一个新的Image"><a href="#1-deployment：适合无状态的服务部署-适合部署无状态的应用服务，用来管理pod和replicaset，具有上线部署、副本设定、滚动更新、回滚等功能，还可提供声明式更新，例如只更新一个新的Image" class="headerlink" title="\1. deployment：适合无状态的服务部署 适合部署无状态的应用服务，用来管理pod和replicaset，具有上线部署、副本设定、滚动更新、回滚等功能，还可提供声明式更新，例如只更新一个新的Image"></a>\1. deployment：适合无状态的服务部署 适合部署无状态的应用服务，用来管理pod和replicaset，具有上线部署、副本设定、滚动更新、回滚等功能，还可提供声明式更新，例如只更新一个新的Image</h5></li></ol><ul><li><h5 id="•-编写yaml文件，并创建nginx服务pod资源。"><a href="#•-编写yaml文件，并创建nginx服务pod资源。" class="headerlink" title="• 编写yaml文件，并创建nginx服务pod资源。"></a>• 编写yaml文件，并创建nginx服务pod资源。</h5></li></ul><ol><li><h5 id="1-StatefullSet：适合有状态的服务部署-适合部署有状态应用，解决Pod的独立生命周期，保持Pod启动顺序和唯一性。"><a href="#1-StatefullSet：适合有状态的服务部署-适合部署有状态应用，解决Pod的独立生命周期，保持Pod启动顺序和唯一性。" class="headerlink" title="\1. StatefullSet：适合有状态的服务部署 适合部署有状态应用，解决Pod的独立生命周期，保持Pod启动顺序和唯一性。"></a>\1. StatefullSet：适合有状态的服务部署 适合部署有状态应用，解决Pod的独立生命周期，保持Pod启动顺序和唯一性。</h5></li></ol><ul><li><h5 id="•-稳定，唯一的网络标识符，持久存储（例如：etcd配置文件，节点地址发生变化，将无法使用）"><a href="#•-稳定，唯一的网络标识符，持久存储（例如：etcd配置文件，节点地址发生变化，将无法使用）" class="headerlink" title="• 稳定，唯一的网络标识符，持久存储（例如：etcd配置文件，节点地址发生变化，将无法使用）"></a>• 稳定，唯一的网络标识符，持久存储（例如：etcd配置文件，节点地址发生变化，将无法使用）</h5></li><li><h5 id="•-有序，优雅的部署和扩展、删除和终止（例如：mysql主从关系，先启动主，再启动从）有序，滚动更新"><a href="#•-有序，优雅的部署和扩展、删除和终止（例如：mysql主从关系，先启动主，再启动从）有序，滚动更新" class="headerlink" title="• 有序，优雅的部署和扩展、删除和终止（例如：mysql主从关系，先启动主，再启动从）有序，滚动更新"></a>• 有序，优雅的部署和扩展、删除和终止（例如：mysql主从关系，先启动主，再启动从）有序，滚动更新</h5></li><li><h5 id="•-应用场景：例如数据库"><a href="#•-应用场景：例如数据库" class="headerlink" title="• 应用场景：例如数据库"></a>• 应用场景：例如数据库</h5></li></ul><h5 id="无状态服务的特点："><a href="#无状态服务的特点：" class="headerlink" title="无状态服务的特点："></a>无状态服务的特点：</h5><ul><li><h5 id="•-deployment-认为所有的pod都是一样的"><a href="#•-deployment-认为所有的pod都是一样的" class="headerlink" title="• deployment 认为所有的pod都是一样的"></a>• deployment 认为所有的pod都是一样的</h5></li><li><h5 id="•-不用考虑顺序的要求"><a href="#•-不用考虑顺序的要求" class="headerlink" title="• 不用考虑顺序的要求"></a>• 不用考虑顺序的要求</h5></li><li><h5 id="•-不用考虑在哪个node节点上运行"><a href="#•-不用考虑在哪个node节点上运行" class="headerlink" title="• 不用考虑在哪个node节点上运行"></a>• 不用考虑在哪个node节点上运行</h5></li><li><h5 id="•-可以随意扩容和缩容"><a href="#•-可以随意扩容和缩容" class="headerlink" title="• 可以随意扩容和缩容"></a>• 可以随意扩容和缩容</h5></li></ul><h5 id="有状态服务的特点："><a href="#有状态服务的特点：" class="headerlink" title="有状态服务的特点："></a>有状态服务的特点：</h5><ul><li><h5 id="•-实例之间有差别，每个实例都有自己的独特性，元数据不同，例如etcd，zookeeper"><a href="#•-实例之间有差别，每个实例都有自己的独特性，元数据不同，例如etcd，zookeeper" class="headerlink" title="• 实例之间有差别，每个实例都有自己的独特性，元数据不同，例如etcd，zookeeper"></a>• 实例之间有差别，每个实例都有自己的独特性，元数据不同，例如etcd，zookeeper</h5></li><li><h5 id="•-实例之间不对等的关系，以及依靠外部存储的应用"><a href="#•-实例之间不对等的关系，以及依靠外部存储的应用" class="headerlink" title="• 实例之间不对等的关系，以及依靠外部存储的应用"></a>• 实例之间不对等的关系，以及依靠外部存储的应用</h5></li><li><h5 id="•-常规的service服务和无头服务的区别"><a href="#•-常规的service服务和无头服务的区别" class="headerlink" title="• 常规的service服务和无头服务的区别"></a>• 常规的service服务和无头服务的区别</h5></li><li><h5 id="•-service：一组Pod访问策略，提供cluster-IP群集之间通讯，还提供负载均衡和服务发现"><a href="#•-service：一组Pod访问策略，提供cluster-IP群集之间通讯，还提供负载均衡和服务发现" class="headerlink" title="• service：一组Pod访问策略，提供cluster-IP群集之间通讯，还提供负载均衡和服务发现"></a>• service：一组Pod访问策略，提供cluster-IP群集之间通讯，还提供负载均衡和服务发现</h5></li><li><h5 id="•-Headless-service-无头服务，不需要cluster-IP，直接绑定具体的Pod的IP，无头服务经常用于statefulset的有状态部署"><a href="#•-Headless-service-无头服务，不需要cluster-IP，直接绑定具体的Pod的IP，无头服务经常用于statefulset的有状态部署" class="headerlink" title="• Headless service 无头服务，不需要cluster-IP，直接绑定具体的Pod的IP，无头服务经常用于statefulset的有状态部署"></a>• Headless service 无头服务，不需要cluster-IP，直接绑定具体的Pod的IP，无头服务经常用于statefulset的有状态部署</h5></li><li><h5 id="•-创建无头服务的service资源和dns资源，由于有状态服务的IP地址是动态的，所以使用无头服务的时候要绑定dns服务"><a href="#•-创建无头服务的service资源和dns资源，由于有状态服务的IP地址是动态的，所以使用无头服务的时候要绑定dns服务" class="headerlink" title="• 创建无头服务的service资源和dns资源，由于有状态服务的IP地址是动态的，所以使用无头服务的时候要绑定dns服务"></a>• 创建无头服务的service资源和dns资源，由于有状态服务的IP地址是动态的，所以使用无头服务的时候要绑定dns服务</h5></li></ul><ol><li><h5 id="1-DaemonSet：一次部署，所有的node节点都会部署，例如一些典型的应用场景：-运行集群存储-daemon，例如在每个Node上运行-glusterd、ceph"><a href="#1-DaemonSet：一次部署，所有的node节点都会部署，例如一些典型的应用场景：-运行集群存储-daemon，例如在每个Node上运行-glusterd、ceph" class="headerlink" title="\1. DaemonSet：一次部署，所有的node节点都会部署，例如一些典型的应用场景： 运行集群存储 daemon，例如在每个Node上运行 glusterd、ceph"></a>\1. DaemonSet：一次部署，所有的node节点都会部署，例如一些典型的应用场景： 运行集群存储 daemon，例如在每个Node上运行 glusterd、ceph</h5></li></ol><ul><li><h5 id="•-在每个Node上运行日志收集-daemon，例如-fluentd、-logstash"><a href="#•-在每个Node上运行日志收集-daemon，例如-fluentd、-logstash" class="headerlink" title="• 在每个Node上运行日志收集 daemon，例如 fluentd、 logstash"></a>• 在每个Node上运行日志收集 daemon，例如 fluentd、 logstash</h5></li><li><h5 id="•-在每个Node上运行监控-daemon，例如-Prometheus-Node-Exporter"><a href="#•-在每个Node上运行监控-daemon，例如-Prometheus-Node-Exporter" class="headerlink" title="• 在每个Node上运行监控 daemon，例如 Prometheus Node Exporter"></a>• 在每个Node上运行监控 daemon，例如 Prometheus Node Exporter</h5></li><li><h5 id="•-在每一个Node上运行一个Pod"><a href="#•-在每一个Node上运行一个Pod" class="headerlink" title="• 在每一个Node上运行一个Pod"></a>• 在每一个Node上运行一个Pod</h5></li><li><h5 id="•-新加入的Node也同样会自动运行一个Pod"><a href="#•-新加入的Node也同样会自动运行一个Pod" class="headerlink" title="• 新加入的Node也同样会自动运行一个Pod"></a>• 新加入的Node也同样会自动运行一个Pod</h5></li><li><h5 id="•-应用场景：监控，分布式存储，日志收集等"><a href="#•-应用场景：监控，分布式存储，日志收集等" class="headerlink" title="• 应用场景：监控，分布式存储，日志收集等"></a>• 应用场景：监控，分布式存储，日志收集等</h5></li></ul><ol><li><h5 id="1-Job：一次性的执行任务"><a href="#1-Job：一次性的执行任务" class="headerlink" title="\1. Job：一次性的执行任务"></a>\1. Job：一次性的执行任务</h5></li></ol><ul><li><h5 id="•-一次性执行任务，类似Linux中的job"><a href="#•-一次性执行任务，类似Linux中的job" class="headerlink" title="• 一次性执行任务，类似Linux中的job"></a>• 一次性执行任务，类似Linux中的job</h5></li><li><h5 id="•-应用场景：如离线数据处理，视频解码等业务"><a href="#•-应用场景：如离线数据处理，视频解码等业务" class="headerlink" title="• 应用场景：如离线数据处理，视频解码等业务"></a>• 应用场景：如离线数据处理，视频解码等业务</h5></li></ul><ol><li><h5 id="1-Cronjob：周期性的执行任务"><a href="#1-Cronjob：周期性的执行任务" class="headerlink" title="\1. Cronjob：周期性的执行任务"></a>\1. Cronjob：周期性的执行任务</h5></li></ol><ul><li><h5 id="•-周期性任务，像Linux的Crontab一样"><a href="#•-周期性任务，像Linux的Crontab一样" class="headerlink" title="• 周期性任务，像Linux的Crontab一样"></a>• 周期性任务，像Linux的Crontab一样</h5></li><li><h5 id="•-应用场景：如通知，备份等"><a href="#•-应用场景：如通知，备份等" class="headerlink" title="• 应用场景：如通知，备份等"></a>• 应用场景：如通知，备份等</h5></li><li><h5 id="•-使用cronjob要慎重，用完之后要删掉，不然会占用很多资源"><a href="#•-使用cronjob要慎重，用完之后要删掉，不然会占用很多资源" class="headerlink" title="• 使用cronjob要慎重，用完之后要删掉，不然会占用很多资源"></a>• 使用cronjob要慎重，用完之后要删掉，不然会占用很多资源</h5></li></ul><h2 id="ingress-controller的工作机制"><a href="#ingress-controller的工作机制" class="headerlink" title="ingress-controller的工作机制"></a>ingress-controller的工作机制</h2><h5 id="通常情况下，service和pod的IP仅可在集群内部访问"><a href="#通常情况下，service和pod的IP仅可在集群内部访问" class="headerlink" title="通常情况下，service和pod的IP仅可在集群内部访问"></a>通常情况下，service和pod的IP仅可在集群内部访问</h5><ul><li><h5 id="•-k8s提供了service方式：NodePort-来提供对外的服务，外部的服务可以通过访问Node节点ip-NodePort端口来访问集群内部的资源，外部的请求先到达service所选中的节点上，然后负载均衡到每一个节点上。"><a href="#•-k8s提供了service方式：NodePort-来提供对外的服务，外部的服务可以通过访问Node节点ip-NodePort端口来访问集群内部的资源，外部的请求先到达service所选中的节点上，然后负载均衡到每一个节点上。" class="headerlink" title="• k8s提供了service方式：NodePort 来提供对外的服务，外部的服务可以通过访问Node节点ip+NodePort端口来访问集群内部的资源，外部的请求先到达service所选中的节点上，然后负载均衡到每一个节点上。"></a>• k8s提供了service方式：NodePort 来提供对外的服务，外部的服务可以通过访问Node节点ip+NodePort端口来访问集群内部的资源，外部的请求先到达service所选中的节点上，然后负载均衡到每一个节点上。</h5></li></ul><h5 id="NodePort虽然提供了对外的方式但也有很大弊端："><a href="#NodePort虽然提供了对外的方式但也有很大弊端：" class="headerlink" title="NodePort虽然提供了对外的方式但也有很大弊端："></a>NodePort虽然提供了对外的方式但也有很大弊端：</h5><ul><li><h5 id="•-由于service的实现方式：user-space-、iptebles、-3-ipvs、方式这三种方式只支持在4层协议通信，不支持7层协议，因此NodePort不能代理https服务。"><a href="#•-由于service的实现方式：user-space-、iptebles、-3-ipvs、方式这三种方式只支持在4层协议通信，不支持7层协议，因此NodePort不能代理https服务。" class="headerlink" title="• 由于service的实现方式：user_space 、iptebles、 3 ipvs、方式这三种方式只支持在4层协议通信，不支持7层协议，因此NodePort不能代理https服务。"></a>• 由于service的实现方式：user_space 、iptebles、 3 ipvs、方式这三种方式只支持在4层协议通信，不支持7层协议，因此NodePort不能代理https服务。</h5></li><li><h5 id="•-NodePort-需要暴露service所属每个node节点上端口，当需求越来越多，端口数量过多，导致维护成本过高，并且集群不好管理。"><a href="#•-NodePort-需要暴露service所属每个node节点上端口，当需求越来越多，端口数量过多，导致维护成本过高，并且集群不好管理。" class="headerlink" title="• NodePort 需要暴露service所属每个node节点上端口，当需求越来越多，端口数量过多，导致维护成本过高，并且集群不好管理。"></a>• NodePort 需要暴露service所属每个node节点上端口，当需求越来越多，端口数量过多，导致维护成本过高，并且集群不好管理。</h5></li></ul><h5 id="原理"><a href="#原理" class="headerlink" title="原理"></a>原理</h5><ul><li><h5 id="•-Ingress也是Kubernetes-API的标准资源类型之一，它其实就是一组基于DNS名称（host）或URL路径把请求转发到指定的Service资源的规则。用于将集群外部的请求流量转发到集群内部完成的服务发布。我们需要明白的是，Ingress资源自身不能进行“流量穿透”，仅仅是一组规则的集合，这些集合规则还需要其他功能的辅助，比如监听某套接字，然后根据这些规则的匹配进行路由转发，这些能够为Ingress资源监听套接字并将流量转发的组件就是Ingress-Controller。"><a href="#•-Ingress也是Kubernetes-API的标准资源类型之一，它其实就是一组基于DNS名称（host）或URL路径把请求转发到指定的Service资源的规则。用于将集群外部的请求流量转发到集群内部完成的服务发布。我们需要明白的是，Ingress资源自身不能进行“流量穿透”，仅仅是一组规则的集合，这些集合规则还需要其他功能的辅助，比如监听某套接字，然后根据这些规则的匹配进行路由转发，这些能够为Ingress资源监听套接字并将流量转发的组件就是Ingress-Controller。" class="headerlink" title="• Ingress也是Kubernetes API的标准资源类型之一，它其实就是一组基于DNS名称（host）或URL路径把请求转发到指定的Service资源的规则。用于将集群外部的请求流量转发到集群内部完成的服务发布。我们需要明白的是，Ingress资源自身不能进行“流量穿透”，仅仅是一组规则的集合，这些集合规则还需要其他功能的辅助，比如监听某套接字，然后根据这些规则的匹配进行路由转发，这些能够为Ingress资源监听套接字并将流量转发的组件就是Ingress Controller。"></a>• Ingress也是Kubernetes API的标准资源类型之一，它其实就是一组基于DNS名称（host）或URL路径把请求转发到指定的Service资源的规则。用于将集群外部的请求流量转发到集群内部完成的服务发布。我们需要明白的是，Ingress资源自身不能进行“流量穿透”，仅仅是一组规则的集合，这些集合规则还需要其他功能的辅助，比如监听某套接字，然后根据这些规则的匹配进行路由转发，这些能够为Ingress资源监听套接字并将流量转发的组件就是Ingress Controller。</h5></li><li><h5 id="•-Ingress-控制器不同于Deployment-等pod控制器的是，Ingress控制器不直接运行为kube-controller-manager的一部分，它仅仅是Kubernetes集群的一个附件，类似于CoreDNS，需要在集群上单独部署。"><a href="#•-Ingress-控制器不同于Deployment-等pod控制器的是，Ingress控制器不直接运行为kube-controller-manager的一部分，它仅仅是Kubernetes集群的一个附件，类似于CoreDNS，需要在集群上单独部署。" class="headerlink" title="• Ingress 控制器不同于Deployment 等pod控制器的是，Ingress控制器不直接运行为kube-controller-manager的一部分，它仅仅是Kubernetes集群的一个附件，类似于CoreDNS，需要在集群上单独部署。"></a>• Ingress 控制器不同于Deployment 等pod控制器的是，Ingress控制器不直接运行为kube-controller-manager的一部分，它仅仅是Kubernetes集群的一个附件，类似于CoreDNS，需要在集群上单独部署。</h5></li><li><h5 id="•-ingress-controller通过监视api-server获取相关ingress、service、endpoint、secret、node、configmap对象，并在程序内部不断循环监视相关service是否有新的endpoints变化，一旦发生变化则自动更新nginx-conf模板配置并产生新的配置文件进行reload"><a href="#•-ingress-controller通过监视api-server获取相关ingress、service、endpoint、secret、node、configmap对象，并在程序内部不断循环监视相关service是否有新的endpoints变化，一旦发生变化则自动更新nginx-conf模板配置并产生新的配置文件进行reload" class="headerlink" title="• ingress controller通过监视api server获取相关ingress、service、endpoint、secret、node、configmap对象，并在程序内部不断循环监视相关service是否有新的endpoints变化，一旦发生变化则自动更新nginx.conf模板配置并产生新的配置文件进行reload"></a>• ingress controller通过监视api server获取相关ingress、service、endpoint、secret、node、configmap对象，并在程序内部不断循环监视相关service是否有新的endpoints变化，一旦发生变化则自动更新nginx.conf模板配置并产生新的配置文件进行reload</h5></li></ul><h2 id="k8s的调度机制"><a href="#k8s的调度机制" class="headerlink" title="k8s的调度机制"></a>k8s的调度机制</h2><ol><li><h5 id="1-Scheduler工作原理：-请求及Scheduler调度步骤："><a href="#1-Scheduler工作原理：-请求及Scheduler调度步骤：" class="headerlink" title="\1. Scheduler工作原理： 请求及Scheduler调度步骤："></a>\1. Scheduler工作原理： 请求及Scheduler调度步骤：</h5></li></ol><ul><li><h5 id="•-节点预选-Predicate-：排除完全不满足条件的节点，如内存大小，端口等条件不满足。"><a href="#•-节点预选-Predicate-：排除完全不满足条件的节点，如内存大小，端口等条件不满足。" class="headerlink" title="• 节点预选(Predicate)：排除完全不满足条件的节点，如内存大小，端口等条件不满足。"></a>• 节点预选(Predicate)：排除完全不满足条件的节点，如内存大小，端口等条件不满足。</h5></li><li><h5 id="•-节点优先级排序-Priority-：根据优先级选出最佳节点"><a href="#•-节点优先级排序-Priority-：根据优先级选出最佳节点" class="headerlink" title="• 节点优先级排序(Priority)：根据优先级选出最佳节点"></a>• 节点优先级排序(Priority)：根据优先级选出最佳节点</h5></li><li><h5 id="•-节点择优-Select-：根据优先级选定节点"><a href="#•-节点择优-Select-：根据优先级选定节点" class="headerlink" title="• 节点择优(Select)：根据优先级选定节点"></a>• 节点择优(Select)：根据优先级选定节点</h5></li></ul><ol><li><h5 id="1-具体步骤："><a href="#1-具体步骤：" class="headerlink" title="\1. 具体步骤："></a>\1. 具体步骤：</h5></li></ol><ul><li><h5 id="•-首先用户通过-Kubernetes-客户端-Kubectl-提交创建-Pod-的-Yaml-的文件，向Kubernetes-系统发起资源请求，该资源请求被提交到"><a href="#•-首先用户通过-Kubernetes-客户端-Kubectl-提交创建-Pod-的-Yaml-的文件，向Kubernetes-系统发起资源请求，该资源请求被提交到" class="headerlink" title="• 首先用户通过 Kubernetes 客户端 Kubectl 提交创建 Pod 的 Yaml 的文件，向Kubernetes 系统发起资源请求，该资源请求被提交到"></a>• 首先用户通过 Kubernetes 客户端 Kubectl 提交创建 Pod 的 Yaml 的文件，向Kubernetes 系统发起资源请求，该资源请求被提交到</h5></li><li><h5 id="•-Kubernetes-系统中，用户通过命令行工具-Kubectl-向-Kubernetes-集群即-APIServer-用-的方式发送“POST”请求，即创建-Pod-的请求。"><a href="#•-Kubernetes-系统中，用户通过命令行工具-Kubectl-向-Kubernetes-集群即-APIServer-用-的方式发送“POST”请求，即创建-Pod-的请求。" class="headerlink" title="• Kubernetes 系统中，用户通过命令行工具 Kubectl 向 Kubernetes 集群即 APIServer 用 的方式发送“POST”请求，即创建 Pod 的请求。"></a>• Kubernetes 系统中，用户通过命令行工具 Kubectl 向 Kubernetes 集群即 APIServer 用 的方式发送“POST”请求，即创建 Pod 的请求。</h5></li><li><h5 id="•-APIServer-接收到请求后把创建-Pod-的信息存储到-Etcd-中，从集群运行那一刻起，资源调度系统-Scheduler-就会定时去监控-APIServer"><a href="#•-APIServer-接收到请求后把创建-Pod-的信息存储到-Etcd-中，从集群运行那一刻起，资源调度系统-Scheduler-就会定时去监控-APIServer" class="headerlink" title="• APIServer 接收到请求后把创建 Pod 的信息存储到 Etcd 中，从集群运行那一刻起，资源调度系统 Scheduler 就会定时去监控 APIServer"></a>• APIServer 接收到请求后把创建 Pod 的信息存储到 Etcd 中，从集群运行那一刻起，资源调度系统 Scheduler 就会定时去监控 APIServer</h5></li><li><h5 id="•-通过-APIServer-得到创建-Pod-的信息，Scheduler-采用-watch-机制，一旦-Etcd-存储-Pod-信息成功便会立即通知APIServer，"><a href="#•-通过-APIServer-得到创建-Pod-的信息，Scheduler-采用-watch-机制，一旦-Etcd-存储-Pod-信息成功便会立即通知APIServer，" class="headerlink" title="• 通过 APIServer 得到创建 Pod 的信息，Scheduler 采用 watch 机制，一旦 Etcd 存储 Pod 信息成功便会立即通知APIServer，"></a>• 通过 APIServer 得到创建 Pod 的信息，Scheduler 采用 watch 机制，一旦 Etcd 存储 Pod 信息成功便会立即通知APIServer，</h5></li><li><h5 id="•-APIServer会立即把Pod创建的消息通知Scheduler，Scheduler发现-Pod-的属性中-Dest-Node-为空时（Dest-Node-””）便会立即触发调度流程进行调度。"><a href="#•-APIServer会立即把Pod创建的消息通知Scheduler，Scheduler发现-Pod-的属性中-Dest-Node-为空时（Dest-Node-””）便会立即触发调度流程进行调度。" class="headerlink" title="• APIServer会立即把Pod创建的消息通知Scheduler，Scheduler发现 Pod 的属性中 Dest Node 为空时（Dest Node&#x3D;””）便会立即触发调度流程进行调度。"></a>• APIServer会立即把Pod创建的消息通知Scheduler，Scheduler发现 Pod 的属性中 Dest Node 为空时（Dest Node&#x3D;””）便会立即触发调度流程进行调度。</h5></li><li><h5 id="•-而这一个创建Pod对象，在调度的过程当中有3个阶段：节点预选、节点优选、节点选定，从而筛选出最佳的节点"><a href="#•-而这一个创建Pod对象，在调度的过程当中有3个阶段：节点预选、节点优选、节点选定，从而筛选出最佳的节点" class="headerlink" title="• 而这一个创建Pod对象，在调度的过程当中有3个阶段：节点预选、节点优选、节点选定，从而筛选出最佳的节点"></a>• 而这一个创建Pod对象，在调度的过程当中有3个阶段：节点预选、节点优选、节点选定，从而筛选出最佳的节点</h5></li><li><ul><li><h5 id="•-节点预选：基于一系列的预选规则对每个节点进行检查，将那些不符合条件的节点过滤，从而完成节点的预选"><a href="#•-节点预选：基于一系列的预选规则对每个节点进行检查，将那些不符合条件的节点过滤，从而完成节点的预选" class="headerlink" title="• 节点预选：基于一系列的预选规则对每个节点进行检查，将那些不符合条件的节点过滤，从而完成节点的预选"></a>• 节点预选：基于一系列的预选规则对每个节点进行检查，将那些不符合条件的节点过滤，从而完成节点的预选</h5></li><li><h5 id="•-节点优选：对预选出的节点进行优先级排序，以便选出最合适运行Pod对象的节点"><a href="#•-节点优选：对预选出的节点进行优先级排序，以便选出最合适运行Pod对象的节点" class="headerlink" title="• 节点优选：对预选出的节点进行优先级排序，以便选出最合适运行Pod对象的节点"></a>• 节点优选：对预选出的节点进行优先级排序，以便选出最合适运行Pod对象的节点</h5></li><li><h5 id="•-节点选定：从优先级排序结果中挑选出优先级最高的节点运行Pod，当这类节点多于1个时，则进行随机选择"><a href="#•-节点选定：从优先级排序结果中挑选出优先级最高的节点运行Pod，当这类节点多于1个时，则进行随机选择" class="headerlink" title="• 节点选定：从优先级排序结果中挑选出优先级最高的节点运行Pod，当这类节点多于1个时，则进行随机选择"></a>• 节点选定：从优先级排序结果中挑选出优先级最高的节点运行Pod，当这类节点多于1个时，则进行随机选择</h5></li></ul></li></ul><ol><li><h5 id="1-k8s的调用工作方式"><a href="#1-k8s的调用工作方式" class="headerlink" title="\1. k8s的调用工作方式"></a>\1. k8s的调用工作方式</h5></li></ol><ul><li><h5 id="•-Kubernetes调度器作为集群的大脑，在如何提高集群的资源利用率、保证集群中服务的稳定运行中也会变得越来越重要Kubernetes的资源分为两种属性。"><a href="#•-Kubernetes调度器作为集群的大脑，在如何提高集群的资源利用率、保证集群中服务的稳定运行中也会变得越来越重要Kubernetes的资源分为两种属性。" class="headerlink" title="• Kubernetes调度器作为集群的大脑，在如何提高集群的资源利用率、保证集群中服务的稳定运行中也会变得越来越重要Kubernetes的资源分为两种属性。"></a>• Kubernetes调度器作为集群的大脑，在如何提高集群的资源利用率、保证集群中服务的稳定运行中也会变得越来越重要Kubernetes的资源分为两种属性。</h5></li><li><ul><li><h5 id="•-可压缩资源（例如CPU循环，Disk-I-O带宽）都是可以被限制和被回收的，对于一个Pod来说可以降低这些资源的使用量而不去杀掉Pod。"><a href="#•-可压缩资源（例如CPU循环，Disk-I-O带宽）都是可以被限制和被回收的，对于一个Pod来说可以降低这些资源的使用量而不去杀掉Pod。" class="headerlink" title="• 可压缩资源（例如CPU循环，Disk I&#x2F;O带宽）都是可以被限制和被回收的，对于一个Pod来说可以降低这些资源的使用量而不去杀掉Pod。"></a>• 可压缩资源（例如CPU循环，Disk I&#x2F;O带宽）都是可以被限制和被回收的，对于一个Pod来说可以降低这些资源的使用量而不去杀掉Pod。</h5></li><li><h5 id="•-不可压缩资源（例如内存、硬盘空间）一般来说不杀掉Pod就没法回收。未来Kubernetes会加入更多资源，如网络带宽，存储IOPS的支持。"><a href="#•-不可压缩资源（例如内存、硬盘空间）一般来说不杀掉Pod就没法回收。未来Kubernetes会加入更多资源，如网络带宽，存储IOPS的支持。" class="headerlink" title="• 不可压缩资源（例如内存、硬盘空间）一般来说不杀掉Pod就没法回收。未来Kubernetes会加入更多资源，如网络带宽，存储IOPS的支持。"></a>• 不可压缩资源（例如内存、硬盘空间）一般来说不杀掉Pod就没法回收。未来Kubernetes会加入更多资源，如网络带宽，存储IOPS的支持。</h5></li></ul></li></ul><h2 id="kube-proxy的三种工作模式和原理"><a href="#kube-proxy的三种工作模式和原理" class="headerlink" title="kube-proxy的三种工作模式和原理"></a>kube-proxy的三种工作模式和原理</h2><ol><li><h5 id="1-userspace-模式"><a href="#1-userspace-模式" class="headerlink" title="\1. userspace 模式"></a>\1. userspace 模式</h5></li></ol><ul><li><h5 id="•-该模式下kube-proxy会为每一个Service创建一个监听端口。发向Cluster-IP的请求被Iptables规则重定向到Kube-proxy监听的端口上，Kube-proxy根据LB算法选择一个提供服务的Pod并和其建立链接，以将请求转发到Pod上。"><a href="#•-该模式下kube-proxy会为每一个Service创建一个监听端口。发向Cluster-IP的请求被Iptables规则重定向到Kube-proxy监听的端口上，Kube-proxy根据LB算法选择一个提供服务的Pod并和其建立链接，以将请求转发到Pod上。" class="headerlink" title="• 该模式下kube-proxy会为每一个Service创建一个监听端口。发向Cluster IP的请求被Iptables规则重定向到Kube-proxy监听的端口上，Kube-proxy根据LB算法选择一个提供服务的Pod并和其建立链接，以将请求转发到Pod上。"></a>• 该模式下kube-proxy会为每一个Service创建一个监听端口。发向Cluster IP的请求被Iptables规则重定向到Kube-proxy监听的端口上，Kube-proxy根据LB算法选择一个提供服务的Pod并和其建立链接，以将请求转发到Pod上。</h5></li><li><h5 id="•-该模式下，Kube-proxy充当了一个四层Load-balancer的角色。由于kube-proxy运行在userspace中，在进行转发处理时会增加两次内核和用户空间之间的数据拷贝，效率较另外两种模式低一些；好处是当后端的Pod不可用时，kube-proxy可以重试其他Pod。"><a href="#•-该模式下，Kube-proxy充当了一个四层Load-balancer的角色。由于kube-proxy运行在userspace中，在进行转发处理时会增加两次内核和用户空间之间的数据拷贝，效率较另外两种模式低一些；好处是当后端的Pod不可用时，kube-proxy可以重试其他Pod。" class="headerlink" title="• 该模式下，Kube-proxy充当了一个四层Load balancer的角色。由于kube-proxy运行在userspace中，在进行转发处理时会增加两次内核和用户空间之间的数据拷贝，效率较另外两种模式低一些；好处是当后端的Pod不可用时，kube-proxy可以重试其他Pod。"></a>• 该模式下，Kube-proxy充当了一个四层Load balancer的角色。由于kube-proxy运行在userspace中，在进行转发处理时会增加两次内核和用户空间之间的数据拷贝，效率较另外两种模式低一些；好处是当后端的Pod不可用时，kube-proxy可以重试其他Pod。</h5></li></ul><ol><li><h5 id="1-iptables-模式"><a href="#1-iptables-模式" class="headerlink" title="\1. iptables 模式"></a>\1. iptables 模式</h5></li></ol><ul><li><h5 id="•-为了避免增加内核和用户空间的数据拷贝操作，提高转发效率，Kube-proxy提供了iptables模式。在该模式下，Kube-proxy为service后端的每个Pod创建对应的iptables规则，直接将发向Cluster-IP的请求重定向到一个Pod-IP。"><a href="#•-为了避免增加内核和用户空间的数据拷贝操作，提高转发效率，Kube-proxy提供了iptables模式。在该模式下，Kube-proxy为service后端的每个Pod创建对应的iptables规则，直接将发向Cluster-IP的请求重定向到一个Pod-IP。" class="headerlink" title="• 为了避免增加内核和用户空间的数据拷贝操作，提高转发效率，Kube-proxy提供了iptables模式。在该模式下，Kube-proxy为service后端的每个Pod创建对应的iptables规则，直接将发向Cluster IP的请求重定向到一个Pod IP。"></a>• 为了避免增加内核和用户空间的数据拷贝操作，提高转发效率，Kube-proxy提供了iptables模式。在该模式下，Kube-proxy为service后端的每个Pod创建对应的iptables规则，直接将发向Cluster IP的请求重定向到一个Pod IP。</h5></li><li><h5 id="•-该模式下Kube-proxy不承担四层代理的角色，只负责创建iptables规则。该模式的优点是较userspace模式效率更高，但不能提供灵活的LB策略，当后端Pod不可用时也无法进行重试。"><a href="#•-该模式下Kube-proxy不承担四层代理的角色，只负责创建iptables规则。该模式的优点是较userspace模式效率更高，但不能提供灵活的LB策略，当后端Pod不可用时也无法进行重试。" class="headerlink" title="• 该模式下Kube-proxy不承担四层代理的角色，只负责创建iptables规则。该模式的优点是较userspace模式效率更高，但不能提供灵活的LB策略，当后端Pod不可用时也无法进行重试。"></a>• 该模式下Kube-proxy不承担四层代理的角色，只负责创建iptables规则。该模式的优点是较userspace模式效率更高，但不能提供灵活的LB策略，当后端Pod不可用时也无法进行重试。</h5></li></ul><ol><li><h5 id="1-该模式和iptables类似，kube-proxy监控Pod的变化并创建相应的ipvs-rules。ipvs也是在kernel模式下通过netfilter实现的，但采用了hash-table来存储规则，因此在规则较多的情况下，Ipvs相对iptables转发效率更高。除此以外，ipvs支持更多的LB算法。如果要设置kube-proxy为ipvs模式，必须在操作系统中安装IPVS内核模块。"><a href="#1-该模式和iptables类似，kube-proxy监控Pod的变化并创建相应的ipvs-rules。ipvs也是在kernel模式下通过netfilter实现的，但采用了hash-table来存储规则，因此在规则较多的情况下，Ipvs相对iptables转发效率更高。除此以外，ipvs支持更多的LB算法。如果要设置kube-proxy为ipvs模式，必须在操作系统中安装IPVS内核模块。" class="headerlink" title="\1. 该模式和iptables类似，kube-proxy监控Pod的变化并创建相应的ipvs rules。ipvs也是在kernel模式下通过netfilter实现的，但采用了hash table来存储规则，因此在规则较多的情况下，Ipvs相对iptables转发效率更高。除此以外，ipvs支持更多的LB算法。如果要设置kube-proxy为ipvs模式，必须在操作系统中安装IPVS内核模块。"></a>\1. 该模式和iptables类似，kube-proxy监控Pod的变化并创建相应的ipvs rules。ipvs也是在kernel模式下通过netfilter实现的，但采用了hash table来存储规则，因此在规则较多的情况下，Ipvs相对iptables转发效率更高。除此以外，ipvs支持更多的LB算法。如果要设置kube-proxy为ipvs模式，必须在操作系统中安装IPVS内核模块。</h5></li></ol><figure class="highlight mipsasm"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs mipsasm"><span class="hljs-number">1</span>.提交创建pod的yaml文件到kube-apiserver<br><span class="hljs-number">2</span>.kube-controller-manager创建资源对象<br><span class="hljs-number">3</span>.kubecontroller-manager将pod的配置信息存到etcd<br><span class="hljs-number">4</span>.kube-<span class="hljs-keyword">scheduler将pod调度到合适的节点</span><br><span class="hljs-keyword"></span><span class="hljs-number">5</span>.在该节点的kubelet上创建pod，pod建好后将pod的信息存到etcd<br></code></pre></td></tr></table></figure>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>2024-4-17 面试总结</title>
    <link href="/2024/12/05/2024-4-17%20%E9%9D%A2%E8%AF%95%E6%80%BB%E7%BB%93/"/>
    <url>/2024/12/05/2024-4-17%20%E9%9D%A2%E8%AF%95%E6%80%BB%E7%BB%93/</url>
    
    <content type="html"><![CDATA[<figure class="highlight apache"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs apache"><span class="hljs-attribute">1</span>.查看主机的硬件信息的命令 包括cpu 网卡 显卡这些<br><span class="hljs-attribute">2</span>.查看cpu负载情况 load的数值的含义<br><span class="hljs-attribute">3</span>.优化做过哪些？内核参数优化，具体讲下<br><span class="hljs-attribute">4</span>.nginx上的服务访问不通 怎么排查<br><span class="hljs-attribute">5</span>.<span class="hljs-number">502</span>和<span class="hljs-number">504</span>的区别<br><span class="hljs-attribute">6</span>.k8s的组件有哪些<br><span class="hljs-attribute">7</span>.kubelet和kube-proxy在主节点上会跑吗<br><span class="hljs-attribute">8</span>.k8s的网络插件用过哪些，calico的BGP模式讲下<br></code></pre></td></tr></table></figure><p><strong>1.查看主机的硬件信息的命令 包括cpu 网卡 显卡这些</strong></p><figure class="highlight tcl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs tcl">CPU信息：<br><br>在终端输入：lscpu<br>显卡信息：<br>cat /etc/cpuinfo 或 cat /<span class="hljs-keyword">proc</span>/cpuinfo<br><br>使用命令：lspci |<span class="hljs-title"> grep</span> VGA （通常用于PCIe接口的显卡）<br>或者<span class="hljs-title"> lshw</span> -c<span class="hljs-title"> display</span> 获取更详细的显卡信息<br>网卡信息：<br><br>使用命令：lspci |<span class="hljs-title"> grep</span> Ethernet （对于PCIe接口的网卡）<br>或者<span class="hljs-title"> ifconfig</span> -a 或<span class="hljs-title"> ip</span> addr<span class="hljs-title"> show</span> 查看网络接口及其IP配置<br>若要获取更详细的硬件信息，可以使用<span class="hljs-title"> ethtool</span> -i [interface_name]，其中 [interface_name] 是你想要查询的网卡名称。<br></code></pre></td></tr></table></figure><p><strong>2.查看cpu负载情况 load的数值的含义</strong></p><figure class="highlight x86asm"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs x86asm"><span class="hljs-number">12</span>:<span class="hljs-number">34</span>:<span class="hljs-number">56</span> <span class="hljs-meta">up</span> <span class="hljs-number">1</span> day, <span class="hljs-number">3</span>:<span class="hljs-number">23</span>,  <span class="hljs-number">3</span> users,  load average: <span class="hljs-number">0.23</span>, <span class="hljs-number">0.25</span>, <span class="hljs-number">0.28</span><br><br>其中，“load average”后面的三个数字分别表示过去<span class="hljs-number">1</span>分钟、<span class="hljs-number">5</span>分钟和<span class="hljs-number">15</span>分钟的平均负载。这些数值表示的是在这段时间内，系统中处于运行（包括正在运行和等待运行）状态的平均进程数（或线程数）。<br><br>负载数值的含义：<br><br>单核<span class="hljs-meta">CPU</span>情况下，当负载平均值为<span class="hljs-number">1.0</span>时，意味着<span class="hljs-meta">CPU</span>在这段时间里一直忙于处理进程，没有闲置时间，达到了理论上的满载状态。<br>多核<span class="hljs-meta">CPU</span>环境中，负载平均值应当参照<span class="hljs-meta">CPU</span>核心数量。例如，对于一个<span class="hljs-number">8</span>核<span class="hljs-meta">CPU</span>来说，理想的负载应该低于<span class="hljs-number">8.0</span>，即所有核心都被充分利用且无进程等待。不过，实际上，由于进程并非总是能够完全并行处理，所以理想的负载水平往往低于实际核心数，一般认为是核心数的<span class="hljs-number">0.7</span>倍左右较为健康。<br>解读负载值：<br><br>负载值小于<span class="hljs-number">1</span>表明系统整体上是轻载的，<span class="hljs-meta">CPU</span>有足够的处理能力和时间处理新的请求。<br>负载值接近或超过<span class="hljs-meta">CPU</span>核心数，意味着系统可能正面临资源瓶颈，有较多的进程在等待<span class="hljs-meta">CPU</span>时间片分配，可能会出现性能下降或响应延迟。<br>长时间持续较高的负载可能意味着系统配置不足或者有进程出现了性能问题，需要进一步排查优化。<br></code></pre></td></tr></table></figure><p><strong>3.优化做过哪些？内核参数优化，具体讲下</strong></p><figure class="highlight stylus"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br></pre></td><td class="code"><pre><code class="hljs stylus">内核参数优化是Linux系统管理员进行系统性能调优的重要手段之一，主要目的是为了让内核更好地适应特定的应用场景和硬件环境，从而提升系统性能、稳定性和安全性。下面是一些具体的内核参数优化方向和实例：<br><br><span class="hljs-number">1</span>. 处理器调度相关参数<br>kernel<span class="hljs-selector-class">.sched_migration_cost_ns</span>: 进程迁移成本，影响CPU亲和性策略下的任务调度决策。<br>kernel<span class="hljs-selector-class">.sched_child_runs_first</span>: 控制子进程是否优先执行，对于某些实时性要求高的应用程序可能需要调整。<br>kernel<span class="hljs-selector-class">.sched_min_granularity_ns</span> 和 kernel<span class="hljs-selector-class">.sched_latency_ns</span>: 定义调度器最小粒度和目标延迟，影响任务切换频率。<br><span class="hljs-number">2</span>. 内存管理参数<br>vm<span class="hljs-selector-class">.swappiness</span>: 控制系统在物理内存不足时多快地开始交换内存页到磁盘。较小的值倾向于保持更多的内存数据不被换出。<br>vm<span class="hljs-selector-class">.dirty_ratio</span> 和 vm<span class="hljs-selector-class">.dirty_background_ratio</span>: 控制内核脏页缓存的比例，影响I/O性能和数据同步策略。<br><span class="hljs-number">3</span>. 文件系统和I/O相关参数<br>fs<span class="hljs-selector-class">.file-max</span>: 系统允许的最大同时打开文件描述符数量，对高并发服务如数据库和Web服务器尤其重要。<br>vm<span class="hljs-selector-class">.max_map_count</span>: 最大可映射区域数量，对于Elasticsearch等依赖大量内存映射的应用很重要。<br>fs<span class="hljs-selector-class">.inotify</span><span class="hljs-selector-class">.max_user_watches</span>, fs<span class="hljs-selector-class">.inotify</span><span class="hljs-selector-class">.max_queued_events</span> 和 fs<span class="hljs-selector-class">.inotify</span><span class="hljs-selector-class">.max_user_instances</span>: 优化inotify事件监听机制，对需要监控大量文件变化的应用有用。<br><span class="hljs-number">4</span>. 网络栈相关参数<br>net<span class="hljs-selector-class">.ipv4</span><span class="hljs-selector-class">.tcp_syncookies</span>: 控制是否启用SYN Cookies，有助于缓解SYN洪泛攻击。<br>net<span class="hljs-selector-class">.ipv4</span><span class="hljs-selector-class">.tcp_max_tw_buckets</span>: 控制TIME_WAIT状态连接的最大数量，对高并发短连接场景进行优化。<br>net<span class="hljs-selector-class">.core</span><span class="hljs-selector-class">.somaxconn</span>: 套接字监听队列的最大长度，影响服务器接受新连接的能力。<br><span class="hljs-number">5</span>. TCP/IP参数<br>net<span class="hljs-selector-class">.ipv4</span><span class="hljs-selector-class">.tcp_keepalive_time</span>: TCP连接保持活动探测的时间间隔。<br>net<span class="hljs-selector-class">.ipv4</span><span class="hljs-selector-class">.tcp_fin_timeout</span> 和 net<span class="hljs-selector-class">.ipv4</span><span class="hljs-selector-class">.tcp_syn_retries</span>: 控制TCP连接关闭和建立过程中的超时次数。<br>net<span class="hljs-selector-class">.ipv4</span><span class="hljs-selector-class">.tcp_window_scaling</span> 和 net<span class="hljs-selector-class">.ipv4</span><span class="hljs-selector-class">.tcp_timestamps</span>: 开启TCP窗口缩放和时间戳选项，有利于提高带宽利用率。<br><span class="hljs-number">6</span>. 其他性能和安全相关参数<br>kernel<span class="hljs-selector-class">.pid_max</span>: 系统允许的最大PID数量。<br>kernel<span class="hljs-selector-class">.shmmax</span> 和 kernel<span class="hljs-selector-class">.shmall</span>: 控制共享内存段的最大大小和页数，对依赖共享内存的应用有意义。<br>进行内核参数优化时，需结合实际应用场景分析系统瓶颈，不应盲目调整，否则可能导致系统不稳定或无法达到预期效果。调整后，通常会通过sysctl命令动态修改，并持久化至/etc/sysctl.conf文件中，确保重启后依然生效。对于某些关键系统服务，还应配合工具如systemd-tuned或sysctl.d目录下的配置文件进行更加精细化的调优。<br><br><br><br><br><br><br><br><br><br><span class="hljs-selector-id">#modify</span> the value of  kernel<span class="hljs-selector-class">.shmmax</span><br>sed -<span class="hljs-selector-tag">i</span>  <span class="hljs-string">&#x27;s/kernel\.shmmax/#kernel\.shmmax/&#x27;</span>  /etc/sysctl<span class="hljs-selector-class">.conf</span><br>sed -<span class="hljs-selector-tag">i</span>  <span class="hljs-string">&quot;/#kernel\.shmmax/a \kernel.shmmax = $half_mem&quot;</span> /etc/sysctl<span class="hljs-selector-class">.conf</span><br>echo <span class="hljs-string">&quot;kernel.shmmax = $half_mem&quot;</span>  &gt;&gt; /etc/sysctl<span class="hljs-selector-class">.conf</span><br>echo <span class="hljs-string">&quot;kernel.shmall = 2097152&quot;</span>  &gt;&gt; /etc/sysctl<span class="hljs-selector-class">.conf</span><br>echo <span class="hljs-string">&quot;kernel.pid_max = 65535&quot;</span>  &gt;&gt; /etc/sysctl<span class="hljs-selector-class">.conf</span><br>echo <span class="hljs-string">&quot;net.ipv4.neigh.default.gc_thresh1 = 4096&quot;</span>  &gt;&gt; /etc/sysctl<span class="hljs-selector-class">.conf</span><br>echo <span class="hljs-string">&quot;net.ipv4.neigh.default.gc_thresh2 = 8192&quot;</span>  &gt;&gt; /etc/sysctl<span class="hljs-selector-class">.conf</span><br>echo <span class="hljs-string">&quot;net.ipv4.neigh.default.gc_thresh3 = 8192&quot;</span>  &gt;&gt; /etc/sysctl<span class="hljs-selector-class">.conf</span><br>echo <span class="hljs-string">&quot;kernel.shmmni = 4096&quot;</span>                             &gt;&gt;/etc/sysctl<span class="hljs-selector-class">.conf</span><br>echo <span class="hljs-string">&quot;kernel.sem = 250 32000 100 128&quot;</span>           &gt;&gt;/etc/sysctl<span class="hljs-selector-class">.conf</span><br>echo <span class="hljs-string">&quot;kernel.shmmni = 4096&quot;</span>                             &gt;&gt;/etc/sysctl<span class="hljs-selector-class">.conf</span><br>echo <span class="hljs-string">&quot;kernel.sem = 250 32000 100 128&quot;</span>           &gt;&gt;/etc/sysctl<span class="hljs-selector-class">.conf</span><br><span class="hljs-selector-id">#set</span> the other kernel parameters as follows:<br>echo <span class="hljs-string">&quot;fs.aio-max-nr = 1048576&quot;</span>                  &gt;&gt; /etc/sysctl<span class="hljs-selector-class">.conf</span><br>echo <span class="hljs-string">&quot;fs.file-max = 6815744&quot;</span>                    &gt;&gt;/etc/sysctl<span class="hljs-selector-class">.conf</span><br>echo <span class="hljs-string">&quot;net.ipv4.ip_local_port_range = 9000 65500&quot;</span>&gt;&gt;/etc/sysctl<span class="hljs-selector-class">.conf</span><br>echo <span class="hljs-string">&quot;net.core.rmem_default = 262144&quot;</span>   &gt;&gt;/etc/sysctl<span class="hljs-selector-class">.conf</span><br>echo <span class="hljs-string">&quot;net.core.rmem_max = 4194304&quot;</span>              &gt;&gt;/etc/sysctl<span class="hljs-selector-class">.conf</span><br>echo <span class="hljs-string">&quot;net.core.wmem_default = 262144&quot;</span>   &gt;&gt;/etc/sysctl<span class="hljs-selector-class">.conf</span><br>echo <span class="hljs-string">&quot;net.core.wmem_max = 1048586&quot;</span>              &gt;&gt;/etc/sysctl<span class="hljs-selector-class">.conf</span><br>echo <span class="hljs-string">&quot;net.ipv4.ip_forward=1&quot;</span>              &gt;&gt;/etc/sysctl<span class="hljs-selector-class">.conf</span><br>echo <span class="hljs-string">&quot;net.bridge.bridge-nf-call-iptables=1&quot;</span> &gt;&gt;/etc/sysctl<span class="hljs-selector-class">.conf</span><br>echo <span class="hljs-string">&quot;net.bridge.bridge-nf-call-ip6tables=1&quot;</span> &gt;&gt;/etc/sysctl<span class="hljs-selector-class">.conf</span><br><br><br><br><br><br>这段脚本是对Linux系统内核参数进行设置，通过修改/etc/sysctl.conf文件，使得重启后这些参数也能生效。以下是各项参数的含义：<br><br>sed -<span class="hljs-selector-tag">i</span> <span class="hljs-string">&#x27;s/kernel.shmmax/#kernel.shmmax/&#x27;</span> /etc/sysctl.conf：注释掉原有的kernel.shmmax配置项。shmmax是系统允许单个共享内存段的最大大小（单位通常是KB）。如果之前已存在该配置，则将其注释掉以便重新定义。<br><br>sed -<span class="hljs-selector-tag">i</span> <span class="hljs-string">&quot;/#kernel.shmmax/a \kernel.shmmax = $half_mem&quot;</span> /etc/sysctl.conf：在注释后的同一行添加新的kernel.shmmax配置，其值等于变量<span class="hljs-variable">$half_mem</span>，假设这个变量存储了系统一半的内存大小作为共享内存最大值。<br><br>后续的echo语句都是向/etc/sysctl.conf追加新的配置项：<br><br>kernel.shmall：决定系统范围内共享内存页面的数量，这里的值设为<span class="hljs-number">2097152</span>。<br><br>kernel.pid_max：系统支持的最大进程ID数，设为<span class="hljs-number">65535</span>。<br><br>net<span class="hljs-selector-class">.ipv4</span><span class="hljs-selector-class">.neigh</span><span class="hljs-selector-class">.default</span>.gc_thresh1，gc_thresh2和gc_thresh3：IPv4邻居表条目的垃圾回收阈值，分别设定为<span class="hljs-number">4096</span>，<span class="hljs-number">8192</span>和<span class="hljs-number">8192</span>，用于控制ARP高速缓存表的清理策略。<br><br>kernel.shmmni：系统范围内共享内存段的最大数目，设为<span class="hljs-number">4096</span>。<br><br>kernel.sem：信号量的四个参数，分别为<span class="hljs-built_in">SEMMSL</span>(每个信号量集合中信号量的最大数目)、<span class="hljs-built_in">SEMMNS</span>(系统范围内的信号量最大数目)、<span class="hljs-built_in">SEMOPM</span>(单个semop系统调用中操作数的最大数目)、<span class="hljs-built_in">SEMMNI</span>(信号量集合的最大数目)，这里设为<span class="hljs-number">250</span>, <span class="hljs-number">32000</span>, <span class="hljs-number">100</span>, <span class="hljs-number">128</span>。<br><br>fs.aio-max-nr：异步I/O挂起操作的最大数量，设为<span class="hljs-number">1048576</span>。<br><br>fs.file-max：系统允许的最大文件句柄数，设为<span class="hljs-number">6815744</span>。<br><br>net<span class="hljs-selector-class">.ipv4</span>.ip_local_port_range：本地端口范围，用于向外发起连接时可用的源端口范围，设为<span class="hljs-number">9000</span>到<span class="hljs-number">65500</span>。<br><br>net<span class="hljs-selector-class">.core</span>.rmem_default和net<span class="hljs-selector-class">.core</span>.rmem_max：接收缓冲区默认大小和最大大小，单位字节，分别设为<span class="hljs-number">262144</span>和<span class="hljs-number">4194304</span>。<br><br>net<span class="hljs-selector-class">.core</span>.wmem_default和net<span class="hljs-selector-class">.core</span>.wmem_max：发送缓冲区默认大小和最大大小，单位字节，分别设为<span class="hljs-number">262144</span>和<span class="hljs-number">1048586</span>。<br><br>net<span class="hljs-selector-class">.ipv4</span>.ip_forward：开启IP转发功能，设为<span class="hljs-number">1</span>，表示允许本机做为路由器进行IP包转发。<br><br>net<span class="hljs-selector-class">.bridge</span>.bridge-nf-call-iptables和net<span class="hljs-selector-class">.bridge</span>.bridge-nf-call-ip6tables：允许在桥接模式下应用iptables规则，这对于虚拟化环境和容器环境中的网络过滤十分重要。<br><br>以上参数的设置需要根据实际系统需求和应用场景进行合理配置，过大的值可能会浪费系统资源，而过小的值则可能导致性能瓶颈。<br></code></pre></td></tr></table></figure><p><strong>4.nginx上的服务访问不通 怎么排查</strong></p><figure class="highlight"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs"><br></code></pre></td></tr></table></figure><p>如果 <strong>Nginx</strong> 上的服务无法访问，可能有多个原因，导致请求无法成功到达或响应。下面是一些常见的排查步骤，帮助你找出并解决问题。</p><h3 id="1-检查-Nginx-配置文件"><a href="#1-检查-Nginx-配置文件" class="headerlink" title="1. 检查 Nginx 配置文件"></a><strong>1. 检查 Nginx 配置文件</strong></h3><p>首先，确认 Nginx 配置是否正确，常见的问题包括端口未正确绑定、<code>server_name</code> 设置不正确、反向代理配置错误等。</p><h4 id="（1）检查-Nginx-配置文件"><a href="#（1）检查-Nginx-配置文件" class="headerlink" title="（1）检查 Nginx 配置文件"></a>（1）检查 Nginx 配置文件</h4><p>Nginx 的默认配置文件通常位于 <code>/etc/nginx/nginx.conf</code> 或 <code>/etc/nginx/sites-available/default</code>（具体路径取决于你的操作系统和安装方式）。确保配置文件中没有语法错误。</p><p>你可以运行以下命令来检查 Nginx 配置文件的语法：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> nginx -t<br></code></pre></td></tr></table></figure><p>如果有错误，Nginx 会给出详细的错误信息。解决配置问题后，重新加载配置：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> systemctl reload nginx<br></code></pre></td></tr></table></figure><h4 id="（2）检查-server-name-和监听端口"><a href="#（2）检查-server-name-和监听端口" class="headerlink" title="（2）检查 server_name 和监听端口"></a>（2）检查 <code>server_name</code> 和监听端口</h4><p>确保你的配置文件中有正确的 <code>server_name</code>（即域名或 IP 地址）和端口号。例如：</p><figure class="highlight nginx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs nginx"><span class="hljs-section">server</span> &#123;<br>    <span class="hljs-attribute">listen</span> <span class="hljs-number">80</span>;<br>    <span class="hljs-attribute">server_name</span> example.com;  <span class="hljs-comment"># 确保这个地址是正确的</span><br>    <span class="hljs-section">location</span> / &#123;<br>        <span class="hljs-attribute">proxy_pass</span> http://127.0.0.1:3000;  <span class="hljs-comment"># 正确设置代理的目标服务</span><br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><h4 id="（3）检查代理服务配置"><a href="#（3）检查代理服务配置" class="headerlink" title="（3）检查代理服务配置"></a>（3）检查代理服务配置</h4><p>如果 Nginx 作为反向代理，确保代理目标服务的地址和端口正确，且服务正在运行。</p><hr><h3 id="2-检查防火墙设置"><a href="#2-检查防火墙设置" class="headerlink" title="2. 检查防火墙设置"></a><strong>2. 检查防火墙设置</strong></h3><p>防火墙设置可能阻止外部访问 Nginx 或应用服务的端口。</p><h4 id="（1）检查-Nginx-端口是否开放"><a href="#（1）检查-Nginx-端口是否开放" class="headerlink" title="（1）检查 Nginx 端口是否开放"></a>（1）检查 Nginx 端口是否开放</h4><p>确保防火墙允许通过 Nginx 使用的端口（通常是 80 和 443）。</p><ul><li><p>对于 </p><p>ufw</p><p> 防火墙：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> ufw allow <span class="hljs-string">&#x27;Nginx Full&#x27;</span><br><span class="hljs-built_in">sudo</span> ufw <span class="hljs-built_in">enable</span><br></code></pre></td></tr></table></figure></li><li><p>对于 </p><p>firewalld</p><p>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> firewall-cmd --permanent --add-service=http<br><span class="hljs-built_in">sudo</span> firewall-cmd --permanent --add-service=https<br><span class="hljs-built_in">sudo</span> firewall-cmd --reload<br></code></pre></td></tr></table></figure></li></ul><h4 id="（2）检查端口是否被占用"><a href="#（2）检查端口是否被占用" class="headerlink" title="（2）检查端口是否被占用"></a>（2）检查端口是否被占用</h4><p>运行以下命令检查 Nginx 是否在监听指定的端口：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> netstat -tulnp | grep nginx<br></code></pre></td></tr></table></figure><p>或者使用：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> ss -tulnp | grep nginx<br></code></pre></td></tr></table></figure><hr><h3 id="3-检查应用服务是否运行"><a href="#3-检查应用服务是否运行" class="headerlink" title="3. 检查应用服务是否运行"></a><strong>3. 检查应用服务是否运行</strong></h3><p>如果 Nginx 配置了反向代理，确保目标应用服务正在运行。</p><h4 id="（1）检查应用服务"><a href="#（1）检查应用服务" class="headerlink" title="（1）检查应用服务"></a>（1）检查应用服务</h4><p>检查应用程序（如 Node.js、Python 等）是否在指定端口上运行。例如，对于 Node.js，可以运行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">ps aux | grep node<br></code></pre></td></tr></table></figure><p>或者检查应用服务的状态：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> systemctl status &lt;your-app-service&gt;<br></code></pre></td></tr></table></figure><h4 id="（2）检查应用日志"><a href="#（2）检查应用日志" class="headerlink" title="（2）检查应用日志"></a>（2）检查应用日志</h4><p>查看应用服务的日志，确保没有报错。应用的日志文件路径通常在配置文件中指定，常见的日志文件包括：</p><ul><li><code>/var/log/nginx/access.log</code></li><li><code>/var/log/nginx/error.log</code></li><li>应用的日志文件（如 <code>/var/log/myapp.log</code>）</li></ul><hr><h3 id="4-检查-Nginx-和应用服务的日志"><a href="#4-检查-Nginx-和应用服务的日志" class="headerlink" title="4. 检查 Nginx 和应用服务的日志"></a><strong>4. 检查 Nginx 和应用服务的日志</strong></h3><p>查看 Nginx 错误日志可以帮助定位访问问题。</p><h4 id="（1）查看-Nginx-错误日志"><a href="#（1）查看-Nginx-错误日志" class="headerlink" title="（1）查看 Nginx 错误日志"></a>（1）查看 Nginx 错误日志</h4><p>Nginx 错误日志通常位于 <code>/var/log/nginx/error.log</code>。查看日志中是否有任何明显的错误信息，例如连接超时、权限问题等：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> <span class="hljs-built_in">tail</span> -f /var/log/nginx/error.log<br></code></pre></td></tr></table></figure><h4 id="（2）查看-Nginx-访问日志"><a href="#（2）查看-Nginx-访问日志" class="headerlink" title="（2）查看 Nginx 访问日志"></a>（2）查看 Nginx 访问日志</h4><p>访问日志可以帮助你确认请求是否成功到达 Nginx，以及是否成功被处理：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> <span class="hljs-built_in">tail</span> -f /var/log/nginx/access.log<br></code></pre></td></tr></table></figure><hr><h3 id="5-测试网络连接"><a href="#5-测试网络连接" class="headerlink" title="5. 测试网络连接"></a><strong>5. 测试网络连接</strong></h3><p>使用 <code>curl</code> 或 <code>wget</code> 等工具测试从本地或外部访问 Nginx。</p><h4 id="（1）从本地访问"><a href="#（1）从本地访问" class="headerlink" title="（1）从本地访问"></a>（1）从本地访问</h4><p>可以使用 <code>curl</code> 测试本地 Nginx 服务是否能正常访问：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">curl http://localhost<br></code></pre></td></tr></table></figure><p>如果本地访问没有问题，但外部访问无法连接，可能是防火墙、网络设置或端口配置问题。</p><h4 id="（2）从外部访问"><a href="#（2）从外部访问" class="headerlink" title="（2）从外部访问"></a>（2）从外部访问</h4><p>如果你的服务器有公网 IP，可以尝试从外部机器访问：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">curl http://your-server-ip<br></code></pre></td></tr></table></figure><hr><h3 id="6-检查-Nginx-服务状态"><a href="#6-检查-Nginx-服务状态" class="headerlink" title="6. 检查 Nginx 服务状态"></a><strong>6. 检查 Nginx 服务状态</strong></h3><p>检查 Nginx 服务是否正常运行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> systemctl status nginx<br></code></pre></td></tr></table></figure><p>如果 Nginx 没有运行，可以尝试重新启动服务：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> systemctl restart nginx<br></code></pre></td></tr></table></figure><hr><h3 id="7-检查-DNS-配置"><a href="#7-检查-DNS-配置" class="headerlink" title="7. 检查 DNS 配置"></a><strong>7. 检查 DNS 配置</strong></h3><p>如果你使用域名来访问 Nginx 服务，确保 DNS 解析正确。你可以使用 <code>nslookup</code> 或 <code>dig</code> 命令检查 DNS 是否正确解析到服务器 IP。</p><h4 id="（1）使用-nslookup-检查域名解析"><a href="#（1）使用-nslookup-检查域名解析" class="headerlink" title="（1）使用 nslookup 检查域名解析"></a>（1）使用 <code>nslookup</code> 检查域名解析</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">nslookup example.com<br></code></pre></td></tr></table></figure><h4 id="（2）使用-dig-命令"><a href="#（2）使用-dig-命令" class="headerlink" title="（2）使用 dig 命令"></a>（2）使用 <code>dig</code> 命令</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">dig example.com<br></code></pre></td></tr></table></figure><hr><h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a><strong>总结</strong></h3><p>通过这些排查步骤，你应该能找出为什么 Nginx 服务无法访问的原因。常见的原因包括：</p><ul><li>配置错误（例如端口绑定、反向代理错误）</li><li>防火墙或端口未开放</li><li>应用服务未运行或配置错误</li><li>DNS 配置问题</li></ul><p>逐步检查并解决这些问题，通常可以恢复服务的正常访问。如果需要更具体的帮助，可以提供错误日志或配置详情，我可以帮助你进一步分析。</p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>kubeasz部署k8s</title>
    <link href="/2024/12/04/2024-12-05%20kubeasz%E9%83%A8%E7%BD%B2k8s/"/>
    <url>/2024/12/04/2024-12-05%20kubeasz%E9%83%A8%E7%BD%B2k8s/</url>
    
    <content type="html"><![CDATA[<h2 id="00-集群规划和基础参数设定"><a href="#00-集群规划和基础参数设定" class="headerlink" title="00-集群规划和基础参数设定"></a>00-集群规划和基础参数设定</h2><h3 id="HA-architecture"><a href="#HA-architecture" class="headerlink" title="HA architecture"></a>HA architecture</h3><p><img src="https://picture-base.oss-cn-hangzhou.aliyuncs.com/image-20241204235826382.png" alt="image-20241204235826382"></p><ul><li>注意1：确保各节点时区设置一致、时间同步。 如果你的环境没有提供NTP 时间同步，推荐集成安装<a href="../guide/chrony.md">chrony</a></li><li>注意2：确保在干净的系统上开始安装，不要使用曾经装过kubeadm或其他k8s发行版的环境</li><li>注意3：建议操作系统升级到新的稳定内核，请结合阅读<a href="../guide/kernel_upgrade.md">内核升级文档</a></li><li>注意4：在公有云上创建多主集群，请结合阅读<a href="kubeasz_on_public_cloud.md">在公有云上部署 kubeasz</a></li></ul><h2 id="高可用集群所需节点配置如下"><a href="#高可用集群所需节点配置如下" class="headerlink" title="高可用集群所需节点配置如下"></a>高可用集群所需节点配置如下</h2><table><thead><tr><th align="left">角色</th><th align="left">数量</th><th align="left">描述</th></tr></thead><tbody><tr><td align="left">部署节点</td><td align="left">1</td><td align="left">运行ansible&#x2F;ezctl命令，一般复用第一个master节点</td></tr><tr><td align="left">etcd节点</td><td align="left">3</td><td align="left">注意etcd集群需要1,3,5,…奇数个节点，一般复用master节点</td></tr><tr><td align="left">master节点</td><td align="left">2</td><td align="left">高可用集群至少2个master节点</td></tr><tr><td align="left">node节点</td><td align="left">n</td><td align="left">运行应用负载的节点，可根据需要提升机器配置&#x2F;增加节点数</td></tr></tbody></table><p>机器配置：</p><ul><li>master节点：4c&#x2F;8g内存&#x2F;50g硬盘</li><li>worker节点：建议8c&#x2F;32g内存&#x2F;200g硬盘以上</li></ul><p>注意：默认配置下容器运行时和kubelet会占用&#x2F;var的磁盘空间，如果磁盘分区特殊，可以设置config.yml中的容器运行时和kubelet数据目录：<code>CONTAINERD_STORAGE_DIR</code> <code>DOCKER_STORAGE_DIR</code> <code>KUBELET_ROOT_DIR</code></p><p>在 kubeasz 2x 版本，多节点高可用集群安装可以使用2种方式</p><ul><li>1.按照本文步骤先规划准备，预先配置节点信息后，直接安装多节点高可用集群</li><li>2.先部署单节点集群 <a href="quickStart.md">AllinOne部署</a>，然后通过 <a href="../op/op-index.md">节点添加</a> 扩容成高可用集群</li></ul><h2 id="部署步骤"><a href="#部署步骤" class="headerlink" title="部署步骤"></a>部署步骤</h2><p>以下示例创建一个4节点的多主高可用集群，文档中命令默认都需要root权限运行。</p><h3 id="1-基础系统配置"><a href="#1-基础系统配置" class="headerlink" title="1.基础系统配置"></a>1.基础系统配置</h3><ul><li>2c&#x2F;4g内存&#x2F;40g硬盘（该配置仅测试用）</li><li>最小化安装<code>Ubuntu 16.04 server</code>或者<code>CentOS 7 Minimal</code></li><li>配置基础网络、更新源、SSH登录等</li></ul><h3 id="2-在每个节点安装依赖工具"><a href="#2-在每个节点安装依赖工具" class="headerlink" title="2.在每个节点安装依赖工具"></a>2.在每个节点安装依赖工具</h3><p>推荐使用ansible in docker 容器化方式运行，无需安装额外依赖。</p><h3 id="3-准备ssh免密登陆"><a href="#3-准备ssh免密登陆" class="headerlink" title="3.准备ssh免密登陆"></a>3.准备ssh免密登陆</h3><p>配置从部署节点能够ssh免密登陆所有节点，并且设置python软连接</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><code class="hljs bash">本地客户端生成公私钥：（一路回车默认即可）<br><br>ssh-keygen<br><br>ssh-copy-id -i ~/.ssh/id_rsa.pub root@192.168.0.51<br>ssh-copy-id -i ~/.ssh/id_rsa.pub root@192.168.0.52<br>ssh-copy-id -i ~/.ssh/id_rsa.pub root@192.168.0.53<br><br><span class="hljs-comment">#$IP为所有节点地址包括自身，按照提示输入yes 和root密码</span><br>ssh-copy-id <span class="hljs-variable">$IP</span> <br><br>ssh-copy-id 192.168.0.51<br>ssh-copy-id 192.168.0.52<br>ssh-copy-id 192.168.0.53<br></code></pre></td></tr></table></figure><h3 id="4-在部署节点编排k8s安装"><a href="#4-在部署节点编排k8s安装" class="headerlink" title="4.在部署节点编排k8s安装"></a>4.在部署节点编排k8s安装</h3><ul><li>4.1 下载项目源码、二进制及离线镜像</li></ul><p>下载工具脚本ezdown，举例使用kubeasz版本3.5.0</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">export</span> release=3.5.0<br>wget https://github.com/easzlab/kubeasz/releases/download/<span class="hljs-variable">$&#123;release&#125;</span>/ezdown<br><span class="hljs-built_in">chmod</span> +x ./ezdown<br><br><br><span class="hljs-built_in">export</span> release=3.6.1<br>wget https://github.com/easzlab/kubeasz/releases/download/<span class="hljs-variable">$&#123;release&#125;</span>/ezdown<br><span class="hljs-built_in">chmod</span> +x ./ezdown<br></code></pre></td></tr></table></figure><p>下载kubeasz代码、二进制、默认容器镜像（更多关于ezdown的参数，运行.&#x2F;ezdown 查看）</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 国内环境</span><br>./ezdown -D<br><span class="hljs-comment"># 海外环境</span><br><span class="hljs-comment">#./ezdown -D -m standard</span><br></code></pre></td></tr></table></figure><p>【可选】下载额外容器镜像（cilium,flannel,prometheus等）</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 按需下载</span><br>./ezdown -X flannel<br>./ezdown -X prometheus<br>...<br></code></pre></td></tr></table></figure><p>【可选】下载离线系统包 (适用于无法使用yum&#x2F;apt仓库情形)</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">./ezdown -P<br></code></pre></td></tr></table></figure><p>上述脚本运行成功后，所有文件（kubeasz代码、二进制、离线镜像）均已整理好放入目录<code>/etc/kubeasz</code></p><ul><li><p>4.2 创建集群配置实例</p></li><li><pre><code class="hljs">centos7-2009-1centos7-2009-2centos7-2009-3<figure class="highlight routeros"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><code class="hljs routeros"><br>  <br><br>``` bash<br><span class="hljs-comment"># 容器化运行kubeasz</span><br>./ezdown -S<br><br><span class="hljs-comment"># 创建新集群 k8s-01</span><br>docker exec -it kubeasz ezctl new k8s-01<br>2021-01-19 10:48:23 <span class="hljs-built_in">DEBUG</span> generate custom cluster files <span class="hljs-keyword">in</span> /etc/kubeasz/clusters/k8s-01<br>2021-01-19 10:48:23 <span class="hljs-built_in">DEBUG</span> <span class="hljs-built_in">set</span> version of common plugins<br>2021-01-19 10:48:23 <span class="hljs-built_in">DEBUG</span> cluster k8s-01: files successfully created.<br>2021-01-19 10:48:23 <span class="hljs-built_in">INFO</span> next steps 1: <span class="hljs-keyword">to</span><span class="hljs-built_in"> config </span><span class="hljs-string">&#x27;/etc/kubeasz/clusters/k8s-01/hosts&#x27;</span><br>2021-01-19 10:48:23 <span class="hljs-built_in">INFO</span> next steps 2: <span class="hljs-keyword">to</span><span class="hljs-built_in"> config </span><span class="hljs-string">&#x27;/etc/kubeasz/clusters/k8s-01/config.yml&#x27;</span><br></code></pre></td></tr></table></figure>然后根据提示配置&#39;/etc/kubeasz/clusters/k8s-01/hosts&#39; 和 &#39;/etc/kubeasz/clusters/k8s-01/config.yml&#39;：根据前面节点规划修改hosts 文件和其他集群层面的主要配置选项；其他集群组件等配置项可以在config.yml 文件中修改。</code></pre></li><li><p>4.3 开始安装<br>如果你对集群安装流程不熟悉，请阅读项目首页 <strong>安装步骤</strong> 讲解后分步安装，并对 <strong>每步都进行验证</strong></p></li></ul><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment">#建议使用alias命令，查看~/.bashrc 文件应该包含：alias dk=&#x27;docker exec -it kubeasz&#x27;</span><br><span class="hljs-built_in">source</span> ~/.bashrc<br><br><span class="hljs-comment"># 一键安装，等价于执行docker exec -it kubeasz ezctl setup k8s-01 all</span><br>dk ezctl setup k8s-01 all<br><br><span class="hljs-comment"># 或者分步安装，具体使用 dk ezctl help setup 查看分步安装帮助信息</span><br><span class="hljs-comment"># dk ezctl setup k8s-01 01</span><br><span class="hljs-comment"># dk ezctl setup k8s-01 02</span><br><span class="hljs-comment"># dk ezctl setup k8s-01 03</span><br><span class="hljs-comment"># dk ezctl setup k8s-01 04</span><br>...<br>dk ezctl setup k8s-01 07<br><br><br>[root@centos7-2009-1 ~]# systemctl stop etcd<br>[root@centos7-2009-1 ~]# systemctl stop kube-apiserver<br>[root@centos7-2009-1 ~]# systemctl stop kube-scheduler<br>[root@centos7-2009-1 ~]# systemctl stop kube-controller-manager<br>[root@centos7-2009-1 ~]# systemctl stop kubelet<br><br></code></pre></td></tr></table></figure><figure class="highlight prolog"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs prolog">fatal: [<span class="hljs-number">192.168</span><span class="hljs-number">.0</span><span class="hljs-number">.52</span>]: <span class="hljs-symbol">FAILED</span>! =&gt; &#123;<span class="hljs-string">&quot;changed&quot;</span>: true, <span class="hljs-string">&quot;cmd&quot;</span>: <span class="hljs-string">&quot;/etc/kubeasz/bin/kubectl config set-credentials system:node:worker-02 --client-certificate=/etc/kubeasz/clusters/k8s-01/ssl/worker-02-kubelet.pem --embed-certs=true --client-key=/etc/kubeasz/clusters/k8s-01/ssl/worker-02-kubelet-key.pem --kubeconfig=/etc/kubeasz/clusters/k8s-01/worker-02-kubelet.kubeconfig&quot;</span>, <span class="hljs-string">&quot;delta&quot;</span>: <span class="hljs-string">&quot;0:00:00.046326&quot;</span>, <span class="hljs-string">&quot;end&quot;</span>: <span class="hljs-string">&quot;2023-08-09 14:37:27.535734&quot;</span>, <span class="hljs-string">&quot;msg&quot;</span>: <span class="hljs-string">&quot;non-zero return code&quot;</span>, <span class="hljs-string">&quot;rc&quot;</span>: <span class="hljs-number">1</span>, <span class="hljs-string">&quot;start&quot;</span>: <span class="hljs-string">&quot;2023-08-09 14:37:27.489408&quot;</span>, <span class="hljs-string">&quot;stderr&quot;</span>: <span class="hljs-string">&quot;error: open /etc/kubeasz/clusters/k8s-01/worker-02-kubelet.kubeconfig.lock: file exists&quot;</span>, <span class="hljs-string">&quot;stderr_lines&quot;</span>: [<span class="hljs-string">&quot;error: open /etc/kubeasz/clusters/k8s-01/worker-02-kubelet.kubeconfig.lock: file exists&quot;</span>], <span class="hljs-string">&quot;stdout&quot;</span>: <span class="hljs-string">&quot;&quot;</span>, <span class="hljs-string">&quot;stdout_lines&quot;</span>: []&#125;<br><br></code></pre></td></tr></table></figure><p>更多ezctl使用帮助，请参考<a href="ezctl.md">这里</a></p><p><a href="01-CA_and_prerequisite.md">后一篇</a></p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>2024-3-19 两张图全面理解k8s原理</title>
    <link href="/2024/12/04/2024-3-19%20%E4%B8%A4%E5%BC%A0%E5%9B%BE%E5%85%A8%E9%9D%A2%E7%90%86%E8%A7%A3k8s%E5%8E%9F%E7%90%86/"/>
    <url>/2024/12/04/2024-3-19%20%E4%B8%A4%E5%BC%A0%E5%9B%BE%E5%85%A8%E9%9D%A2%E7%90%86%E8%A7%A3k8s%E5%8E%9F%E7%90%86/</url>
    
    <content type="html"><![CDATA[<h1 id="Kubernetes-简介"><a href="#Kubernetes-简介" class="headerlink" title="Kubernetes 简介"></a>Kubernetes 简介</h1><p><strong>Kubernetes</strong> 源于希腊语，意为“舵手”。k8s 缩写是因为 k 和 s 之间有八个字符的原因。它是 Google 在 2015 开源的容器调度编排的平台。</p><p><strong>Kubernetes</strong> 作为一款优秀的容器编排工具，拥有非常精妙的架构设计。</p><h1 id="Kubernetes-架构"><a href="#Kubernetes-架构" class="headerlink" title="Kubernetes 架构"></a>Kubernetes 架构</h1><h5 id="Kubernetes-是一个-Master-Worker-的架构，Master-可理解为-Kubernetes-的控制面，Worker-理解为-Kubernetes-的数据面。"><a href="#Kubernetes-是一个-Master-Worker-的架构，Master-可理解为-Kubernetes-的控制面，Worker-理解为-Kubernetes-的数据面。" class="headerlink" title="Kubernetes 是一个 Master + Worker 的架构，Master 可理解为 Kubernetes 的控制面，Worker 理解为 Kubernetes 的数据面。"></a><strong>Kubernetes</strong> 是一个 <strong>Master + Worker</strong> 的架构，<strong>Master</strong> 可理解为 Kubernetes 的控制面，<strong>Worker</strong> 理解为 Kubernetes 的数据面。</h5><h5 id="Master-节点一般只运行-Kubernetes-控制组件，是整个集群的大脑，一般不运行业务容器。"><a href="#Master-节点一般只运行-Kubernetes-控制组件，是整个集群的大脑，一般不运行业务容器。" class="headerlink" title="Master 节点一般只运行 Kubernetes 控制组件，是整个集群的大脑，一般不运行业务容器。"></a>Master 节点一般只运行 Kubernetes 控制组件，是整个集群的大脑，一般不运行业务容器。</h5><h5 id="Worker-节点是运行业务容器的节点。"><a href="#Worker-节点是运行业务容器的节点。" class="headerlink" title="Worker 节点是运行业务容器的节点。"></a>Worker 节点是运行业务容器的节点。</h5><p><img src="https://picture-base.oss-cn-hangzhou.aliyuncs.com/picture/202403191453702.png" alt="image-20240319145332645"></p><h3 id="Master"><a href="#Master" class="headerlink" title="Master"></a>Master</h3><h5 id="Kubernetes-Master-节点需要运行以下组件："><a href="#Kubernetes-Master-节点需要运行以下组件：" class="headerlink" title="Kubernetes Master 节点需要运行以下组件："></a>Kubernetes <strong>Master</strong> 节点需要运行以下组件：</h5><ul><li><h5 id="•-Kube-apiserver：Kube-apiserver-是-Kubernetes-的-API-入口，是集群流量入口；"><a href="#•-Kube-apiserver：Kube-apiserver-是-Kubernetes-的-API-入口，是集群流量入口；" class="headerlink" title="• Kube-apiserver：Kube-apiserver 是 Kubernetes 的 API 入口，是集群流量入口；"></a>• <strong>Kube-apiserver：</strong>Kube-apiserver 是 Kubernetes 的 API 入口，是集群流量入口；</h5></li><li><h5 id="•-Kube-controller-manager：Kube-controller-manager-包含很多个-Controller，用于调谐集群中的资源；"><a href="#•-Kube-controller-manager：Kube-controller-manager-包含很多个-Controller，用于调谐集群中的资源；" class="headerlink" title="• Kube-controller-manager：Kube-controller-manager 包含很多个 Controller，用于调谐集群中的资源；"></a>• <strong>Kube-controller-manager：</strong>Kube-controller-manager 包含很多个 <strong>Controller</strong>，用于调谐集群中的资源；</h5></li><li><h5 id="•-Kube-scheduler：Kube-scheduler-是集群中默认调度器，给-Pod-选择最优节点调度；"><a href="#•-Kube-scheduler：Kube-scheduler-是集群中默认调度器，给-Pod-选择最优节点调度；" class="headerlink" title="• Kube-scheduler：Kube-scheduler 是集群中默认调度器，给 Pod 选择最优节点调度；"></a>• <strong>Kube-scheduler：</strong>Kube-scheduler 是集群中默认调度器，给 Pod 选择最优节点调度；</h5></li><li><h5 id="•-Etcd：Kuebernetes-的后端存储，集群中所有可持久化数据都存储在-Etcd-中。"><a href="#•-Etcd：Kuebernetes-的后端存储，集群中所有可持久化数据都存储在-Etcd-中。" class="headerlink" title="• Etcd：Kuebernetes 的后端存储，集群中所有可持久化数据都存储在 Etcd 中。"></a>• <strong>Etcd：</strong>Kuebernetes 的后端存储，集群中所有可持久化数据都存储在 Etcd 中。</h5></li></ul><blockquote><h5 id="Kubernetes-中采用声明式-API，即-API-不需要关心具体功能实现，只需关心最终状态。一个-API-对应一个-Controller，具体功能由-Controller-实现同时调谐至预期状态。"><a href="#Kubernetes-中采用声明式-API，即-API-不需要关心具体功能实现，只需关心最终状态。一个-API-对应一个-Controller，具体功能由-Controller-实现同时调谐至预期状态。" class="headerlink" title="Kubernetes 中采用声明式 API，即 API 不需要关心具体功能实现，只需关心最终状态。一个 API 对应一个 Controller，具体功能由 Controller 实现同时调谐至预期状态。"></a>Kubernetes 中采用<strong>声明式 API</strong>，即 API 不需要关心具体功能实现，只需关心最终状态。一个 API 对应一个 Controller，具体功能由 Controller 实现同时调谐至预期状态。</h5></blockquote><h5 id="Master-节点的高可用一般取决于-Etcd，Etcd-高可用推荐三节点或者五节点，所以-Master-节点通常为三个或者五个，如果接入外部-Etcd-集群，那么-Master-节点可以是偶数个。"><a href="#Master-节点的高可用一般取决于-Etcd，Etcd-高可用推荐三节点或者五节点，所以-Master-节点通常为三个或者五个，如果接入外部-Etcd-集群，那么-Master-节点可以是偶数个。" class="headerlink" title="Master 节点的高可用一般取决于 Etcd，Etcd 高可用推荐三节点或者五节点，所以 Master 节点通常为三个或者五个，如果接入外部 Etcd 集群，那么 Master 节点可以是偶数个。"></a>Master 节点的<strong>高可用</strong>一般取决于 Etcd，Etcd 高可用推荐<strong>三节点或者五节点</strong>，所以 Master 节点通常为三个或者五个，如果接入<strong>外部 Etcd 集群</strong>，那么 Master 节点可以是偶数个。</h5><h5 id="上图-Kubernetes-使用内部-Etcd，即-Etcd-与-Master-节点个数一致，部署在-Kubernetes-集群中。"><a href="#上图-Kubernetes-使用内部-Etcd，即-Etcd-与-Master-节点个数一致，部署在-Kubernetes-集群中。" class="headerlink" title="上图 Kubernetes 使用内部 Etcd，即 Etcd 与 Master 节点个数一致，部署在 Kubernetes 集群中。"></a>上图 Kubernetes 使用内部 Etcd，即 Etcd 与 Master 节点个数一致，部署在 Kubernetes 集群中。</h5><blockquote><h5 id="Etcd-集群一般要求三个、五个类似奇数个实例，Etcd-集群选举机制要求集群中半数以上的实例投票选举，如果集群是两个实例，那么一个实例宕机，剩下一个实例没有办法选举。同样四个实例和三个实例实际上效果是一样的。"><a href="#Etcd-集群一般要求三个、五个类似奇数个实例，Etcd-集群选举机制要求集群中半数以上的实例投票选举，如果集群是两个实例，那么一个实例宕机，剩下一个实例没有办法选举。同样四个实例和三个实例实际上效果是一样的。" class="headerlink" title="Etcd 集群一般要求三个、五个类似奇数个实例，Etcd 集群选举机制要求集群中半数以上的实例投票选举，如果集群是两个实例，那么一个实例宕机，剩下一个实例没有办法选举。同样四个实例和三个实例实际上效果是一样的。"></a>Etcd 集群一般要求三个、五个类似奇数个实例，Etcd 集群选举机制要求集群中<strong>半数以上</strong>的实例投票选举，如果集群是两个实例，那么一个实例宕机，剩下一个实例没有办法选举。同样四个实例和三个实例实际上效果是一样的。</h5></blockquote><h3 id="Worker"><a href="#Worker" class="headerlink" title="Worker"></a>Worker</h3><h5 id="Kubernetes-Worker-节点作为容器运行节点，需要部署以下组件："><a href="#Kubernetes-Worker-节点作为容器运行节点，需要部署以下组件：" class="headerlink" title="Kubernetes Worker 节点作为容器运行节点，需要部署以下组件："></a>Kubernetes <strong>Worker</strong> 节点作为容器运行节点，需要部署以下组件：</h5><ul><li><h5 id="•-CRI：容器运行时，管理容器生命周期；"><a href="#•-CRI：容器运行时，管理容器生命周期；" class="headerlink" title="• CRI：容器运行时，管理容器生命周期；"></a>• <strong>CRI：</strong>容器运行时，管理容器生命周期；</h5></li><li><h5 id="•-Kubelet：管理-Pod-生命周期，Pod-是-Kubernetes-中最小调度单元；"><a href="#•-Kubelet：管理-Pod-生命周期，Pod-是-Kubernetes-中最小调度单元；" class="headerlink" title="• Kubelet：管理 Pod 生命周期，Pod 是 Kubernetes 中最小调度单元；"></a>• <strong>Kubelet：</strong>管理 <strong>Pod</strong> 生命周期，Pod 是 Kubernetes 中最小调度单元；</h5></li><li><h5 id="•-CNI：容器网络接口，实现-Kubernetes-中-Pod-间网络联通；"><a href="#•-CNI：容器网络接口，实现-Kubernetes-中-Pod-间网络联通；" class="headerlink" title="• CNI：容器网络接口，实现 Kubernetes 中 Pod 间网络联通；"></a>• <strong>CNI：</strong>容器网络接口，实现 Kubernetes 中 Pod 间网络联通；</h5></li><li><h5 id="•-CSI：容器存储接口，屏蔽底层存储实现，方便用户使用第三方存储；"><a href="#•-CSI：容器存储接口，屏蔽底层存储实现，方便用户使用第三方存储；" class="headerlink" title="• CSI：容器存储接口，屏蔽底层存储实现，方便用户使用第三方存储；"></a>• <strong>CSI：</strong>容器存储接口，屏蔽底层存储实现，方便用户使用第三方存储；</h5></li><li><h5 id="•-Kube-proxy：该组件主要实现多组-Pod-的负载均衡；"><a href="#•-Kube-proxy：该组件主要实现多组-Pod-的负载均衡；" class="headerlink" title="• Kube-proxy：该组件主要实现多组 Pod 的负载均衡；"></a>• <strong>Kube-proxy：</strong>该组件主要实现多组 Pod 的负载均衡；</h5></li></ul><blockquote><h5 id="为什么-Kubernetes-需要在容器上之上抽象一个-Pod-资源呢？大部分情况是一个-Pod-对应一个容器，有的场景就需要一个-Pod-对应多个容器，例如日志收集场景，每个-Pod-都会包含一个业务容器和一个日志收集容器，将这两个容器放在一个-Pod-里可用共享日志-Volume。"><a href="#为什么-Kubernetes-需要在容器上之上抽象一个-Pod-资源呢？大部分情况是一个-Pod-对应一个容器，有的场景就需要一个-Pod-对应多个容器，例如日志收集场景，每个-Pod-都会包含一个业务容器和一个日志收集容器，将这两个容器放在一个-Pod-里可用共享日志-Volume。" class="headerlink" title="为什么 Kubernetes 需要在容器上之上抽象一个 Pod 资源呢？大部分情况是一个 Pod 对应一个容器，有的场景就需要一个 Pod 对应多个容器，例如日志收集场景，每个 Pod 都会包含一个业务容器和一个日志收集容器，将这两个容器放在一个 Pod 里可用共享日志 Volume。"></a>为什么 Kubernetes 需要在容器上之上抽象一个 Pod 资源呢？大部分情况是一个 Pod 对应一个容器，有的场景就需要一个 Pod 对应多个容器，例如日志收集场景，每个 Pod 都会包含一个业务容器和一个日志收集容器，将这两个容器放在一个 Pod 里可用共享日志 Volume。</h5></blockquote><h5 id="Worker-节点的-Kubelet-需要注册到集群中，就需要每个-Worker-节点的-Kubelet-能够连接-Master-节点的-Kube-apiserver。如果集群中-Master-采用高可用部署，就会存在多个-Master，那么-Worker-节点的-Kubelet-就需要同时连接所有的-Kube-apiserver-保证高可用。实现这种高可用的方式有很多种，例如-Haproxy-Keepalived-、Nginx、Envoy-等。上图就是-LB-组件就代表这些实现负载-Kube-apsierver-的组件。"><a href="#Worker-节点的-Kubelet-需要注册到集群中，就需要每个-Worker-节点的-Kubelet-能够连接-Master-节点的-Kube-apiserver。如果集群中-Master-采用高可用部署，就会存在多个-Master，那么-Worker-节点的-Kubelet-就需要同时连接所有的-Kube-apiserver-保证高可用。实现这种高可用的方式有很多种，例如-Haproxy-Keepalived-、Nginx、Envoy-等。上图就是-LB-组件就代表这些实现负载-Kube-apsierver-的组件。" class="headerlink" title="Worker 节点的 Kubelet 需要注册到集群中，就需要每个 Worker 节点的 Kubelet 能够连接 Master 节点的 Kube-apiserver。如果集群中 Master 采用高可用部署，就会存在多个 Master，那么 Worker 节点的 Kubelet 就需要同时连接所有的 Kube-apiserver 保证高可用。实现这种高可用的方式有很多种，例如 Haproxy + Keepalived 、Nginx、Envoy 等。上图就是 LB 组件就代表这些实现负载 Kube-apsierver 的组件。"></a>Worker 节点的 Kubelet 需要<strong>注册</strong>到集群中，就需要每个 Worker 节点的 Kubelet 能够连接 Master 节点的 Kube-apiserver。如果集群中 Master 采用高可用部署，就会存在多个 Master，那么 Worker 节点的 Kubelet 就需要同时连接<strong>所有的 Kube-apiserver</strong> 保证高可用。实现这种高可用的方式有很多种，例如 <code>Haproxy + Keepalived 、Nginx、Envoy</code> 等。上图就是 <strong>LB 组件</strong>就代表这些实现负载 Kube-apsierver 的组件。</h5><h1 id="创建一个-Pod-需要经历哪些流程"><a href="#创建一个-Pod-需要经历哪些流程" class="headerlink" title="创建一个 Pod 需要经历哪些流程"></a>创建一个 Pod 需要经历哪些流程</h1><h5 id="当用户创建一个-Deployment-的时候，Kubernetes-中各组件的工作流程是如何的？"><a href="#当用户创建一个-Deployment-的时候，Kubernetes-中各组件的工作流程是如何的？" class="headerlink" title="当用户创建一个 Deployment 的时候，Kubernetes 中各组件的工作流程是如何的？"></a>当用户创建一个 <strong>Deployment</strong> 的时候，Kubernetes 中各组件的<strong>工作流程</strong>是如何的？</h5><ul><li><h5 id="•-用户通过-kubectl-创建一个-Deployment，请求会发给-Kube-apiserver；"><a href="#•-用户通过-kubectl-创建一个-Deployment，请求会发给-Kube-apiserver；" class="headerlink" title="• 用户通过 kubectl 创建一个 Deployment，请求会发给 Kube-apiserver；"></a>• 用户通过 <code>kubectl</code> 创建一个 <strong>Deployment</strong>，请求会发给 <strong>Kube-apiserver</strong>；</h5></li><li><h5 id="•-Kube-apiserver-会将-Deployment-的描述信息写入-Etcd-中，Kube-apiserver-将请求结果返回给用户；"><a href="#•-Kube-apiserver-会将-Deployment-的描述信息写入-Etcd-中，Kube-apiserver-将请求结果返回给用户；" class="headerlink" title="• Kube-apiserver 会将 Deployment 的描述信息写入 Etcd 中，Kube-apiserver 将请求结果返回给用户；"></a>• <strong>Kube-apiserver</strong> 会将 <strong>Deployment</strong> 的描述信息写入 <strong>Etcd</strong> 中，<strong>Kube-apiserver</strong> 将请求结果返回给用户；</h5></li><li><p>• <strong>Kube-controller-manager</strong> 的 <strong>Deployment Controller</strong> 从 <strong>Kube-apiserver</strong> <code>Watch</code> 到 <strong>Deployment</strong> 的创建事件，并创建一个 <strong>ReplicaSet</strong>；</p></li><li><h5 id="•-Kube-apiserver-会将-ReplicaSet-的描述信息写入-Etcd-中；"><a href="#•-Kube-apiserver-会将-ReplicaSet-的描述信息写入-Etcd-中；" class="headerlink" title="• Kube-apiserver 会将 ReplicaSet 的描述信息写入 Etcd 中；"></a>• <strong>Kube-apiserver</strong> 会将 <strong>ReplicaSet</strong> 的描述信息写入 <strong>Etcd</strong> 中；</h5></li><li><p>• <strong>Kube-controller-manager</strong> 的 <strong>ReplicaSet Controller</strong> 从 <strong>Kube-apiserver</strong> <code>Watch</code> 到 <strong>ReplicaSet</strong> 的创建事件，并创建一个 <strong>Pod</strong>；</p></li><li><h5 id="•-Kube-apiserver-会将-Pod-的描述信息写入-Etcd-中；"><a href="#•-Kube-apiserver-会将-Pod-的描述信息写入-Etcd-中；" class="headerlink" title="• Kube-apiserver 会将 Pod 的描述信息写入 Etcd 中；"></a>• <strong>Kube-apiserver</strong> 会将 <strong>Pod</strong> 的描述信息写入 <strong>Etcd</strong> 中；</h5></li><li><h5 id="•-Kube-scheduler-从-Kube-apiserver-Watch-到-Pod-的创建事件，并根据调度算法从集群中选择一个最优的节点，并更新-Pod-的-nodeName-字段；"><a href="#•-Kube-scheduler-从-Kube-apiserver-Watch-到-Pod-的创建事件，并根据调度算法从集群中选择一个最优的节点，并更新-Pod-的-nodeName-字段；" class="headerlink" title="• Kube-scheduler 从 Kube-apiserver Watch 到 Pod 的创建事件，并根据调度算法从集群中选择一个最优的节点，并更新 Pod 的 nodeName 字段；"></a>• <strong>Kube-scheduler</strong> 从 <strong>Kube-apiserver</strong> <code>Watch</code> 到 <strong>Pod</strong> 的创建事件，并根据<strong>调度算法</strong>从集群中选择一个<strong>最优</strong>的节点，并更新 Pod 的 <code>nodeName</code> 字段；</h5></li><li><h5 id="•-Kube-apiserver-会将-Pod-的更新信息写入-Etcd-中；"><a href="#•-Kube-apiserver-会将-Pod-的更新信息写入-Etcd-中；" class="headerlink" title="• Kube-apiserver 会将 Pod 的更新信息写入 Etcd 中；"></a>• <strong>Kube-apiserver</strong> 会将 <strong>Pod</strong> 的更新信息写入 <strong>Etcd</strong> 中；</h5></li><li><h5 id="•-上述绑定的节点-Kubelet-从-Kube-apiserver-Watch-到-Pod-绑定节点是自身，直接调用-CRI-创建容器；"><a href="#•-上述绑定的节点-Kubelet-从-Kube-apiserver-Watch-到-Pod-绑定节点是自身，直接调用-CRI-创建容器；" class="headerlink" title="• 上述绑定的节点 Kubelet 从 Kube-apiserver Watch 到 Pod 绑定节点是自身，直接调用 CRI 创建容器；"></a>• 上述<strong>绑定的节点 Kubelet</strong> 从 <strong>Kube-apiserver</strong> <code>Watch</code> 到 Pod 绑定节点是自身，直接调用 <strong>CRI</strong> 创建容器；</h5></li><li><h5 id="•-结果返回，并写入-Etcd。"><a href="#•-结果返回，并写入-Etcd。" class="headerlink" title="• 结果返回，并写入 Etcd。"></a>• 结果返回，并写入 <strong>Etcd</strong>。</h5></li><li><p><img src="https://picture-base.oss-cn-hangzhou.aliyuncs.com/picture/202403191455657.png" alt="image-20240319145504578"></p></li></ul><p>可以发现，Kubernetes 中各组件基本都是与 <strong>Kube-apiserver</strong> 进行数据流发送，整体流程非常清晰。</p><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><h5 id="Kubernetes-中组件较多，且初学较难理解，需要结合实践才能更深刻地掌握。"><a href="#Kubernetes-中组件较多，且初学较难理解，需要结合实践才能更深刻地掌握。" class="headerlink" title="Kubernetes 中组件较多，且初学较难理解，需要结合实践才能更深刻地掌握。"></a>Kubernetes 中组件较多，且初学较难理解，需要结合实践才能更深刻地掌握。</h5><h5 id="Kubernetes-不管是架构设计还是软件开发设计思想都值得我们去深度思考和学习，都可以应用到平常项目开发中，例如声明式-API-思想、Master-Worker-架构设计等。"><a href="#Kubernetes-不管是架构设计还是软件开发设计思想都值得我们去深度思考和学习，都可以应用到平常项目开发中，例如声明式-API-思想、Master-Worker-架构设计等。" class="headerlink" title="Kubernetes 不管是架构设计还是软件开发设计思想都值得我们去深度思考和学习，都可以应用到平常项目开发中，例如声明式 API 思想、Master + Worker 架构设计等。"></a>Kubernetes 不管是架构设计还是软件开发设计思想都值得我们去深度思考和学习，都可以应用到平常项目开发中，例如<strong>声明式 API 思想、Master + Worker 架构设计</strong>等。</h5>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>使用 Keepalived 和 HAproxy 创建高可用 Kubernetes 集群</title>
    <link href="/2024/12/04/2024-10-20%20%E5%88%9B%E5%BB%BA%E9%AB%98%E5%8F%AF%E7%94%A8%E9%9B%86%E7%BE%A4/"/>
    <url>/2024/12/04/2024-10-20%20%E5%88%9B%E5%BB%BA%E9%AB%98%E5%8F%AF%E7%94%A8%E9%9B%86%E7%BE%A4/</url>
    
    <content type="html"><![CDATA[<h1 id="使用-Keepalived-和-HAproxy-创建高可用-Kubernetes-集群"><a href="#使用-Keepalived-和-HAproxy-创建高可用-Kubernetes-集群" class="headerlink" title="使用 Keepalived 和 HAproxy 创建高可用 Kubernetes 集群"></a>使用 Keepalived 和 HAproxy 创建高可用 Kubernetes 集群</h1><p>高可用 Kubernetes 集群能够确保应用程序在运行时不会出现服务中断，这也是生产的需求之一。为此，有很多方法可供选择以实现高可用。</p><p>本教程演示了如何配置 Keepalived 和 HAproxy 使负载均衡、实现高可用。步骤如下：</p><ol><li>准备主机。</li><li>配置 Keepalived 和 HAproxy。</li><li>使用 KubeKey 创建 Kubernetes 集群，并安装 KubeSphere。</li></ol><h2 id="集群架构"><a href="#集群架构" class="headerlink" title="集群架构"></a>集群架构</h2><p>示例集群有三个主节点，三个工作节点，两个用于负载均衡的节点，以及一个虚拟 IP 地址。本示例中的虚拟 IP 地址也可称为“浮动 IP 地址”。这意味着在节点故障的情况下，该 IP 地址可在节点之间漂移，从而实现高可用。</p><p><img src="https://kubesphere.io/images/docs/v3.x/installing-on-linux/high-availability-configurations/set-up-ha-cluster-using-keepalived-haproxy/architecture-ha-k8s-cluster.png" alt="architecture-ha-k8s-cluster"></p><p>请注意，在本示例中，Keepalived 和 HAproxy 没有安装在任何主节点上。但您也可以这样做，并同时实现高可用。然而，配置两个用于负载均衡的特定节点（您可以按需增加更多此类节点）会更加安全。这两个节点上只安装 Keepalived 和 HAproxy，以避免与任何 Kubernetes 组件和服务的潜在冲突。</p><h2 id="准备主机"><a href="#准备主机" class="headerlink" title="准备主机"></a>准备主机</h2><table><thead><tr><th align="left">IP 地址</th><th align="left">主机名</th><th align="left">角色</th></tr></thead><tbody><tr><td align="left">172.16.0.2</td><td align="left">lb1</td><td align="left">Keepalived &amp; HAproxy</td></tr><tr><td align="left">172.16.0.3</td><td align="left">lb2</td><td align="left">Keepalived &amp; HAproxy</td></tr><tr><td align="left">172.16.0.4</td><td align="left">master1</td><td align="left">master, etcd</td></tr><tr><td align="left">172.16.0.5</td><td align="left">master2</td><td align="left">master, etcd</td></tr><tr><td align="left">172.16.0.6</td><td align="left">master3</td><td align="left">master, etcd</td></tr><tr><td align="left">172.16.0.7</td><td align="left">worker1</td><td align="left">worker</td></tr><tr><td align="left">172.16.0.8</td><td align="left">worker2</td><td align="left">worker</td></tr><tr><td align="left">172.16.0.9</td><td align="left">worker3</td><td align="left">worker</td></tr><tr><td align="left">172.16.0.10</td><td align="left"></td><td align="left">虚拟 IP 地址</td></tr></tbody></table><p>有关更多节点、网络、依赖项等要求的信息，请参见<a href="https://kubesphere.io/zh/docs/v3.4/installing-on-linux/introduction/multioverview/#step-1-prepare-linux-hosts">多节点安装</a>。</p><h2 id="配置负载均衡"><a href="#配置负载均衡" class="headerlink" title="配置负载均衡"></a>配置负载均衡</h2><p><a href="https://www.keepalived.org/">Keepalived</a> 提供 VRRP 实现，并允许您配置 Linux 机器使负载均衡，预防单点故障。<a href="http://www.haproxy.org/">HAProxy</a> 提供可靠、高性能的负载均衡，能与 Keepalived 完美配合。</p><p>由于 <code>lb1</code> 和 <code>lb2</code> 上安装了 Keepalived 和 HAproxy，如果其中一个节点故障，虚拟 IP 地址（即浮动 IP 地址）将自动与另一个节点关联，使集群仍然可以正常运行，从而实现高可用。若有需要，也可以此为目的，添加更多安装 Keepalived 和 HAproxy 的节点。</p><p>先运行以下命令安装 Keepalived 和 HAproxy。</p><figure class="highlight cmake"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cmake">yum <span class="hljs-keyword">install</span> keepalived haproxy psmisc -y<br></code></pre></td></tr></table></figure><h3 id="HAproxy"><a href="#HAproxy" class="headerlink" title="HAproxy"></a>HAproxy</h3><ol><li><p>在两台用于负载均衡的机器上运行以下命令以配置 Proxy（两台机器的 Proxy 配置相同）：</p><figure class="highlight awk"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs awk">vi <span class="hljs-regexp">/etc/</span>haproxy/haproxy.cfg<br></code></pre></td></tr></table></figure></li><li><p>以下是示例配置，供您参考（请注意 <code>server</code> 字段。请记住 <code>6443</code> 是 <code>apiserver</code> 端口）：</p><figure class="highlight smali"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br></pre></td><td class="code"><pre><code class="hljs smali">global<br>    log /dev/log  local0 warning<br>    chroot      /var/lib/haproxy<br>    pidfile     /var/run/haproxy.pid<br>    maxconn     4000<br>    user        haproxy<br>    group       haproxy<br>    daemon<br><br>   stats socket /var/lib/haproxy/stats<br><br>defaults<br>  log global<br>  option  httplog<br>  option  dontlognull<br>        timeout connect 5000<br>        timeout client 50000<br>        timeout server 50000<br><br>frontend kube-apiserver<br>  bind *:6443<br>  mode tcp<br>  option tcplog<br>  default_backend kube-apiserver<br><br>backend kube-apiserver<br>    mode tcp<br>    option tcplog<br>    option tcp-check<br>    balance roundrobin<br>    default-server inter 10s downinter 5s rise 2 fall 2 slowstart 60s maxconn 250 maxqueue 256 weight 100<br>    server kube-apiserver-1 172.16.0.4:6443<span class="hljs-built_in"> check </span><span class="hljs-comment"># Replace the IP address with your own.</span><br>    server kube-apiserver-2 172.16.0.5:6443<span class="hljs-built_in"> check </span><span class="hljs-comment"># Replace the IP address with your own.</span><br>    server kube-apiserver-3 172.16.0.6:6443<span class="hljs-built_in"> check </span><span class="hljs-comment"># Replace the IP address with your own.</span><br></code></pre></td></tr></table></figure></li><li><p>保存文件并运行以下命令以重启 HAproxy。</p><figure class="highlight ebnf"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs ebnf"><span class="hljs-attribute">systemctl restart haproxy</span><br></code></pre></td></tr></table></figure></li><li><p>使 HAproxy 在开机后自动运行：</p><figure class="highlight routeros"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs routeros">systemctl <span class="hljs-built_in">enable</span> haproxy<br></code></pre></td></tr></table></figure></li><li><p>确保您在另一台机器 (<code>lb2</code>) 上也配置了 HAproxy。</p></li></ol><h3 id="Keepalived"><a href="#Keepalived" class="headerlink" title="Keepalived"></a>Keepalived</h3><p>两台机器上必须都安装 Keepalived，但在配置上略有不同。</p><ol><li><p>运行以下命令以配置 Keepalived。</p><figure class="highlight vim"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs vim"><span class="hljs-keyword">vi</span> /etc/keepalived/keepalived.<span class="hljs-keyword">conf</span><br></code></pre></td></tr></table></figure></li><li><p>以下是示例配置 (<code>lb1</code>)，供您参考：</p><figure class="highlight angelscript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br></pre></td><td class="code"><pre><code class="hljs angelscript">global_defs &#123;<br>  notification_email &#123;<br>  &#125;<br>  router_id LVS_DEVEL<br>  vrrp_skip_check_adv_addr<br>  vrrp_garp_interval <span class="hljs-number">0</span><br>  vrrp_gna_interval <span class="hljs-number">0</span><br>&#125;<br><br>vrrp_script chk_haproxy &#123;<br>  script <span class="hljs-string">&quot;killall -0 haproxy&quot;</span><br>  <span class="hljs-built_in">int</span>erval <span class="hljs-number">2</span><br>  weight <span class="hljs-number">2</span><br>&#125;<br><br>vrrp_instance haproxy-vip &#123;<br>  state BACKUP<br>  priority <span class="hljs-number">100</span><br>  <span class="hljs-keyword">interface</span> <span class="hljs-symbol">eth0</span>                       # <span class="hljs-symbol">Network</span> <span class="hljs-symbol">card</span><br>  <span class="hljs-symbol">virtual_router_id</span> <span class="hljs-symbol">60</span><br>  <span class="hljs-symbol">advert_int</span> <span class="hljs-symbol">1</span><br>  <span class="hljs-symbol">authentication</span> &#123;<br>    auth_type PASS<br>    auth_pass <span class="hljs-number">1111</span><br>  &#125;<br>  unicast_src_ip <span class="hljs-number">172.16</span><span class="hljs-number">.0</span><span class="hljs-number">.2</span>      # The IP address of <span class="hljs-keyword">this</span> machine<br>  unicast_peer &#123;<br>    <span class="hljs-number">172.16</span><span class="hljs-number">.0</span><span class="hljs-number">.3</span>                         # The IP address of peer machines<br>  &#125;<br><br>  virtual_ipaddress &#123;<br>    <span class="hljs-number">172.16</span><span class="hljs-number">.0</span><span class="hljs-number">.10</span>/<span class="hljs-number">24</span>                  # The VIP address<br>  &#125;<br><br>  track_script &#123;<br>    chk_haproxy<br>  &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>备注</p><ul><li>对于 <code>interface</code> 字段，您必须提供自己的网卡信息。您可以在机器上运行 <code>ifconfig</code> 以获取该值。</li><li>为 <code>unicast_src_ip</code> 提供的 IP 地址是您当前机器的 IP 地址。对于也安装了 HAproxy 和 Keepalived 进行负载均衡的其他机器，必须在字段 <code>unicast_peer</code> 中输入其 IP 地址。</li></ul></li><li><p>保存文件并运行以下命令以重启 Keepalived。</p><figure class="highlight ebnf"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs ebnf"><span class="hljs-attribute">systemctl restart keepalived</span><br></code></pre></td></tr></table></figure></li><li><p>使 Keepalived 在开机后自动运行：</p><figure class="highlight routeros"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs routeros">systemctl <span class="hljs-built_in">enable</span> keepalived<br></code></pre></td></tr></table></figure></li><li><p>确保您在另一台机器 (<code>lb2</code>) 上也配置了 Keepalived。</p></li></ol><h2 id="验证高可用"><a href="#验证高可用" class="headerlink" title="验证高可用"></a>验证高可用</h2><p>在开始创建 Kubernetes 集群之前，请确保已经测试了高可用。</p><ol><li><p>在机器 <code>lb1</code> 上，运行以下命令：</p><figure class="highlight pf"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><code class="hljs pf">[root@lb1 ~]<span class="hljs-comment"># ip a s</span><br><span class="hljs-number">1</span>: lo: <span class="hljs-variable">&lt;LOOPBACK,UP,LOWER_UP&gt;</span> mtu <span class="hljs-number">65536</span> qdisc noqueue <span class="hljs-keyword">state</span> UNKNOWN <span class="hljs-keyword">group</span> <span class="hljs-keyword">default</span> qlen <span class="hljs-number">1000</span><br>    link/loopback <span class="hljs-number">00</span>:<span class="hljs-number">00</span>:<span class="hljs-number">00</span>:<span class="hljs-number">00</span>:<span class="hljs-number">00</span>:<span class="hljs-number">00</span> brd <span class="hljs-number">00</span>:<span class="hljs-number">00</span>:<span class="hljs-number">00</span>:<span class="hljs-number">00</span>:<span class="hljs-number">00</span>:<span class="hljs-number">00</span><br>    <span class="hljs-keyword">inet</span> <span class="hljs-number">127.0</span>.<span class="hljs-number">0.1</span>/<span class="hljs-number">8</span> scope host lo<br>       valid_lft forever preferred_lft forever<br>    <span class="hljs-keyword">inet6</span> ::<span class="hljs-number">1</span>/<span class="hljs-number">128</span> scope host<br>       valid_lft forever preferred_lft forever<br><span class="hljs-number">2</span>: eth0: <span class="hljs-variable">&lt;BROADCAST,MULTICAST,UP,LOWER_UP&gt;</span> mtu <span class="hljs-number">1500</span> qdisc mq <span class="hljs-keyword">state</span> UP <span class="hljs-keyword">group</span> <span class="hljs-keyword">default</span> qlen <span class="hljs-number">1000</span><br>    link/ether <span class="hljs-number">52</span>:<span class="hljs-number">54</span>:<span class="hljs-number">9</span>e:<span class="hljs-number">27</span>:<span class="hljs-number">38</span>:c8 brd ff:ff:ff:ff:ff:ff<br>    <span class="hljs-keyword">inet</span> <span class="hljs-number">172.16</span>.<span class="hljs-number">0.2</span>/<span class="hljs-number">24</span> brd <span class="hljs-number">172.16</span>.<span class="hljs-number">0.255</span> scope <span class="hljs-keyword">global</span> noprefixroute dynamic eth0<br>       valid_lft <span class="hljs-number">73334</span>sec preferred_lft <span class="hljs-number">73334</span>sec<br>    <span class="hljs-keyword">inet</span> <span class="hljs-number">172.16</span>.<span class="hljs-number">0.10</span>/<span class="hljs-number">24</span> scope <span class="hljs-keyword">global</span> secondary eth0 <span class="hljs-comment"># The VIP address</span><br>       valid_lft forever preferred_lft forever<br>    <span class="hljs-keyword">inet6</span> fe80::<span class="hljs-number">510</span>e:f96:<span class="hljs-number">98</span>b2:af40/<span class="hljs-number">64</span> scope link noprefixroute<br>       valid_lft forever preferred_lft forever<br></code></pre></td></tr></table></figure></li><li><p>如上图所示，虚拟 IP 地址已经成功添加。模拟此节点上的故障：</p><figure class="highlight arduino"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs arduino">systemctl stop haproxy<br></code></pre></td></tr></table></figure></li><li><p>再次检查浮动 IP 地址，您可以看到该地址在 <code>lb1</code> 上消失了。</p><figure class="highlight pf"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs pf">[root@lb1 ~]<span class="hljs-comment"># ip a s</span><br><span class="hljs-number">1</span>: lo: <span class="hljs-variable">&lt;LOOPBACK,UP,LOWER_UP&gt;</span> mtu <span class="hljs-number">65536</span> qdisc noqueue <span class="hljs-keyword">state</span> UNKNOWN <span class="hljs-keyword">group</span> <span class="hljs-keyword">default</span> qlen <span class="hljs-number">1000</span><br>    link/loopback <span class="hljs-number">00</span>:<span class="hljs-number">00</span>:<span class="hljs-number">00</span>:<span class="hljs-number">00</span>:<span class="hljs-number">00</span>:<span class="hljs-number">00</span> brd <span class="hljs-number">00</span>:<span class="hljs-number">00</span>:<span class="hljs-number">00</span>:<span class="hljs-number">00</span>:<span class="hljs-number">00</span>:<span class="hljs-number">00</span><br>    <span class="hljs-keyword">inet</span> <span class="hljs-number">127.0</span>.<span class="hljs-number">0.1</span>/<span class="hljs-number">8</span> scope host lo<br>       valid_lft forever preferred_lft forever<br>    <span class="hljs-keyword">inet6</span> ::<span class="hljs-number">1</span>/<span class="hljs-number">128</span> scope host<br>       valid_lft forever preferred_lft forever<br><span class="hljs-number">2</span>: eth0: <span class="hljs-variable">&lt;BROADCAST,MULTICAST,UP,LOWER_UP&gt;</span> mtu <span class="hljs-number">1500</span> qdisc mq <span class="hljs-keyword">state</span> UP <span class="hljs-keyword">group</span> <span class="hljs-keyword">default</span> qlen <span class="hljs-number">1000</span><br>    link/ether <span class="hljs-number">52</span>:<span class="hljs-number">54</span>:<span class="hljs-number">9</span>e:<span class="hljs-number">27</span>:<span class="hljs-number">38</span>:c8 brd ff:ff:ff:ff:ff:ff<br>    <span class="hljs-keyword">inet</span> <span class="hljs-number">172.16</span>.<span class="hljs-number">0.2</span>/<span class="hljs-number">24</span> brd <span class="hljs-number">172.16</span>.<span class="hljs-number">0.255</span> scope <span class="hljs-keyword">global</span> noprefixroute dynamic eth0<br>       valid_lft <span class="hljs-number">72802</span>sec preferred_lft <span class="hljs-number">72802</span>sec<br>    <span class="hljs-keyword">inet6</span> fe80::<span class="hljs-number">510</span>e:f96:<span class="hljs-number">98</span>b2:af40/<span class="hljs-number">64</span> scope link noprefixroute<br>       valid_lft forever preferred_lft forever<br></code></pre></td></tr></table></figure></li><li><p>理论上讲，若配置成功，该虚拟 IP 会漂移到另一台机器 (<code>lb2</code>) 上。在 <code>lb2</code> 上运行以下命令，这是预期的输出：</p><figure class="highlight pf"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><code class="hljs pf">[root@lb2 ~]<span class="hljs-comment"># ip a s</span><br><span class="hljs-number">1</span>: lo: <span class="hljs-variable">&lt;LOOPBACK,UP,LOWER_UP&gt;</span> mtu <span class="hljs-number">65536</span> qdisc noqueue <span class="hljs-keyword">state</span> UNKNOWN <span class="hljs-keyword">group</span> <span class="hljs-keyword">default</span> qlen <span class="hljs-number">1000</span><br>    link/loopback <span class="hljs-number">00</span>:<span class="hljs-number">00</span>:<span class="hljs-number">00</span>:<span class="hljs-number">00</span>:<span class="hljs-number">00</span>:<span class="hljs-number">00</span> brd <span class="hljs-number">00</span>:<span class="hljs-number">00</span>:<span class="hljs-number">00</span>:<span class="hljs-number">00</span>:<span class="hljs-number">00</span>:<span class="hljs-number">00</span><br>    <span class="hljs-keyword">inet</span> <span class="hljs-number">127.0</span>.<span class="hljs-number">0.1</span>/<span class="hljs-number">8</span> scope host lo<br>       valid_lft forever preferred_lft forever<br>    <span class="hljs-keyword">inet6</span> ::<span class="hljs-number">1</span>/<span class="hljs-number">128</span> scope host<br>       valid_lft forever preferred_lft forever<br><span class="hljs-number">2</span>: eth0: <span class="hljs-variable">&lt;BROADCAST,MULTICAST,UP,LOWER_UP&gt;</span> mtu <span class="hljs-number">1500</span> qdisc mq <span class="hljs-keyword">state</span> UP <span class="hljs-keyword">group</span> <span class="hljs-keyword">default</span> qlen <span class="hljs-number">1000</span><br>    link/ether <span class="hljs-number">52</span>:<span class="hljs-number">54</span>:<span class="hljs-number">9</span>e:<span class="hljs-number">3</span>f:<span class="hljs-number">51</span>:ba brd ff:ff:ff:ff:ff:ff<br>    <span class="hljs-keyword">inet</span> <span class="hljs-number">172.16</span>.<span class="hljs-number">0.3</span>/<span class="hljs-number">24</span> brd <span class="hljs-number">172.16</span>.<span class="hljs-number">0.255</span> scope <span class="hljs-keyword">global</span> noprefixroute dynamic eth0<br>       valid_lft <span class="hljs-number">72690</span>sec preferred_lft <span class="hljs-number">72690</span>sec<br>    <span class="hljs-keyword">inet</span> <span class="hljs-number">172.16</span>.<span class="hljs-number">0.10</span>/<span class="hljs-number">24</span> scope <span class="hljs-keyword">global</span> secondary eth0   <span class="hljs-comment"># The VIP address</span><br>       valid_lft forever preferred_lft forever<br>    <span class="hljs-keyword">inet6</span> fe80::f67c:bd4f:d6d5:<span class="hljs-number">1</span>d9b/<span class="hljs-number">64</span> scope link noprefixroute<br>       valid_lft forever preferred_lft forever<br></code></pre></td></tr></table></figure></li><li><p>如上所示，高可用已经配置成功。</p></li></ol><h2 id="使用-KubeKey-创建-Kubernetes-集群"><a href="#使用-KubeKey-创建-Kubernetes-集群" class="headerlink" title="使用 KubeKey 创建 Kubernetes 集群"></a>使用 KubeKey 创建 Kubernetes 集群</h2><p><a href="https://github.com/kubesphere/kubekey">KubeKey</a> 是一款用来创建 Kubernetes 集群的工具，高效而便捷。请按照以下步骤下载 KubeKey。</p><ul><li><a href="https://kubesphere.io/zh/docs/v3.4/installing-on-linux/high-availability-configurations/set-up-ha-cluster-using-keepalived-haproxy/#">如果您能正常访问 GitHub&#x2F;Googleapis</a></li><li><a href="https://kubesphere.io/zh/docs/v3.4/installing-on-linux/high-availability-configurations/set-up-ha-cluster-using-keepalived-haproxy/#">如果您访问 GitHub&#x2F;Googleapis 受限</a></li></ul><p>从 <a href="https://github.com/kubesphere/kubekey/releases">GitHub Release Page</a> 下载 KubeKey 或者直接使用以下命令。</p><figure class="highlight dsconfig"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs dsconfig"><span class="hljs-string">curl</span> -<span class="hljs-string">sfL</span> <span class="hljs-string">https</span>://<span class="hljs-built_in">get-kk.kubesphere.io</span> | <span class="hljs-string">VERSION</span>=<span class="hljs-string">v3</span>.<span class="hljs-string">0</span>.<span class="hljs-string">13</span> <span class="hljs-string">sh</span> -<br></code></pre></td></tr></table></figure><p>如果您访问 GitHub&#x2F;Googleapis 受限</p><p>首先运行以下命令，以确保您从正确的区域下载 KubeKey。</p><figure class="highlight routeros"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs routeros"><span class="hljs-built_in">export</span> <span class="hljs-attribute">KKZONE</span>=cn<br></code></pre></td></tr></table></figure><p>运行以下命令来下载 KubeKey：</p><figure class="highlight dsconfig"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs dsconfig"><span class="hljs-string">curl</span> -<span class="hljs-string">sfL</span> <span class="hljs-string">https</span>://<span class="hljs-built_in">get-kk.kubesphere.io</span> | <span class="hljs-string">VERSION</span>=<span class="hljs-string">v3</span>.<span class="hljs-string">0</span>.<span class="hljs-string">13</span> <span class="hljs-string">sh</span> -<br></code></pre></td></tr></table></figure><p>备注</p><p>下载 KubeKey 之后，如果您将其转移到访问 Googleapis 受限的新机器上，请务必再次运行 <code>export KKZONE=cn</code>，然后继续执行以下步骤。</p><p>备注</p><p>通过以上命令，可以下载 KubeKey 的最新版本。您可以更改命令中的版本号来下载特定的版本。</p><p>使 <code>kk</code> 成为可执行文件：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">chmod</span> +x kk<br></code></pre></td></tr></table></figure><p>使用默认配置创建一个示例配置文件。此处以 Kubernetes v1.22.12 作为示例。</p><figure class="highlight jboss-cli"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs jboss-cli"><span class="hljs-string">./kk</span> create config <span class="hljs-params">--with-kubesphere</span> v3.4.1 <span class="hljs-params">--with-kubernetes</span> v1.22.12<br></code></pre></td></tr></table></figure><p>备注</p><ul><li>安装 KubeSphere 3.4 的建议 Kubernetes 版本：v1.20.x、v1.21.x、v1.22.x、v1.23.x、* v1.24.x、* v1.25.x 和 * v1.26.x。带星号的版本可能出现边缘节点部分功能不可用的情况。因此，如需使用边缘节点，推荐安装 v1.23.x。如果不指定 Kubernetes 版本，KubeKey 将默认安装 Kubernetes v1.23.10。有关受支持的 Kubernetes 版本的更多信息，请参见<a href="https://kubesphere.io/zh/docs/v3.4/installing-on-linux/introduction/kubekey/#%E6%94%AF%E6%8C%81%E7%9F%A9%E9%98%B5">支持矩阵</a>。</li><li>如果您没有在本步骤的命令中添加标志 <code>--with-kubesphere</code>，那么除非您使用配置文件中的 <code>addons</code> 字段进行安装，或者稍后使用 <code>./kk create cluster</code> 时再添加该标志，否则 KubeSphere 将不会被部署。</li><li>如果您添加标志 <code>--with-kubesphere</code> 时未指定 KubeSphere 版本，则会安装最新版本的 KubeSphere。</li></ul><h2 id="部署-KubeSphere-和-Kubernetes"><a href="#部署-KubeSphere-和-Kubernetes" class="headerlink" title="部署 KubeSphere 和 Kubernetes"></a>部署 KubeSphere 和 Kubernetes</h2><p>运行上述命令后，将创建配置文件 <code>config-sample.yaml</code>。编辑文件以添加机器信息、配置负载均衡器等。</p><p>备注</p><p>如果自定义文件名，那么文件名可能会有所不同。</p><h3 id="config-sample-yaml-示例"><a href="#config-sample-yaml-示例" class="headerlink" title="config-sample.yaml 示例"></a>config-sample.yaml 示例</h3><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><code class="hljs yaml"><span class="hljs-string">...</span><br><span class="hljs-attr">spec:</span><br>  <span class="hljs-attr">hosts:</span><br>  <span class="hljs-bullet">-</span> &#123;<span class="hljs-attr">name:</span> <span class="hljs-string">master1</span>, <span class="hljs-attr">address:</span> <span class="hljs-number">172.16</span><span class="hljs-number">.0</span><span class="hljs-number">.4</span>, <span class="hljs-attr">internalAddress:</span> <span class="hljs-number">172.16</span><span class="hljs-number">.0</span><span class="hljs-number">.4</span>, <span class="hljs-attr">user:</span> <span class="hljs-string">root</span>, <span class="hljs-attr">password:</span> <span class="hljs-string">Testing123</span>&#125;<br>  <span class="hljs-bullet">-</span> &#123;<span class="hljs-attr">name:</span> <span class="hljs-string">master2</span>, <span class="hljs-attr">address:</span> <span class="hljs-number">172.16</span><span class="hljs-number">.0</span><span class="hljs-number">.5</span>, <span class="hljs-attr">internalAddress:</span> <span class="hljs-number">172.16</span><span class="hljs-number">.0</span><span class="hljs-number">.5</span>, <span class="hljs-attr">user:</span> <span class="hljs-string">root</span>, <span class="hljs-attr">password:</span> <span class="hljs-string">Testing123</span>&#125;<br>  <span class="hljs-bullet">-</span> &#123;<span class="hljs-attr">name:</span> <span class="hljs-string">master3</span>, <span class="hljs-attr">address:</span> <span class="hljs-number">172.16</span><span class="hljs-number">.0</span><span class="hljs-number">.6</span>, <span class="hljs-attr">internalAddress:</span> <span class="hljs-number">172.16</span><span class="hljs-number">.0</span><span class="hljs-number">.6</span>, <span class="hljs-attr">user:</span> <span class="hljs-string">root</span>, <span class="hljs-attr">password:</span> <span class="hljs-string">Testing123</span>&#125;<br>  <span class="hljs-bullet">-</span> &#123;<span class="hljs-attr">name:</span> <span class="hljs-string">worker1</span>, <span class="hljs-attr">address:</span> <span class="hljs-number">172.16</span><span class="hljs-number">.0</span><span class="hljs-number">.7</span>, <span class="hljs-attr">internalAddress:</span> <span class="hljs-number">172.16</span><span class="hljs-number">.0</span><span class="hljs-number">.7</span>, <span class="hljs-attr">user:</span> <span class="hljs-string">root</span>, <span class="hljs-attr">password:</span> <span class="hljs-string">Testing123</span>&#125;<br>  <span class="hljs-bullet">-</span> &#123;<span class="hljs-attr">name:</span> <span class="hljs-string">worker2</span>, <span class="hljs-attr">address:</span> <span class="hljs-number">172.16</span><span class="hljs-number">.0</span><span class="hljs-number">.8</span>, <span class="hljs-attr">internalAddress:</span> <span class="hljs-number">172.16</span><span class="hljs-number">.0</span><span class="hljs-number">.8</span>, <span class="hljs-attr">user:</span> <span class="hljs-string">root</span>, <span class="hljs-attr">password:</span> <span class="hljs-string">Testing123</span>&#125;<br>  <span class="hljs-bullet">-</span> &#123;<span class="hljs-attr">name:</span> <span class="hljs-string">worker3</span>, <span class="hljs-attr">address:</span> <span class="hljs-number">172.16</span><span class="hljs-number">.0</span><span class="hljs-number">.9</span>, <span class="hljs-attr">internalAddress:</span> <span class="hljs-number">172.16</span><span class="hljs-number">.0</span><span class="hljs-number">.9</span>, <span class="hljs-attr">user:</span> <span class="hljs-string">root</span>, <span class="hljs-attr">password:</span> <span class="hljs-string">Testing123</span>&#125;<br>  <span class="hljs-attr">roleGroups:</span><br>    <span class="hljs-attr">etcd:</span><br>    <span class="hljs-bullet">-</span> <span class="hljs-string">master1</span><br>    <span class="hljs-bullet">-</span> <span class="hljs-string">master2</span><br>    <span class="hljs-bullet">-</span> <span class="hljs-string">master3</span><br>    <span class="hljs-attr">control-plane:</span><br>    <span class="hljs-bullet">-</span> <span class="hljs-string">master1</span><br>    <span class="hljs-bullet">-</span> <span class="hljs-string">master2</span><br>    <span class="hljs-bullet">-</span> <span class="hljs-string">master3</span><br>    <span class="hljs-attr">worker:</span><br>    <span class="hljs-bullet">-</span> <span class="hljs-string">worker1</span><br>    <span class="hljs-bullet">-</span> <span class="hljs-string">worker2</span><br>    <span class="hljs-bullet">-</span> <span class="hljs-string">worker3</span><br>  <span class="hljs-attr">controlPlaneEndpoint:</span><br>    <span class="hljs-attr">domain:</span> <span class="hljs-string">lb.kubesphere.local</span><br>    <span class="hljs-attr">address:</span> <span class="hljs-number">172.16</span><span class="hljs-number">.0</span><span class="hljs-number">.10</span>   <span class="hljs-comment"># The VIP address</span><br>    <span class="hljs-attr">port:</span> <span class="hljs-number">6443</span><br><span class="hljs-string">...</span><br></code></pre></td></tr></table></figure><p>备注</p><ul><li>请使用您自己的 VIP 地址来替换 <code>controlPlaneEndpoint.address</code> 的值。</li><li>有关更多本配置文件中不同参数的信息，请参见<a href="https://kubesphere.io/zh/docs/v3.4/installing-on-linux/introduction/multioverview/#2-edit-the-configuration-file">多节点安装</a>。</li></ul><h3 id="开始安装"><a href="#开始安装" class="headerlink" title="开始安装"></a>开始安装</h3><p>完成配置之后，可以执行以下命令开始安装：</p><figure class="highlight lua"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs lua">./kk <span class="hljs-built_in">create</span> cluster -f <span class="hljs-built_in">config</span>-sample.yaml<br></code></pre></td></tr></table></figure><h3 id="验证安装"><a href="#验证安装" class="headerlink" title="验证安装"></a>验证安装</h3><ol><li><p>运行以下命令以检查安装日志。</p><figure class="highlight routeros"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs routeros">kubectl logs -n kubesphere-system $(kubectl <span class="hljs-built_in">get</span> pod -n kubesphere-system -l <span class="hljs-string">&#x27;app in (ks-install, ks-installer)&#x27;</span> -o <span class="hljs-attribute">jsonpath</span>=<span class="hljs-string">&#x27;&#123;.items[0].metadata.name&#125;&#x27;</span>) -f<br></code></pre></td></tr></table></figure></li><li><p>看到以下信息时，表明高可用集群已成功创建。</p><figure class="highlight clean"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><code class="hljs clean">#####################################################<br>###              Welcome to KubeSphere!           ###<br>#####################################################<br><br>Console: http:<span class="hljs-comment">//172.16.0.4:30880</span><br>Account: admin<br>Password: P@<span class="hljs-number">88</span>w0rd<br><br>NOTES：<br>  <span class="hljs-number">1.</span> After you log into the console, please check the<br>     monitoring status <span class="hljs-keyword">of</span> service components <span class="hljs-keyword">in</span><br>     the <span class="hljs-string">&quot;Cluster Management&quot;</span>. If any service is not<br>     ready, please wait patiently until all components<br>     are up and running.<br>  <span class="hljs-number">2.</span> Please change the default password after login.<br><br>#####################################################<br>https:<span class="hljs-comment">//kubesphere.io             2020-xx-xx xx:xx:xx</span><br>#####################################################<br></code></pre></td></tr></table></figure></li></ol><p>反馈</p><p>这篇文章对您有帮助吗？</p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>Hello World</title>
    <link href="/2024/12/04/hello-world/"/>
    <url>/2024/12/04/hello-world/</url>
    
    <content type="html"><![CDATA[<p>Welcome to <a href="https://hexo.io/">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues">GitHub</a>.</p><h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">$ hexo new <span class="hljs-string">&quot;My New Post&quot;</span><br></code></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/writing.html">Writing</a></p><h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">$ hexo server<br></code></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/server.html">Server</a></p><h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">$ hexo generate<br></code></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/generating.html">Generating</a></p><h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">$ hexo deploy<br></code></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/one-command-deployment.html">Deployment</a></p>]]></content>
    
    
    
  </entry>
  
  
  
  
</search>
